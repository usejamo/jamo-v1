---
plan: "14.2-05"
phase: "14.2"
status: complete
---

## Summary

Wired PendingEditsPlugin into SectionEditorBlock, implemented materializePendingEdits with full error paths and idempotent ghost-content guard, integrated EditSummaryCard into AIChatPanel, fixed scroll bug, deleted DiffPreview.tsx.

## Tasks Completed

1. **PendingEditsPlugin registered in SectionEditorBlock** — Extension added alongside existing TipTap extensions; useEffect subscribes to pending_edits state changes and calls materializePendingEdits
2. **materializePendingEdits implemented** — Idempotent by edit ID; ghostContentLeakDetected runs before SET_PENDING_EDITS dispatch; handles editor not mounted, section not active, stale paragraph IDs, malformed payloads
3. **EditSummaryCard integrated into AIChatPanel** — Replaces DiffPreview; includes message_id prop; onAcceptAll dispatches BATCH_ACCEPT_PENDING_EDITS; batch PM transaction sorted descending by position with error recovery
4. **Scroll bug fixed** — scrollIntoView fires only when message count increases
5. **DiffPreview.tsx deleted** — All import sites removed first
6. **insertChainRef removed from AIChatPanel.tsx**

## Key Files

- `src/components/editor/SectionEditorBlock.tsx` — PendingEditsPlugin registered; materializePendingEdits with ghost guard and error paths; useEffect refresh
- `src/components/AIChatPanel.tsx` — EditSummaryCard rendering; scroll fix; insertChainRef removed; propose_edit routes through materializePendingEdits
- `src/components/chat/EditSummaryCard.tsx` — Full implementation with message_id, tally, drill-down, aria-labels
- `src/components/chat/DiffPreview.tsx` — DELETED

## Deviations

None — implementation matches plan must-haves.

## Self-Check: PASSED
