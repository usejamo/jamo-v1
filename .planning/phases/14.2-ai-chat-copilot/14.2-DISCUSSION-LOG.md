# Phase 14.2: AI Chat Co-pilot — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in 14.2-CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-15
**Phase:** 14.2-ai-chat-copilot
**Areas discussed:** Part A: Ghost UX & a11y, Part A: Write granularity, Part B: Action queue design, Part B: Resume / trigger / edge cases

---

## Part A: Accept/Reject UI Density

| Option | Description | Selected |
|--------|-------------|----------|
| Individual affordances | Each ghost shows its own Accept/Reject buttons | |
| Floating action bar | Single bar operates on focused ghost | |
| Hybrid: individual + batch Accept/Reject All in card | Per-ghost affordances + card-level batch escape hatch | ✓ |

**User's choice:** Individual affordances on each ghost; Batch Accept All / Reject All in EditSummaryCard (not the editor). Batch Accept = single ProseMirror transaction (single undo step).

---

## Part A: Ghost Anchor Staleness

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-reject with notice | Ghost auto-rejected; summary card shows "Anchor edited — suggestion discarded" | ✓ |
| Stale state with disabled Accept | Ghost stays visible, Accept disabled | |
| Best-effort relocation | Plugin attempts to follow paragraph via mapping; fallback to auto-reject | |

**User's choice:** Auto-reject with `auto_rejected_stale` enum value. Tally distinguishes stale from rejected. Content-hash the anchor paragraph at materialization; staleness = anchor content changed (not surrounding paragraphs). Deletions of anchor = stale.

---

## Part A: Decoration Accessibility

| Option | Description | Selected |
|--------|-------------|----------|
| Icon prefix + aria-label | Small icon (✏/+/−) + aria-label on wrapper | |
| Text label badge + aria-label | "Edit"/"Insert"/"Delete" chip + full-sentence aria-label | ✓ |
| You decide | Leave to planner based on design system | |

**User's choice:** Text badges ("Edit"/"Insert"/"Delete") muted/small in border color family, top-left of widget. Full-sentence `aria-label` on wrapper. Both in `CHANGE_TYPE_LABELS: Record<ChangeType, { badge: string; aria: string }>` alongside `TOOL_STATUS_LABELS`.

---

## Part A: EditSummaryCard Terminal State

| Option | Description | Selected |
|--------|-------------|----------|
| Tally + hide affordance | Card shows tally, "Review in editor →" disappears | ✓ |
| Tally + keep affordance disabled | Affordance grayed out permanently | |
| Collapse to compact chip | Card collapses to "Edit applied" chip | |

**User's choice:** Tally + hide affordance. Slight card desaturation, section title shifts to plain text. Per-paragraph drill-down preserved in terminal state. Tally uses specific enum breakdown: "4 accepted, 1 stale" etc.

---

## Part A: Resolution Write Granularity

| Option | Description | Selected |
|--------|-------------|----------|
| One write per action | Each Accept/Reject writes immediately | ✓ |
| Debounce with short window | 300–500ms debounce, batch into 1–2 writes | |
| Batch on full resolution | Write only when all changes resolved | |

**User's choice:** One write per action, fire-and-forget. Optimistic UI (ProseMirror transaction commits, decorations clear, tally updates — none await DB write). Failure → toast/retry queue. Matches existing autosave pattern.

---

## Part B: Action Queue Placement

| Option | Description | Selected |
|--------|-------------|----------|
| Above chat history | Queue at top of message list, collapses when task starts | ✓ |
| Separate tab | Panel has "Actions" and "Chat" tabs | |
| Sticky header above input | Compact strip pinned above free-text input | |

**User's choice:** Above chat history. Auto-collapses to "N items in queue" header with expand affordance (not hidden). Low-key empty state: "No suggestions right now. Ask me anything below."

---

## Part B: Action Queue Ordering

| Option | Description | Selected |
|--------|-------------|----------|
| Fixed type priority | compliance > conflict > gap > missing | ✓ |
| Type priority + Haiku score | Type tiers + AI severity scoring within tier | |

**User's choice:** Fixed type priority. Within tier: section position in document (not detection time). gap ≠ missing — gap = section incomplete, missing = required section not started.

---

## Part B: Queue Dismissibility

| Option | Description | Selected |
|--------|-------------|----------|
| Dismissible with snooze | Move to dismissed state, re-surface on content change | ✓ |
| Permanently dismissible | Remove from pending_actions entirely | |
| Not dismissible | Items clear only via resolution or stale-clearing | |

**User's choice:** Dismissible with content-hash re-surfacing. No reason required. Dismissed list visible on demand (collapsed, expandable). Re-surfaced items labeled "Re-surfaced — section content changed since you dismissed this."

---

## Part B: Queue Item Cap

| Option | Description | Selected |
|--------|-------------|----------|
| Cap at 8 with remainder count | Show top 8; "+N more — focus on these first" footer | ✓ |
| Cap at 5 with "Show more" | Top 5 with progressive disclosure | |
| No cap | Show all, scrollable | |

**User's choice:** Cap at 8 with per-tier distribution (max 4 compliance, max 2 conflict, max 2 gap, max 2 missing). Footer is inline expand affordance. Constants in one tunable file. Section name visible in queue item text (not tooltip).

---

## Part B: Proactive Detection Trigger

| Option | Description | Selected |
|--------|-------------|----------|
| After each section save | Fire-and-forget, ~3s debounce | ✓ |
| On panel open | Analysis on explicit panel expand | |
| On-demand button | User-triggered "Refresh suggestions" | |

**User's choice:** Fire-and-forget after save. Content-hash skip (no content change = no analysis). 30s/proposal cooldown. Full proposal context (not just changed section). Race condition: run ID tagging, latest wins. Initial population question left to planner (D-35).

---

## Part B: Resume-on-Reload

| Option | Description | Selected |
|--------|-------------|----------|
| Re-render from DB state | Re-display last ask_user card from proposal_chats | ✓ |
| Send fresh "continue" message | Fresh edge function call with active_task context | |
| Hybrid: re-render + context update | Re-render + one fetch for updated proposal state | |

**User's choice:** Re-render from DB. Verify premise valid before showing Continue. Content-hash drift check at resume (prompt if drifted). Active walkthrough surfaced as high-priority queue item.

---

## Part B: Walkthrough Cancel UX

| Option | Description | Selected |
|--------|-------------|----------|
| Leave accepted edits in place | active_task clears, paragraph edits persist | ✓ |
| Prompt: keep or revert | One-question modal on cancel | |
| Full undo to pre-walkthrough state | Revert all accepted edits | |

**User's choice:** Leave accepted edits in place. Button: "Stop walkthrough." Adjacent copy: "Accepted changes will stay in your document." `not_reached` enum for unreviewed paragraphs. `active_task` cleared optimistically.

---

## Part B: Concurrent User Sessions

| Option | Description | Selected |
|--------|-------------|----------|
| Last-write-wins + warning | No infrastructure, silent overwrite | |
| Lock active_task with user ID | locked_by field, 10-min auto-expire | |
| Per-user chat_sessions | user_id column, composite PK | ✓ |

**User's choice:** Per-user `chat_sessions` with `(proposal_id, user_id)` composite PK. Migration: backfill from most recent `proposal_chats` author. `pending_actions` global per proposal; dismissals per-user. Two users on same section: allowed, same last-write-wins model as editor.

---

## Part B: Section-Scoped Thread Filter

| Option | Description | Selected |
|--------|-------------|----------|
| Defer — design from usage | Skip in 14.2 | ✓ |
| Build it — cosmetic filter | Chip row filters by section_target_id | |

**User's choice:** Defer. Ensure `section_target_id` captured reliably on all messages in 14.2 to enable future filter without schema migration.

---

## Claude's Discretion

- Schema design for per-user dismissal state
- Initial population strategy for first-time proposals (D-35)
- Content-hash algorithm and drift thresholds (consistent across D-06, D-22, D-38)
- RLS policy updates for new per-user `chat_sessions`

## Deferred Ideas

- Section-scoped thread filter (build post-launch with usage data)
- Initial population choice (D-35, planner decides)
- `detectGaps()` retirement (keep as feature-flag fallback, retire in cleanup phase)
- Token-aware history window (14.1 deferred, planner discretion)
- Haiku for lightweight tools (14.1 deferred, planner discretion)
