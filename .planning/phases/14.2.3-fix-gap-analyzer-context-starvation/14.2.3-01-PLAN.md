---
phase: 14.2.3-fix-gap-analyzer-context-starvation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/analyze-proposal-gaps/index.ts
autonomous: true
requirements: [D-1, D-2, D-5]
must_haves:
  truths:
    - "Sections under 5000 chars reach Haiku whole (not 300-char sliced)"
    - "Over-ceiling sections are truncated at 5000 chars with a meta marker appended at the end"
    - "The prompt carries 4 placeholder-family few-shot examples, each showing the gap-vs-missing decision"
    - "A placeholder-heavy proposal can surface up to 4 gap and 4 missing findings (QUEUE_CAP 10)"
    - "The analyzer still makes a single Haiku call with all sections (no per-section loop)"
  artifacts:
    - path: "supabase/functions/analyze-proposal-gaps/index.ts"
      provides: "Full-content excerpt builder, sharpened prompt, rebalanced tier caps"
      contains: "section truncated for length"
  key_links:
    - from: "sections.map summaries builder (index.ts ~:267)"
      to: "single Haiku call (index.ts ~:279)"
      via: "excerpt field carrying full content"
      pattern: "slice\\(0, 5000\\)"
---

<objective>
Land the three independent edge-function changes that do not depend on the new DB column: full section content up to a 5000-char ceiling with an end-marker (D-1), a sharpened placeholder-detection prompt with exactly 4 few-shot examples (D-2), and rebalanced tier caps (D-5).

This is the substance of the fix — it stops hiding section content from Haiku. It is fully independent of the migration (Plan 02) and the resolve-time plumbing (Plan 03), so it runs in Wave 1.

Purpose: A 946-char section currently shows Haiku only its polished first ~32%; the placeholders live in the body the model never receives. Sending full content + telling the model what placeholders look like is what makes the analyzer return findings on placeholder-laden proposals.
Output: Modified `supabase/functions/analyze-proposal-gaps/index.ts` (three in-file edits, single Haiku call shape unchanged).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.2.3-fix-gap-analyzer-context-starvation/CONTEXT.md
@.planning/phases/14.2.3-fix-gap-analyzer-context-starvation/14.2.3-PATTERNS.md

<interfaces>
<!-- Established in-file patterns the executor modifies. Extracted from the codebase — no exploration needed. -->

Tier caps (index.ts:14–16, CURRENT — to change):
```typescript
const QUEUE_CAP = 8
const TIER_CAPS = { compliance: 4, conflict: 2, gap: 2, missing: 2 } as const
```
Applied unchanged at index.ts:308–314 (reads the constants; do not touch the application logic).

Excerpt builder (index.ts:267–271, CURRENT — to change):
```typescript
const summaries = sections.map((s) => ({
  key: s.key,
  title: s.title,
  excerpt: s.content.replace(/<[^>]*>/g, '').replace(/\s+/g, ' ').trim().slice(0, 300),
}))
```
These `summaries` flow into the single Haiku call at index.ts:279–285. Keep the single-call shape.

Prompt (index.ts ANALYSIS_SYSTEM_PROMPT, :96–127). Current sole few-shot is the "TBD" → missing example at :125–127. `buildResolvedBlock` (:60–94) is concatenated onto this prompt at :278 — new few-shots MUST stay inside ANALYSIS_SYSTEM_PROMPT so the resolved-items block still appends after them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebalance tier caps (D-5) — pure constant swap</name>
  <read_first>
    - supabase/functions/analyze-proposal-gaps/index.ts:14–16 (the constants), :308–314 (application logic — read to confirm it only reads the constants, do NOT modify it)
    - CONTEXT.md Decision 5 (the rebalanced values + rationale)
  </read_first>
  <action>
    At index.ts:14–16 change the two named constants to exactly:
    ```typescript
    const QUEUE_CAP = 10
    const TIER_CAPS = { compliance: 4, conflict: 2, gap: 4, missing: 4 } as const
    ```
    Keep them as named constants (do not inline). Do NOT touch the application logic at :308–314 — it already reads `TIER_CAPS[tier]` and `.slice(0, QUEUE_CAP)` and needs no change.
  </action>
  <acceptance_criteria>
    - `index.ts` contains the literal string `const QUEUE_CAP = 10`
    - `index.ts` contains the literal string `gap: 4, missing: 4`
    - `index.ts` contains the literal string `compliance: 4, conflict: 2`
    - `index.ts` no longer contains `const QUEUE_CAP = 8`
    - `index.ts` still contains `.slice(0, QUEUE_CAP)` (application logic untouched)
  </acceptance_criteria>
  <done>QUEUE_CAP is 10; TIER_CAPS is { compliance: 4, conflict: 2, gap: 4, missing: 4 }; application logic at :308–314 unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Full content up to 5000-char ceiling + end-marker (D-1)</name>
  <read_first>
    - supabase/functions/analyze-proposal-gaps/index.ts:267–271 (the excerpt builder to modify), :279–285 (the single Haiku call the summaries feed — confirm it stays one call)
    - CONTEXT.md Decision 1 + Discussion Resolution 1 (marker form: appended at END, clearly meta, not a structured field, not prepended)
    - 14.2.3-PATTERNS.md §3b
  </read_first>
  <action>
    Replace ONLY the `.slice(0, 300)` step in the excerpt builder at index.ts:267–271. Keep the HTML-strip + whitespace-collapse + trim chain verbatim. Compute the cleaned string once, then branch on the 5000-char ceiling. Replace the builder with:
    ```typescript
    const CONTENT_CEILING = 5000
    const TRUNCATION_MARKER = ' […section truncated for length…]'
    const summaries = sections.map((s) => {
      const cleaned = s.content.replace(/<[^>]*>/g, '').replace(/\s+/g, ' ').trim()
      const excerpt =
        cleaned.length > CONTENT_CEILING
          ? cleaned.slice(0, CONTENT_CEILING) + TRUNCATION_MARKER
          : cleaned
      return { key: s.key, title: s.title, excerpt }
    })
    ```
    The marker is appended at the END so the model reads real content first, then learns it is incomplete. The marker text "section truncated for length" must read as clearly meta — it must NOT look like unfilled placeholder scaffolding (Decision 1 risk: it could otherwise trip the placeholder hunt and produce a spurious finding). Do NOT introduce a structured `truncated: true` field. Do NOT split into a per-section loop — `summaries` continues into the single Haiku call at :279–285 unchanged.
    Optional (planner's call per D-1, low value since 5000 chars covers nearly all sections): you MAY add one sentence to ANALYSIS_SYSTEM_PROMPT instructing the model to judge a section ending in the truncation marker on its visible content only and not infer completeness of the unseen tail. If you add it, it is a single line; do not expand it into a section.
  </action>
  <acceptance_criteria>
    - `index.ts` contains `5000` (as `CONTENT_CEILING = 5000` or a `.slice(0, 5000)` / `.slice(0, CONTENT_CEILING)`)
    - `index.ts` contains the literal substring `section truncated for length`
    - `index.ts` no longer contains `.slice(0, 300)`
    - `index.ts` still contains `.replace(/<[^>]*>/g, '').replace(/\s+/g, ' ').trim()` (HTML-strip + collapse + trim retained)
    - There is still exactly one Anthropic/Haiku messages call in the file (grep for `messages.create` or the existing fetch to the Anthropic endpoint returns a single call site — no new call introduced)
  </acceptance_criteria>
  <done>Sections ≤5000 chars sent whole; >5000 truncated at 5000 + end-marker; HTML-strip/collapse/trim retained; single Haiku call preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sharpen prompt with 4 placeholder-family few-shot examples (D-2)</name>
  <read_first>
    - supabase/functions/analyze-proposal-gaps/index.ts:96–127 (ANALYSIS_SYSTEM_PROMPT, including the existing "TBD" → missing few-shot at :125–127), :278 (where buildResolvedBlock is concatenated — new few-shots MUST precede this append)
    - CONTEXT.md Decision 2 + Discussion Resolution 2 (exactly 4 examples, one per family, each showing the gap-vs-missing decision + rationale, framed as category representatives, realistic paraphrased CRO fragments)
    - 14.2.3-PATTERNS.md §3c
  </read_first>
  <behavior>
    - The prompt instructs the model to treat unfilled placeholder text (text that should have been replaced with real content) as a finding, recognized by SEMANTIC FAMILY rather than an exhaustive literal list.
    - Exactly 4 few-shot examples are present, one per observed family:
      1. Bracket placeholders (e.g. `[CRO Name]`, `[CLIENT NAME]`)
      2. Descriptive-noun placeholders (e.g. `CRO legal entity name`, `investigational product name`)
      3. Incomplete contact/address details (e.g. `mailing address`, `Name and title of RFP contact at …`, `full corporate address`) — keeps its OWN anchor; do NOT merge into descriptive-noun
      4. Explicit incompleteness markers (e.g. `TBD`, `TODO`, `to be determined`, `[insert …]`)
    - Each example shows the classification DECISION + rationale: substantive content + placeholders ⇒ `gap`; predominantly scaffolding/placeholders ⇒ `missing`.
    - Each example is framed as a category representative ("recognize the general pattern, not only these exact strings").
    - Fragments are realistic, lightly-paraphrased CRO proposal text — NOT synthetic `[PLACEHOLDER]` toys.
    - No fifth example is added (4 maps one-to-one onto the distinct surface forms; 5+ overfits).
  </behavior>
  <action>
    Extend ANALYSIS_SYSTEM_PROMPT (index.ts:96–127) — do NOT replace its overall structure. Keep the existing `gap` vs `missing` type vocabulary and the existing JSON output shape exactly as the current "TBD" example demonstrates. Add a short guidance sentence framing placeholder detection by semantic family (unfilled template text that should have been replaced with real content; recognize the general pattern, not only the exact strings shown), then add EXACTLY 4 few-shot examples — one per family listed in <behavior>. Each example must:
    1. Use a realistic, lightly-paraphrased CRO proposal fragment as the INPUT excerpt (e.g. a clinical-operations or budget section), not a synthetic `[PLACEHOLDER]` toy.
    2. Show the OUTPUT classification AND a one-line rationale stating WHY it is `gap` vs `missing` (decision boundary: substantive content + placeholders ⇒ gap; predominantly scaffolding ⇒ missing).
    3. Be framed as a category representative.
    Suggested family mapping (use as anchors; paraphrase into realistic fragments):
    - Bracket → a cover-letter line still reading `[CRO Name] is pleased to submit…` amid otherwise real prose ⇒ gap.
    - Descriptive-noun → a study-design paragraph referencing `the investigational product name` / `CRO legal entity name` where a real name belongs ⇒ gap.
    - Incomplete contact/address → a contacts block listing `Name and title of RFP contact at …`, `mailing address`, `full corporate address` with little else ⇒ missing (predominantly scaffolding).
    - Explicit marker → an eligibility/section excerpt that is essentially `TBD` / `to be determined` ⇒ missing.
    Keep all 4 examples INSIDE ANALYSIS_SYSTEM_PROMPT so `buildResolvedBlock` still appends after them at :278. Do not exceed 4. Update the AI-SPEC note only if Task is extended (out of scope here — see Plan 02 doc note).
  </action>
  <verify>
    <automated>MISSING — no automated test asserts prompt content; verify via grep acceptance_criteria below (the prompt is a string constant, not executable logic). A Haiku live re-run against the failing proposal is the functional check, performed in the Plan 02 verification + manual UAT.</automated>
  </verify>
  <acceptance_criteria>
    - ANALYSIS_SYSTEM_PROMPT contains a phrase framing placeholders by family — grep for case-insensitive `placeholder` returns ≥1 hit inside the prompt block (:96–~:160 after the edit)
    - The prompt contains a category-representative framing phrase — grep for `general pattern` (case-insensitive) returns ≥1 hit
    - The prompt contains all four family anchors — grep returns ≥1 hit each for: `[` followed by a bracket placeholder example; `investigational product` (or `legal entity name`); `mailing address` (or `RFP contact`); `TBD` (or `to be determined`)
    - Exactly 4 new few-shot INPUT/OUTPUT example pairs exist beyond the original TBD example — count `OUTPUT:` occurrences in the prompt block; the total must be 5 (1 original TBD + 4 new) OR the original TBD is folded into the explicit-marker example yielding exactly 4 `OUTPUT:` lines. Either is acceptable; 6+ `OUTPUT:` lines is a fail (over 4 distinct families).
    - `buildResolvedBlock` is still concatenated AFTER the prompt (grep confirms the `:278` concatenation site still references ANALYSIS_SYSTEM_PROMPT + buildResolvedBlock output, unmodified)
    - Each `gap`/`missing` example string is adjacent to a rationale word — grep for case-insensitive `rationale` or `because` returns ≥4 hits in the prompt block
  </acceptance_criteria>
  <done>Prompt carries exactly 4 placeholder-family few-shots (one per family, contact/address keeps its own anchor), each with a realistic CRO fragment + gap-vs-missing decision + rationale + category-representative framing; resolved-items block still appends after.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → analyze-proposal-gaps edge | Untrusted request body (proposal_id, sections, run_id) crosses here; identity already JWT-derived, never from body |
| edge → Anthropic Haiku | Section content (now full, was 300 chars) leaves to the LLM provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1423-01 | Information Disclosure | Full section content → Haiku (was 300-char slice) | accept | No NEW exfiltration surface: section content already left the client for analysis pre-14.2.3 (just truncated). Same provider, same JWT-scoped read, same single call. The change is volume, not a new sink. Section content is user-authored proposal text, not credentials/PII secrets. |
| T-1423-02 | Denial of Service | Larger prompt could slow Haiku or hit limits | accept | ~5–8K input tokens for 9 full sections vs Haiku's 200K window; 5000-char ceiling caps the worst-case pathological section; `max_tokens` is output-only and unchanged. Low severity. |
| T-1423-03 | Tampering | Truncation marker mistaken for placeholder scaffolding → spurious finding | mitigate | Marker phrased as clearly meta ("section truncated for length") so it cannot read as unfilled scaffolding (Decision 1); acceptance criteria assert the exact meta phrasing. |

No high-severity threats. Identity derivation (JWT, never body) and the user-scoped anon client are unchanged by this plan.
</threat_model>

<verification>
- Edge function still compiles/deploys (Deno) with one Haiku call.
- `QUEUE_CAP = 10` and `TIER_CAPS = { compliance: 4, conflict: 2, gap: 4, missing: 4 }`.
- Excerpt builder sends full content ≤5000 chars + end-marker beyond; `.slice(0, 300)` gone; HTML-strip/collapse/trim retained.
- Prompt carries exactly 4 placeholder-family few-shots, each with the gap-vs-missing decision + rationale.
- Functional confirmation (failing 9-section proposal now yields multiple placeholder findings) is exercised after deploy in Plan 02's verification + manual UAT (this plan does not redeploy).
</verification>

<success_criteria>
- Acceptance criteria for Tasks 1–3 all pass under grep.
- Single-call, all-sections structure preserved (Acceptance #2).
- Tier caps reflect rebalanced values (Acceptance #7).
- Over-ceiling section truncates with a visible meta marker (Acceptance #3).
</success_criteria>

<output>
After completion, create `.planning/phases/14.2.3-fix-gap-analyzer-context-starvation/14.2.3-01-SUMMARY.md`.
Note in the summary whether the optional "judge truncated sections on visible content" prompt line was added, and whether the original TBD few-shot was folded into the explicit-marker example or kept separate (affects the `OUTPUT:` count downstream readers will see).
</output>
