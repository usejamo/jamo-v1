---
status: partial
phase: 14.2.3-fix-gap-analyzer-context-starvation
source: [14.2.3-VERIFICATION.md]
started: 2026-06-04
updated: 2026-06-04
---

## Current Test

[awaiting human testing]

## Tests

### 1. Placeholder-laden proposal now yields findings
expected: Re-analyzing the failing 9-section Phase III proposal (full of unfilled placeholders — "CRO legal entity name", "investigational product name", "Name and title of RFP contact at…", "mailing address") produces MULTIPLE findings flagging the placeholder sections (was zero before). A placeholder-heavy proposal can surface up to 4 `gap` and 4 `missing` findings. (Acceptance #1, #7)
result: [pending]

### 2. Unchanged reopen does NOT re-fire (the >30s test)
expected: Open a proposal, navigate to the dashboard, wait MORE THAN 30 seconds, reopen the same unchanged proposal — NO second Haiku call fires; the cached pending_actions render from chat_sessions. (Acceptance #4.) The >30s gap is mandatory: a shorter gap would pass trivially via the 30s server cooldown without exercising the persistent hash gate.
result: [pending]

### 3. Edit + save + reopen DOES re-analyze
expected: Edit any section, save, reopen the proposal — the analyzer re-runs (whole-proposal hash changed). A save to one section invalidates the cached analysis for the WHOLE proposal, not just that section. (Acceptance #5, #6)
result: [pending]

### 4. Stale dismissals self-correct + new dismissals stick
expected: The two pre-existing stale "section not started" dismissals no longer suppress legitimate findings after the fix (their content changed, so they annotate as `content_changed_since_action`). A NEW dismissal made right after editing a section stays dismissed on the next analysis (flush-then-hash made the resolve-time hash match the persisted content). (Acceptance #8 + D-6 behavior)
result: [pending]

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps
