---
phase: 14.2.3-fix-gap-analyzer-context-starvation
plan: 02
subsystem: gap-analysis
tags: [gap-analysis, haiku, caching, content-hash, chat_sessions, edge-function]
requires:
  - chat_sessions table with RLS + chat_sessions_org_isolation
  - analyze-proposal-gaps edge function (Plan 01: full-content + 5000 ceiling + caps)
  - useGapAnalysisTrigger hook (ac7d8ee: always-fire mount + empty-content gate)
  - computeHash (whole-proposal SHA-256, useGapAnalysisTrigger.ts:28-41)
provides:
  - Durable whole-proposal content-hash cache gate across navigate-away-and-back
  - chat_sessions.pending_actions_content_hash (nullable text, atomic with pending_actions)
  - Edge content_hash request field + atomic hash write
  - Client mount read-gate (skip Haiku invoke when persisted hash == current hash)
affects:
  - supabase/functions/analyze-proposal-gaps/index.ts
  - src/hooks/useGapAnalysisTrigger.ts
  - src/types/database.types.ts (Plan 02 Task 1)
  - supabase/migrations/ (Plan 02 Task 1)
tech-stack:
  added: []
  patterns:
    - Atomic sibling-key write (hash in the SAME upsert as pending_actions — desync guard)
    - Client-first cache gate; server 30s cooldown as backstop
    - null persisted hash == run (same path as mismatch)
key-files:
  created:
    - supabase/migrations/20260603000001_chat_sessions_pending_actions_hash.sql
  modified:
    - supabase/functions/analyze-proposal-gaps/index.ts
    - src/hooks/useGapAnalysisTrigger.ts
    - src/types/database.types.ts
    - .planning/phases/14.2-ai-chat-copilot/14.2-AI-SPEC.md
decisions:
  - D-3: persist whole-proposal hash; client gate FIRST, 30s cooldown is backstop
  - D-4: whole-proposal hash (one section's save invalidates the whole cached analysis)
metrics:
  duration: ~25m (Tasks 2-4 continuation)
  completed: 2026-06-04
requirements: [D-3, D-4]
---

# Phase 14.2.3 Plan 02: Persist Whole-Proposal Content Hash Summary

Durable `chat_sessions.pending_actions_content_hash` lets the mount trigger skip a redundant Haiku call across navigate-away-and-back; written atomically with `pending_actions` and gated client-side before invoke, with the 30s server cooldown as backstop.

## What Was Built

**Task 1 (prior agent + orchestrator — 8892acb, 1d3b697):** Migration `20260603000001_chat_sessions_pending_actions_hash.sql` (`ALTER TABLE chat_sessions ADD COLUMN pending_actions_content_hash text`, nullable, no DEFAULT/backfill) applied to the live linked project (ref `fuuvdcvbliijffogjnwg`) and `database.types.ts` regenerated (3 hits confirmed: Row `string | null`, Insert/Update optional).

**Task 2 — Edge (cf85d27):** In `analyze-proposal-gaps/index.ts`: added `content_hash: z.string().optional()` to `RequestSchema`, destructured `contentHash = parsed.data.content_hash`, and wrote `pending_actions_content_hash: contentHash ?? null` as a SIBLING key in the single existing `chat_sessions` upsert. Old clients (no field) write null ⇒ next mount runs.

**Task 3 — Client (997c8e1):** In `useGapAnalysisTrigger.ts`: added `content_hash: hash` to the invoke body; inserted a mount read-gate in the mount IIFE BEFORE `runAnalysis` that reads persisted `pending_actions_content_hash` via per-(proposal,user) `.maybeSingle()`, compares to current `computeHash`, and on equality seeds `hashRef.current` and returns (skips invoke). null/mismatch falls through to run. Realtime path and 30s cooldown untouched.

**Task 4 — Manual UAT:** human-verify checkpoint; documented below as PENDING (not blocking).

## pending_actions Write-Path Audit (D-3 desync guard)

Confirmed `analyze-proposal-gaps/index.ts` is the SOLE writer of `pending_actions`: exactly one `.upsert(` call across two `from('chat_sessions')` references (the other is the new mount read in the hook, a `.select`). `chat-with-jamo` only `.select`s `pending_actions`. No separate hash write exists — atomicity holds.

## Migration Application

- **Timestamp chosen:** `20260603000001` (strictly greater than prior `20260601000001`).
- **How applied:** live linked project per Task 1 (committed in 1d3b697 — DB push + `npm run db:types` regen). Column verified live and present in regenerated types.

## AI-SPEC Drift Annotation

Added a one-line SUPERSEDED banner above `14.2-AI-SPEC.md:306` (the "first 300 characters per section" rule), pointing to Phase 14.2.3 Plan 01 full-content + 5000-char ceiling. Historical plan/research/summary copies of the 300-char note left as record.

## Deviations from Plan

None to the implementation. The plan's automated check for Task 3 (`npm run test:run`) surfaced pre-existing failures — see below.

## Deferred Issues (SCOPE BOUNDARY)

`useGapAnalysisTrigger.spec.ts` has 3 failing tests (lines 177, 193, 282). **Confirmed pre-existing:** stashing Plan 02's hook edits and re-running the spec yields the IDENTICAL 3 failures (4 pass / 3 fail), so Plan 02 did not introduce them. Root cause: commit `ac7d8ee` made the mount path always-fire and removed the old D-35 "skip when row exists" contract; the spec still asserts the old behavior and does not stub the new mount `chat_sessions` read. Plan 02's own files type-check clean (`tsc --noEmit` exit 0). Logged in `deferred-items.md` with a test-maintenance follow-up. Other run-to-run flaky failures (`DocumentList`, `resolved-items`, `SectionEditorBlock`) are network/timeout-dependent and untouched by this phase.

## Manual UAT — PENDING (Task 4 human-verify, run against dev server + authed session + failing 9-section proposal)

1. (#1/#3) Open the failing 9-section Phase III placeholder proposal — analyzer must return MULTIPLE placeholder findings (was zero). Sections >5000 chars end with the meta truncation marker.
2. (#2) Network tab: exactly ONE `analyze-proposal-gaps` invoke (not N) — cross-section detection intact.
3. (#4 — MANDATORY >30s gap) Open, navigate away, WAIT past 30s cooldown, reopen UNCHANGED proposal — ZERO new Haiku invoke (hash gate, not 429). A shorter gap is invalid.
4. (#5) Edit a section, save, reopen — re-analysis DOES fire (whole-proposal hash changed).
5. (#6) Edit ONE section, reopen — the WHOLE-proposal cached analysis is invalidated (D-4).
6. Dismiss-after-edit stays dismissed (resolve-time flush-then-hash, Plan 03).

The orchestrator runs phase verification next.

## Self-Check: PASSED

Files verified present: migration, index.ts, useGapAnalysisTrigger.ts, SUMMARY.md. Commits verified: cf85d27, 997c8e1. Hook contains 2 `pending_actions_content_hash` references and 1 `content_hash: hash` invoke-body entry.

## Threat Flags

None. New surface (content_hash field + hash column + mount read) covered by the plan's existing threat register (T-1423-04..07): RLS-inherited column, JWT-derived identity, advisory non-sensitive hash, atomic write.
