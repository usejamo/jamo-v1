---
phase: 08-section-workspace-rich-text-editor
plan: 05
type: execute
wave: 4
depends_on: ["08-04"]
files_modified:
  - src/pages/ProposalDetail.tsx
  - src/components/editor/SectionEditorBlock.tsx
  - src/components/editor/SectionWorkspace.tsx
autonomous: false
requirements:
  - REQ-5.1
  - REQ-5.2
  - REQ-5.3
  - REQ-5.4
  - REQ-5.5
  - REQ-5.6
  - REQ-5.7
  - REQ-5.8
must_haves:
  truths:
    - "ProposalDetail renders SectionWorkspace instead of ProposalDraftRenderer"
    - "User can edit proposal sections in TipTap editors"
    - "All AI actions stream into preview before accept"
    - "Lock/unlock works per section"
    - "Version history shows and restores snapshots"
    - "Compliance flags appear after accept"
    - "Consistency check banner appears after full generation"
    - "All existing tests still pass"
  artifacts:
    - path: "src/pages/ProposalDetail.tsx"
      provides: "ProposalDetail page with SectionWorkspace integration"
      contains: "SectionWorkspace"
  key_links:
    - from: "src/pages/ProposalDetail.tsx"
      to: "src/components/editor/SectionWorkspace.tsx"
      via: "renders SectionWorkspace in place of ProposalDraftRenderer"
      pattern: "SectionWorkspace"
---

<objective>
Wire SectionWorkspace into ProposalDetail page, replacing ProposalDraftRenderer. Convert test stubs to passing tests. Human verify the full editing workspace.

Purpose: Integration and verification — connect all Phase 8 components to the live application and confirm the workspace works end-to-end.

Output: Updated ProposalDetail.tsx, activated test stubs, human-verified workspace.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-section-workspace-rich-text-editor/08-CONTEXT.md
@.planning/phases/08-section-workspace-rich-text-editor/08-UI-SPEC.md
@.planning/phases/08-section-workspace-rich-text-editor/08-00-SUMMARY.md
@.planning/phases/08-section-workspace-rich-text-editor/08-01-SUMMARY.md
@.planning/phases/08-section-workspace-rich-text-editor/08-02-SUMMARY.md
@.planning/phases/08-section-workspace-rich-text-editor/08-03-SUMMARY.md
@.planning/phases/08-section-workspace-rich-text-editor/08-04-SUMMARY.md

<interfaces>
From src/components/editor/SectionWorkspace.tsx:
```typescript
interface SectionWorkspaceProps {
  proposalId: string
  sections: Array<{ section_key: string; content: string; is_locked: boolean; status: string; last_saved_content: string | null }>
  orgId: string
}
export default function SectionWorkspace(props: SectionWorkspaceProps)
```

From src/pages/ProposalDetail.tsx (current):
```typescript
import ProposalDraftRenderer from '../components/ProposalDraftRenderer'
// renders <ProposalDraftRenderer ... /> in the center area
// also renders <AIChatPanel /> in the right panel
```
</interfaces>

@src/pages/ProposalDetail.tsx
@src/components/editor/SectionWorkspace.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire SectionWorkspace into ProposalDetail and activate test stubs</name>
  <files>
    src/pages/ProposalDetail.tsx,
    src/components/editor/SectionEditorBlock.tsx,
    src/components/editor/SectionWorkspace.tsx,
    src/components/editor/__tests__/SectionWorkspace.test.tsx,
    src/components/editor/__tests__/SectionEditorBlock.test.tsx,
    src/components/editor/__tests__/SectionActionToolbar.test.tsx,
    src/components/editor/__tests__/VersionHistoryOverlay.test.tsx,
    src/components/editor/__tests__/ComplianceFlag.test.tsx,
    src/components/editor/__tests__/ConsistencyCheckBanner.test.tsx
  </files>
  <read_first>
    src/pages/ProposalDetail.tsx,
    src/components/editor/SectionWorkspace.tsx,
    src/components/editor/SectionEditorBlock.tsx,
    src/components/editor/__tests__/SectionWorkspace.test.tsx,
    src/components/editor/__tests__/ComplianceFlag.test.tsx,
    src/components/editor/__tests__/ConsistencyCheckBanner.test.tsx,
    src/types/workspace.ts,
    src/types/generation.ts,
    src/hooks/useProposalGeneration.ts
  </read_first>
  <action>
**Update `src/pages/ProposalDetail.tsx`:**

1. Add import: `import SectionWorkspace from '../components/editor/SectionWorkspace'`
2. Keep `ProposalDraftRenderer` import for streaming mode (Phase 7 streaming cards are still used during active generation)
3. Find the area where `<ProposalDraftRenderer>` is rendered. Replace the **non-streaming** usage (the review mode / completed state) with `<SectionWorkspace>`:
   ```tsx
   // When generation is complete or idle (not actively streaming):
   <SectionWorkspace
     proposalId={id ?? ''}
     sections={proposalSections.map(s => ({
       section_key: s.section_key,
       content: s.content ?? '',
       is_locked: s.is_locked ?? false,
       status: s.status ?? 'missing',
       last_saved_content: s.last_saved_content ?? null,
     }))}
     orgId={profile?.org_id ?? ''}
   />
   ```
4. Keep `ProposalDraftRenderer` for the active streaming state (when `genState` has sections in `generating` status). This preserves the Phase 7 streaming UX during generation. Once generation completes, SectionWorkspace takes over.
5. Move `<AIChatPanel>` rendering to be compatible with the new layout. SectionWorkspace already has a right-column slot — for now, keep AIChatPanel in its existing position outside SectionWorkspace (Phase 9 will move it into the right slot).
6. Ensure the `proposal_sections` data fetch includes `last_saved_content` and `is_locked` fields.

**Activate test stubs:**

Convert `it.skip` stubs to real tests in all 6 test files. Use the established test pattern:
- Mock `supabase` using the inline `vi.mock` pattern (not dynamic imports — see STATE.md)
- For TipTap-dependent tests: mock `@tiptap/react` to return a mock `useEditor` that provides `{ commands: { setContent: vi.fn(), insertContentAt: vi.fn() }, getHTML: vi.fn(() => '<p>test</p>'), setEditable: vi.fn() }`
- For component render tests: use `@testing-library/react` `render` and check DOM output
- Keep tests focused on verifiable assertions (button presence, class names, text content)

**Priority test conversions** (convert at least these; leave complex integration tests as skip if they would OOM):
- ComplianceFlag.test.tsx: all 3 stubs (pure render, no TipTap needed)
- ConsistencyCheckBanner.test.tsx: all 4 stubs (pure render)
- SectionActionToolbar.test.tsx: at least 3 of 5 (button rendering, disabled state)
- SectionWorkspace.test.tsx: at least 1 of 3 (renders with mock children)
- SectionEditorBlock.test.tsx: convert 2 of 5 (leave TipTap-heavy ones as skip if happy-dom has issues)
- VersionHistoryOverlay.test.tsx: convert 2 of 5 (render and button presence)

Run `npm run test:run` after each file conversion to catch regressions early.
  </action>
  <verify>
    <automated>npm run test:run</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/ProposalDetail.tsx contains `SectionWorkspace`
    - src/pages/ProposalDetail.tsx contains `import SectionWorkspace`
    - At least 12 test stubs converted from it.skip to passing it() calls
    - ComplianceFlag.test.tsx has 0 remaining it.skip calls
    - ConsistencyCheckBanner.test.tsx has 0 remaining it.skip calls
    - npm run test:run exits 0
  </acceptance_criteria>
  <done>SectionWorkspace integrated into ProposalDetail replacing ProposalDraftRenderer for completed sections. At least 12 test stubs converted to passing tests. All existing tests still green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify full Section Workspace</name>
  <what-built>
    Full TipTap-powered Section Workspace replacing ProposalDraftRenderer:
    - Three-panel layout (section nav / TipTap editors / right slot)
    - Per-section TipTap editors with rich text editing
    - Action toolbar: Generate/Regenerate/Expand/Condense/Rewrite
    - AI action previews (inline for Expand/Condense, diff for Rewrite)
    - Lock/unlock toggle per section
    - Version history overlay panel
    - Section nav with status dots (green/amber/gray)
    - Compliance flags inline after accept
    - Consistency check banner after full generation
    - Autosave indicator
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app in browser
    2. Navigate to a proposal that has generated sections
    3. Verify three-panel layout: left nav with section names and status dots, center with TipTap editors, empty right column
    4. Click into a section editor — verify you can type, bold (Ctrl+B), italic (Ctrl+I), create headings
    5. Check autosave indicator shows "Saving..." then "Saved" after typing
    6. Click the lock icon on a section — verify editor becomes read-only, action buttons are disabled (grayed out), lock icon turns amber
    7. Click lock again to unlock — verify editor is editable again
    8. Click "Generate Section" on an empty section (or "Expand" on a completed section) — verify streaming preview appears below editor in blue box with animated cursor
    9. After streaming completes, click "Accept" — verify content loads into TipTap editor with green flash
    10. Verify Cmd+Z after accept restores previous content
    11. Click History icon — verify overlay slides in from right with semi-transparent backdrop
    12. Verify version entries show action labels and timestamps
    13. Close overlay by clicking X or outside backdrop
    14. Click a section name in left nav — verify page scrolls to that section
    15. Verify active section is highlighted with jamo-500 left border in nav
    16. Check compliance flags appear inline below section after accepting AI content
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run test:run` exits 0 with at least 12 new passing tests
- ProposalDetail renders SectionWorkspace for completed proposals
- Full editing workflow functional in browser
</verification>

<success_criteria>
SectionWorkspace fully integrated into ProposalDetail. All Phase 8 features operational: TipTap editing, AI action preview/accept flow, lock/unlock, version history, section nav, compliance flags, consistency check. Human verified and approved.
</success_criteria>

<output>
After completion, create `.planning/phases/08-section-workspace-rich-text-editor/08-05-SUMMARY.md`
</output>
