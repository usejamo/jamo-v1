---
phase: 14.1
plan: 08
type: summary
status: complete
commit: f4ce9d7
---

# Plan 14.1-08 Summary: tool_data Persistence and History Reconstruction

## What Was Implemented

Three targeted changes to `src/components/AIChatPanel.tsx` (70 insertions, 9 deletions):

### Change 1: History Load — tool_data Added to SELECT Query (L148, L159-164)
- Added `tool_data` to the Supabase SELECT on mount
- Maps each row's `tool_data` JSON to `ChatMessage.toolData` via a version guard
- Version guard: `td.version !== 1` returns `undefined` (backward compat for future schema changes)
- Legacy rows with `tool_data = null` still render as plain text (no change to that path)

### Change 2: Assistant Message Save — tool_data Included (L459, L501)
- Tool result saves: `tool_data: toolData as unknown as Json` (fire-and-forget insert with silent fail)
- Plain text saves: `tool_data: null` (explicit null, backward compat)
- Added `import type { Json } from '../types/database.types'` (L6)

### Change 3: State Mutation Persistence — `persistToolDataState` Helper (L311-322)
- `useCallback` helper reads current `msg.toolData` from local state, merges new state, and UPDATEs `proposal_chats`
- Wired to 4 call sites (L633, L655, L702, L739):
  - `DiffPreview onAccept` — persists `resolutions[paragraphId] = 'accepted'`
  - `DiffPreview onReject` — persists `resolutions[paragraphId] = 'rejected'`
  - `ComplianceCard onDismiss` — persists `dismissed_indices`
  - `AskUserCard onAnswer` — persists `answered`
- All mutations fire-and-forget (silent fail per D-07 convention)

## Acceptance Criteria — All Met

| Check | Result |
|-------|--------|
| `grep "tool_data"` ≥ 5 matches | 7 matches |
| `grep "tool_data: null"` present | ✓ L501 |
| `grep "persistToolDataState"` definition + ≥ 2 call sites | 1 definition + 4 call sites |
| `grep "td.version !== 1"` version guard | ✓ L162 |
| Backward compat: legacy rows render as plain text | ✓ (null guard on L159) |

## Files Modified

- `src/components/AIChatPanel.tsx` — tool_data read/write full lifecycle
