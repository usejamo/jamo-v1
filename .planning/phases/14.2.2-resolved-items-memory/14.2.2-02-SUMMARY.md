---
phase: 14.2.2-resolved-items-memory
plan: 02
subsystem: chat / resolved-items writer
tags: [resolved-items, writer, retry, identity-key, vitest, helpers]
dependency_graph:
  requires:
    - src/types/chat.ts (ResolvedItem, OriginatingActionSnapshot, AcceptanceSummary, FindingType)
    - src/constants/chat.ts (APPLIED_CHANGES_MAX_CHARS)
    - src/utils/sectionHash.ts (sha256OfSection)
    - supabase RPC append_resolved_item (Plan 01 migration; not yet pushed)
  provides:
    - "appendResolvedItem (module-level fire-and-forget writer with retry/backoff)"
    - "concatChangeSummaries (smart truncation to APPLIED_CHANGES_MAX_CHARS)"
    - "buildResolvedItemEntry (binary user_action derivation + acceptance_summary)"
    - "identityKey (stable section_key|finding_type|title triple)"
    - "rebuildFilterSet (dual id+ik membership keys, D-20)"
  affects:
    - "Plan 03 (cta snapshot helpers)"
    - "Plan 04 (AIChatPanel wire-up — consumes all five exports)"
    - "Plan 05/06 (hooks + edge function injection of resolved_items context)"
tech_stack:
  added: []
  patterns:
    - "Module-level async fn for fire-and-forget writes (survives component unmount, D-24)"
    - "Exponential backoff with bounded retries + log-and-drop (D-25)"
    - "Structured warn logging with stable field schema for grep-ability (D-26)"
    - "Binary user_action from AcceptanceSummary.accepted threshold (D-13)"
    - "Dual identity key (id primary, ik fallback) for filter-set membership (D-20)"
key_files:
  created:
    - src/chat/resolved-items.ts
    - src/chat/__tests__/resolved-items.test.ts
    - src/chat/__tests__/applied-changes.test.ts
    - src/chat/__tests__/build-entry.test.ts
    - src/chat/__tests__/filter-set.test.ts
  modified: []
decisions:
  - "Used relative imports (../types/chat, ../constants/chat, ../utils/sectionHash) rather than @/ alias because tsconfig.json defines no paths mapping. Plan 04 wire-up will follow the same pattern."
metrics:
  duration_minutes: ~15
  tasks_completed: 2
  files_created: 5
  files_modified: 0
  test_count: 20
  tests_passing: 20
  completed_date: 2026-06-01
---

# Phase 14.2.2 Plan 02: Resolved-Items Writer + Helpers Summary

Module-level `appendResolvedItem` with bounded exponential-backoff retries (500ms, 1500ms, 4000ms) plus four pure helpers (`concatChangeSummaries`, `buildResolvedItemEntry`, `identityKey`, `rebuildFilterSet`) wired through Supabase RPC `append_resolved_item` — backed by 20 vitest specs across 4 spec files, all green.

## Must-Haves Verification (8 truths)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | appendResolvedItem retries 3× with exp backoff [500, 1500, 4000] before log-and-drop (D-25) | PASS | `RETRY_DELAYS_MS = [500, 1500, 4000]`; test "retries up to 3 times then logs dropped" asserts 3 RPC calls + dropped warn |
| 2 | Failed writes emit structured console.warn with {proposal_id, user_id, action_id, attempt, error} (D-26) | PASS | All warn calls include those keys; test "structured warn fields on retry include attempt count" asserts shape |
| 3 | appendResolvedItem is module-level (not hook-bound) so in-flight promises survive unmount (D-24) | PASS | Top-level `export async function` in `src/chat/resolved-items.ts`; no React imports |
| 4 | concatChangeSummaries joins in doc order, truncates ≤200 on word/sentence boundary (D-16) | PASS | 7 specs cover empty, doc-order, short, sentence-boundary, word-boundary, hard-truncate |
| 5 | buildResolvedItemEntry derives user_action='fixed' if accepted≥1 else 'dismissed' (D-13); populates acceptance_summary (D-14) | PASS | 4 specs cover accepted=1, rejected=1, stale-only, hash/timestamp shape |
| 6 | identityKey produces stable 'section_key\|finding_type\|title' string | PASS | Test "identityKey is stable pipe-separated triple" |
| 7 | rebuildFilterSet adds both `id:{originating_action_id}` (when non-null) AND `ik:{identityKey}` (D-20) | PASS | Test "rebuildFilterSet has both id and ik for non-null originating_action_id" |
| 8 | Filter Set membership lookup matches by id (primary) or identity-key (fallback) (D-20) | PASS | Test "null originating_action_id contributes only ik key" + "multiple entries accumulate correctly" |

## Test Results

`npx vitest run src/chat/__tests__/{resolved-items,applied-changes,build-entry,filter-set}.test.ts`

| Spec File | Tests | Status |
|-----------|-------|--------|
| `src/chat/__tests__/resolved-items.test.ts` | 4 | 4/4 PASS |
| `src/chat/__tests__/applied-changes.test.ts` | 7 | 7/7 PASS |
| `src/chat/__tests__/build-entry.test.ts` | 4 | 4/4 PASS |
| `src/chat/__tests__/filter-set.test.ts` | 5 | 5/5 PASS |
| **Total** | **20** | **20/20 PASS** |

Plan minimum was 12 tests (3+6+4+4); delivered 20.

## TypeScript Check

`npx tsc --noEmit -p tsconfig.json` — Zero errors in the 5 files this plan created. Pre-existing tsc errors exist in unrelated files (`AIChatPanel.tsx`, `DocumentList.tsx`, several other component tests). Per scope-boundary rules these are not addressed; logged below.

## I2 Closure

- `grep -c "runAllTimersAsync" src/chat/__tests__/resolved-items.test.ts` = **4** (at least 1 required)
- `grep -c "advanceTimersByTimeAsync" src/chat/__tests__/resolved-items.test.ts` = **0** (must not appear)
- `grep -c "RETRY_DELAYS_MS" src/chat/__tests__/resolved-items.test.ts` = **0** (test doesn't import the private constant)

The retry test uses **`vi.runAllTimersAsync()` exclusively** for draining timer queues. No fragile `advanceTimersByTimeAsync(5000) × 3` pattern.

## Commits

| Hash | Type | Message |
|------|------|---------|
| `c2d8215` | feat | `feat(14.2.2-02): add resolved-items writer + helpers module` |
| `687f78c` | test | `test(14.2.2-02): add vitest specs for resolved-items writer + helpers` |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] Replaced `@/` import alias with relative paths**
- **Found during:** Task 1, verifying the plan's recommended `@/types/chat` imports
- **Issue:** Project `tsconfig.json` has no `compilerOptions.paths` mapping. Using `@/...` would fail TS resolution.
- **Fix:** Switched all imports to relative (`../types/chat`, `../constants/chat`, `../utils/sectionHash`). Plan 02 explicitly says "If the project's path alias for `src/` is something other than `@/`, adjust imports accordingly — check tsconfig.json before committing." → followed that guidance.
- **Files modified:** `src/chat/resolved-items.ts` and all four spec files.
- **Impact:** None functional. Plan 04 must follow the same convention for its imports.

### Auth Gates

None.

### Other Deviations

None — RESEARCH §Pattern 2 retry-delay constants `[500, 1500, 4000]` used verbatim. No deviations from §Common Ops 1, 2, 3.

## Deferred Issues (out of scope)

- `src/chat/cta-snapshot-map.test.ts` and `src/chat/ctaSnapshotMap.ts` (both untracked, NOT created by this plan) exist on the working tree. The test uses `@/chat/ctaSnapshotMap` alias which doesn't resolve under the current tsconfig — `npx vitest run src/chat/__tests__/` reports 1 failed suite from this file (the other 4 suites — all this plan's — pass with 20/20). This is pre-existing scaffolding for a future plan (likely Plan 03); intentionally left untouched per scope-boundary rules.
- 114 pre-existing tsc errors across unrelated component files (`AIChatPanel.tsx`, `DocumentList.tsx`, several `__tests__/*.tsx`). None touch this plan's surface; out of scope per Rule "only auto-fix issues DIRECTLY caused by the current task's changes."

## Known Stubs

None. All five exports have full functional implementations; the writer talks to a real RPC (mocked in tests).

## Threat Flags

None. No new network endpoints, auth paths, file access, or schema changes introduced beyond what the threat model in PLAN.md already covers (T-14.2.2-07 through T-14.2.2-11).

## Self-Check: PASSED

- FOUND: `src/chat/resolved-items.ts`
- FOUND: `src/chat/__tests__/resolved-items.test.ts`
- FOUND: `src/chat/__tests__/applied-changes.test.ts`
- FOUND: `src/chat/__tests__/build-entry.test.ts`
- FOUND: `src/chat/__tests__/filter-set.test.ts`
- FOUND commit: `c2d8215` (feat module)
- FOUND commit: `687f78c` (test specs)
- VERIFIED: 20/20 vitest specs pass on first run
- VERIFIED: 7 acceptance-criteria greps from PLAN return expected counts (1/1/1/1/1/1/1 for module; 0/4/0/3 for test grep guards)
