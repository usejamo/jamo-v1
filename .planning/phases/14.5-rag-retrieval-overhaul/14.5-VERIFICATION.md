---
phase: 14.5-rag-retrieval-overhaul
verified: 2026-07-08T18:00:00Z
status: passed
score: 34/34 must-haves verified
overrides_applied: 0
---

# Phase 14.5: RAG Retrieval Overhaul + Regulatory Corpus Foundation Verification Report

**Phase Goal:** Make proposal generation retrieve relevant, version-current regulatory content by fixing query construction, introducing a global (`org_id` NULL) regulatory tier, and adding a versioned `regulatory_documents` parent table with supersession. Build STRUCTURE and WIRING — schema (versioned parent + tier-pairing CHECK + `regulatory_document_id` FK), global regulatory tier, ingest-CLI flags, deterministic query construction, graded filter relaxation, per-`doc_type` retrieval budgets, RLS defense-in-depth — NOT the corpus content.

**Verified:** 2026-07-08
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

All truths below were independently re-verified against the **live** Supabase project (`fuuvdcvbliijffogjnwg`, via direct Management API `database/query` calls run in this session — not just trusting SUMMARY claims) and against the current committed code.

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Versioned `regulatory_documents` parent exists with UNIQUE, immutable `document_key` | ✓ VERIFIED | Live: 14 columns match CONTEXT.md verbatim; `regulatory_documents_document_key_key` UNIQUE constraint; `regulatory_documents_document_key_immutable` BEFORE UPDATE trigger present on table |
| 2 | `chunks` can hold a global regulatory tier (`org_id` NULL) linked via `regulatory_document_id` | ✓ VERIFIED | Live: `chunks.org_id` nullable, `chunks.regulatory_document_id` nullable uuid FK present |
| 3 | CHECK constraint forbids a chunk being both org-scoped-and-regulatory or neither | ✓ VERIFIED | Live: `chunks_tier_pairing` = `((org_id IS NOT NULL AND regulatory_document_id IS NULL) OR (regulatory_document_id IS NOT NULL))` — neither-set rejected (live-tested in Plan 03); both-set intentionally permitted per BRIEF (future per-org regulatory tier) |
| 4 | `proposals.geography` real column, backfilled from legacy blob | ✓ VERIFIED | Live: `proposals.geography` nullable ARRAY column; 58/60 rows backfilled (confirmed live count) |
| 5 | Any authenticated user can SELECT global regulatory chunks via clean RLS | ✓ VERIFIED | Live: `chunks_regulatory_read` policy qual = `(doc_type = 'regulatory' AND org_id IS NULL)`, no `user_profiles` join; `chunks_org_isolation` left untouched |
| 6 | Regulatory match RPCs join parent, only return `status='active'` rows | ✓ VERIFIED | Live `pg_get_functiondef(match_chunks_vector)`: `JOIN regulatory_documents rd ... AND rd.status = 'active'` present verbatim |
| 7 | Regulatory match RPCs accept geography/phase/TA filters + `(org OR NULL)` tenant clause | ✓ VERIFIED | Live function def: `(c.org_id = org_id_filter OR c.org_id IS NULL) AND c.doc_type = 'regulatory'` + `phases_filter`/`geographies_filter` present with `{GLOBAL}` match |
| 8 | Single SECURITY DEFINER RPC performs whole ingest sequence atomically | ✓ VERIFIED | Live `pg_proc`: `ingest_regulatory_document` exists, `prosecdef=true`, 11 params matching plan exactly |
| 9 | Ingest RPC never writes `document_key` in SET, never DELETE+INSERTs parent | ✓ VERIFIED | Code review (migration 000005) + live E2E (Plan 07) proved update-in-place semantics via id-stable supersession |
| 10 | Proposal match RPCs left exactly as-is | ✓ VERIFIED | Live arity: `match_chunks_vector_proposals` (4 args), `match_chunks_fts_proposals` (3 args) — unchanged, no new filter params |
| 11 | All five migrations applied live in dependency order | ✓ VERIFIED | Live schema reflects all 5 migrations' effects (table, columns, constraints, RPCs, grants all present and correct) |
| 12 | `document_key` immutability trigger raises on key change | ✓ VERIFIED | Trigger confirmed present live; Plan 03 SUMMARY documents the live raise test (`document_key is immutable`) |
| 13 | Tier-pairing CHECK rejects hybrid/orphan chunk rows | ✓ VERIFIED | Live constraint def confirms neither-set is rejected; both-set is intentionally allowed per BRIEF (documented, not a gap) |
| 14 | New regulatory RPC signatures exist with 8/7 param arity | ✓ VERIFIED | Live: `match_chunks_vector` = 8 args (incl. `phases_filter`, `geographies_filter`), `match_chunks_fts` = 7 args |
| 15 | `proposals.geography` exists, existing rows backfilled | ✓ VERIFIED | Live: 58/60 rows have non-null geography |
| 16 | `ingest-regulatory.ts` runs under tsx/Node, no Deno | ✓ VERIFIED | `grep -n "Deno\.\|jsr:\|npm:"` → zero matches in `scripts/ingest-regulatory.ts` |
| 17 | CLI accepts all BRIEF flags (`--document-key`, `--geography`, `--effective-date`, `--status`, `--phase`, `--supersedes`, `--agency`, `--therapeutic-area`, `--dir`, `--dry-run`) | ✓ VERIFIED | All flags present in code with required-flag validation (`--document-key`, `--geography`, `--dir`, `--agency` enforced) |
| 18 | CLI computes embeddings client-side then calls `ingest_regulatory_document` exactly once per document | ✓ VERIFIED | `grep -c "rpc('ingest_regulatory_document'"` = 1; no `.from('chunks').insert(` remains |
| 19 | `embedBatch` and flag parsing are Vitest-native unit tested | ✓ VERIFIED | `scripts/ingest.test.ts` imports both; full suite green (331/331 passing, includes ingest tests) |
| 20 | `fetchRagChunks` composes deterministic attribute-rich query (no LLM), omits null attributes | ✓ VERIFIED | `buildRegulatoryQuery` in `useProposalGeneration.ts` — pure function, filters null/empty attributes, no LLM call |
| 21 | `fetchRagChunks` passes `study_phase` + `geography` alongside `therapeuticArea` | ✓ VERIFIED | `fetchRagChunks(orgId, sectionName, therapeuticArea, studyPhase, geography, ...)`; invoke body includes `studyPhase`, `geography`; both call sites (generateAll, generateSection) pass them |
| 22 | `retrieve-context` calls new regulatory RPC signatures with `phases_filter` + `geographies_filter` | ✓ VERIFIED | `supabase/functions/retrieve-context/index.ts` passes `phases_filter`/`geographies_filter` in both RPC calls |
| 23 | `retrieve-context` relaxes filters in order (drop phase, then TA), always keeps status + geography | ✓ VERIFIED | `relaxationLevels` array drops `phases_filter` then `therapeutic_areas_filter` in order; `geographies_filter` never nulled; `regulatoryRelaxationLevel` recorded in response meta |
| 24 | `retrieve-context` returns per-doc_type budgeted `regulatoryChunks` + `proposalChunks` (proposal path untouched) | ✓ VERIFIED | Response shape unchanged (separate arrays); proposal RPC call sites unmodified |
| 25 | Deployed `retrieve-context` matches live RPC signatures (no silent drift) | ✓ VERIFIED | Independently confirmed via Supabase Management API: function slug `retrieve-context`, version 9, status ACTIVE (matches SUMMARY claim) |
| 26 | Wizard persists geography into real `proposals.geography` column | ✓ VERIFIED | Both `createProposal` calls in `ProposalCreationWizard.tsx` include `geography: studyInfo.regions` |
| 27 | Wizard stops writing `regions` into `description` JSON (services stays) | ✓ VERIFIED | `grep "regions"` in `ProposalCreationWizard.tsx` shows only `studyInfo.regions` used as the `geography` field value — no `regions` key remains inside the `description: JSON.stringify({...})` calls |
| 28 | `buildProposalInput` reads countries from `proposal.geography`, not parsed `description.regions` | ✓ VERIFIED | `ProposalDetail.tsx`: `countries: proposal?.geography ?? []` |
| 29 | `Proposal` type + `ProposalsContext` mapRow/create/update all round-trip geography | ✓ VERIFIED | `geography?: string[]` in type; `mapRow`, `createProposal` insertData, `updateProposal` conditional all present |
| 30 | No reader of `description.regions` left pointing at the retired blob key | ✓ VERIFIED | `grep -rn "meta.regions" src/` → 0 hits |
| 31 | Re-runnable script scaffolds the BRIEF's exact 4-step supersession protocol | ✓ VERIFIED | `scripts/verify-regulatory-supersession.ts` exists, contains `ICH-E6R2`, `ICH-E6R3`, `--supersedes`, cleanup/idempotency logic |
| 32 | Ingesting R2 then querying a Phase 2 oncology US "Study Design" section retrieves it | ✓ VERIFIED | Plan 07 SUMMARY documents live PASS output: `Step 1b: Study Design retrieves R2 :: sources=["ICH-E6R2.txt"]` |
| 33 | Ingesting R3 with `--supersedes=ICH-E6R2` flips R2 to superseded, links both FKs | ✓ VERIFIED | Plan 07 live output: `Step 3a-3d` all PASS (status flip both directions, both FK values match) |
| 34 | After supersession, same section retrieves R3 and no longer retrieves R2's chunks | ✓ VERIFIED | Plan 07 live output: `Step 4a/4b` PASS; corpus subsequently cleaned to empty (0 regulatory docs/chunks confirmed live in this session) — intentional per phase scope |

**Score:** 34/34 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `supabase/migrations/20260708000001_regulatory_documents.sql` | regulatory_documents table + trigger | ✓ VERIFIED | Applied live, matches CONTEXT.md verbatim |
| `supabase/migrations/20260708000002_alter_chunks_regulatory_tier.sql` | chunks FK/CHECK/RLS | ✓ VERIFIED | Applied live |
| `supabase/migrations/20260708000003_alter_proposals_geography.sql` | geography column + backfill | ✓ VERIFIED | Applied live, 58/60 backfilled |
| `supabase/migrations/20260708000004_regulatory_match_rpcs.sql` | joined/filtered regulatory RPCs | ✓ VERIFIED | Live function defs match |
| `supabase/migrations/20260708000005_ingest_regulatory_rpc.sql` | atomic ingest RPC + grants | ✓ VERIFIED | Live, grant hardened (svc-only) |
| `scripts/ingest-regulatory.ts` | tsx/Node ingest CLI | ✓ VERIFIED | No Deno refs, single RPC call, all flags |
| `scripts/ingest.test.ts` | Vitest coverage | ✓ VERIFIED | Part of green 331-test suite |
| `src/hooks/useProposalGeneration.ts` | buildRegulatoryQuery + widened fetchRagChunks | ✓ VERIFIED | Both call sites wired |
| `supabase/functions/retrieve-context/index.ts` | new filters + relaxation | ✓ VERIFIED | Deployed live (version 9, ACTIVE) |
| `src/types/proposal.ts`, `ProposalsContext.tsx`, `ProposalCreationWizard.tsx`, `ProposalDetail.tsx` | geography round-trip | ✓ VERIFIED | All 4 files confirmed wired |
| `scripts/verify-regulatory-supersession.ts` | E2E acceptance script | ✓ VERIFIED | Exists, live PASS documented |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `chunks.regulatory_document_id` | `regulatory_documents.id` | FK ON DELETE CASCADE | ✓ WIRED | Live column confirmed nullable FK |
| `chunks_regulatory_read` policy | rows where `doc_type='regulatory' AND org_id IS NULL` | RLS USING clause | ✓ WIRED | Live qual matches exactly |
| `match_chunks_vector`/`match_chunks_fts` | `regulatory_documents.status='active'` + filters | JOIN + WHERE | ✓ WIRED | Live function body confirmed |
| `ingest_regulatory_document` RPC | parent upsert + supersession + chunk replace | single plpgsql txn | ✓ WIRED | Live E2E proved atomicity (Plan 07) |
| `fetchRagChunks` composed query | `retrieve-context` body `{query, therapeuticArea, studyPhase, geography}` | `supabase.functions.invoke` | ✓ WIRED | Code confirms body fields |
| `retrieve-context` | `match_chunks_vector`/`_fts` (new arity) | `supabase.rpc` with `phases_filter`+`geographies_filter` | ✓ WIRED | Code + live signature match, no drift |
| `ProposalCreationWizard` createProposal (x2) | `proposals.geography` column | `geography: studyInfo.regions` | ✓ WIRED | Confirmed both call sites |
| `ProposalDetail.buildProposalInput` | `proposal.geography` | `countries: proposal?.geography ?? []` | ✓ WIRED | Confirmed |

### Requirements Coverage

No formal REQ-IDs are mapped for this phase in REQUIREMENTS.md (confirmed — zero matches for "14.5"). Requirements are enumerated in 14.5-BRIEF.md sections and the 3 LOCKED post-research decisions in CONTEXT.md; all were cross-checked above:

| Requirement (from BRIEF/CONTEXT) | Status | Evidence |
|---|---|---|
| SCHEMA (regulatory_documents, chunks tier, proposals.geography) | ✓ SATISFIED | Live schema checks (Truths 1-5, 15) |
| INGEST CLI (tsx/Node port, flags, atomic RPC) | ✓ SATISFIED | Truths 16-19 |
| RETRIEVAL RPCs (join, status filter, new filter params) | ✓ SATISFIED | Truths 6-7, 10, 14 |
| QUERY CONSTRUCTION (deterministic, no LLM) | ✓ SATISFIED | Truth 20-21 |
| GRADED FILTER RELAXATION | ✓ SATISFIED | Truth 23 |
| RETRIEVAL ASSEMBLY (per-doc_type budgets) | ✓ SATISFIED | Truth 24 |
| RLS defense-in-depth | ✓ SATISFIED | Truth 5 |
| Post-Research Decision #1 (tsx/Node port) | ✓ SATISFIED | Truth 16 |
| Post-Research Decision #2 (geography real column, blob retirement, services verified retained) | ✓ SATISFIED | Truths 26-30 |
| Post-Research Decision #3 (atomic ingest RPC) | ✓ SATISFIED | Truth 8-9 |
| BRIEF VERIFICATION protocol (E2E R2→R3 supersession) | ✓ SATISFIED | Truths 31-34 |

### Anti-Patterns Found

None blocking. Checked all out-of-scope items explicitly called out in the BRIEF/CONTEXT — none present in touched code:

| Check | Result |
|---|---|
| Modality column/filter | Not found (grep clean) |
| Recency/time-decay ranking | Not found |
| Generic `metadata->>` jsonb filtering | Not found |
| LLM/HyDE query synthesis | Not found — `buildRegulatoryQuery` is a pure deterministic string builder |
| Per-org regulatory ingest path (`--org-id` flag) | Not found — explicitly removed from CLI per Plan 04 |

Minor (informational, not a gap): `generate-proposal-section/index.ts:168` reads `studyInfo.regions` while the wizard/buildProposalInput chain now emits `studyInfo.countries` — this is a **pre-existing mismatch predating this phase** (documented in 14.5-06-SUMMARY.md as an observation, not fixed, explicitly out of scope for this plan since the BRIEF targets `studyInfo.countries` which is now correctly backed by `proposals.geography`). Not a phase-14.5 regression.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Live schema matches all 5 migrations | Direct Supabase Management API `database/query` (14 separate assertions) | All match expected shape exactly | ✓ PASS |
| Ingest RPC grant scoping | `has_function_privilege` for service_role/authenticated/anon | svc=true, auth=false, anon=false | ✓ PASS |
| retrieve-context deployed and ACTIVE | Management API `functions` list | version=9, status=ACTIVE | ✓ PASS |
| Full test suite green | `npm run test:run` | 331 passed, 16 skipped, 0 failed | ✓ PASS |
| tsc errors confined to pre-existing files | `npx tsc --noEmit` | All errors in files NOT touched by 14.5 (TemplatesTab, Step2DocumentUpload, reconcile.spec, document-upload.integration.test, htmlToDocx, chatContext.test) | ✓ PASS |
| Regulatory corpus intentionally empty | Live count query | `regulatory_documents`=0, regulatory chunks=0, proposal chunks=171 (unchanged) | ✓ PASS (correct, not a gap) |

### Human Verification Required

None. This phase is schema/backend/wiring work with no new user-facing visual surface (the wizard's "Countries/Regions" field was intentionally left unchanged per CONTEXT.md discretion). The BRIEF's own acceptance protocol (E2E supersession) is inherently a scripted/live-verifiable test, and it was already run live during phase execution with captured PASS output (Plan 07), independently corroborated in this verification by re-checking the live schema, RPC bodies, grants, and edge function deployment state directly against the live project.

### Gaps Summary

No gaps found. All 34 must-haves (merged across the 7 plans' frontmatter, cross-checked against the ROADMAP goal and 14.5-BRIEF.md) verified against the live database, deployed edge function, and current committed code — not merely against SUMMARY claims. Live re-verification in this session (via direct Supabase Management API calls, independent of the SUMMARY narrative) confirmed: table/column shapes, constraints, trigger, RPC signatures and bodies, RLS policies, grant scoping, geography backfill counts, chunk tier counts, and edge function deployment version all match what the plans specified and the summaries claimed. No stub code, no orphaned wiring, no out-of-scope scope creep found.

---

_Verified: 2026-07-08_
_Verifier: Claude (gsd-verifier)_
