---
phase: 14.5-rag-retrieval-overhaul
plan: 07
subsystem: testing
tags: [e2e, verification, supersession, rag, regulatory, tsx]

requires:
  - phase: 14.5-03
    provides: applied schema + RPCs
  - phase: 14.5-04
    provides: ingest embedBatch + RPC path
  - phase: 14.5-05
    provides: deployed retrieve-context + match RPC signatures
provides:
  - scripts/verify-regulatory-supersession.ts — re-runnable BRIEF 4-step acceptance protocol
  - live phase acceptance evidence (supersession + retrieval switch proven end to end)
affects: []

tech-stack:
  added: []
  patterns:
    - "Scripted, idempotent E2E acceptance test using fixture text (verifies wiring, not corpus)"

key-files:
  created:
    - scripts/verify-regulatory-supersession.ts
  modified: []

key-decisions:
  - "Ingest via the atomic RPC with fixture text (no KB PDFs committed) — verifies wiring per plan"
  - "Retrieval asserted via direct match_chunks_vector RPC with a low threshold (fixture-friendly)"
  - "Cleaned up fixture ICH-E6R2/R3 rows after the pass — phase builds structure, not corpus; corpus left empty"

patterns-established:
  - "E2E distinguishes documents by per-doc chunk source; asserts both FK directions + status on supersession"

requirements-completed: [VERIFICATION, INGEST-CLI, RETRIEVAL-RPC]

duration: ~15 min
completed: 2026-07-08
---

# Phase 14.5 Plan 07: Live E2E Supersession Summary

**The BRIEF's exact 4-step supersession protocol, scripted and run live: ingest ICH-E6R2 → retrieve it → ingest ICH-E6R3 (supersedes R2) → status flips with both FKs linked → retrieval switches to R3 and excludes the superseded R2. All steps PASSED.**

## Performance

- **Duration:** ~15 min
- **Completed:** 2026-07-08
- **Tasks:** 2 (1 script + 1 live checkpoint)
- **Files modified:** 1 (created)

## Accomplishments
- `scripts/verify-regulatory-supersession.ts`: re-runnable, idempotent (cleans prior rows first), 8 per-step assertions, non-zero exit on failure.
- Ran live against `fuuvdcvbliijffogjnwg` — full stack (schema + ingest RPC + embeddings + match RPC + status filter) proven coherent.

## Task Commits

1. **Task 1: scripted 4-step protocol** — `feat(14.5-07): scripted BRIEF 4-step supersession acceptance protocol`
2. **Task 2: [BLOCKING] live E2E run** — verification run (no repo change)

## Live 4-Step PASS Output

```
Cleaned up any prior ICH-E6R2/R3 rows.

PASS — Step 1a: ICH-E6R2 ingested active :: status=active
PASS — Step 1b: Study Design retrieves R2 :: sources=["ICH-E6R2.txt"]
PASS — Step 3a: R2 -> superseded :: status=superseded
PASS — Step 3b: R2.superseded_by = R3.id :: 443aeb63... vs 443aeb63...
PASS — Step 3c: R3 active :: status=active
PASS — Step 3d: R3.supersedes = R2.id :: 01fc13af... vs 01fc13af...
PASS — Step 4a: Study Design now retrieves R3 :: sources=["ICH-E6R3.txt"]
PASS — Step 4b: R2 chunks no longer retrieved :: sources=["ICH-E6R3.txt"]

ALL STEPS PASSED ✓
```

## Files Created/Modified
- `scripts/verify-regulatory-supersession.ts` — scripted BRIEF acceptance protocol

## Decisions Made
- Used fixture "Study Design" excerpts (no authoritative ICH PDFs are committed); the phase verifies wiring, not corpus completeness (per BRIEF + plan).
- Post-run cleanup of the fixture ICH-E6R2/R3 rows (see below).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Data hygiene] Clean up fixture regulatory rows after the pass**
- **Found during:** Task 2 (post-run)
- **Issue:** The E2E left fixture ICH-E6R2/R3 documents + chunks in the LIVE regulatory corpus. Those are test text, not authoritative ICH content — real Phase 2 oncology proposal generation would retrieve them.
- **Fix:** Deleted both fixture parents (CASCADE removed their chunks) after capturing the pass. Verified: regulatory_documents=0, regulatory chunks=0, proposal chunks=171 (unchanged). Corpus restored to its intended empty state (real ICH docs get ingested by an operator later).
- **Verification:** execute_sql count query post-cleanup.

---

**Total deviations:** 1 (data hygiene). **Impact:** Prevents test fixtures polluting live regulatory retrieval; corpus left clean.

## Issues Encountered
None — all four protocol steps passed on the first live run.

## User Setup Required
None (used .env OPENAI_API_KEY + SUPABASE_SERVICE_ROLE_KEY). To populate the real corpus later:
`npx tsx scripts/ingest-regulatory.ts --document-key=<key> --agency=ICH --geography=US,EU,GLOBAL --dir=<pdf-dir> [--supersedes=<predecessor-key>]`

## Next Phase Readiness
- **Phase 14.5 acceptance proven.** Structure + wiring for version-current regulatory retrieval is live and verified end to end. The regulatory corpus is intentionally empty pending operator ingestion of authoritative ICH/FDA/EMA documents.

---
*Phase: 14.5-rag-retrieval-overhaul*
*Completed: 2026-07-08*
