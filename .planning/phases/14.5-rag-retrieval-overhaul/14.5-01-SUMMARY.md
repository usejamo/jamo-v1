---
phase: 14.5-rag-retrieval-overhaul
plan: 01
subsystem: database
tags: [postgres, supabase, migrations, rls, ddl, regulatory, rag]

requires:
  - phase: 14.5
    provides: 14.5-BRIEF.md schema decisions + CONTEXT.md post-research decisions
provides:
  - regulatory_documents versioned parent table with UNIQUE immutable document_key
  - chunks global regulatory tier (regulatory_document_id FK, nullable org_id, tier-pairing CHECK)
  - clean chunks_regulatory_read RLS SELECT policy (defense-in-depth)
  - proposals.geography text[] column + one-time regions backfill from description JSON
affects: [14.5-02, 14.5-03, 14.5-04, 14.5-06, 14.5-07]

tech-stack:
  added: []
  patterns:
    - "Immutable identity key enforced via BEFORE UPDATE trigger in private schema"
    - "Tier-pairing CHECK constraint to prevent hybrid/orphan chunk rows"
    - "Row-by-row JSONB backfill with per-row EXCEPTION handling for unparseable data"

key-files:
  created:
    - supabase/migrations/20260708000001_regulatory_documents.sql
    - supabase/migrations/20260708000002_alter_chunks_regulatory_tier.sql
    - supabase/migrations/20260708000003_alter_proposals_geography.sql
  modified: []

key-decisions:
  - "regulatory_documents has no RLS — parent metadata is read via SECURITY DEFINER RPC join, no direct client read path this phase"
  - "New regulatory-read RLS policy uses clean FOR SELECT TO authenticated shape, NOT the broken chunks_org_isolation pattern"
  - "geography column is nullable; write-side rewire deferred to Plan 06 (authoring only here)"

patterns-established:
  - "document_key immutability via private.enforce_regulatory_document_key_immutable() BEFORE UPDATE trigger"
  - "chunks_tier_pairing CHECK: (org_id NOT NULL AND reg_doc_id NULL) OR (reg_doc_id NOT NULL)"

requirements-completed: [SCHEMA-REGDOCS, SCHEMA-CHUNKS, SCHEMA-GEO, SCHEMA-RLS]

duration: ~8 min
completed: 2026-07-08
---

# Phase 14.5 Plan 01: Schema Migrations Summary

**Three authored SQL migrations establishing the versioned regulatory_documents parent table (immutable document_key), the global regulatory chunk tier with a tier-pairing CHECK + clean RLS read policy, and a real proposals.geography column with a safe one-time backfill.**

## Performance

- **Duration:** ~8 min
- **Completed:** 2026-07-08
- **Tasks:** 3
- **Files modified:** 3 (created)

## Accomplishments
- `regulatory_documents` table with all 14 columns, UNIQUE `document_key`, status CHECK, self-referential supersedes/superseded_by FKs, and a BEFORE UPDATE immutability trigger on `document_key`.
- `chunks` altered: `regulatory_document_id` FK (ON DELETE CASCADE), `org_id` made nullable, `chunks_tier_pairing` CHECK, index on the new FK, and the additive `chunks_regulatory_read` RLS SELECT policy.
- `proposals.geography text[]` added with a row-by-row backfill parsing `regions` out of the legacy `description` JSON blob (guarded + per-row exception-safe).

## Task Commits

1. **Task 1: regulatory_documents table + immutability trigger** — `0ec82c8` (feat)
2. **Task 2: chunks regulatory tier + tier-pairing CHECK + RLS** — `3d6a439` (feat)
3. **Task 3: proposals.geography column + regions backfill** — `cd30be7` (feat)

## Files Created/Modified
- `supabase/migrations/20260708000001_regulatory_documents.sql` — versioned parent table + document_key immutability trigger
- `supabase/migrations/20260708000002_alter_chunks_regulatory_tier.sql` — regulatory_document_id FK, nullable org_id, tier-pairing CHECK, regulatory-read RLS
- `supabase/migrations/20260708000003_alter_proposals_geography.sql` — geography text[] + one-time regions backfill

## Decisions Made
- None beyond the plan — executed verbatim from CONTEXT.md/BRIEF DDL.

## Deviations from Plan

None - plan executed exactly as written.

(Minor: reworded an inline SQL comment to remove the literal string `user_profiles` so the migration file satisfies the acceptance criterion that the string be absent — no functional change.)

## Issues Encountered
None.

## User Setup Required
None - migrations are applied live in Plan 03.

## Next Phase Readiness
- SQL authored and committed. **Not yet applied to the live DB** — Plan 02 authors the RPC migrations, Plan 03 applies all five in order.

---
*Phase: 14.5-rag-retrieval-overhaul*
*Completed: 2026-07-08*
