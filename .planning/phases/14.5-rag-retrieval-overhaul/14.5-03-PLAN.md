---
phase: 14.5-rag-retrieval-overhaul
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified: []
autonomous: true
requirements: [APPLY, SCHEMA-REGDOCS, SCHEMA-CHUNKS, SCHEMA-GEO, SCHEMA-RLS, RETRIEVAL-RPC, INGEST-RPC]
must_haves:
  truths:
    - "All five 14.5 migrations are applied live to project fuuvdcvbliijffogjnwg in dependency order"
    - "The document_key immutability trigger raises on an attempted key change"
    - "The chunks tier-pairing CHECK rejects a hybrid or orphan chunk row"
    - "The new regulatory match RPC signatures exist with the 8/7 param arity"
    - "proposals.geography exists and existing rows are backfilled from the legacy blob"
  artifacts:
    - path: "(live database schema)"
      provides: "applied 14.5 schema + RPCs"
      contains: "regulatory_documents, chunks.regulatory_document_id, proposals.geography, ingest_regulatory_document"
  key_links:
    - from: "Supabase MCP apply_migration"
      to: "live project fuuvdcvbliijffogjnwg"
      via: "MCP tool (NOT supabase db push — history diverged)"
      pattern: "apply_migration"
---

<objective>
Apply the five 14.5 migrations to the live Supabase project in strict dependency order, then run live schema smoke checks proving the trigger, CHECK, RPC signatures, column, and backfill all landed correctly.

Purpose: Build/type checks pass WITHOUT applying (types come from config, not the live DB), which is a false-positive verification state. This plan is the [BLOCKING] gate that makes the schema real before any code (CLI, edge) runs against it.
Output: Live schema changes + a verification record in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.5-rag-retrieval-overhaul/14.5-CONTEXT.md
@.planning/phases/14.5-rag-retrieval-overhaul/14.5-RESEARCH.md

<interfaces>
<!-- Deployment constraints (LOCKED, repo-specific):
     Migrations apply via Supabase MCP apply_migration — NOT `supabase db push` (history diverged).
     Project ref: fuuvdcvbliijffogjnwg -->
Migration files authored in Plans 01+02 (apply in THIS order):
  1. 20260708000001_regulatory_documents.sql   (parent table + trigger)
  2. 20260708000002_alter_chunks_regulatory_tier.sql  (needs #1 for the FK)
  3. 20260708000003_alter_proposals_geography.sql     (independent)
  4. 20260708000004_regulatory_match_rpcs.sql  (needs #1 + #2 — joins parent, reads new columns)
  5. 20260708000005_ingest_regulatory_rpc.sql  (needs #1 + #2 — writes parent + chunks)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Apply all five migrations via Supabase MCP apply_migration (in order)</name>
  <files></files>
  <read_first>
    - supabase/migrations/20260708000001_regulatory_documents.sql
    - supabase/migrations/20260708000002_alter_chunks_regulatory_tier.sql
    - supabase/migrations/20260708000003_alter_proposals_geography.sql
    - supabase/migrations/20260708000004_regulatory_match_rpcs.sql
    - supabase/migrations/20260708000005_ingest_regulatory_rpc.sql
    - .planning/phases/14.5-rag-retrieval-overhaul/14.5-CONTEXT.md (### Deployment constraints)
  </read_first>
  <action>
Read each migration file's SQL and apply it via the Supabase MCP `apply_migration` tool (name = migration filename without extension, query = file contents), STRICTLY in this order: 20260708000001 → 000002 → 000003 → 000004 → 000005. Do NOT use `supabase db push` (migration history has diverged in this repo — it will fail).

If any apply returns an error, STOP and report it (do not continue to later migrations that depend on the failed one). After all five apply cleanly, regenerate database types if the repo convention requires it (types are generated from the live DB; run the repo's type-gen command if present, e.g. `npx supabase gen types typescript --project-id fuuvdcvbliijffogjnwg > src/types/database.types.ts`). If type-gen is not part of this repo's flow for RPCs, note that in the SUMMARY.
  </action>
  <acceptance_criteria>
    - Supabase MCP `list_tables` (or execute_sql on information_schema) shows table `regulatory_documents` exists
    - `execute_sql` on information_schema.columns shows `chunks.regulatory_document_id` and `proposals.geography` both exist
    - `execute_sql` querying pg_proc shows `ingest_regulatory_document` exists
    - No apply step returned an error
  </acceptance_criteria>
  <verify>
    <automated>Supabase MCP execute_sql: `SELECT to_regclass('public.regulatory_documents') IS NOT NULL AS reg_docs, EXISTS(SELECT 1 FROM information_schema.columns WHERE table_name='chunks' AND column_name='regulatory_document_id') AS chunk_fk, EXISTS(SELECT 1 FROM information_schema.columns WHERE table_name='proposals' AND column_name='geography') AS geo, EXISTS(SELECT 1 FROM pg_proc WHERE proname='ingest_regulatory_document') AS ingest_rpc;` — all four columns TRUE</automated>
  </verify>
  <done>All five migrations applied live in order; regulatory_documents, chunks.regulatory_document_id, proposals.geography, and ingest_regulatory_document all confirmed present.</done>
</task>

<task type="auto">
  <name>Task 2: Live schema smoke — trigger raises, CHECK rejects, RPC arity, backfill populated</name>
  <files></files>
  <read_first>
    - .planning/phases/14.5-rag-retrieval-overhaul/14.5-RESEARCH.md (## Validation Architecture — Phase Requirements → Test Map, SQL/integration rows)
    - .planning/phases/14.5-rag-retrieval-overhaul/14.5-BRIEF.md (## SCHEMA)
  </read_first>
  <action>
Run these live assertions via Supabase MCP `execute_sql`. Each must produce the expected outcome; capture results in the SUMMARY.

1. Immutability trigger: insert a temp regulatory_documents row (document_key='SMOKE-KEY', title/agency/source dummy, geography='{GLOBAL}'), then attempt `UPDATE regulatory_documents SET document_key='SMOKE-KEY-2' WHERE document_key='SMOKE-KEY'` — MUST raise `document_key is immutable`. Then confirm a benign `UPDATE ... SET title='x' WHERE document_key='SMOKE-KEY'` succeeds. Clean up (DELETE the temp row).
2. Tier-pairing CHECK: attempt `INSERT INTO chunks (org_id, doc_type, source, content) VALUES (NULL, 'proposal', 's', 'c')` (regulatory_document_id NULL AND org_id NULL) — MUST fail chunks_tier_pairing. Attempt an insert with BOTH org_id set AND regulatory_document_id set — MUST also fail.
3. RPC arity: `SELECT pg_get_function_identity_arguments(oid) FROM pg_proc WHERE proname IN ('match_chunks_vector','match_chunks_fts')` — MUST include `phases_filter` and `geographies_filter`. Confirm `match_chunks_vector_proposals`/`match_chunks_fts_proposals` arguments are UNCHANGED (no phases_filter/geographies_filter).
4. RLS policy: `SELECT polname FROM pg_policy WHERE polrelid='chunks'::regclass` — MUST include `chunks_regulatory_read`; also confirm its qual has no reference to user_profiles.
5. Grant: `SELECT has_function_privilege('service_role','public.ingest_regulatory_document(text,text,text,text,text[],text[],date,text,text,text,jsonb)','EXECUTE')` = true; and `has_function_privilege('authenticated', <same>, 'EXECUTE')` = false.
6. Backfill: `SELECT count(*) FROM proposals WHERE geography IS NOT NULL` — record the count; spot-check one row's geography matches its old description.regions.
  </action>
  <acceptance_criteria>
    - Assertion 1: document_key UPDATE raises; title UPDATE succeeds
    - Assertion 2: both malformed chunk inserts violate chunks_tier_pairing
    - Assertion 3: regulatory RPCs expose phases_filter + geographies_filter; _proposals RPCs do not
    - Assertion 4: chunks_regulatory_read policy exists with no user_profiles reference
    - Assertion 5: service_role has EXECUTE; authenticated does not
    - Assertion 6: geography backfill count recorded (>= 0; if prior proposals had regions, > 0)
  </acceptance_criteria>
  <verify>
    <automated>Supabase MCP execute_sql for each numbered assertion above; all expected outcomes observed and captured in SUMMARY</automated>
  </verify>
  <done>All six live smoke assertions pass; results recorded in the SUMMARY as the schema-landed evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor -> live production DB | apply_migration writes directly to the live project; wrong order or db push corrupts schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.5-08 | Tampering/Integrity | migration apply order | mitigate | Strict documented order (parent → chunks → proposals → match RPCs → ingest RPC); stop-on-error prevents dependent migrations applying against a failed predecessor. |
| T-14.5-09 | Denial of Service | wrong deploy mechanism | mitigate | apply_migration only; `supabase db push` explicitly forbidden (diverged history would fail/partially apply). |
| T-14.5-10 | Elevation of Privilege | ingest RPC grant | mitigate | Assertion 5 actively verifies authenticated has NO execute right on the ingest RPC. |
</threat_model>

<verification>
- All five migrations applied in order with no errors.
- Six live smoke assertions produce their expected outcomes (recorded in SUMMARY).
- No `supabase db push` used.
</verification>

<success_criteria>
- Live schema matches the authored migrations: parent table, chunk FK + CHECK + RLS, proposals.geography, both RPC sets.
- Immutability trigger, tier CHECK, RPC arity, RLS policy shape, and ingest-grant scoping all live-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/14.5-rag-retrieval-overhaul/14.5-03-SUMMARY.md` including the raw results of the six smoke assertions.
</output>
