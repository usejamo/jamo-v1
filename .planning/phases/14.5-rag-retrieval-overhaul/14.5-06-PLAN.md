---
phase: 14.5-rag-retrieval-overhaul
plan: 06
type: execute
wave: 3
depends_on: [03]
files_modified:
  - src/types/wizard.ts
  - src/types/proposal.ts
  - src/context/ProposalsContext.tsx
  - src/components/ProposalCreationWizard.tsx
  - src/pages/ProposalDetail.tsx
  - src/components/wizard/Step1StudyInfo.tsx
autonomous: true
requirements: [GEO-WIRING, SCHEMA-GEO]
must_haves:
  truths:
    - "The wizard persists geography into the real proposals.geography column, not the description JSON blob"
    - "The wizard stops writing regions into proposals.description (services stays — verified read path)"
    - "buildProposalInput reads countries from proposal.geography, not from parsed description.regions"
    - "Proposal type + ProposalsContext mapRow/create/update all round-trip geography"
    - "No reader of description.regions is left pointing at the retired blob key (audited)"
  artifacts:
    - path: "src/context/ProposalsContext.tsx"
      provides: "geography round-trip in mapRow/createProposal/updateProposal"
      contains: "geography"
    - path: "src/pages/ProposalDetail.tsx"
      provides: "buildProposalInput reads proposal.geography"
      contains: "proposal?.geography"
  key_links:
    - from: "ProposalCreationWizard createProposal calls (x2)"
      to: "proposals.geography column"
      via: "geography: studyInfo.regions in insertData"
      pattern: "geography: studyInfo"
    - from: "ProposalDetail buildProposalInput"
      to: "proposal.geography"
      via: "countries mapping"
      pattern: "countries: proposal\\?\\.geography"
---

<objective>
Rewire the geography persistence chain so the wizard writes to the real proposals.geography column and readers consume it — retiring the regions-in-description blob atomically. Includes the LOCKED blocking audit of every description-JSON reader.

Purpose: Post-Research Decision #2. The column + backfill already landed (Plans 01/03); this closes the loop on the write and read sides in one change so there is no silent empty-regions regression (Pitfall 5).
Output: 6 files rewired; description.regions retired; services blob confirmed and retained.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.5-rag-retrieval-overhaul/14.5-CONTEXT.md
@.planning/phases/14.5-rag-retrieval-overhaul/14.5-PATTERNS.md
@.planning/phases/14.5-rag-retrieval-overhaul/14.5-RESEARCH.md

<interfaces>
<!-- The full chain (RESEARCH item 7/8, PATTERNS excerpts):
 1. src/types/wizard.ts StudyInfo.regions: string[] (line 13); DEFAULT_WIZARD_STATE.studyInfo.regions = []
 2. src/types/proposal.ts Proposal: no geography field (add geography?: string[])
 3. src/context/ProposalsContext.tsx:
      mapRow (9-25): row.snake_case ?? default  -> add geography: row.geography ?? []
      createProposal insertData (73-85)          -> add geography: data.geography ?? null
      updateProposal conditional (103-112)       -> add if (data.geography !== undefined) updateData.geography = data.geography
 4. src/components/ProposalCreationWizard.tsx: TWO createProposal calls (143-153, 204-214), both currently
      description: JSON.stringify({ services: studyInfo.services, regions: studyInfo.regions })
 5. src/pages/ProposalDetail.tsx buildProposalInput (410-428): countries: meta.regions ?? []  (repoint)
 6. supabase/functions/generate-proposal-section/index.ts:263 reads studyInfo.countries (downstream consumer — no change needed)
 7. src/components/wizard/Step1StudyInfo.tsx (146-160): "Countries / Regions" free-text comma-split -> studyInfo.regions
    (UI may stay as-is per CONTEXT discretion; it already yields string[])
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add geography to Proposal type + ProposalsContext round-trip (mapRow/create/update)</name>
  <files>src/types/proposal.ts, src/context/ProposalsContext.tsx, src/types/wizard.ts</files>
  <read_first>
    - src/types/proposal.ts (Proposal interface — optional-field convention)
    - src/context/ProposalsContext.tsx (mapRow 9-25, createProposal 70-97, updateProposal 99-118)
    - src/types/wizard.ts (StudyInfo 8-16, DEFAULT_WIZARD_STATE)
    - .planning/phases/14.5-rag-retrieval-overhaul/14.5-PATTERNS.md (proposal.ts / ProposalsContext / wizard.ts sections)
  </read_first>
  <action>
- src/types/proposal.ts: add `geography?: string[]` to the Proposal interface, following the existing optional-field convention (indication?, description?, selected_template_id?).
- src/context/ProposalsContext.tsx:
  - mapRow: add `geography: row.geography ?? [],` (same `row.x ?? default` idiom).
  - createProposal insertData: add `geography: data.geography ?? null,`.
  - updateProposal conditional builder: add `if (data.geography !== undefined) updateData.geography = data.geography`.
- src/types/wizard.ts: keep StudyInfo.regions: string[] as the wizard-internal field name (UI unchanged per discretion). No rename required; ensure DEFAULT_WIZARD_STATE.studyInfo.regions stays `[]`. (The camelCase→column mapping to `geography` happens at the createProposal call in Task 2, passing studyInfo.regions as the geography field.)
  </action>
  <acceptance_criteria>
    - `geography?: string[]` present in Proposal interface
    - `geography: row.geography ?? []` present in mapRow
    - `geography: data.geography ?? null` present in createProposal insertData
    - `if (data.geography !== undefined) updateData.geography = data.geography` present in updateProposal
    - tsc for the touched files is clean
  </acceptance_criteria>
  <verify>
    <automated>grep -c "geography" src/types/proposal.ts src/context/ProposalsContext.tsx; npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <done>Proposal type carries geography and ProposalsContext reads/writes it across mapRow, create, and update.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING AUDIT] Rewire wizard writes + reader; retire regions blob; confirm services read path</name>
  <files>src/components/ProposalCreationWizard.tsx, src/pages/ProposalDetail.tsx, src/components/wizard/Step1StudyInfo.tsx</files>
  <read_first>
    - src/components/ProposalCreationWizard.tsx (both createProposal calls 143-153, 204-214)
    - src/pages/ProposalDetail.tsx (buildProposalInput 410-428)
    - src/components/wizard/Step1StudyInfo.tsx (Countries/Regions field 146-160)
    - .planning/phases/14.5-rag-retrieval-overhaul/14.5-CONTEXT.md (Post-Research Decision #2 — VERIFY FIRST + Also verify services)
    - .planning/phases/14.5-rag-retrieval-overhaul/14.5-RESEARCH.md (item 7/8 — full chain; Pitfall 5)
  </read_first>
  <action>
BLOCKING AUDIT FIRST (do before editing): grep the whole src/ tree for every reader/writer of the description JSON blob and of `.regions`:
  `grep -rn "description" src/pages src/components src/context | grep -i "JSON.parse\|JSON.stringify\|regions\|services"`
Enumerate every hit in the SUMMARY. Known hits: ProposalCreationWizard (2 writers), ProposalDetail.buildProposalInput (reader of meta.regions + meta.services). Confirm whether `proposals.services_requested` is the actual read path for services or whether services is read from the description blob:
  `grep -rn "services_requested\|meta.services\|studyInfo.services" src/ supabase/functions`
Per RESEARCH: buildProposalInput reads meta.services from the description blob (NOT services_requested), and generate-proposal-section reads studyInfo.services — so SERVICES STAYS in the description blob. Record this as the verified decision. If the audit contradicts RESEARCH (services actually read from services_requested), then ALSO drop services from the blob and note it; otherwise retain services and leave a one-line "services-only description blob retained; future cleanup" note in the SUMMARY.

THEN edit:
- ProposalCreationWizard.tsx, BOTH createProposal calls: add `geography: studyInfo.regions,` to the object, AND change `description: JSON.stringify({ services: studyInfo.services, regions: studyInfo.regions })` to `description: JSON.stringify({ services: studyInfo.services })` (drop the regions key — retire it from the blob).
- ProposalDetail.tsx buildProposalInput: change `countries: meta.regions ?? []` to `countries: proposal?.geography ?? []`. Keep parsing `meta.services` from description unchanged. Keep the useCallback([proposal]) deps (proposal.geography is covered).
- Step1StudyInfo.tsx: no functional change required (free-text comma-split producing string[] is retained per discretion). Optional: leave the label as "Countries / Regions".
  </action>
  <acceptance_criteria>
    - SUMMARY lists the audit results (every description/regions/services reader enumerated) and the verified services decision
    - both createProposal calls include `geography: studyInfo.regions`
    - neither createProposal `description:` JSON.stringify contains the `regions` key any longer
    - `services: studyInfo.services` still present in both description JSON.stringify calls (services retained)
    - buildProposalInput uses `countries: proposal?.geography ?? []` and no longer reads `meta.regions`
    - grep for `meta.regions` across src/ returns zero hits
    - tsc clean; `npm run test:run` green
  </acceptance_criteria>
  <verify>
    <automated>grep -rn "meta.regions" src/ | wc -l; grep -c "geography: studyInfo.regions" src/components/ProposalCreationWizard.tsx; grep -c "countries: proposal?.geography" src/pages/ProposalDetail.tsx; npm run test:run 2>&1 | tail -12</automated>
  </verify>
  <done>Wizard writes geography to the real column and stops writing regions to description; buildProposalInput reads proposal.geography; services blob confirmed/retained; no reader left on the retired regions key.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard client -> proposals.geography (via ProposalsContext insert/update) | user-entered geography persisted; RLS on proposals unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.5-18 | Tampering/Correctness | silent empty-regions regression | mitigate | Write-side (wizard) and read-side (buildProposalInput) are rewired in the SAME plan/task; the blocking audit enumerates all description readers so none is left on the retired key (Pitfall 5). |
| T-14.5-19 | Information Disclosure | proposals org scoping | accept | geography persists via the existing ProposalsContext insert/update path; proposals RLS/org isolation is unchanged by this plan. |
</threat_model>

<verification>
- `npm run test:run` green; tsc clean.
- Audit results + verified services decision recorded in SUMMARY.
- geography round-trips wizard → column → buildProposalInput → studyInfo.countries → generate-proposal-section.
- No `meta.regions` reader remains; regions key gone from description writes; services retained.
</verification>

<success_criteria>
- Geography is a first-class column round-tripped end to end.
- The regions-in-description blob is retired without regression; services intentionally retained.
</success_criteria>

<output>
After completion, create `.planning/phases/14.5-rag-retrieval-overhaul/14.5-06-SUMMARY.md` including the description-reader audit table.
</output>
