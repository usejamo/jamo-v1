---
phase: 14.5-rag-retrieval-overhaul
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - scripts/ingest-regulatory.ts
  - scripts/ingest.test.ts
autonomous: true
requirements: [INGEST-CLI, INGEST-RPC]
must_haves:
  truths:
    - "scripts/ingest-regulatory.ts runs under tsx/Node (no Deno) following backfill-proposal-chunks.ts"
    - "The CLI accepts --document-key, --geography, --effective-date, --status, --phase, --supersedes, --agency, --therapeutic-area, --dir, --dry-run"
    - "Per run the CLI computes embeddings client-side then calls ingest_regulatory_document exactly once per document"
    - "embedBatch and flag parsing are unit-tested Vitest-native (no live DB, no Deno)"
  artifacts:
    - path: "scripts/ingest-regulatory.ts"
      provides: "tsx/Node regulatory ingest CLI calling the atomic RPC"
      contains: "ingest_regulatory_document"
    - path: "scripts/ingest.test.ts"
      provides: "Vitest coverage for embedBatch (fetch-mocked) + flag parsing"
      contains: "embedBatch"
  key_links:
    - from: "scripts/ingest-regulatory.ts"
      to: "supabase.rpc('ingest_regulatory_document', {...})"
      via: "single service-role RPC call per document"
      pattern: "rpc\\('ingest_regulatory_document'"
    - from: "embedBatch"
      to: "OpenAI /v1/embeddings"
      via: "raw fetch (no openai npm package)"
      pattern: "api.openai.com/v1/embeddings"
---

<objective>
Port scripts/ingest-regulatory.ts from Deno to tsx/Node (LOCKED Post-Research Decision #1), add the new BRIEF flags, and route the write through the atomic ingest_regulatory_document RPC. Update ingest.test.ts to Vitest-native coverage.

Purpose: Deno is not installed here; the BRIEF's E2E verification (Plan 07) must run the CLI twice in this dev env. Converging on tsx/Node makes it runnable and Vitest-testable.
Output: Rewritten CLI + updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.5-rag-retrieval-overhaul/14.5-BRIEF.md
@.planning/phases/14.5-rag-retrieval-overhaul/14.5-CONTEXT.md
@.planning/phases/14.5-rag-retrieval-overhaul/14.5-PATTERNS.md

<interfaces>
<!-- Proven tsx/Node analog: scripts/backfill-proposal-chunks.ts (loadEnv, embedBatch raw-fetch, --flag= parsing) -->
<!-- Shared chunker: src/lib/chunker.ts exports chunkDocument (section-heading regex, 400-600 token windows,
     100-token overlap, cl100k_base). Reuse as-is. -->
<!-- pdf-parse is a real npm dependency; use `import pdfParse from 'pdf-parse'` + node:fs.readFileSync.
     Do NOT add the `openai` npm package — use raw fetch per backfill-proposal-chunks.ts. -->
<!-- Ingest RPC contract (Plan 02): ingest_regulatory_document(
     p_document_key text, p_title text, p_agency text, p_therapeutic_area text, p_phase text[],
     p_geography text[], p_effective_date date, p_status text, p_source text,
     p_supersedes_document_key text, p_chunks jsonb) RETURNS uuid
     p_chunks element shape: { content, embedding (number[]), guideline_type, source, token_count } -->
<!-- Env: SUPABASE service-role client (VITE_SUPABASE_URL/service role key) + OPENAI_API_KEY, from .env -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port ingest-regulatory.ts to tsx/Node with new flags + atomic RPC call</name>
  <files>scripts/ingest-regulatory.ts</files>
  <read_first>
    - scripts/ingest-regulatory.ts (current Deno version — preserve exported pure embedBatch + import guard intent)
    - scripts/backfill-proposal-chunks.ts (verbatim analog: loadEnv 30-49, embedBatch 51-89, --flag= parsing 93-95)
    - src/lib/chunker.ts (chunkDocument — reuse unchanged)
    - .planning/phases/14.5-rag-retrieval-overhaul/14.5-PATTERNS.md (ingest-regulatory.ts rewrite section)
    - .planning/phases/14.5-rag-retrieval-overhaul/14.5-RESEARCH.md (item 6 + Pitfall 1)
  </read_first>
  <behavior>
    - parseFlags(argv): extracts --document-key (required), --geography (required, comma-split string[]), --effective-date, --status (default 'active'), --phase (comma-split), --supersedes, --agency, --therapeutic-area, --dir (required), --dry-run (boolean). Missing --document-key OR --geography OR --dir -> exits non-zero with a clear message.
    - embedBatch(texts, apiKey): batches of 100, POSTs api.openai.com/v1/embeddings, exp backoff on 429/5xx, asserts each embedding length === 1536, preserves input order.
    - buildRpcArgs: maps parsed flags + embedded chunks into the ingest_regulatory_document param object; p_chunks = array of {content, embedding, guideline_type, source, token_count}.
    - --dry-run: parses + chunks + (optionally) embeds but does NOT call the RPC; prints intended parent metadata + chunk count.
  </behavior>
  <action>
Rewrite the file for tsx/Node, mirroring backfill-proposal-chunks.ts:
- Replace Deno APIs: `Deno.args` -> `process.argv.slice(2)`; `Deno.env` -> loadEnv() (copy verbatim from backfill 30-49); `Deno.readFile`/`jsr:@std/fs/walk` -> `node:fs` readdirSync/readFileSync; drop all `jsr:`/`npm:` dynamic imports.
- PDF: `import pdfParse from 'pdf-parse'` + readFileSync. Read every PDF in --dir; all chunks belong to the one --document-key document.
- embedBatch: swap the Deno OpenAI-client version for the raw-fetch apiKey version from backfill (same batch=100 / backoff / 1536-dim assert). Keep it EXPORTED and pure so ingest.test.ts imports it.
- Flags: use the `args.find(a => a.startsWith('--x='))?.split('=')[1]` idiom for every flag. --geography and --phase comma-split into string[]. --status defaults 'active'. REMOVE --org-id (regulatory tier is global, org_id NULL).
- Chunking: reuse chunkDocument unchanged; guideline_type = chunk.sectionRef ?? null; each chunk source = filename.
- Write path: replace the old flat `.insert(rows)` with ONE `await supabase.rpc('ingest_regulatory_document', { p_document_key, p_title, p_agency, p_therapeutic_area, p_phase, p_geography, p_effective_date, p_status, p_source, p_supersedes_document_key, p_chunks })` per document. p_title defaults to --document-key if no title flag; p_source = primary filename. Pass embeddings as number[][].
- CLI entry guard so importing for tests does not run main() (Node equivalent of import.meta.main, e.g. compare fileURLToPath(import.meta.url) to process.argv[1], or reuse backfill's guard).
- Preserve clear-error console style; exit non-zero on RPC error.
  </action>
  <acceptance_criteria>
    - the file contains NO `Deno.` references and NO `jsr:`/`npm:` import specifiers
    - `import pdfParse from 'pdf-parse'` present; `openai` package NOT imported
    - `api.openai.com/v1/embeddings` present; `1536` dim assertion present
    - all six new flags present as literals: --document-key, --geography, --effective-date, --status, --phase, --supersedes
    - no `--org-id` required-guard remains
    - exactly one `rpc('ingest_regulatory_document'` call; no `.from('chunks').insert(` remains
    - `npx tsx scripts/ingest-regulatory.ts` (no args) exits non-zero with a usage/missing-required message
  </acceptance_criteria>
  <verify>
    <automated>grep -c "process.argv\|pdf-parse\|--document-key\|--geography\|--supersedes\|rpc('ingest_regulatory_document'" scripts/ingest-regulatory.ts; grep -L "Deno\." scripts/ingest-regulatory.ts</automated>
  </verify>
  <done>CLI runs under tsx/Node, parses all new flags, embeds via raw fetch, and calls the atomic ingest RPC once per document; no Deno/jsr/npm specifiers remain.</done>
</task>

<task type="auto">
  <name>Task 2: Update ingest.test.ts to Vitest-native embedBatch + flag-parsing coverage</name>
  <files>scripts/ingest.test.ts</files>
  <read_first>
    - scripts/ingest.test.ts (current tests — imports embedBatch, mocks the OpenAI client)
    - scripts/ingest-regulatory.ts (post-port exports: embedBatch + parseFlags)
    - .planning/phases/14.5-rag-retrieval-overhaul/14.5-PATTERNS.md (ingest.test.ts extend section)
    - .planning/STATE.md (test mock/pool decisions: forks/singleFork, vi.stubGlobal, no --coverage)
  </read_first>
  <action>
Update the existing Vitest file (do not recreate):
- embedBatch now takes (texts, apiKey) + raw fetch. Replace the mock-client construction with `vi.stubGlobal('fetch', vi.fn(...))` returning a shaped `{ data: [{ index, embedding: number[1536] }, ...] }` response. Keep the three existing assertions: correct dim count, batching at the 100 boundary, and dim-mismatch throw.
- Add parseFlags unit tests (pure, no I/O): (a) valid argv with all flags returns the expected object incl. comma-split geography/phase arrays and status default 'active'; (b) missing --document-key rejects/exits non-zero; (c) missing --geography rejects; (d) --supersedes is parsed through when present and undefined when absent.
- Keep runtime under the repo's ~15s convention; no watch flags.
  </action>
  <acceptance_criteria>
    - `vi.stubGlobal('fetch'` present (fetch-mocked, not a mock client object)
    - tests import both `embedBatch` and `parseFlags` from './ingest-regulatory'
    - a test asserts geography comma-splits to a string[] and status defaults to 'active'
    - a test asserts missing --document-key is rejected
    - `npm run test:run` exits 0 with the new ingest tests green
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run 2>&1 | tail -20</automated>
  </verify>
  <done>ingest.test.ts covers the ported embedBatch (fetch-mocked) and parseFlags branch logic; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI (service-role key) -> ingest_regulatory_document | CLI holds the service-role key; a malformed call must not corrupt the regulatory canon |
| local files (PDFs) -> chunk content -> embeddings | untrusted file content flows into embeddings + DB rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.5-11 | Tampering/Integrity | partial-write on ingest failure | mitigate | All writes go through the single atomic RPC (Plan 02) — the CLI never issues discrete insert/update/delete calls, so a crash mid-run cannot leave a half-superseded state. |
| T-14.5-12 | Information Disclosure | service-role key handling | mitigate | Key loaded from .env (gitignored) via the same loadEnv used by backfill; never logged. Regulatory rows written with org_id NULL only (no tenant data touched). |
| T-14.5-13 | Input Validation | required flags | mitigate | parseFlags rejects missing --document-key/--geography/--dir before any DB call; unit-tested. |
</threat_model>

<verification>
- `npm run test:run` green (embedBatch fetch-mocked + parseFlags branches).
- `npx tsx scripts/ingest-regulatory.ts` with no args exits non-zero with usage.
- No Deno/jsr/npm specifiers remain; single atomic RPC write path.
- Live end-to-end run of the CLI (R2/R3 ingest) is exercised in Plan 07.
</verification>

<success_criteria>
- CLI ported to tsx/Node with all BRIEF flags, raw-fetch embeddings, and the atomic RPC write.
- Vitest coverage for embedBatch + parseFlags; suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/14.5-rag-retrieval-overhaul/14.5-04-SUMMARY.md`.
</output>
