---
phase: 14.5-rag-retrieval-overhaul
plan: 05
type: execute
wave: 3
depends_on: [03]
files_modified:
  - src/hooks/useProposalGeneration.ts
  - src/hooks/useProposalGeneration.test.ts
  - supabase/functions/retrieve-context/index.ts
autonomous: false
requirements: [QUERY, RELAXATION, ASSEMBLY, RETRIEVAL-RPC, APPLY]
user_setup: []
must_haves:
  truths:
    - "fetchRagChunks composes a deterministic attribute-rich query string (no LLM) and omits null attributes"
    - "fetchRagChunks passes study_phase + geography into retrieve-context alongside therapeuticArea"
    - "retrieve-context calls the new regulatory RPC signatures with phases_filter + geographies_filter"
    - "retrieve-context relaxes filters in order (drop phase, then TA) when regulatory results are under budget, always keeping status + geography"
    - "retrieve-context returns per-doc_type budgeted regulatoryChunks + proposalChunks (proposal path untouched)"
    - "The deployed retrieve-context matches the live RPC signatures (no silent empty results)"
  artifacts:
    - path: "src/hooks/useProposalGeneration.ts"
      provides: "deterministic query builder + widened fetchRagChunks signature threaded at both call sites"
      contains: "study of"
    - path: "supabase/functions/retrieve-context/index.ts"
      provides: "new request fields, new RPC params, graded relaxation loop"
      contains: "phases_filter"
  key_links:
    - from: "fetchRagChunks composed query"
      to: "retrieve-context body {query, therapeuticArea, studyPhase, geography}"
      via: "supabase.functions.invoke"
      pattern: "studyPhase"
    - from: "retrieve-context"
      to: "match_chunks_vector / match_chunks_fts (new arity)"
      via: "supabase.rpc with phases_filter + geographies_filter"
      pattern: "geographies_filter"
---

<objective>
Fix query construction (deterministic attribute string), extend retrieve-context to pass/relax the new regulatory filters, and DEPLOY the edge function so it matches the live RPC signatures applied in Plan 03.

Purpose: Retrieval today uses a bare section title (near-random for regulatory). This wires the proposal's structured attributes into both the embedded query and the pre-filters, adds graded relaxation, and closes Pitfall 2 (RPC signature drift → silent empty results) via a sequenced deploy + smoke check.
Output: Updated client hook + tests, updated + deployed edge function.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.5-rag-retrieval-overhaul/14.5-BRIEF.md
@.planning/phases/14.5-rag-retrieval-overhaul/14.5-CONTEXT.md
@.planning/phases/14.5-rag-retrieval-overhaul/14.5-PATTERNS.md

<interfaces>
<!-- fetchRagChunks current (src/hooks/useProposalGeneration.ts:176-197): (orgId, sectionName, therapeuticArea)
     const query = sectionName  // bug. invoke body { orgId, query, therapeuticArea }.
     Call sites: generateAll ~450-455 (enrichedContext.studyInfo), generateSection ~516-520 (proposalContext.studyInfo).
     studyInfo already carries studyPhase, therapeuticArea, indication, countries — no new plumbing needed. -->
<!-- retrieve-context (supabase/functions/retrieve-context/index.ts):
     consts RETRIEVAL_K_REGULATORY=5, RETRIEVAL_K_PROPOSALS=5, RETRIEVAL_SIMILARITY_THRESHOLD=0.65
     RetrieveRequest {orgId, query, therapeuticArea?, k_regulatory?, k_proposal?}  (no studyPhase/geography today)
     Caller-shape branch (182-195, PRESERVE UNCHANGED — 14.3 fix): isInternalServiceRoleCall first, else getAuthedUserAndOrg + 403 on mismatch; effectiveOrgId feeds org_id_filter.
     Reg RPC calls: vector 220-227, fts 234-240. Proposal calls 247-252, 259-263 (DO NOT TOUCH).
     Non-throwing error style: console.warn + empty-array fallback (KEEP). k capped at 20 (KEEP).
     Response already returns separate regulatoryChunks/proposalChunks arrays (per-doc_type budget already satisfied). -->
<!-- New reg RPC arity (Plan 02): match_chunks_vector(query_embedding, org_id_filter, agencies_filter,
     therapeutic_areas_filter, phases_filter, geographies_filter, similarity_threshold, match_count);
     match_chunks_fts(query_text, org_id_filter, agencies_filter, therapeutic_areas_filter, phases_filter, geographies_filter, match_count) -->
<!-- Inlining constraint: mergeHybridResults + any new helper MUST be inlined in index.ts (Deno cannot resolve src/lib/). -->
<!-- Deploy: npx supabase functions deploy retrieve-context --project-ref fuuvdcvbliijffogjnwg (committed != live). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic query construction in fetchRagChunks + thread study_phase/geography</name>
  <files>src/hooks/useProposalGeneration.ts, src/hooks/useProposalGeneration.test.ts</files>
  <read_first>
    - src/hooks/useProposalGeneration.ts (fetchRagChunks 176-197; call sites generateAll 450-455, generateSection 516-520)
    - src/hooks/useProposalGeneration.test.ts (existing — extend, do not recreate)
    - .planning/phases/14.5-rag-retrieval-overhaul/14.5-CONTEXT.md (### Query construction)
    - .planning/phases/14.5-rag-retrieval-overhaul/14.5-PATTERNS.md (fetchRagChunks section)
  </read_first>
  <behavior>
    - buildRegulatoryQuery({ sectionName, studyPhase, therapeuticArea, indication }): returns `"{sectionName} — {study_phase} {therapeutic_area} study of {indication}"` with each null/empty attribute omitted and whitespace collapsed (no dangling "—", no double spaces, no trailing "study of"). Cases: all present → full string; only sectionName → just the section name; missing indication → drop "study of {indication}".
    - fetchRagChunks passes studyPhase + geography into the invoke body.
  </behavior>
  <action>
Add an exported pure `buildRegulatoryQuery` helper (deterministic, NO LLM). Extend fetchRagChunks signature to `(orgId, sectionName, therapeuticArea, studyPhase, geography, indication)` (or an options object). Replace `const query = sectionName` with `const query = buildRegulatoryQuery({ sectionName, studyPhase, therapeuticArea, indication })`. Add `studyPhase` and `geography` to the invoke body alongside orgId/query/therapeuticArea. Keep the try/catch + console.warn + empty-array fallback exactly (non-blocking RAG). Thread the new args at BOTH call sites from the already-in-scope studyInfo (generateAll: enrichedContext.studyInfo.studyPhase / .countries / .indication; generateSection: proposalContext.studyInfo.*). Map studyInfo.countries → geography.
Extend the test file: assert buildRegulatoryQuery omits null attributes and produces the documented shapes; assert fetchRagChunks includes studyPhase + geography in the invoke body (mock supabase.functions.invoke).
  </action>
  <acceptance_criteria>
    - `buildRegulatoryQuery` exported and used in place of `const query = sectionName`
    - the composed string template contains `study of` and uses `—`
    - fetchRagChunks invoke body includes `studyPhase` and `geography` keys
    - both call sites pass the new args (grep `buildRegulatoryQuery` or the widened fetchRagChunks call twice)
    - new tests assert null-attribute omission + body fields; `npm run test:run` green
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run 2>&1 | tail -20</automated>
  </verify>
  <done>fetchRagChunks builds a deterministic attribute-rich query and forwards study_phase + geography; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: retrieve-context — new filter params + graded relaxation + preserve proposal path</name>
  <files>supabase/functions/retrieve-context/index.ts</files>
  <read_first>
    - supabase/functions/retrieve-context/index.ts (full — RetrieveRequest 18-24, caller branch 182-195, reg RPC 220-240, proposal RPC 247-263)
    - .planning/phases/14.5-rag-retrieval-overhaul/14.5-CONTEXT.md (### Graded filter relaxation; ### Retrieval assembly)
    - .planning/phases/14.5-rag-retrieval-overhaul/14.5-PATTERNS.md (retrieve-context section)
    - .planning/phases/14.5-rag-retrieval-overhaul/14.5-RESEARCH.md (item 4 + Pitfall 2)
  </read_first>
  <action>
- Extend RetrieveRequest with `studyPhase?: string` and `geography?: string[]` (additive body fields only — DO NOT touch the caller-shape branch at 182-195; that is the 14.3 security fix).
- Derive filter arrays: `therapeutic_areas_filter` from therapeuticArea (existing), `phases_filter = studyPhase ? [studyPhase] : null`, `geographies_filter = geography && geography.length ? geography : null`.
- Add the new params `phases_filter`, `geographies_filter` to BOTH regulatory RPC calls (match_chunks_vector 220-227, match_chunks_fts 234-240), matching the Plan 02 arity exactly.
- Wrap the regulatory calls in an inlined graded-relaxation loop: attempt 0 = full {agencies, therapeutic_areas, phases, geographies}; if merged regulatory result count < RETRIEVAL_K_REGULATORY (the per-doc_type budget), attempt 1 = drop phases_filter (pass null); if still under budget, attempt 2 = drop therapeutic_areas_filter (pass null). ALWAYS keep geographies_filter and rd.status='active' (status is enforced in-RPC). Record the relaxation level reached (include as a diagnostic field in the response or a console.log).
- Keep merging 70/30 vector/FTS via the existing mergeHybridResults within the regulatory doc_type. Keep the console.warn + empty-array fallback per RPC attempt (never throw). Keep the k<=20 cap.
- DO NOT touch the proposal RPC calls (247-263) or the response shape (already returns separate arrays).
- Any new relaxation helper MUST be inlined in this file (Deno cannot resolve src/lib/).
  </action>
  <acceptance_criteria>
    - RetrieveRequest declares `studyPhase?` and `geography?`
    - both regulatory `supabase.rpc('match_chunks_vector'|'match_chunks_fts'` calls pass `phases_filter` and `geographies_filter`
    - a relaxation loop exists that drops phases_filter before therapeutic_areas_filter and never nulls geographies_filter
    - the caller-shape branch (isInternalServiceRoleCall / getAuthedUserAndOrg / effectiveOrgId) is byte-for-byte preserved
    - proposal RPC call sites unchanged (grep match_chunks_vector_proposals / _proposals still called with original params)
    - `deno`/tsc lint of the file passes if runnable; otherwise grep-verified per repo Deno-unavailable convention
  </acceptance_criteria>
  <verify>
    <automated>grep -c "studyPhase\|geographies_filter\|phases_filter" supabase/functions/retrieve-context/index.ts</automated>
  </verify>
  <done>retrieve-context accepts study_phase/geography, calls the new reg RPC arity, relaxes phase→TA while keeping geography+status, and leaves the proposal path + 14.3 auth branch untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Deploy retrieve-context + live smoke (RPC signatures match, no silent errors)</name>
  <files>supabase/functions/retrieve-context/index.ts</files>
  <read_first>
    - .planning/phases/14.5-rag-retrieval-overhaul/14.5-CONTEXT.md (### Deployment constraints)
    - scripts/verify-retrieve-context.ts (existing live smoke script — hits the deployed function via internal service-role path)
    - .planning/phases/14.5-rag-retrieval-overhaul/14.5-RESEARCH.md (Pitfall 2 — silent empty results on signature drift)
  </read_first>
  <what-built>
retrieve-context updated to pass phases_filter/geographies_filter and relax gracefully. Plan 03 already applied the matching RPC signatures live, so deploy order is correct (migration before edge).
  </what-built>
  <action>
Deploy: `npx supabase functions deploy retrieve-context --project-ref fuuvdcvbliijffogjnwg`. Committed != live — this MUST run explicitly. Then smoke:
- Run `npx tsx scripts/verify-retrieve-context.ts` (or invoke retrieve-context directly) — assert HTTP 200 and that the response contains `regulatoryChunks` + `proposalChunks` arrays. Regulatory MAY be empty at this point (corpus not yet ingested — that non-empty assertion belongs to Plan 07). The key check here is NO RPC signature error.
- Via Supabase MCP `get_logs` (service: edge-function / retrieve-context), confirm there is NO `function match_chunks_vector(...) does not exist for this argument list` (or similar) console.warn after the deploy — that warning is the silent-drift signature (Pitfall 2).
If the deploy CLI requires interactive login, authenticate and retry (this is why the plan is autonomous:false).
  </action>
  <how-to-verify>
1. `npx supabase functions deploy retrieve-context --project-ref fuuvdcvbliijffogjnwg` completes without error.
2. `npx tsx scripts/verify-retrieve-context.ts` returns 200 with both chunk arrays present.
3. Supabase MCP get_logs for retrieve-context shows no "function ... does not exist for this argument list" warning after the deploy timestamp.
  </how-to-verify>
  <resume-signal>Type "approved" once deploy succeeds and logs show no RPC signature error, or describe the failure.</resume-signal>
  <done>retrieve-context is deployed live, responds 200 with both chunk arrays, and its RPC calls match the live signatures (no silent-drift warning).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (useProposalGeneration) -> retrieve-context | user-influenced studyPhase/geography flow into RPC filter params |
| retrieve-context -> match RPCs | relaxed regulatory tenant clause must not leak proposal chunks |
| internal service-role caller (chat-with-jamo/rag.ts) -> retrieve-context | 14.3 branch must remain intact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.5-14 | Information Disclosure | new body fields vs 14.3 auth branch | mitigate | studyPhase/geography are additive body fields only; the isInternalServiceRoleCall/getAuthedUserAndOrg/effectiveOrgId branch is preserved byte-for-byte, so cross-tenant org resolution is unchanged. |
| T-14.5-15 | Input Validation | studyPhase/geography -> RPC filters | mitigate | Passed as parameterized supabase.rpc args (TEXT[]/TEXT), never string-concatenated into SQL; array-overlap is not injectable. |
| T-14.5-16 | Availability/Correctness | RPC signature drift (Pitfall 2) | mitigate | Deploy sequenced AFTER Plan 03 migration apply; post-deploy get_logs smoke asserts absence of the silent "function does not exist" warning. |
| T-14.5-17 | Information Disclosure | relaxed regulatory clause | mitigate | Relaxation only nulls phase/TA filters; the in-RPC `doc_type='regulatory'` + `(org_id OR NULL)` clause (Plan 02) still gates rows, so proposal chunks are never reachable. |
</threat_model>

<verification>
- `npm run test:run` green (query builder + body fields).
- retrieve-context passes phases_filter/geographies_filter and relaxes phase→TA, geography always kept.
- 14.3 auth branch + proposal RPC path unchanged.
- Edge function deployed; live 200 smoke; get_logs shows no signature-drift warning.
</verification>

<success_criteria>
- Deterministic attribute query replaces the bare title; study_phase + geography threaded end to end.
- retrieve-context relaxes gracefully and returns per-doc_type budgeted results.
- Deployed function matches live RPC signatures.
</success_criteria>

<output>
After completion, create `.planning/phases/14.5-rag-retrieval-overhaul/14.5-05-SUMMARY.md`.
</output>
