---
phase: 14.5-rag-retrieval-overhaul
plan: 07
type: execute
wave: 4
depends_on: [03, 04, 05]
files_modified:
  - scripts/verify-regulatory-supersession.ts
autonomous: false
requirements: [VERIFICATION, INGEST-CLI, RETRIEVAL-RPC]
user_setup:
  - service: openai
    why: "The E2E ingest run computes real embeddings for the ICH E6 documents"
    env_vars:
      - name: OPENAI_API_KEY
        source: ".env (already used by backfill-proposal-chunks.ts / scripts)"
  - service: supabase
    why: "E2E writes regulatory rows + reads retrieval against the live project"
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: ".env (service-role key for the ingest RPC)"
must_haves:
  truths:
    - "A re-runnable script scaffolds the BRIEF's exact 4-step supersession protocol"
    - "Ingesting ICH-E6R2 then querying a Phase 2 oncology US 'Study Design' section retrieves R2"
    - "Ingesting ICH-E6R3 with --supersedes=ICH-E6R2 flips R2 to superseded and links both FKs"
    - "After supersession the same section retrieves R3 and no longer retrieves R2's chunks"
  artifacts:
    - path: "scripts/verify-regulatory-supersession.ts"
      provides: "scripted BRIEF verification protocol (ingest R2, verify, ingest R3, verify flip)"
      contains: "ICH-E6R2"
  key_links:
    - from: "scripts/verify-regulatory-supersession.ts"
      to: "ingest-regulatory CLI + retrieve-context"
      via: "runs the ported CLI twice then asserts retrieval + supersession state"
      pattern: "ICH-E6R3"
---

<objective>
Script and run the BRIEF's exact end-to-end acceptance protocol: ingest ICH E6(R2), confirm retrieval; ingest E6(R3) with --supersedes; confirm the status flip and that retrieval switches from R2 to R3.

Purpose: This IS the phase acceptance test. Scripting it (not a one-off manual session) makes it re-runnable and captures the full-stack proof that schema + RPCs + CLI + edge all cohere.
Output: A reusable verification script + a captured live pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.5-rag-retrieval-overhaul/14.5-BRIEF.md
@.planning/phases/14.5-rag-retrieval-overhaul/14.5-CONTEXT.md
@.planning/phases/14.5-rag-retrieval-overhaul/14.5-RESEARCH.md

<interfaces>
<!-- BRIEF ## VERIFICATION (authoritative 4 steps):
 Ingest E6(R2) and E6(R3) with distinct --document-key (ICH-E6R2, ICH-E6R3), agency ICH, geography {'US','EU','GLOBAL'}.
 1. Ingest R2 (active). Confirm a Phase 2 oncology US proposal "Study Design" section retrieves it.
 2. Ingest R3 with --supersedes=ICH-E6R2.
 3. Confirm R2 -> status='superseded', superseded_by=R3; R3 -> active, supersedes=R2.
 4. Confirm the same section now retrieves R3 and no longer retrieves R2's chunks. -->
<!-- CLI (Plan 04): npx tsx scripts/ingest-regulatory.ts --document-key=ICH-E6R2 --agency=ICH
     --geography=US,EU,GLOBAL --therapeutic-area=Oncology --phase=Phase 2 --dir=<pdf dir> [--supersedes=ICH-E6R2] -->
<!-- Retrieval: retrieve-context (deployed Plan 05) via scripts/verify-retrieve-context.ts pattern, or
     call match RPCs directly via Supabase MCP execute_sql with a query embedding. -->
<!-- Supersession state assertions: Supabase MCP execute_sql on regulatory_documents (status, supersedes, superseded_by). -->
<!-- No regulatory KB PDFs are committed (memory: rag-proposal-ingestion-wired). Task 1 must supply small
     fixture text for ICH-E6R2/R3 (a short representative "Study Design" excerpt per doc is sufficient — this
     phase verifies wiring, not corpus completeness). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/verify-regulatory-supersession.ts (BRIEF 4-step protocol)</name>
  <files>scripts/verify-regulatory-supersession.ts</files>
  <read_first>
    - scripts/verify-retrieve-context.ts (live-verification structure analog — env load, service-role client, edge invoke)
    - scripts/ingest-regulatory.ts (post-port CLI — invoked or its exported functions reused)
    - .planning/phases/14.5-rag-retrieval-overhaul/14.5-BRIEF.md (## VERIFICATION)
    - .planning/phases/14.5-rag-retrieval-overhaul/14.5-RESEARCH.md (## Validation Architecture — E2E row)
  </read_first>
  <action>
Create a tsx/Node script mirroring verify-retrieve-context.ts (loadEnv, service-role supabase client). It must, idempotently (clean up prior ICH-E6R2/R3 rows first):
1. Ingest ICH-E6R2: invoke the ported ingest CLI (child_process `npx tsx scripts/ingest-regulatory.ts --document-key=ICH-E6R2 --agency=ICH --geography=US,EU,GLOBAL --therapeutic-area=Oncology --phase="Phase 2" --dir=<fixture dir>`) OR import + call its exported ingest function directly with a small bundled "Study Design" fixture text. Assert one regulatory_documents row exists with document_key='ICH-E6R2', status='active'.
2. Retrieve: build a Phase 2 oncology US "Study Design" query (use buildRegulatoryQuery from useProposalGeneration or replicate the string), embed it, and either call retrieve-context (geography=['US'], studyPhase='Phase 2', therapeuticArea='Oncology') or call match_chunks_vector directly via execute_sql. Assert R2's chunk(s) are returned.
3. Ingest ICH-E6R3 with --supersedes=ICH-E6R2. Assert: R2 status='superseded' AND superseded_by = R3.id; R3 status='active' AND supersedes = R2.id.
4. Re-run the same retrieval. Assert R3's chunk(s) are returned AND none of R2's chunk ids are returned (status='active' filter excludes superseded).
Print a clear PASS/FAIL per step; exit non-zero on any failure. Keep it re-runnable (delete ICH-E6R2/R3 parents — CASCADE removes their chunks — at start).
  </action>
  <acceptance_criteria>
    - script contains literals `ICH-E6R2` and `ICH-E6R3` and `--supersedes`
    - the four protocol steps are each implemented with an explicit assertion + PASS/FAIL print
    - a cleanup/idempotency step deletes prior ICH-E6R2/R3 parents before running
    - script exits non-zero if any step's assertion fails
  </acceptance_criteria>
  <verify>
    <automated>grep -c "ICH-E6R2\|ICH-E6R3\|--supersedes\|superseded_by\|supersedes" scripts/verify-regulatory-supersession.ts</automated>
  </verify>
  <done>A re-runnable script implements the BRIEF's exact 4-step supersession protocol with per-step assertions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Run the E2E supersession protocol live (phase acceptance)</name>
  <files>scripts/verify-regulatory-supersession.ts</files>
  <read_first>
    - scripts/verify-regulatory-supersession.ts
    - .planning/phases/14.5-rag-retrieval-overhaul/14.5-BRIEF.md (## VERIFICATION)
    - .planning/STATE.md (memory: edge functions need deploy — confirm retrieve-context is the freshly deployed Plan 05 build)
  </read_first>
  <what-built>
The full stack: schema (Plan 03 applied), ingest RPC + ported CLI (Plans 02/04), deterministic query + relaxation + deployed retrieve-context (Plan 05). This runs the BRIEF's acceptance protocol against the live project.
  </what-built>
  <action>
Ensure .env has OPENAI_API_KEY + SUPABASE_SERVICE_ROLE_KEY. Confirm retrieve-context is the Plan 05 deploy (redeploy if unsure). Run `npx tsx scripts/verify-regulatory-supersession.ts` against project fuuvdcvbliijffogjnwg. This spends real OpenAI embedding credits and writes live regulatory rows — hence human-gated. Capture the full PASS/FAIL output in the SUMMARY. If step 4 shows R2 chunks still returned, the regulatory RPC status='active' filter or the supersession flip is broken — stop and report (do not mark the phase done).
  </action>
  <how-to-verify>
1. `npx tsx scripts/verify-regulatory-supersession.ts` prints PASS for all four steps and exits 0.
2. Supabase MCP execute_sql: `SELECT document_key, status, supersedes, superseded_by FROM regulatory_documents WHERE document_key IN ('ICH-E6R2','ICH-E6R3')` shows R2 superseded (superseded_by=R3) and R3 active (supersedes=R2).
3. The step-4 retrieval output contains R3's source/chunk and none of R2's.
  </how-to-verify>
  <resume-signal>Type "approved" once all four steps PASS live, or paste the failing step output.</resume-signal>
  <done>The BRIEF's 4-step supersession protocol passes live end to end; results captured in SUMMARY as phase acceptance evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E script (service-role + OpenAI key) -> live DB + external API | writes regulatory canon + spends embedding credits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.5-20 | Tampering/Integrity | supersession correctness | mitigate | Step 3 asserts both FK directions and both status values; step 4 asserts superseded chunks are excluded from retrieval — proving the status='active' RPC filter + atomic flip work together. |
| T-14.5-21 | Availability | stale edge deploy masking a real failure | mitigate | Task 2 explicitly confirms/redeploys retrieve-context before running, per the edge-functions-need-deploy memory. |
| T-14.5-22 | Cost/DoS | real embedding spend | accept | Small fixture excerpts per document keep token spend minimal; script is idempotent (cleans up) so re-runs don't accumulate rows. |
</threat_model>

<verification>
- Script implements the exact BRIEF 4-step protocol, re-runnable + idempotent.
- Live run: all four steps PASS; R2 superseded with FKs linked both ways; retrieval switches R2→R3.
</verification>

<success_criteria>
- Re-runnable verification script committed.
- Live phase acceptance: supersession + retrieval-switch proven end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/14.5-rag-retrieval-overhaul/14.5-07-SUMMARY.md` including the live 4-step PASS output.
</output>
