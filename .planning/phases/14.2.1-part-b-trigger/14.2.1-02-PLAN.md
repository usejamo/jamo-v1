---
phase: 14.2.1-part-b-trigger
plan: 02
type: execute
wave: 2
depends_on: ["14.2.1-01"]
files_modified:
  - src/components/AIChatPanel.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "AIChatPanel calls useGapAnalysisTrigger({ proposalId, userId }) so the Part B trigger is live"
    - "The dead `void triggerAnalysis` line and its 'suppresses unused warning' comment are removed from AIChatPanel.tsx"
    - "The old inline triggerAnalysis useCallback is either deleted (since the hook owns it) OR left intact with a clear note explaining why both exist — but no `void IDENT` no-op references remain"
    - "Opening the AI chat panel on a proposal with no prior chat_sessions row populates pendingActions within ~10s (D-35 verified manually)"
    - "Editing a proposal section repopulates pendingActions within ~3-5s of save (D-30 verified manually)"
    - "A 429 response from analyze-proposal-gaps does not surface to the user as a toast or error"
  artifacts:
    - path: "src/components/AIChatPanel.tsx"
      provides: "Wired AI chat panel — Part B trigger is live, no dead no-op references"
      contains: "useGapAnalysisTrigger"
  key_links:
    - from: "src/components/AIChatPanel.tsx"
      to: "src/hooks/useGapAnalysisTrigger.ts"
      via: "useGapAnalysisTrigger({ proposalId, userId }) call inside the component body"
      pattern: "useGapAnalysisTrigger\\s*\\("
---

<objective>
Wire `useGapAnalysisTrigger` (from Plan 01) into `AIChatPanel.tsx` and remove the dead `void triggerAnalysis` stub plus its misleading comment. This is the line of code that makes the entire Part B proactive co-pilot surface (ActionQueue, WalkthroughProgress, ResumeTaskBanner, Sidebar gap badge) start working.

Purpose: close the verification gap from `14.2-08-PLAN.md` Steps 12, 14-26.

Output: a one-line `useGapAnalysisTrigger({ proposalId, userId })` call inside `AIChatPanel`, removal of dead code, and an end-of-task manual smoke verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.2.1-part-b-trigger/14.2.1-CONTEXT.md
@.planning/phases/14.2.1-part-b-trigger/14.2.1-01-SUMMARY.md
@src/hooks/useGapAnalysisTrigger.ts
</context>

<interfaces>
Hook signature (from Plan 01):

```ts
export function useGapAnalysisTrigger(params: {
  proposalId: string | null | undefined
  userId: string | null | undefined
}): void
```

Current dead code being replaced (`AIChatPanel.tsx:228-242`):

```ts
const triggerAnalysis = useCallback(async (sectionSummaries) => { ... }, [isAnalyzing, proposalId])

// Expose triggerAnalysis for potential future use (suppresses unused warning)
void triggerAnalysis
```

After this plan: `triggerAnalysis` (the inline useCallback) is GONE — the hook owns the invocation logic. `isAnalyzing` / `setIsAnalyzing` state in `AIChatPanel` is also no longer needed (the hook owns its own in-flight ref). Verify no other code in `AIChatPanel` reads `isAnalyzing` before removing the state.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire useGapAnalysisTrigger into AIChatPanel and remove dead stub</name>
  <files>src/components/AIChatPanel.tsx</files>
  <read_first>
    - src/components/AIChatPanel.tsx (full file — needed for Edit, and to confirm nothing else references `triggerAnalysis` or `isAnalyzing`)
    - src/hooks/useGapAnalysisTrigger.ts (the new hook's exports and props shape)
    - .planning/phases/14.2.1-part-b-trigger/14.2.1-CONTEXT.md
  </read_first>
  <action>
    1. Add import near the other hook/component imports at the top of `AIChatPanel.tsx`:
       `import { useGapAnalysisTrigger } from '@/hooks/useGapAnalysisTrigger'` (or the relative path matching the project's existing import style — check how other hooks/components are imported in this file and mirror it).

    2. Inside the `AIChatPanel` component body, after `userId` and `proposalId` are available (right after the existing `isAnalyzing` state declaration at line 157 is a fine spot), insert:
       `useGapAnalysisTrigger({ proposalId, userId })`

    3. DELETE the entire `triggerAnalysis = useCallback(...)` block at lines 228-239 — the hook owns this now. Also delete:
       - Line 241 comment: `// Expose triggerAnalysis for potential future use (suppresses unused warning)`
       - Line 242: `void triggerAnalysis`

    4. Audit `isAnalyzing` / `setIsAnalyzing` usage in `AIChatPanel.tsx`:
       - `grep -n "isAnalyzing\|setIsAnalyzing" src/components/AIChatPanel.tsx` — if the ONLY references are the state declaration at line 157 and the in-flight guard inside the deleted `triggerAnalysis`, delete the state declaration too.
       - If `isAnalyzing` is read anywhere else (e.g., to disable a button or show a spinner), LEAVE the state declaration and add a brief comment `// isAnalyzing tracks UI-only analysis state; gap-trigger has its own internal ref` — do NOT delete it.

    5. Audit the `useCallback` import: if `useCallback` is no longer used anywhere in the file after the deletion, remove it from the React import line. Run `grep -c "useCallback" src/components/AIChatPanel.tsx` to confirm before stripping.

    6. Do NOT change any other behavior — no edits to the existing `chat_sessions` Realtime subscription, no edits to ActionQueue/WalkthroughProgress/ResumeTaskBanner rendering, no schema changes, no edge function changes.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "AIChatPanel" ; if [ $? -eq 0 ]; then exit 1; else exit 0; fi</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "void triggerAnalysis" src/components/AIChatPanel.tsx` exits NON-ZERO (the dead line is gone).
    - `grep -q "suppresses unused warning" src/components/AIChatPanel.tsx` exits NON-ZERO (the comment is gone).
    - `grep -q "useGapAnalysisTrigger" src/components/AIChatPanel.tsx` exits 0 (the hook is called).
    - `grep -qE "useGapAnalysisTrigger\\s*\\(\\s*\\{\\s*proposalId\\s*,\\s*userId\\s*\\}\\s*\\)" src/components/AIChatPanel.tsx` exits 0 (called with the right props).
    - `npx tsc --noEmit -p tsconfig.json` produces no new errors mentioning `AIChatPanel.tsx`.
    - `npx eslint src/components/AIChatPanel.tsx` exits 0 (no `no-unused-vars` from leftover state) — if eslint config differs, run the project's lint command instead.
  </acceptance_criteria>
  <done>`AIChatPanel.tsx` imports and calls `useGapAnalysisTrigger({ proposalId, userId })`; the dead `void triggerAnalysis` line, its comment, and the now-orphaned inline `triggerAnalysis` useCallback are deleted; the file compiles and lints clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual end-to-end smoke verification (Steps 12, 26 from 14.2-08-PLAN.md)</name>
  <what-built>
    `AIChatPanel` now wires the Part B gap-analysis trigger. On panel open with no prior session, `analyze-proposal-gaps` should fire once (D-35). On section save, it should fire again ~3s later (D-30). 429 responses should be silent.
  </what-built>
  <how-to-verify>
    Run the dev server (`npm run dev` or the project's equivalent) and:

    1. **D-35 initial population (Step 12 from 14.2-08-PLAN.md):**
       a. Pick a proposal that has thin/incomplete sections AND has NEVER had the AI chat panel opened (or delete its `chat_sessions` row first: `delete from chat_sessions where proposal_id = '<id>' and user_id = '<your-user-id>'`).
       b. Open the AI chat panel on that proposal.
       c. Within ~10 seconds, ActionQueue items should appear under the "Suggestions" header.
       d. Open browser DevTools → Network tab → confirm exactly ONE call to `/functions/v1/analyze-proposal-gaps` on panel open. Status should be 200.

    2. **D-30 ongoing trigger (Step 26-adjacent):**
       a. With the AI chat panel still open on the same proposal, edit one section's content and let it autosave (~1.5s).
       b. ~3 seconds after autosave completes (~4.5s after last keystroke), a SECOND call to `/functions/v1/analyze-proposal-gaps` should fire.
       c. ActionQueue should update with refreshed suggestions.

    3. **Debounce coalescing (D-30):**
       a. Make 5 rapid edits to different sections within 2 seconds.
       b. Confirm only ONE invoke fires ~3s after the last edit (not 5).

    4. **429 silence (server cooldown):**
       a. Within 30 seconds of a successful invoke, force another trigger (e.g., another save).
       b. The call should return 429 (visible in Network tab).
       c. Confirm: no toast, no red error in the UI, no `console.error` in the JS console. (A `console.debug` is acceptable.)

    5. **Switch proposals (Step 26):**
       a. Navigate to a different proposal.
       b. The queue should clear (briefly) and repopulate with the new proposal's suggestions.

    6. **No regressions:** Confirm `chat_sessions` Realtime updates still flow (cross-tab walkthrough state) — open the same proposal in two tabs, start a walkthrough in one, observe the other shows the resume banner.
  </how-to-verify>
  <resume-signal>Type "approved" once all 6 checks pass, or describe which check failed and what was observed (network status, console output, UI state).</resume-signal>
</task>

</tasks>

<verification>
- `grep -q "void triggerAnalysis" src/components/AIChatPanel.tsx` exits NON-ZERO.
- `grep -q "useGapAnalysisTrigger" src/components/AIChatPanel.tsx` exits 0.
- Manual smoke checks 1-6 above all pass.
- `npx vitest run` exits 0 (no regressions in existing tests).
</verification>

<success_criteria>
- The Part B trigger is live in production code paths.
- ActionQueue populates on panel open (D-35) and refreshes on section save (D-30).
- 429 responses are silent end-to-end.
- No dead `void IDENT` stubs remain in `AIChatPanel.tsx`.
- Steps 12, 14-26 of `14.2-08-PLAN.md` are now executable (and should be re-run as the next phase's verification pass).
</success_criteria>

<output>
After completion, create `.planning/phases/14.2.1-part-b-trigger/14.2.1-02-SUMMARY.md` noting which smoke checks passed and any 14.2-08 step that should be formally re-verified.
</output>
