---
phase: 14.2.1-part-b-trigger
plan: 02
subsystem: ai-chat / proactive-copilot
tags: [react, hooks, wiring, part-b, gap-analysis]
requires:
  - src/hooks/useGapAnalysisTrigger.ts (Plan 01)
  - .planning/phases/14.2.1-part-b-trigger/14.2.1-CONTEXT.md
provides:
  - Wired Part B gap-analysis trigger in production code (AIChatPanel renders it)
affects:
  - src/components/AIChatPanel.tsx
tech-stack:
  added: []
  patterns:
    - "Single-line hook invocation pattern: useGapAnalysisTrigger({ proposalId, userId })"
    - "Dead-stub removal: orphaned useCallback + void IDENT no-op deleted, in-component isAnalyzing state retired (hook owns its own ref)"
key-files:
  created: []
  modified:
    - src/components/AIChatPanel.tsx
decisions:
  - "Deleted the inline triggerAnalysis useCallback entirely (rather than leaving it with a note) — grep confirmed no callers outside the deleted `void triggerAnalysis` line, so keeping it would orphan dead code in violation of the plan's must_have"
  - "Removed the isAnalyzing/setIsAnalyzing state along with triggerAnalysis — grep -n 'isAnalyzing|setIsAnalyzing' confirmed those identifiers existed ONLY inside the deleted block (declaration at :157, three reads/writes inside the useCallback at :229-237, and the deps array at :239). No UI button or spinner read it."
  - "Kept the `useCallback` import — still used at AIChatPanel.tsx:306 (persistToolDataState) and :323 (handleSendMessage)"
  - "Placed `useGapAnalysisTrigger({ proposalId, userId })` immediately after the Part B state declarations (replacing the deleted isAnalyzing state slot) so the trigger lives next to the state it ultimately feeds (pendingActions, activeTask)"
metrics:
  duration: ~8m
  completed: 2026-05-19
  tasks: 1 (Task 2 is a manual checkpoint deferred to live-env verification)
  files: 1
---

# Phase 14.2.1 Plan 02: Wire useGapAnalysisTrigger Summary

**One-liner:** Replaced the dead `void triggerAnalysis` stub in `AIChatPanel.tsx` with a single `useGapAnalysisTrigger({ proposalId, userId })` call, deleting the orphaned inline `triggerAnalysis` useCallback and the `isAnalyzing` state it managed — the Part B proactive co-pilot trigger (ActionQueue / WalkthroughProgress / Sidebar gap badge) is now live in production code paths.

## What shipped

**Modified `src/components/AIChatPanel.tsx`:**

1. **Added import:** `import { useGapAnalysisTrigger } from '../hooks/useGapAnalysisTrigger'` (mirrors the relative-path style used by the other hooks/context imports in the file).
2. **Added hook call** in the Part B state block, immediately after `setCrossTabUpdate`:
   ```ts
   // ── Part B trigger (D-30 Realtime debounce + D-35 initial-population) ─────
   // Hook owns its own in-flight ref, content-hash skip, and 429-silence.
   // See src/hooks/useGapAnalysisTrigger.ts.
   useGapAnalysisTrigger({ proposalId, userId })
   ```
3. **Deleted** the orphaned inline `triggerAnalysis = useCallback(...)` block (formerly lines 228-239), its `// Expose triggerAnalysis for potential future use (suppresses unused warning)` comment, and the `void triggerAnalysis` line.
4. **Deleted** `const [isAnalyzing, setIsAnalyzing] = useState(false)` (formerly line 157) — confirmed grep-clean that the only references were inside the deleted useCallback.
5. **Retained** `useCallback` import — still in use at two later sites (`persistToolDataState`, `handleSendMessage`).

Net diff: `1 file changed, 6 insertions(+), 18 deletions(-)`.

## Verification

| Check                                                                                | Result |
| ------------------------------------------------------------------------------------ | ------ |
| `grep -q "void triggerAnalysis" src/components/AIChatPanel.tsx` exits NON-ZERO       | PASS   |
| `grep -q "suppresses unused warning" src/components/AIChatPanel.tsx` exits NON-ZERO  | PASS   |
| `grep -q "useGapAnalysisTrigger" src/components/AIChatPanel.tsx` exits 0             | PASS   |
| `grep -qE "useGapAnalysisTrigger\(\s*\{\s*proposalId\s*,\s*userId\s*\}\s*\)" ...`    | PASS   |
| `grep -cE "isAnalyzing\|setIsAnalyzing\|triggerAnalysis" src/components/AIChatPanel.tsx` returns 0 | PASS   |
| `npx tsc --noEmit -p tsconfig.json` produces no new errors mentioning `AIChatPanel.tsx` (pre-existing errors in unrelated test files only) | PASS   |
| `npx vitest run src/hooks/__tests__/useGapAnalysisTrigger.spec.ts` (regression check) | PASS (7/7) |

## Manual smoke verification (Task 2 checkpoint — deferred)

Task 2 is a `checkpoint:human-verify` requiring a running dev server. Per plan context, these items are noted here for a follow-on manual pass; they do not block the plan's automated completion. The items map to Steps 12 + 14–26 of `14.2-08-PLAN.md` and should be re-run as that phase's verification pass:

1. **D-35 initial population (Step 12):** Open AI chat panel on a proposal with no prior `chat_sessions` row → ActionQueue items appear within ~10s, exactly one `POST /functions/v1/analyze-proposal-gaps` (status 200) in Network tab.
2. **D-30 ongoing trigger:** Edit a section and let it autosave → a second invoke fires ~3s after autosave; ActionQueue refreshes.
3. **Debounce coalescing (D-30):** Five rapid edits in <2s → exactly ONE invoke fires ~3s after the last edit.
4. **429 silence:** Force another trigger within 30s of a successful invoke → 429 visible in Network, NO toast, NO red UI error, NO `console.error` (a `console.debug` is acceptable).
5. **Switch proposals (Step 26):** Navigate to a different proposal → queue clears and repopulates with the new proposal's suggestions.
6. **No regressions:** Cross-tab walkthrough state still flows via `chat_sessions` Realtime (open same proposal in two tabs, start walkthrough in tab A → tab B shows resume banner).

The hook's unit tests (Plan 01) already cover items 1, 3, and 4 against a mocked Supabase client; the manual pass exists to validate the wiring + real-network behavior end-to-end.

## Deviations from Plan

None — the plan was followed exactly. The `<action>` block's step 4 audit confirmed `isAnalyzing` had no surviving readers, so the state declaration was deleted as the plan permitted. Step 5's `useCallback` audit confirmed two surviving uses, so the import was retained as the plan permitted.

## Known Stubs

None. No `void IDENT` no-ops remain in `AIChatPanel.tsx`. No placeholder data or hardcoded empty arrays were introduced.

## Threat Flags

None. No new network endpoints, auth paths, or schema changes. The wiring uses the same `analyze-proposal-gaps` edge function and `chat_sessions` / `proposal_sections` Realtime subscriptions already in the 14.2 threat model.

## Self-Check: PASSED

Verified file modified:
- `src/components/AIChatPanel.tsx` — FOUND (modified)

Verified commit in `git log`:
- `600fcc0` feat(14.2.1): wire useGapAnalysisTrigger into AIChatPanel — FOUND
