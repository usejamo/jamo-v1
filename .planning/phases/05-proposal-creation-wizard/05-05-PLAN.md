---
phase: 05-proposal-creation-wizard
plan: "05"
type: execute
wave: 4
depends_on:
  - "05-04"
files_modified: []
autonomous: false

requirements:
  - REQ-1.1
  - REQ-1.2
  - REQ-1.3
  - REQ-1.4
  - REQ-1.5
  - REQ-1.6
  - REQ-1.7
  - REQ-9.4

must_haves:
  truths:
    - "New Proposal button opens a 3-step wizard (not the old single form)"
    - "Step indicator shows Study Info, Document Upload, Template & Generate"
    - "Required fields block forward navigation and show inline errors"
    - "Skip to Fast Draft jumps to Step 3"
    - "Step 3 passive context indicator is informational — not a warning banner"
    - "Generate button creates proposal and navigates to ProposalDetail"
    - "Existing edit proposal flow still works"
    - "Modal animation matches original ProposalEditorModal behavior"
  artifacts:
    - path: "src/components/ProposalCreationWizard.tsx"
      provides: "Complete wizard shell"
    - path: "src/components/wizard/Step1StudyInfo.tsx"
      provides: "Study info form"
    - path: "src/components/wizard/Step2DocumentUpload.tsx"
      provides: "Document upload step"
    - path: "src/components/wizard/Step3Generate.tsx"
      provides: "Generate trigger with passive context summary"
  key_links:
    - from: "ProposalEditorModal"
      to: "ProposalCreationWizard"
      via: "modalProposal === undefined branch"
      pattern: "ProposalCreationWizard"
---

<objective>
Human verification of the complete wizard flow — confirm visual design, animation, and interactive behavior match expectations before phase close.

Purpose: Automated tests cannot verify Framer Motion animation fidelity, visual pill toggle appearance, or the "passive / not alarming" feel of the context indicator. This checkpoint confirms the UX decisions from CONTEXT.md are correctly implemented.
Output: Approval to close Phase 5.
</objective>

<execution_context>
@C:/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-proposal-creation-wizard/05-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete 3-step proposal creation wizard:
- Step 1: Study Info form with required field validation and grouped services pill toggles
- Step 2: Document Upload (FileUpload component)
- Step 3: Passive context indicator + Generate button
- Skip to Fast Draft button (Step 1 onward)
- sessionStorage persistence across navigation
- Supabase proposal record creation on Generate + navigation to ProposalDetail
- ProposalEditorModal: wizard for create, existing form for edit
  </what-built>
  <how-to-verify>
Run `npm run dev` and open the app.

**Flow 1 — Full wizard:**
1. Click "New Proposal" button — wizard modal opens with slide-in animation (same feel as before)
2. Verify 3-step indicator shows: "1 · Study Info", "2 · Document Upload", "3 · Template & Generate"
3. Click Next with empty fields — inline "Required" errors appear below sponsor name, therapeutic area, indication, study phase. No advance.
4. Fill all required fields. Optional fields (regions, due date, services) show a gentle completeness nudge (not an error).
5. Click service pills — selected pills fill solid jamo-500 blue, unselected are outlined gray. Visually unambiguous.
6. Click Next — advances to Step 2.
7. Step 2 shows document upload UI. Click Back — returns to Step 1 with data preserved.
8. Advance to Step 3.
9. Step 3 passive context indicator shows a quiet summary row (e.g., "Study info ✓ · Documents: none · Output quality: reduced"). NOT a warning banner.
10. Click Generate — spinner on button, then navigates to /proposals/:id.
11. Confirm proposal appears in the list and ProposalDetail page loads.

**Flow 2 — Skip to Fast Draft:**
1. Open new proposal wizard.
2. Click "Skip to Fast Draft" from Step 1 — jumps directly to Step 3.
3. Context indicator shows "Study info: none · Documents: none · Output quality: limited".
4. Click Generate — creates proposal with empty fields (title auto-generated from empty values).

**Flow 3 — Edit flow unchanged:**
1. Click edit on an existing proposal — old single-form modal opens (not the wizard).
2. Confirm edit still works.

**Flow 4 — sessionStorage:**
1. Open wizard, fill Step 1 fields.
2. Navigate away (close modal, reopen) — fields are cleared on modal close (expected, sessionStorage cleared on close).
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 5, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All 4 flows verified by human. Wizard visual and interactive behavior matches CONTEXT.md decisions.
</verification>

<success_criteria>
Human approves wizard flow. Phase 5 complete.
</success_criteria>

<output>
After approval, create `.planning/phases/05-proposal-creation-wizard/05-05-SUMMARY.md`
</output>
