---
phase: 05-proposal-creation-wizard
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/__tests__/ProposalCreationWizard.test.tsx
autonomous: true
requirements:
  - REQ-1.1
  - REQ-1.2
  - REQ-1.5
  - REQ-1.6
  - REQ-1.7
  - REQ-9.4

must_haves:
  truths:
    - "Test file exists with stub tests for all wizard requirements"
    - "npm run test:run exits 0 with stubs skipped"
  artifacts:
    - path: "src/components/__tests__/ProposalCreationWizard.test.tsx"
      provides: "Nyquist-compliant test stubs for Phase 5"
      contains: "it.skip"
  key_links:
    - from: "src/components/__tests__/ProposalCreationWizard.test.tsx"
      to: "src/components/ProposalCreationWizard.tsx"
      via: "import (stubbed — file not yet created)"
      pattern: "it\\.skip"
---

<objective>
Wave 0 Nyquist compliance: create test stub file so every subsequent implementation task has an automated verify target from the start.

Purpose: Prevents "MISSING — Wave 0 must create test file first" in all downstream plans. Establishes test file with it.skip stubs for all 6 wizard requirements before any production code exists.
Output: src/components/__tests__/ProposalCreationWizard.test.tsx with skipped stubs. npm run test:run exits 0.
</objective>

<execution_context>
@C:/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-proposal-creation-wizard/05-CONTEXT.md
@.planning/phases/05-proposal-creation-wizard/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProposalCreationWizard test stub file</name>
  <files>src/components/__tests__/ProposalCreationWizard.test.tsx</files>
  <action>
Create src/components/__tests__/ProposalCreationWizard.test.tsx with it.skip stubs for all Phase 5 requirements. Do NOT import ProposalCreationWizard (it does not exist yet) — use a comment placeholder. Per the established stub test pattern: use it.skip (not dynamic imports), no describe-level imports of unbuilt files.

Stubs to include (all it.skip):
- REQ-1.1: "renders 3-step indicator with labels Study Info, Document Upload, Template & Generate"
- REQ-1.2: "Step 1 renders sponsor name, therapeutic area, indication, study phase fields"
- REQ-1.2: "Step 1 hard-required fields block forward navigation when empty"
- REQ-1.5: "Skip to Fast Draft button jumps to Step 3 from Step 1"
- REQ-1.6: "wizard state serialized to sessionStorage on field change"
- REQ-1.6: "wizard state hydrated from sessionStorage on remount"
- REQ-1.7: "forward navigation blocked and errors shown when required fields are empty"
- REQ-9.4: "Step 3 renders Generate button and calls createProposal on click"

File structure:
```typescript
import { describe, it } from 'vitest'

// ProposalCreationWizard tests — stubs only (component not yet built)
// Wave 0: All tests skipped until implementation plans complete

describe('ProposalCreationWizard', () => {
  it.skip('REQ-1.1: renders 3-step indicator ...', () => {})
  // ... remaining stubs
})
```
  </action>
  <verify>
    <automated>npm run test:run</automated>
  </verify>
  <done>npm run test:run exits 0. ProposalCreationWizard.test.tsx exists with 8 it.skip stubs. No imports of unbuilt files.</done>
</task>

</tasks>

<verification>
npm run test:run exits 0. All existing tests still pass. New test file has 8 skipped stubs.
</verification>

<success_criteria>
Test infrastructure in place. Every downstream plan can reference "npm run test:run" as its automated verify command without hitting MISSING errors.
</success_criteria>

<output>
After completion, create `.planning/phases/05-proposal-creation-wizard/05-00-SUMMARY.md`
</output>
