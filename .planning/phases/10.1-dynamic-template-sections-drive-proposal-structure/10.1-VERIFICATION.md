---
phase: 10.1-dynamic-template-sections-drive-proposal-structure
verified: 2026-04-27T00:00:00Z
status: human_needed
score: 17/17 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Create a new proposal in the wizard, select any template on Step 4, click Generate. Verify proposal_sections rows are created before navigation, and generation fires using those DB rows ordered by position."
    expected: "9 proposal_sections rows created with status=pending, then generation streams each section in position order (1–9), executive_summary and cover_letter last."
    why_human: "Cannot verify Supabase DB inserts and SSE streaming end-to-end without running the app."
  - test: "Upload a DOCX template in Settings > Templates. Verify the extracted sections show role dropdowns in the disclosure, with LLM-guessed roles pre-filled."
    expected: "Each section in the disclosure shows a role <select> pre-filled from LLM classification; None option is available; changing a role saves to DB."
    why_human: "LLM classification call to Claude Haiku is a live external call; extraction pipeline requires a running Supabase edge function."
  - test: "Open ProposalDetail for a generated proposal that has a selected_template_id. Verify the 'Template: [name]' badge renders above the section workspace."
    expected: "Small muted text badge showing 'Template: Standard Proposal' (or custom template name) appears above the section list."
    why_human: "UI rendering requires browser."
  - test: "Open ProposalsList. Verify proposals that have a selected_template_id show the template name on their cards."
    expected: "Each proposal card with a template shows the template name in small muted text."
    why_human: "UI rendering requires browser."
---

# Phase 10.1: Dynamic Template Sections Drive Proposal Structure — Verification Report

**Phase Goal:** Dynamic template sections drive proposal structure — template sections from the database determine what sections a proposal has, in what order, and what the AI generates for each one. No more hardcoded section arrays.
**Verified:** 2026-04-27
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | proposal_sections table has name, description, position, role columns | ✓ VERIFIED | Migration adds all 4 columns (`ADD COLUMN IF NOT EXISTS name text`, `description text`, `position integer`, `role text`) |
| 2 | templates table has is_default boolean column | ✓ VERIFIED | Migration: `ADD COLUMN IF NOT EXISTS is_default boolean NOT NULL DEFAULT false` |
| 3 | Exactly one template has is_default = true (Standard Proposal) | ✓ VERIFIED | Unique partial index `templates_single_default` enforces this; Standard Proposal seeded with `is_default = true` |
| 4 | Standard Proposal template has 9 template_sections rows with executive_summary=8 and cover_letter=9 | ✓ VERIFIED | Migration inserts all 9 rows with correct positions 1–9; executive_summary at pos 8, cover_letter at pos 9 |
| 5 | Existing proposal_sections backfilled with position and name from section_key | ✓ VERIFIED | Migration steps 6 and 7 backfill position and name via CASE on section_key |
| 6 | SECTION_WAVE_MAP, SECTION_NAMES, createInitialSections(), getWaveSections() no longer exist in generation.ts | ✓ VERIFIED | generation.ts contains none of these exports; file is clean rewrite with only new types |
| 7 | GenerateSectionPayloadV2 exists with version: 2, sectionId (UUID), sectionName, sectionDescription, sectionRole, priorSections[] | ✓ VERIFIED | generation.ts lines 36–65: full interface present with all required fields |
| 8 | useProposalGeneration generates sections by fetching proposal_sections ordered by position, not hardcoded keys | ✓ VERIFIED | Hook line 248: `.order('position', { ascending: true })`; generateAll fetches by position at line 395 |
| 9 | At Generate click, wizard copies template_sections → proposal_sections before navigating to ProposalDetail | ✓ VERIFIED | ProposalCreationWizard: fetches template_sections, inserts proposal_sections with status='pending', then navigates |
| 10 | GenerationState.sections is keyed by UUID, not role string | ✓ VERIFIED | generation.ts: `sections: Record<string, SectionState>` keyed by UUID; reducer builds via `[s.id]: s` |
| 11 | Edge function accepts version: 2 payload and routes to v2 code path | ✓ VERIFIED | index.ts line 368: `const isV2 = body.version === 2`; v2 branch calls `buildSectionPromptV2` |
| 12 | v2 path writes completed content via UPDATE WHERE id = sectionId (not upsert by section_key) | ✓ VERIFIED | `writeSectionById` at line 259: `.update({...}).eq('id', sectionId)` |
| 13 | priorSections content injected into system prompt as prior context | ✓ VERIFIED | `buildSectionPromptV2` lines 220–226: injects last 3 prior sections with 800-char truncation |
| 14 | ProposalDetail fetches proposal_sections with name, position columns and orders by position | ✓ VERIFIED | ProposalDetail lines 170–172: `select('id, section_key, name, position, ...')` + `.order('position', { ascending: true })` |
| 15 | Template badge 'Template: [name]' renders above section list | ✓ VERIFIED | ProposalDetail lines 163, 182–193, 558–561: templateName state fetched from templates table; badge renders `Template: {templateName}` |
| 16 | ProposalsList shows template name on each proposal card | ✓ VERIFIED | ProposalsList: `templateNames` state map populated from templates table join; rendered per proposal card |
| 17 | template-extract captures section description + LLM role classification via Claude Haiku | ✓ VERIFIED | template-extract: `parseSectionsFromHtml/Text` return `{name, description}` objects; `classifyRoles` calls `claude-haiku-4-5-20251001`; `roleMap` applied on insert |

**Score:** 17/17 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `supabase/migrations/20260427000024_template_driven_sections.sql` | Schema changes + seed data | ✓ VERIFIED | All 8 SQL operations present; 9 template_sections seeded |
| `src/types/generation.ts` | GenerateSectionPayloadV2, updated GenerationState | ✓ VERIFIED | All legacy exports deleted; v2 interface exported |
| `src/hooks/useProposalGeneration.ts` | Position-ordered sequential generation | ✓ VERIFIED | 517 lines; fetches by position; v2 payload sent |
| `src/components/ProposalCreationWizard.tsx` | Section pre-creation at Generate click | ✓ VERIFIED | template_sections fetch + proposal_sections insert before navigate |
| `supabase/functions/generate-proposal-section/index.ts` | v2 payload handling | ✓ VERIFIED | isV2 branch, buildSectionPromptV2, writeSectionById all present |
| `src/pages/ProposalDetail.tsx` | Dynamic section fetch by position, template badge | ✓ VERIFIED | order by position, templateName state, badge JSX |
| `src/pages/ProposalsList.tsx` | Template name on proposal cards | ✓ VERIFIED | templateNames state, templates fetch, rendering |
| `supabase/functions/template-extract/index.ts` | Description capture + LLM role classification | ✓ VERIFIED | classifyRoles, KNOWN_ROLES, parseSectionsFromHtml/Text updated |
| `src/components/settings/TemplatesTab.tsx` | Role editing UI per section | ✓ VERIFIED | handleRoleChange, role select dropdown, None option |
| `src/components/wizard/Step4Generate.tsx` | Default template pre-selection | ✓ VERIFIED | is_default in select, defaultTemplate.id dispatched via SET_TEMPLATE |
| `src/types/database.types.ts` | Regenerated with is_default, position columns | ✓ VERIFIED | is_default at line 594; position at line 377 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `template_sections` | `proposal_sections` | position ordering + name/description/role copy at instantiation | ✓ WIRED | ProposalCreationWizard maps ts.name/description/role/position into insert |
| `ProposalCreationWizard.tsx` | `proposal_sections` | INSERT before navigate | ✓ WIRED | `supabase.from('proposal_sections').upsert(sectionInserts, ...)` before `navigate(...)` |
| `useProposalGeneration.ts` | `proposal_sections` | SELECT ordered by position | ✓ WIRED | `.order('position', { ascending: true })` at both hydration and generateAll |
| `useProposalGeneration.ts` | `generate-proposal-section/index.ts` | v2 payload body | ✓ WIRED | `payload: GenerateSectionPayloadV2 = { version: 2, ... }` sent to edge function |
| `ProposalDetail.tsx` | `proposal_sections` | SELECT with name, position ORDER BY position | ✓ WIRED | Line 170: `select('id, section_key, name, position, ...')` with `.order('position', ...)` |
| `ProposalsList.tsx` | `templates` | JOIN on proposals.selected_template_id | ✓ WIRED | `.from('templates').select('id, name').in('id', templateIds)` |
| `template-extract/index.ts` | `template_sections` | INSERT with description and role columns | ✓ WIRED | sectionInserts includes `description: s.description, role: roleMap[s.name] ?? null` |
| `Step4Generate.tsx` | `templates` | SELECT WHERE is_default = true for pre-selection | ✓ WIRED | `.find((t) => t.is_default === true)` → `dispatch({ type: 'SET_TEMPLATE', templateId: defaultTemplate.id })` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `useProposalGeneration.ts` | `sections` / `sortedSections` | `proposal_sections` DB query ordered by position | Yes — DB fetch at hydration and generateAll | ✓ FLOWING |
| `ProposalDetail.tsx` | `proposalSections` | `proposal_sections` DB query | Yes — useEffect fetch + refetchSections callback | ✓ FLOWING |
| `ProposalDetail.tsx` | `templateName` | `templates` DB query by selected_template_id | Yes — single row fetch | ✓ FLOWING |
| `ProposalsList.tsx` | `templateNames` | `templates` DB query by ID set | Yes — fetches name for all unique template IDs | ✓ FLOWING |
| `generate-proposal-section/index.ts` | `fullText` → `proposal_sections` | Anthropic SSE stream → writeSectionById | Yes — streamed from Anthropic, written on flush | ✓ FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED for migration and edge functions (require live Supabase connection). Client-side checks confirmed via static analysis above.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| REQ-9.1 | Plans 01, 05 | 2–3 pre-built CRO proposal templates seeded | ✓ SATISFIED | Standard Proposal seeded with 9 sections; migration step 4–5 |
| REQ-9.2 | Plan 05 | Organizations can upload their own proposal templates | ✓ SATISFIED | template-extract updated to capture descriptions + classify roles |
| REQ-9.4 | Plans 01–05 | Template selection in Step 4 of wizard | ✓ SATISFIED | Step4Generate pre-selects default; ProposalCreationWizard instantiates from selection |
| REQ-4.1 | Plans 02, 03 | Generation runs in Supabase Edge Functions — API key never in browser | ✓ SATISFIED | Edge function handles all Anthropic calls; key in Deno.env |
| REQ-4.2 | Plans 02, 03 | Sections generated in dependency order; executive_summary and cover_letter last | ✓ SATISFIED | Position ordering enforced: exec_summary=8, cover_letter=9 |
| REQ-4.6 | Plan 02 | Frontend subscribes to Supabase Realtime for section updates | ✓ SATISFIED | useProposalGeneration: Realtime subscription dispatches SECTION_COMPLETE by UUID |
| REQ-5.1 | Plan 04 | Three-panel layout: section navigation / editor / AI panel | ✓ SATISFIED | ProposalDetail passes dynamically-fetched sections to SectionWorkspace |
| REQ-5.2 | Plan 04 | Rich text editor (TipTap v2) in center panel | ✓ SATISFIED | SectionWorkspace receives sections keyed by UUID with name from DB |

Note: REQ-4.9 (RAG context) appears in Plan 03's requirements field but not in the task instruction's listed IDs. It is satisfied — `fetchRagChunks` uses `sectionName` (not hardcoded key) and is called per section in the position loop.

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `generate-proposal-section/index.ts` | `SECTION_NAMES` constant still present at top of file (lines 9–19) | ℹ️ Info | Used only by v1 path (`SECTION_NAMES[sectionId]`); v2 path does not use it. Not a blocker — v1 backward-compat intentional. |
| `ProposalCreationWizard.tsx` | `upsert(sectionInserts, { onConflict: 'proposal_id,section_key' })` — proposal_sections may not have a unique constraint on this pair | ⚠️ Warning | If constraint is missing, upsert falls back to INSERT which is fine for new proposals. Existing proposals with duplicate section_key could produce extra rows. Not a goal blocker. |

### Human Verification Required

#### 1. End-to-End Template-Driven Generation Flow

**Test:** Create a new proposal through the wizard (fill Step 1–3 minimally), on Step 4 select "Standard Proposal" (or any ready template), click Generate.
**Expected:** 9 proposal_sections rows are created with `status=pending` before navigation; on ProposalDetail the generation loop runs sections in position order 1–9; executive_summary and cover_letter stream last.
**Why human:** Requires live Supabase connection, running edge functions, and browser to observe streaming order.

#### 2. Uploaded Template — Description and Role Classification

**Test:** In Settings > Templates, upload a DOCX template. After parsing completes, expand the template sections disclosure.
**Expected:** Each section shows a role `<select>` pre-filled with the LLM-guessed role (e.g., "understanding", "budget"); None option is present; changing a dropdown value saves to DB.
**Why human:** LLM classification requires live Anthropic API call via edge function.

#### 3. Template Badge in ProposalDetail

**Test:** Open an existing generated proposal that has `selected_template_id` set.
**Expected:** Small muted badge "Template: Standard Proposal" (or custom name) renders above the section workspace nav.
**Why human:** UI rendering requires browser.

#### 4. Template Name on ProposalsList Cards

**Test:** Open the Proposals list; view proposals that have been generated.
**Expected:** Each proposal card with a template shows the template name in small muted text below the proposal title.
**Why human:** UI rendering requires browser.

---

## Gaps Summary

No blocking gaps found. All 17 must-haves are verified in the codebase. The phase goal is achieved at the code level: template sections from the database now determine what sections a proposal has, in what order, and what the AI generates — hardcoded section arrays are fully eliminated.

Four items are routed to human verification because they require a live Supabase connection, running edge functions, or a browser to observe end-to-end behavior.

---

_Verified: 2026-04-27_
_Verifier: Claude (gsd-verifier)_
