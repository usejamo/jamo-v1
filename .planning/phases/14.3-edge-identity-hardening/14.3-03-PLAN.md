---
phase: 14.3-edge-identity-hardening
plan: 03
type: execute
wave: 2
depends_on: ["14.3-01"]
files_modified:
  - supabase/functions/retrieve-context/index.ts
  - supabase/functions/retrieve-context/test.ts
autonomous: true
requirements: ["REQ-2", "D-03"]
must_haves:
  truths:
    - "Internal service-role calls (chat-with-jamo/rag.ts) continue to receive chunks for the passed orgId — behavior unchanged"
    - "User-authenticated calls derive+validate org from the JWT; a mismatched body orgId cannot read another org's chunks"
    - "The effective org (JWT for user calls, body for internal) feeds org_id_filter — raw body orgId no longer feeds it for user calls"
  artifacts:
    - path: "supabase/functions/retrieve-context/index.ts"
      provides: "Branched identity: internal service-role vs user JWT"
      contains: "isInternalServiceRoleCall"
    - path: "supabase/functions/retrieve-context/test.ts"
      provides: "Both-branch test coverage (un-skipped)"
      contains: "isInternalServiceRoleCall"
  key_links:
    - from: "supabase/functions/retrieve-context/index.ts"
      to: "org_id_filter (match_chunks_vector RPC at :199)"
      via: "effectiveOrgId (JWT for user calls, body for internal)"
      pattern: "org_id_filter:\\s*effectiveOrgId"
    - from: "retrieve-context branch"
      to: "supabase/functions/_shared/auth.ts"
      via: "isInternalServiceRoleCall + getAuthedUserAndOrg"
      pattern: "from '\\.\\./_shared/auth\\.ts'"
---

<objective>
Harden `retrieve-context` (REQ-2, D-03) — THE core risk of the phase. It must BRANCH on caller shape, NOT blanket-require `auth.getUser()` (Pitfall 1): the internal RAG caller (`chat-with-jamo/rag.ts`, service-role bearer, no end-user JWT) must be preserved exactly, while user-authenticated calls (`useProposalGeneration.ts:183`) derive+validate org from the JWT.

Purpose: Today `index.ts:161` fully trusts body `orgId`, which flows into `org_id_filter` at `:199` — any caller can read any org's chunks (cross-tenant RAG exfiltration). A naive flip would 401 the internal caller and silently break RAG (rag.ts:51-52,70-72 swallow errors → no visible failure).
Output: Branched `retrieve-context/index.ts` + un-skipped `test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.3-edge-identity-hardening/14.3-RESEARCH.md
@.planning/phases/14.3-edge-identity-hardening/14.3-PATTERNS.md

<interfaces>
From supabase/functions/_shared/auth.ts (created in 14.3-01):
  export function isInternalServiceRoleCall(req: Request): boolean   // token === SUPABASE_SERVICE_ROLE_KEY
  export async function getAuthedUserAndOrg(req, corsHeaders?): Promise<{ userId: string; orgId: string }>  // throws Response on 401

Caller audit (RESEARCH Q1 — the complete list, only two runtime callers):
  INTERNAL: chat-with-jamo/rag.ts:35-50 → fetch /functions/v1/retrieve-context, `Bearer ${SERVICE_ROLE_KEY}`, orgId in body  → MUST stay working
  USER:     useProposalGeneration.ts:183 → supabase.functions.invoke('retrieve-context', …), user session JWT             → must derive+validate org from JWT
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Branch retrieve-context on caller shape and scope by effectiveOrgId</name>
  <read_first>
    - supabase/functions/retrieve-context/index.ts:161 (body-trusted `orgId` destructure), :167 (presence-only validation), :175-178 (service-role client, no auth.getUser()), :199 (`org_id_filter: orgId` — the leak point) and the FTS RPC below it
    - supabase/functions/chat-with-jamo/rag.ts:35-52,70-72 (the internal caller that MUST be preserved; note it silently swallows errors)
    - supabase/functions/_shared/auth.ts (isInternalServiceRoleCall + getAuthedUserAndOrg — from plan 01)
    - PATTERNS.md "retrieve-context" section (Pattern D + branch shape) and RESEARCH.md Q1 branch design
  </read_first>
  <behavior>
    - isInternalServiceRoleCall(req) true → effectiveOrgId = body orgId (unchanged internal behavior)
    - else → effectiveOrgId = JWT orgId from getAuthedUserAndOrg; if body orgId is present AND differs from JWT orgId → reject 403 (Open Q1 recommendation); missing/invalid JWT → 401
    - effectiveOrgId (never the raw body orgId) is what feeds org_id_filter at the vector + FTS RPCs
  </behavior>
  <action>
    1. Add import (relative _shared shape per PATTERNS Pattern A, match file's createClient import style):
    ```ts
    import { isInternalServiceRoleCall, getAuthedUserAndOrg, jsonError } from '../_shared/auth.ts'
    ```
    2. Keep the body destructure at :161 for `query, therapeuticArea, k_regulatory, k_proposal` and the body `orgId` (still needed for the internal branch). Keep the presence validation at :167.
    3. Immediately after parsing the body, compute `effectiveOrgId` via the branch (RESEARCH Q1):
    ```ts
    let effectiveOrgId: string
    if (isInternalServiceRoleCall(req)) {
      // INTERNAL (chat-with-jamo/rag.ts): trust body orgId as-is — behavior UNCHANGED
      if (!orgId) return jsonError(400, 'orgId required', corsHeaders)
      effectiveOrgId = orgId
    } else {
      // USER call (useProposalGeneration.ts): derive+validate org from JWT
      let jwtOrgId: string
      try {
        ({ orgId: jwtOrgId } = await getAuthedUserAndOrg(req, corsHeaders))
      } catch (e) {
        if (e instanceof Response) return e
        throw e
      }
      if (orgId && orgId !== jwtOrgId) return jsonError(403, 'org mismatch', corsHeaders)  // Open Q1: reject loudly
      effectiveOrgId = jwtOrgId
    }
    ```
    4. Replace the raw body `orgId` at the RPC scope point `:199` (`org_id_filter: orgId`) and any sibling FTS RPC org filter with `effectiveOrgId`. Grep-confirm no `org_id_filter: orgId` (raw body) remains — it must be `org_id_filter: effectiveOrgId`.
    5. Keep the existing service-role `supabase` client (:175-178) for the RPCs — it is still correct; only the org VALUE feeding the filter changes. Do NOT remove it and do NOT route the RPCs through a user client (RLS + the internal branch both rely on the service-role client).
    CRITICAL (Pitfall 1): the internal branch must reach the RPCs with zero behavioral change — do not call getAuthedUserAndOrg on the internal path, or the service-role caller (which has no end-user) will 401 and RAG will silently return empty.
  </action>
  <verify>
    <automated>grep -q "isInternalServiceRoleCall" supabase/functions/retrieve-context/index.ts && grep -q "org_id_filter: effectiveOrgId" supabase/functions/retrieve-context/index.ts && ! grep -q "org_id_filter: orgId" supabase/functions/retrieve-context/index.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `retrieve-context/index.ts` contains `import { isInternalServiceRoleCall, getAuthedUserAndOrg` from `'../_shared/auth.ts'`
    - contains `isInternalServiceRoleCall(req)` guarding the internal branch
    - contains `getAuthedUserAndOrg(req` on the ELSE (user) branch only
    - contains `org_id_filter: effectiveOrgId` (or equivalent) AND has ZERO occurrences of `org_id_filter: orgId` (raw body)
    - contains a 403 on user-branch body/JWT org mismatch (grep: `403`)
    - the service-role client at ~:175-178 is retained (grep still finds a `SERVICE_ROLE_KEY` createClient)
  </acceptance_criteria>
  <done>Branch preserves the internal service-role caller unchanged; user calls are JWT-org-scoped; effectiveOrgId feeds the RPC filter; raw body orgId no longer scopes user calls.</done>
</task>

<task type="auto">
  <name>Task 2: Un-skip retrieve-context/test.ts — cover both branches</name>
  <read_first>
    - supabase/functions/retrieve-context/test.ts (existing stub — currently it.skip/ignore:true per RESEARCH Validation Architecture)
    - supabase/functions/retrieve-context/index.ts (post-edit branch, from Task 1)
    - RESEARCH.md Validation Architecture → Test Map (the two retrieve-context rows)
  </read_first>
  <action>
    Flesh out the existing `retrieve-context/test.ts` stub for the two REQ-2 behaviors:
    - "internal service-role caller still returns chunks for the passed orgId (preserved)" — assert isInternalServiceRoleCall path is taken when bearer === SUPABASE_SERVICE_ROLE_KEY (pure-logic assertion where feasible; mark integration parts for the live check).
    - "user call with mismatched body orgId → JWT org (or 403 reject)" — assert the mismatch branch returns 403.
    Where a behavior needs a live Supabase auth/RPC (integration-only), keep a clearly-named `it.skip`/`ignore:true` with a comment pointing to 14.3-05 live validation, but un-skip and implement any assertion that is pure (e.g. the branch predicate). Do not leave the whole file skipped — at minimum the branch-selection logic must be exercised.
  </action>
  <verify>
    <automated>cd supabase/functions && deno test retrieve-context/test.ts --allow-env 2>&1 | grep -Eq "ok|passed" && echo OK || (grep -q "isInternalServiceRoleCall\|403\|service-role" retrieve-context/test.ts && echo "OK (grep — deno absent)")</automated>
  </verify>
  <acceptance_criteria>
    - `retrieve-context/test.ts` references the internal-caller-preserved behavior and the user-mismatch (403) behavior
    - The file is no longer entirely skipped: at least one active (non-skipped) assertion exercises the branch selection
    - Any remaining skips carry a comment pointing to 14.3-05 for live validation
  </acceptance_criteria>
  <done>Both retrieve-context branches are represented in test.ts; branch-selection logic is actively asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| useProposalGeneration.ts:183 → retrieve-context | User JWT call; body orgId untrusted |
| chat-with-jamo/rag.ts → retrieve-context | Internal service-role bearer; trusted origin, body orgId acceptable |
| retrieve-context → match_chunks_vector RPC | org_id_filter must be the caller's real org |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.3-09 | Information Disclosure (cross-tenant RAG) | body orgId → org_id_filter at :199 | mitigate | User branch scopes by JWT org; effectiveOrgId feeds the filter; 403 on mismatch |
| T-14.3-10 | Elevation (confused deputy) | branch selection | mitigate | isInternalServiceRoleCall compares bearer to SUPABASE_SERVICE_ROLE_KEY (unspoofable) |
| T-14.3-11 | DoS/regression (silent RAG break) | naive auth.getUser() on internal path | mitigate | Branch checks internal FIRST; internal path never calls getAuthedUserAndOrg (Pitfall 1). Verified live in 14.3-05. |
| T-14.3-12 | Availability | verify_jwt gateway vs service-role bearer | accept (verify A2) | A2 assumption verified in 14.3-05 before relying on it |
</threat_model>

<verification>
- Grep acceptance in both tasks satisfied.
- No `org_id_filter: orgId` (raw body) remains; `effectiveOrgId` feeds the filter.
- Internal branch does NOT call getAuthedUserAndOrg.
</verification>

<success_criteria>
retrieve-context branches on caller shape; internal service-role caller preserved unchanged; user calls JWT-org-scoped with 403 on mismatch; effectiveOrgId feeds the RPC; tests exercise both branches.
</success_criteria>

<output>
After completion, create `.planning/phases/14.3-edge-identity-hardening/14.3-03-SUMMARY.md`. Flag explicitly that the internal-RAG-preserved behavior MUST be live-verified in 14.3-05 (Pitfall 1).
</output>
