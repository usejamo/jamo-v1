---
phase: 14.3-edge-identity-hardening
plan: 02
type: execute
wave: 2
depends_on: ["14.3-01"]
files_modified:
  - supabase/functions/chat-with-jamo/index.ts
  - supabase/functions/chat-with-jamo/test.ts
autonomous: true
requirements: ["REQ-1", "D-01", "D-05"]
must_haves:
  truths:
    - "chat-with-jamo derives user_id/org_id from the JWT before the first chat_sessions read"
    - "Body-supplied user_id/org_id no longer scope any session read/write or RAG call"
    - "A body identity mismatched against the JWT is served by the JWT identity only"
  artifacts:
    - path: "supabase/functions/chat-with-jamo/index.ts"
      provides: "JWT-hoisted identity for all session/RAG use-sites"
      contains: "getAuthedUserAndOrg"
    - path: "supabase/functions/chat-with-jamo/test.ts"
      provides: "body-tamper + regression test scaffold"
      contains: "getAuthedUserAndOrg"
  key_links:
    - from: "supabase/functions/chat-with-jamo/index.ts"
      to: "supabase/functions/_shared/auth.ts"
      via: "import getAuthedUserAndOrg"
      pattern: "from '\\.\\./_shared/auth\\.ts'"
    - from: "chat-with-jamo identity derivation"
      to: "first chat_sessions read"
      via: "getAuthedUserAndOrg called BEFORE .from('chat_sessions')"
      pattern: "getAuthedUserAndOrg"
---

<objective>
Harden `chat-with-jamo` (REQ-1): derive `user_id`/`org_id` from the verified JWT via the shared helper, HOISTED to the top of the try block (before the first `chat_sessions` read at ~:109), then replace every body-identity use-site with the JWT-derived values.

Purpose: Closes the primary cross-tenant impersonation vector — today `index.ts:85-97` reads `user_id`/`org_id` from the body and never calls `auth.getUser()`.
Output: Hardened `chat-with-jamo/index.ts` + `test.ts` scaffold. (Live regression + cross-tenant validation happens in 14.3-05 after deploy.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.3-edge-identity-hardening/14.3-RESEARCH.md
@.planning/phases/14.3-edge-identity-hardening/14.3-PATTERNS.md

<interfaces>
From supabase/functions/_shared/auth.ts (created in 14.3-01):
  export async function getAuthedUserAndOrg(req, corsHeaders?): Promise<{ userId: string; orgId: string }>
    // throws a Response on 401 (missing header / null user / null org) — wrap in try/catch,
    // and if the caught value `instanceof Response`, return it directly.

Consumption shape (use verbatim near the top of the try block):
  let userId: string, orgId: string
  try {
    ({ userId, orgId } = await getAuthedUserAndOrg(req, corsHeaders))
  } catch (e) {
    if (e instanceof Response) return e
    throw e
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hoist JWT identity derivation and replace all body-identity use-sites</name>
  <read_first>
    - supabase/functions/chat-with-jamo/index.ts:85-97 (body destructure — the vulnerability), :108-118 (first chat_sessions read on anon client, NO auth header), :128-135, :143, :157-162 (authHeader built LATE), :233-310 (active_task writes)
    - supabase/functions/_shared/auth.ts (the helper — Task 1 of plan 01; import target)
    - supabase/functions/analyze-proposal-gaps/index.ts:203-228 (Pattern C — the exact hoisted-derivation shape being replicated)
    - RESEARCH.md Q3 use-site table + PATTERNS.md "chat-with-jamo" use-site table (the authoritative :line → switch map)
  </read_first>
  <behavior>
    - After the edit, `getAuthedUserAndOrg(req)` runs BEFORE the first `.from('chat_sessions')` call
    - `user_id` from the body is no longer used in any `.eq('user_id', ...)` filter — all use the JWT `userId`
    - `org_id` from the body is no longer passed to `fetchRagContext`, `handleCheckCompliance`, or `handleSetFocus` — all use the JWT `orgId`
    - The first session read uses the authed client, not a headerless anon client
  </behavior>
  <action>
    1. Add the import at the top of `chat-with-jamo/index.ts` (match the file's existing createClient import style; relative `_shared` import shape per PATTERNS Pattern A):
    ```ts
    import { getAuthedUserAndOrg } from '../_shared/auth.ts'
    ```
    2. Keep the body destructure at :85-97 for all NON-identity fields (proposal_id, user_message, session_id, target_section, etc.), but DROP reliance on the body `org_id`/`user_id`. Do not read them for identity. (Per D-04 the client may still SEND them; they are simply ignored — backward-compatible.)
    3. **HOIST (Pitfall 2 — this is mandatory; an in-place variable swap is insufficient):** immediately at the top of the try block, BEFORE the `if (proposal_id && user_id)` guard at ~:108-109, derive identity:
    ```ts
    let userId: string, orgId: string
    try {
      ({ userId, orgId } = await getAuthedUserAndOrg(req, corsHeaders))
    } catch (e) {
      if (e instanceof Response) return e
      throw e
    }
    ```
    4. Replace EVERY use-site per the authoritative map (RESEARCH Q3 / PATTERNS table):
       | Line(s) | Today | Replace with |
       |---------|-------|--------------|
       | :109 guard `if (proposal_id && user_id)` | body user_id | `if (proposal_id && userId)` (JWT) |
       | :110-118 first session read (anon, NO auth header) → `.eq('user_id', user_id)` | body user_id + headerless anon client | JWT `userId` AND use the authed client (build it with the auth header, or reuse the client built from `req`'s Authorization). The read must no longer run on a headerless anon client. |
       | :128-135 stale active_task expire `.eq('user_id', user_id)` | body | JWT `userId` |
       | :143 `fetchRagContext(org_id, …)` | body org_id | JWT `orgId` |
       | :233, :239, :260, :278, :289, :310 active_task writes `.eq('user_id', user_id)` | body | JWT `userId` |
       | :253 `handleCheckCompliance(…, org_id)` | body | JWT `orgId` |
       | :285 `handleSetFocus(…, org_id, session_id)` | body | JWT `orgId` |
    5. Remove/rename the local `org_id`/`user_id` bindings so a stray body reference cannot compile silently (e.g. do not destructure them into `user_id`/`org_id` names; if other code paths reference those names, they must now point at the JWT-derived `userId`/`orgId`). Grep the file after editing to confirm no `.eq('user_id', user_id)` (body) remains.
    Do NOT change streaming, tool-dispatch, or handler logic beyond the identity substitutions.
  </action>
  <verify>
    <automated>grep -q "getAuthedUserAndOrg" supabase/functions/chat-with-jamo/index.ts && ! grep -nE "\.eq\('user_id', user_id\)" supabase/functions/chat-with-jamo/index.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `chat-with-jamo/index.ts` contains `import { getAuthedUserAndOrg } from '../_shared/auth.ts'`
    - `chat-with-jamo/index.ts` contains `getAuthedUserAndOrg(req` and that call textually precedes the first `.from('chat_sessions')` occurrence (verify: line number of getAuthedUserAndOrg call < line number of first `chat_sessions` read)
    - Zero occurrences of `.eq('user_id', user_id)` remain (grep returns nothing) — all filters use `userId`
    - `fetchRagContext(` is called with `orgId` (JWT), not `org_id` (body) — grep shows `fetchRagContext(orgId` or `fetchRagContext(\n  orgId`
    - No remaining identity read from the body: grep for `user_id` and `org_id` shows only the (ignored/backward-compat) destructure line, not any filter/scoping use
  </acceptance_criteria>
  <done>Identity is JWT-derived at the top of the try block; all session/RAG use-sites use the JWT userId/orgId; no body-identity filter remains.</done>
</task>

<task type="auto">
  <name>Task 2: Add body-tamper + regression test scaffold (test.ts)</name>
  <read_first>
    - supabase/functions/chat-with-jamo/index.ts (post-edit, from Task 1)
    - supabase/functions/retrieve-context/test.ts (repo Deno test file conventions)
    - RESEARCH.md Q6 (the live regression + cross-tenant negative test that plan 05 runs)
  </read_first>
  <action>
    Create `supabase/functions/chat-with-jamo/test.ts` documenting the two REQ-1 behaviors that plan 14.3-05 validates live (these are integration behaviors requiring a deployed function + two users, so they are manual-only per VALIDATION.md — the file encodes the intent + any pure assertions possible):
    - A test (or clearly-marked `it.skip`/`ignore:true` with a comment) named for "body user_id/org_id mismatched vs JWT → served by JWT identity (session rows + RAG scoped to JWT, not body)".
    - A test/skip named for "existing authenticated chat still lands chat_sessions writes after hoist (no regression)".
    Include a top-of-file comment: "Definitive validation is the LIVE check in 14.3-05 (Q6). These scaffolds mark the REQ-1 acceptance behaviors and any pure/local assertions." If a pure assertion is feasible (e.g. that the handler imports getAuthedUserAndOrg), add it; otherwise keep the two named skips so the behaviors are discoverable.
  </action>
  <verify>
    <automated>grep -q "getAuthedUserAndOrg\|JWT identity\|chat_sessions" supabase/functions/chat-with-jamo/test.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/functions/chat-with-jamo/test.ts` exists
    - Contains a named case referencing "JWT identity" / body-tamper behavior (REQ-1 acceptance)
    - Contains a named case referencing the `chat_sessions` write regression
    - Top comment points to 14.3-05 as the live-validation owner
  </acceptance_criteria>
  <done>test.ts encodes the two REQ-1 behaviors and references the live-validation plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (AIChatPanel.tsx:472) → chat-with-jamo | Streaming fetch; body carries user_id/org_id (untrusted) + JWT (trusted). Only client caller. |
| chat-with-jamo → chat_sessions (RLS) | Session reads/writes must be scoped by JWT-derived user_id, not body |
| chat-with-jamo → retrieve-context | Internal RAG call scoped by JWT-derived org_id (fixed in 14.3-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.3-05 | Spoofing (IDOR) | body user_id at :109-310 | mitigate | Hoist getAuthedUserAndOrg; all `.eq('user_id', …)` use JWT userId |
| T-14.3-06 | Information Disclosure | fetchRagContext(org_id) at :143 | mitigate | Pass JWT orgId → RAG scoped to caller's org, cannot read another org's chunks |
| T-14.3-07 | Elevation | headerless anon read at :110-118 | mitigate | First session read moved onto the authed client after hoist; no headerless identity read |
| T-14.3-08 | DoS/regression | hoist ordering | accept (verified) | Live chat_sessions regression check in 14.3-05 confirms writes still land |
</threat_model>

<verification>
- Grep acceptance in both tasks satisfied.
- getAuthedUserAndOrg call precedes the first chat_sessions read (ordering check).
- No `.eq('user_id', user_id)` remains.
</verification>

<success_criteria>
chat-with-jamo derives identity from the JWT at the top of the try block; every session/RAG use-site uses JWT userId/orgId; body identity is ignored; test scaffold encodes REQ-1 behaviors.
</success_criteria>

<output>
After completion, create `.planning/phases/14.3-edge-identity-hardening/14.3-02-SUMMARY.md`.
</output>
