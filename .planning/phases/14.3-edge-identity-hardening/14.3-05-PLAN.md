---
phase: 14.3-edge-identity-hardening
plan: 05
type: execute
wave: 3
depends_on: ["14.3-02", "14.3-03", "14.3-04"]
files_modified:
  - .planning/phases/14.3-edge-identity-hardening/14.3-DEPLOY-LOG.md
autonomous: false
requirements: ["D-05", "REQ-1", "REQ-2"]
must_haves:
  truths:
    - "All four hardened functions are DEPLOYED to project fuuvdcvbliijffogjnwg (version bumped, not just committed)"
    - "verify_jwt=true still admits the internal service-role RAG call (A2 confirmed)"
    - "An authenticated chat still lands chat_sessions writes after the body→JWT switch (no regression)"
    - "A body identity mismatched vs the JWT is served by the JWT identity only (cross-tenant vector closed, live)"
    - "The internal RAG caller still returns chunks (Pitfall 1 not triggered)"
  artifacts:
    - path: ".planning/phases/14.3-edge-identity-hardening/14.3-DEPLOY-LOG.md"
      provides: "Caller-shape audit record + deploy versions + A2 result + live-check outcomes"
      contains: "verify_jwt"
  key_links:
    - from: "deploy_edge_function (MCP)"
      to: "live project fuuvdcvbliijffogjnwg"
      via: "all sibling files in the files[] array (index.ts + rag.ts + ../_shared/auth.ts)"
      pattern: "auth.ts"
---

<objective>
[BLOCKING GATE] Deploy all four hardened functions to the live project and verify — execute-phase COMMITS but does NOT deploy (Pitfall 3 / D-05). Then run the live checks the earlier plans deferred: A2 (service-role bearer passes verify_jwt), the chat_sessions regression, the cross-tenant body-tamper negative test, and internal-RAG preservation (Pitfall 1). Record the required caller-shape audit (AC-5).

Purpose: This phase is a go-live gate for Phase 15. Committed-but-not-deployed = still vulnerable in production (the exact 14.2.3 failure mode). Nothing is "done" until deployed + live-verified.
Output: All four functions live at a bumped version + `14.3-DEPLOY-LOG.md` recording audit, versions, and check results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.3-edge-identity-hardening/14.3-RESEARCH.md
@.planning/phases/14.3-edge-identity-hardening/14.3-SPEC.md

<deploy_facts>
- Project ref: `fuuvdcvbliijffogjnwg`.
- Deploy path A (preferred): Supabase MCP `deploy_edge_function(project_id, name, entrypoint_path='index.ts', verify_jwt=true, files=[…])`. The MCP uploads ONLY files you pass — include EVERY sibling (Pitfall 4): chat-with-jamo needs `index.ts` + `rag.ts` + `../_shared/auth.ts` (+ any other files it imports); retrieve-context + both salesforce fns need `index.ts` + `../_shared/auth.ts` (+ salesforce fns also `../_shared/salesforce-crypto.ts`).
- Deploy path B (fallback): `npx supabase functions deploy <slug> --project-ref fuuvdcvbliijffogjnwg` (needs SUPABASE_ACCESS_TOKEN; `functions deploy` does NOT trigger the migration-history check that blocks `db push` here — see STATE.md migration gotcha).
- Keep `verify_jwt=true` on all four (matches exemplars; service-role key still passes the gateway — that's A2, verify it).
- Local app: localhost:5173, login usera@jamo.com / password123. Diagnostic: `_gap_debug` table / Supabase MCP query for chat_sessions rows.
</deploy_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify A2 (verify_jwt vs service-role bearer) and deploy all four functions</name>
  <read_first>
    - .planning/phases/14.3-edge-identity-hardening/14.3-RESEARCH.md Q6 (deploy mechanics) + Assumption A2 + Pitfall 4 (sibling files)
    - supabase/functions/chat-with-jamo/index.ts + rag.ts (confirm the sibling-file import list to pass)
    - supabase/functions/{retrieve-context,salesforce-oauth-initiate,salesforce-oauth-disconnect}/index.ts (confirm each function's import list)
    - supabase/functions/_shared/auth.ts (must be uploaded with every function)
  </read_first>
  <action>
    1. **A2 pre-check (do BEFORE relying on it):** use Supabase MCP `list_edge_functions` (project `fuuvdcvbliijffogjnwg`) to record the CURRENT `verify_jwt` setting + version of each of the four functions. Confirm the internal call design (rag.ts sends `Bearer ${SUPABASE_SERVICE_ROLE_KEY}`) is expected to pass the gateway at `verify_jwt=true`. If `list_edge_functions` shows `verify_jwt=false` on any, note it and keep it consistent (do not silently flip). If A2 cannot be confirmed from metadata, flag that the internal-RAG live check (Task 2) is the authoritative A2 test and must pass before the phase is considered green.
    2. **Deploy all four** via MCP `deploy_edge_function` (project `fuuvdcvbliijffogjnwg`, `verify_jwt=true`, `entrypoint_path='index.ts'`), passing the full `files[]` array per function (Pitfall 4):
       - `chat-with-jamo`: index.ts + rag.ts + ../_shared/auth.ts (+ any other imported siblings present in the dir)
       - `retrieve-context`: index.ts + ../_shared/auth.ts
       - `salesforce-oauth-initiate`: index.ts + ../_shared/auth.ts + ../_shared/salesforce-crypto.ts
       - `salesforce-oauth-disconnect`: index.ts + ../_shared/auth.ts
       If MCP deploy is unavailable, fall back to path B (CLI). If neither is available (no SUPABASE_ACCESS_TOKEN, no MCP), raise a checkpoint:human-action for the user to run the CLI deploy — do NOT mark the phase complete on committed-only code (Pitfall 3).
    3. **Confirm version bump:** call `list_edge_functions` again; confirm each of the four has a HIGHER version than the pre-deploy snapshot.
    4. Write `.planning/phases/14.3-edge-identity-hardening/14.3-DEPLOY-LOG.md` recording: the caller-shape audit (AC-5) for all four functions (user-authenticated vs internal service-role — sourced from RESEARCH Q1/Q3/Q4), the pre/post `verify_jwt` + version of each function, and the A2 determination.
  </action>
  <verify>
    <automated>grep -q "verify_jwt" .planning/phases/14.3-edge-identity-hardening/14.3-DEPLOY-LOG.md && grep -q "chat-with-jamo" .planning/phases/14.3-edge-identity-hardening/14.3-DEPLOY-LOG.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `list_edge_functions` shows a bumped version for all four functions vs the pre-deploy snapshot
    - `14.3-DEPLOY-LOG.md` records the caller-shape audit for all four (user vs internal service-role) — satisfies AC-5
    - `14.3-DEPLOY-LOG.md` records pre/post verify_jwt + version + the A2 determination
    - Every function's deploy included `../_shared/auth.ts` in its files[] (chat-with-jamo also `rag.ts`; salesforce fns also `salesforce-crypto.ts`) — recorded in the log
  </acceptance_criteria>
  <done>All four functions deployed at a bumped version with correct sibling files; A2 recorded; caller-shape audit logged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live verification — regression, cross-tenant, internal-RAG</name>
  <what-built>
    All four edge functions now derive identity from the JWT and are deployed live. This checkpoint confirms the three behaviors that can only be verified against the running functions (VALIDATION.md manual-only entries + RESEARCH Q6 steps 3-4).
  </what-built>
  <how-to-verify>
    1. **chat_sessions regression (REQ-1 / D-05):** Log in at http://localhost:5173 as usera@jamo.com / password123. Send a chat message on a proposal that triggers `set_focus` or `ask_user` (writes `active_task` to chat_sessions). Via Supabase MCP (or the `_gap_debug` path), confirm the `chat_sessions` row for that proposal_id + this authenticated user shows the updated `active_task`. EXPECTED: write lands (no regression from the body→JWT hoist).
    2. **Internal-RAG preserved (REQ-2 / Pitfall 1 — the A2-authoritative test):** In the same authed chat, send a message that triggers RAG (regulatory/proposal context). Confirm the answer includes retrieved context and there is NO silent RAG failure (rag.ts swallows errors, so "empty but no error" = FAIL). EXPECTED: chunks return — the internal service-role → retrieve-context call still works at verify_jwt=true (confirms A2).
    3. **Cross-tenant body-tamper (REQ-1/REQ-2, the whole point):** Issue a request to chat-with-jamo (and/or retrieve-context) whose BODY user_id/org_id differ from the JWT (e.g. via a crafted fetch with your real JWT but another org's id in the body). EXPECTED: session rows + RAG are scoped to the JWT identity, NOT the body's (or the mismatch is rejected 403). The body identity must have zero effect.
    4. (Optional) salesforce: with a mismatched body org_id on initiate/disconnect, EXPECTED 403 or JWT-org operation — not the body org.
  </how-to-verify>
  <resume-signal>Type "approved" if all three (regression lands, RAG returns chunks, body-tamper served by JWT) pass; otherwise describe which failed (that becomes a --gaps input).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update the stale memory + record sign-off</name>
  <read_first>
    - .planning/phases/14.3-edge-identity-hardening/14.3-DEPLOY-LOG.md (from Task 1)
    - CONTEXT.md specifics §96 (memory becomes stale after deploy)
  </read_first>
  <action>
    After Task 2 is approved: append the live-check outcomes (all three PASS + any notes) to `14.3-DEPLOY-LOG.md`. Then update the `edge-chat-session-writes-not-landing` memory to reflect that chat-with-jamo now derives `user_id` from the JWT (not the request body) as of 14.3 deploy — the old "must send user_id in body" guidance is superseded (CONTEXT specifics §96). Do this via the memory-update mechanism, not by editing arbitrary files.
  </action>
  <verify>
    <automated>grep -qi "pass\|approved\|live" .planning/phases/14.3-edge-identity-hardening/14.3-DEPLOY-LOG.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `14.3-DEPLOY-LOG.md` records the three live-check outcomes
    - The `edge-chat-session-writes-not-landing` memory is updated to note JWT-derived user_id post-14.3 (or a note recorded if the memory tool is unavailable)
  </acceptance_criteria>
  <done>Live outcomes logged; stale memory corrected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed source → live edge runtime | Deploy gap: prod runs stale (vulnerable) code until explicitly deployed |
| gateway (verify_jwt) → function | Must admit the internal service-role bearer (A2) while still verifying user JWTs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.3-16 | Elevation (deploy gap) | committed-not-deployed | mitigate | Explicit MCP/CLI deploy + version-bump confirmation via list_edge_functions (Pitfall 3) |
| T-14.3-17 | DoS (runtime import break) | MCP dropping sibling files | mitigate | files[] includes rag.ts + ../_shared/auth.ts + salesforce-crypto.ts (Pitfall 4); live RAG check catches a broken import |
| T-14.3-18 | Availability (A2 false) | verify_jwt rejects service-role bearer | mitigate | A2 pre-check + internal-RAG live test; if RAG returns empty, halt and remediate before sign-off |
| T-14.3-19 | Spoofing (residual) | live cross-tenant path | mitigate | Body-tamper negative test confirms JWT identity wins end-to-end |
</threat_model>

<verification>
- list_edge_functions shows bumped versions for all four.
- 14.3-DEPLOY-LOG.md records audit + verify_jwt + A2 + three live-check PASS results.
- Human-verify checkpoint approved.
</verification>

<success_criteria>
All four functions deployed live (version bumped); A2 confirmed; chat_sessions regression passes; internal RAG preserved; cross-tenant body-tamper served by JWT identity; caller-shape audit + stale-memory correction recorded. Phase 15 go-live gate satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/14.3-edge-identity-hardening/14.3-05-SUMMARY.md`.
</output>
