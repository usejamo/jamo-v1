---
phase: 14.3-edge-identity-hardening
plan: 04
type: execute
wave: 2
depends_on: ["14.3-01"]
files_modified:
  - supabase/functions/salesforce-oauth-initiate/index.ts
  - supabase/functions/salesforce-oauth-disconnect/index.ts
  - supabase/functions/salesforce-oauth-initiate/test.ts
  - supabase/functions/salesforce-oauth-disconnect/test.ts
autonomous: true
requirements: ["REQ-2", "D-01"]
must_haves:
  truths:
    - "salesforce-oauth-initiate binds the signed CSRF state to the JWT-derived org, not the body org_id"
    - "salesforce-oauth-disconnect operates on the JWT-derived org — a mismatched body org_id cannot disconnect another tenant"
    - "Both reject with 403 on an explicit body/JWT org_id mismatch"
  artifacts:
    - path: "supabase/functions/salesforce-oauth-initiate/index.ts"
      provides: "JWT-derived org for signState + oauth_pending insert"
      contains: "getAuthedUserAndOrg"
    - path: "supabase/functions/salesforce-oauth-disconnect/index.ts"
      provides: "JWT-derived org for salesforce_connections lookup + Vault revoke"
      contains: "getAuthedUserAndOrg"
  key_links:
    - from: "salesforce-oauth-initiate/index.ts"
      to: "signState(org_id, …) + oauth_pending insert"
      via: "JWT orgId from getAuthedUserAndOrg"
      pattern: "getAuthedUserAndOrg"
    - from: "salesforce-oauth-disconnect/index.ts"
      to: "salesforce_connections .eq('org_id', …)"
      via: "JWT orgId"
      pattern: "getAuthedUserAndOrg"
---

<objective>
Harden `salesforce-oauth-initiate` and `salesforce-oauth-disconnect` (REQ-2, D-01): derive `org_id` from the verified JWT instead of the request body. Both are user-authenticated only (RESEARCH Q4: callers `SalesforceConnection.tsx:68` and `:82`), so both safely adopt the shared helper.

Purpose: initiate's body `org_id` is embedded into a signed CSRF state (`signState` at :50) and `oauth_pending` — JWT-deriving it strengthens the identity binding. disconnect's body `org_id` drives `salesforce_connections .eq('org_id', …)` (:43) + Vault token revoke — a mismatched body org_id could disconnect another tenant's Salesforce.
Output: Both functions hardened + org-tamper test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.3-edge-identity-hardening/14.3-RESEARCH.md
@.planning/phases/14.3-edge-identity-hardening/14.3-PATTERNS.md

<interfaces>
From supabase/functions/_shared/auth.ts (created in 14.3-01):
  export async function getAuthedUserAndOrg(req, corsHeaders?): Promise<{ userId: string; orgId: string }>  // throws Response on 401

Consumption shape:
  let orgId: string
  try { ({ orgId } = await getAuthedUserAndOrg(req, corsHeaders)) }
  catch (e) { if (e instanceof Response) return e; throw e }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden salesforce-oauth-initiate (JWT org → signState + oauth_pending)</name>
  <read_first>
    - supabase/functions/salesforce-oauth-initiate/index.ts:33 (body `org_id, is_sandbox` destructure), :50 (`signState(org_id, nonce, consumerSecret)`), :57-60 (service-role client), :61-66 (`oauth_pending` insert using org_id), :1-7 (existing `../_shared/salesforce-crypto.ts` import — same style for `../_shared/auth.ts`)
    - supabase/functions/section-ai-action/index.ts:53-68 (Pattern B two-client — analog)
    - supabase/functions/_shared/auth.ts (helper — plan 01)
  </read_first>
  <behavior>
    - org_id used by signState + oauth_pending insert is the JWT-derived org, not the body
    - is_sandbox is still read from the body
    - A body org_id that differs from the JWT org_id → 403
  </behavior>
  <action>
    1. Add import (match the existing `../_shared/salesforce-crypto.ts` import shape):
    ```ts
    import { getAuthedUserAndOrg, jsonError } from '../_shared/auth.ts'
    ```
    2. At :33 keep reading `is_sandbox` (and any other non-identity fields) from the body; keep the body `org_id` ONLY to detect a mismatch. Immediately derive the JWT org:
    ```ts
    let orgId: string
    try { ({ orgId } = await getAuthedUserAndOrg(req, corsHeaders)) }
    catch (e) { if (e instanceof Response) return e; throw e }
    if (body_org_id && body_org_id !== orgId) return jsonError(403, 'org mismatch', corsHeaders)
    ```
    (rename the body destructure to `body_org_id` so it can't be mistaken for the trusted value).
    3. Pass the JWT `orgId` into `signState(orgId, nonce, consumerSecret)` at :50 and into the `oauth_pending` insert at :61-66. Keep the existing service-role client (:57-60) for the insert.
    4. Grep-confirm `signState(` and the `oauth_pending` insert reference `orgId` (JWT), not the body value.
  </action>
  <verify>
    <automated>grep -q "getAuthedUserAndOrg" supabase/functions/salesforce-oauth-initiate/index.ts && grep -q "signState(orgId" supabase/functions/salesforce-oauth-initiate/index.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `salesforce-oauth-initiate/index.ts` contains `import { getAuthedUserAndOrg` from `'../_shared/auth.ts'`
    - `signState(` is called with the JWT `orgId` (grep: `signState(orgId`)
    - the `oauth_pending` insert references `orgId` (JWT), not `body_org_id`
    - contains a 403 mismatch guard
    - `is_sandbox` is still read from the body (grep: `is_sandbox`)
  </acceptance_criteria>
  <done>initiate binds CSRF state + oauth_pending to the JWT org; 403 on body mismatch; is_sandbox still from body.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden salesforce-oauth-disconnect (JWT org → connections lookup + Vault revoke)</name>
  <read_first>
    - supabase/functions/salesforce-oauth-disconnect/index.ts:25 (body `org_id` destructure), :34-37 (service-role client), :40-45 (`salesforce_connections .eq('org_id', org_id)` lookup + Vault revoke/delete). Note: this file does NOT yet import from `_shared` — add the import.
    - supabase/functions/section-ai-action/index.ts:53-68 (Pattern B analog)
    - supabase/functions/_shared/auth.ts (helper — plan 01)
  </read_first>
  <behavior>
    - The salesforce_connections lookup + Vault revoke operate on the JWT-derived org
    - A body org_id differing from the JWT org_id → 403 (cannot disconnect another tenant)
  </behavior>
  <action>
    1. Add the import (new — this file has none from _shared yet):
    ```ts
    import { getAuthedUserAndOrg, jsonError } from '../_shared/auth.ts'
    ```
    2. At :25 rename the body destructure to `body_org_id`. Derive the JWT org BEFORE the :34-37 service-role client + :40-45 lookup:
    ```ts
    let orgId: string
    try { ({ orgId } = await getAuthedUserAndOrg(req, corsHeaders)) }
    catch (e) { if (e instanceof Response) return e; throw e }
    if (body_org_id && body_org_id !== orgId) return jsonError(403, 'org mismatch', corsHeaders)
    ```
    3. Use the JWT `orgId` in the `.eq('org_id', orgId)` filter at :43 and everywhere the lookup/revoke references org. Keep the service-role client for the privileged Vault revoke/delete.
    4. Grep-confirm `.eq('org_id', orgId)` (JWT), no `.eq('org_id', body_org_id)` remains.
  </action>
  <verify>
    <automated>grep -q "getAuthedUserAndOrg" supabase/functions/salesforce-oauth-disconnect/index.ts && grep -q "\.eq('org_id', orgId)" supabase/functions/salesforce-oauth-disconnect/index.ts && ! grep -q "\.eq('org_id', body_org_id)" supabase/functions/salesforce-oauth-disconnect/index.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `salesforce-oauth-disconnect/index.ts` contains `import { getAuthedUserAndOrg` from `'../_shared/auth.ts'`
    - the `salesforce_connections` lookup uses `.eq('org_id', orgId)` (JWT)
    - zero occurrences of `.eq('org_id', body_org_id)` (raw body)
    - contains a 403 mismatch guard
  </acceptance_criteria>
  <done>disconnect operates on the JWT org; a mismatched body org_id is rejected with 403; Vault revoke stays service-role.</done>
</task>

<task type="auto">
  <name>Task 3: Add org-tamper test scaffolds for both functions</name>
  <read_first>
    - supabase/functions/salesforce-oauth-initiate/index.ts + salesforce-oauth-disconnect/index.ts (post-edit)
    - supabase/functions/retrieve-context/test.ts (repo Deno test conventions)
    - RESEARCH.md Validation Architecture → Test Map (the salesforce-oauth-* row)
  </read_first>
  <action>
    Create `salesforce-oauth-initiate/test.ts` and `salesforce-oauth-disconnect/test.ts`, each with a named case for "mismatched body org_id → operates on JWT org (or 403)". Where the assertion requires live Supabase auth (integration-only), keep a clearly-named `it.skip`/`ignore:true` pointing to 14.3-05 live validation, but add any pure assertion feasible (e.g. that the handler imports getAuthedUserAndOrg / rejects mismatch). Include a top comment naming 14.3-05 as the live-validation owner.
  </action>
  <verify>
    <automated>grep -q "org" supabase/functions/salesforce-oauth-initiate/test.ts && grep -q "org" supabase/functions/salesforce-oauth-disconnect/test.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - both test files exist and each references the org-tamper / 403 behavior
    - each top comment points to 14.3-05 for live validation
  </acceptance_criteria>
  <done>Both salesforce functions have org-tamper test scaffolds referencing the live-validation plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SalesforceConnection.tsx:68/:82 → oauth functions | User JWT calls; body org_id untrusted |
| initiate → signState (signed CSRF state) | org bound into state must be the authenticated user's org |
| disconnect → salesforce_connections + Vault | Revoke must target the caller's own org connection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.3-13 | Spoofing/Tampering (CSRF binding) | initiate signState(org_id) at :50 | mitigate | Bind signed state to JWT org — attacker cannot mint state for another org |
| T-14.3-14 | Elevation (cross-tenant disconnect) | disconnect .eq('org_id', body) at :43 | mitigate | JWT org in the filter; 403 on mismatch — cannot revoke another tenant's Salesforce |
| T-14.3-15 | Repudiation | oauth_pending org provenance | mitigate | oauth_pending row now records JWT-derived org, provably the authed user's |
</threat_model>

<verification>
- Grep acceptance across all three tasks satisfied.
- No raw-body org_id feeds signState, oauth_pending, or the connections lookup.
</verification>

<success_criteria>
Both salesforce-oauth functions derive org from the JWT; body org_id mismatches are rejected with 403; CSRF state + Vault revoke bound to the authenticated org; test scaffolds present.
</success_criteria>

<output>
After completion, create `.planning/phases/14.3-edge-identity-hardening/14.3-04-SUMMARY.md`.
</output>
