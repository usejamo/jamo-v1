---
phase: 07-proposal-generation-engine
task: 2
total_tasks: 2
status: blocked
last_updated: 2026-03-25T17:58:51.839Z
---

<current_state>
Phase 07 is at the human-verify checkpoint (07-04, Task 2). Task 1 (code wiring) is complete and committed. Waiting for human to verify the end-to-end generation flow in the browser. However, a pre-existing bug was discovered during testing that blocks verification: Step 3 of the wizard shows no assumptions.
</current_state>

<completed_work>

- Plan 07-00: Type contracts (src/types/generation.ts) + Nyquist test stubs — Done
- Plan 07-01: generate-proposal-section Edge Function (SSE proxy, anchor mode, 9 unit tests) — Done
- Plan 07-02: useProposalGeneration hook (three-wave orchestration, 18 tests) — Done
- Plan 07-03: Streaming UI components (StatusBadge, SectionStreamCard, GenerationHeader, GenerationControls, 9 tests) — Done
- Plan 07-04 Task 1: Extend ProposalDraftRenderer with streaming mode, wire ProposalDetail with useProposalGeneration, update wizard Generate trigger to navigate with `?generate=true` — Done (`4b417e4`)
- Bug fix: Infinite assumption re-extraction loop in Step2DocumentUpload — Fixed (`3c168ec`)

</completed_work>

<remaining_work>

- Plan 07-04 Task 2: Human verify end-to-end generation flow (BLOCKED — see blockers below)
  1. Deploy Edge Function: `SUPABASE_ACCESS_TOKEN=sbp_c8b850f4456bd20a89e23d07ead1d3136a7fb1e3 npx supabase functions deploy generate-proposal-section --no-verify-jwt`
  2. Start dev server: `npm run dev`
  3. Fix Bug 2 below, then test the full generation flow
  4. Type "approved" to complete phase

</remaining_work>

<decisions_made>

- Wave 1 agents (07-00, 07-01, 07-02) ran in parallel worktrees; 07-02 agent covered 07-00's type contracts as prerequisite
- Merge conflicts resolved: kept HEAD's real tests for test.ts, took worktree's full generation.ts implementation, merged ROADMAP/STATE tracking both agents' work
- 07-04 agent committed directly to master (no worktree merge needed)
- Assumption loop fix: replaced ephemeral `extractionFiredRef` with persistent `state.extractionStatus === 'idle'` guard

</decisions_made>

<blockers>

**Bug 2 (open): Wizard Step 3 shows 0 assumptions for Vericel RFP document**

- Root cause NOT fully confirmed — needs `extract-assumptions` Edge Function logs
- Evidence gathered:
  - document_extracts HAS the content (10,498 chars, readable clinical trial RFP)
  - extract-assumptions returns HTTP 200 in 11-16 seconds (Claude IS processing it)
  - No rows in proposal_assumptions for today's proposals
  - Same function version worked yesterday with a software dev proposal
- Most likely cause: Claude returns `{"assumptions": [], "missing": [...]}` for a sponsor RFP format vs. a CRO proposal format (system prompt says "CRO proposal documents")
- To diagnose: add `console.log('[extract-assumptions] docs:', documents.length, 'response:', responseText.slice(0, 200))` at line ~165 in `supabase/functions/extract-assumptions/index.ts`, deploy, test, check Supabase dashboard logs
- Alternatively: test with a document that IS a CRO proposal response (not a sponsor RFP)

</blockers>

<context>
Phase 7 wave execution went smoothly. All 4 plans (07-00 through 07-03) executed and merged. 07-04 Task 1 wired everything together. During human verify testing, user discovered the assumption extraction loop bug (pre-existing Phase 6 issue). Fixed the loop. Still need to diagnose and fix why Claude returns empty assumptions for the Vericel RFP before the E2E generation flow can be verified (assumptions feed into the generation prompt).

The worktree merge conflict was a one-time pain from parallel execution — 07-00 and 07-02 agents both touched generation.ts and test files. Resolved cleanly.
</context>

<next_action>
1. Add debug log to `supabase/functions/extract-assumptions/index.ts` around line 165 after `const responseText = ...`:
   `console.log('[debug] docs:', documents.length, 'response:', responseText.slice(0, 300))`
2. Deploy: `SUPABASE_ACCESS_TOKEN=sbp_c8b850f4456bd20a89e23d07ead1d3136a7fb1e3 npx supabase functions deploy extract-assumptions --no-verify-jwt`
3. Run the wizard, check Supabase Edge Function logs to see Claude's actual response
4. Fix accordingly (likely update system prompt to handle RFP documents, or fix parseClaudeResponse to handle array format)
5. Once assumptions work: deploy generate-proposal-section and verify E2E generation flow
6. Type "approved" → phase verification → completion
</next_action>
