---
phase: 14.4-deterministic-multi-section-substitution
plan: 02
type: execute
wave: 1
depends_on: [14.4-01]
files_modified:
  - supabase/functions/chat-with-jamo/tools/substitute-placeholders.ts
  - supabase/functions/chat-with-jamo/index.ts
  - supabase/functions/chat-with-jamo/context.ts
autonomous: true
requirements: [R1, R4]
must_haves:
  truths:
    - "The chat model can emit a substitute_placeholders(value, targets[]) tool call carrying CLASSIFICATION only — no after_html / no generated edit content (D-03)"
    - "The system prompt teaches the model to call substitute_placeholders (not propose_edit) to fill a named placeholder with a supplied value, defaulting scope to all sections containing a matching placeholder"
    - "The model biases to skip: when unsure a single value satisfies a multi-part label, it emits decision:'skip' with a skip_reason (D-12)"
    - "The existing PRESERVE-PLACEHOLDERS rule is scoped to propose_edit only, so substitute_placeholders is the sanctioned fill path"
    - "The single-section propose_edit path is 100% unchanged"
  artifacts:
    - path: "supabase/functions/chat-with-jamo/tools/substitute-placeholders.ts"
      provides: "Anthropic Tool schema (value + targets[{section_key, placeholder_id, decision, skip_reason?}]) + pass-through handler"
      contains: "substitute_placeholders"
    - path: "supabase/functions/chat-with-jamo/index.ts"
      provides: "Tool registration in tools array + explicit dispatch case"
      contains: "substituteePlaceholdersTool"
    - path: "supabase/functions/chat-with-jamo/context.ts"
      provides: "buildSystemPrompt guidance: tool-selection line + bias-to-skip + scoped preserve rule"
      contains: "substitute_placeholders"
  key_links:
    - from: "index.ts tools array"
      to: "substituteePlaceholdersTool"
      via: "import + array entry"
      pattern: "substituteePlaceholdersTool"
    - from: "index.ts switch(currentToolName)"
      to: "handleSubstitutePlaceholders"
      via: "case 'substitute_placeholders' pass-through"
      pattern: "case .substitute_placeholders."
---

<objective>
Register a NEW dedicated edge tool `substitute_placeholders` in `chat-with-jamo` that carries the model's CLASSIFICATION/ROUTING only (D-02, D-03) — the literal `value` plus per-target `{ section_key, placeholder_id, decision, skip_reason? }` — with NO `after_html` and NO generated edit content. Teach the model (in `buildSystemPrompt`) when to call it vs `propose_edit`, how to scope from natural language (default = all sections with a matching placeholder), and to bias-to-skip on uncertainty (D-12). Leave the shipped single-section `propose_edit` path untouched.

Deno is unavailable and `supabase/**` is excluded from vitest, so this plan verifies via grep/static acceptance (matching how 14.3 handled edge changes). The actual deploy is Plan 05's blocking task.

Purpose: Provide the classification surface (R1) and the bias-to-skip routing signal (R4) that the client fan-out consumes.
Output: one new tool file + two edits to index.ts + one edit to context.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.4-deterministic-multi-section-substitution/14.4-SPEC.md
@.planning/phases/14.4-deterministic-multi-section-substitution/14.4-PATTERNS.md
@.planning/phases/14.4-deterministic-multi-section-substitution/14.4-RESEARCH.md
@supabase/functions/chat-with-jamo/tools/propose-edit.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the substitute-placeholders edge tool (schema mirror + pass-through handler)</name>
  <read_first>
    - supabase/functions/chat-with-jamo/tools/substitute-placeholders.ts (file being created)
    - supabase/functions/chat-with-jamo/tools/propose-edit.ts (EXACT shape to mirror — Tool import, input_schema, pass-through handler; do NOT extend it, D-02)
  </read_first>
  <action>
    Create `supabase/functions/chat-with-jamo/tools/substitute-placeholders.ts`. Import `type { Tool } from "npm:@anthropic-ai/sdk/resources/messages"` (match propose-edit.ts:1). Export `substituteePlaceholdersTool: Tool` with `name: "substitute_placeholders"`, a description carrying the bias-to-skip rule (D-12) and the "classification only, no edit content" contract (D-03), and `input_schema`:
    - `value: { type: "string", description: "The single literal value to substitute into every matching single-value placeholder." }`
    - `targets: { type: "array", items: { type: "object", properties: { section_key: {type:"string"}, placeholder_id: {type:"string", description:"matches data-placeholder-id"}, decision: {type:"string", enum:["substitute","skip"]}, skip_reason: {type:"string", description:"present when decision is skip"} }, required: ["section_key","placeholder_id","decision"] } }`
    - `required: ["value", "targets"]`
    Export `interface SubstitutePlaceholdersInput { value: string; targets: Array<{ section_key: string; placeholder_id: string; decision: 'substitute' | 'skip'; skip_reason?: string }> }` and `handleSubstitutePlaceholders(input: SubstitutePlaceholdersInput)` that returns `input` VERBATIM (pass-through — NO after_html, NO generated content, D-03). Description text must state: "If unsure a single value satisfies a multi-part placeholder label, set decision:'skip' with a short skip_reason — never substitute."
  </action>
  <acceptance_criteria>
    - `grep -n 'substitute_placeholders' supabase/functions/chat-with-jamo/tools/substitute-placeholders.ts` shows the tool name.
    - `grep -nE 'enum.*substitute.*skip|substitute.*,.*skip' supabase/functions/chat-with-jamo/tools/substitute-placeholders.ts` confirms the decision enum.
    - `grep -n 'after_html\|changes\|overall_summary' supabase/functions/chat-with-jamo/tools/substitute-placeholders.ts` returns NOTHING (classification-only guardrail, D-03).
    - `grep -n 'skip_reason' ...` present; `grep -n 'handleSubstitutePlaceholders' ...` present.
  </acceptance_criteria>
  <verify>grep confirms tool name + decision enum + skip_reason present, and after_html/changes/overall_summary ABSENT.</verify>
  <done>Tool file exists, mirrors propose-edit.ts structure, carries value+targets classification schema, handler is a verbatim pass-through with no edit content.</done>
</task>

<task type="auto">
  <name>Task 2: Register substitute_placeholders in index.ts (tools array + dispatch case)</name>
  <read_first>
    - supabase/functions/chat-with-jamo/index.ts (file being modified — tools array ~72-78, switch(currentToolName) ~258-344, SSE emit ~350)
    - supabase/functions/chat-with-jamo/tools/substitute-placeholders.ts (Task 1 exports to import)
  </read_first>
  <action>
    Edit `supabase/functions/chat-with-jamo/index.ts`: (1) import `substituteePlaceholdersTool, handleSubstitutePlaceholders, type SubstitutePlaceholdersInput` from `./tools/substitute-placeholders.ts` alongside the existing propose-edit import; (2) add `substituteePlaceholdersTool` to the `tools` array (~line 72, after the existing entries); (3) add an explicit `case "substitute_placeholders":` in the `switch (currentToolName)` dispatch (~line 258) that does `toolResult = handleSubstitutePlaceholders(toolInput as SubstitutePlaceholdersInput); break;` — this mirrors the `propose_edit` case for validation/logging parity (the `default` at ~342 already returns toolInput, but add the explicit case). DO NOT touch the SSE emit at ~350 (`sendSSE(controller, { type: "tool_result", tool: currentToolName, result: toolResult })` is already tool-agnostic and multi-tool-turn safe). DO NOT touch the propose_edit case.
  </action>
  <acceptance_criteria>
    - `grep -n 'substituteePlaceholdersTool' supabase/functions/chat-with-jamo/index.ts` shows BOTH an import and the tools-array entry (2+ hits).
    - `grep -n 'case "substitute_placeholders"' supabase/functions/chat-with-jamo/index.ts` shows the dispatch case.
    - `grep -n 'handleSubstitutePlaceholders' supabase/functions/chat-with-jamo/index.ts` present.
    - `grep -c 'propose_edit' supabase/functions/chat-with-jamo/index.ts` unchanged from before the edit (propose_edit path untouched — capture the baseline count first).
  </acceptance_criteria>
  <verify>grep shows substituteePlaceholdersTool imported + in tools array, and case "substitute_placeholders" in the dispatch switch; propose_edit occurrences unchanged.</verify>
  <done>Tool registered in the tools array and dispatched via an explicit pass-through case; SSE emit and propose_edit path unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Extend buildSystemPrompt — tool selection + bias-to-skip + scope PRESERVE rule to propose_edit</name>
  <read_first>
    - supabase/functions/chat-with-jamo/context.ts (file being modified — buildSystemPrompt, toolNames build ~21, tool-selection guide ~42-47, PRESERVE PLACEHOLDERS rule ~55)
  </read_first>
  <action>
    Edit `supabase/functions/chat-with-jamo/context.ts` inside `buildSystemPrompt`: (1) add a tool-selection line for `substitute_placeholders`: "Call substitute_placeholders when the user supplies ONE literal value to drop into a NAMED placeholder across one/several/all sections (e.g. 'replace every section's investigational product name with albacore'). Default scope = ALL sections containing a matching placeholder unless the user names specific sections. Emit one target per matching placeholder span with its section_key + placeholder_id."; (2) add the bias-to-skip rule (D-12): "For each target, set decision:'substitute' only when a single literal value fully satisfies the placeholder's label. If the label is a multi-part prompt (e.g. 'Confirm name, mechanism of action, drug class, and regulatory precedent') or you are unsure, set decision:'skip' with a short skip_reason. Never generate edit content — substitute_placeholders carries routing only; the client performs the substitution."; (3) SCOPE the existing PRESERVE-PLACEHOLDERS rule (~line 55, "You MUST copy these spans verbatim … never replace, inline, or remove them") to `propose_edit` ONLY — reword so it reads that propose_edit must preserve spans, and that filling a placeholder with a supplied value is done via substitute_placeholders (the sanctioned fill path), resolving the RESEARCH §Finding 6 conflict.
  </action>
  <acceptance_criteria>
    - `grep -n 'substitute_placeholders' supabase/functions/chat-with-jamo/context.ts` shows the selection + bias-to-skip guidance.
    - `grep -niE 'bias|unsure|skip_reason|multi-part' supabase/functions/chat-with-jamo/context.ts` confirms the bias-to-skip text.
    - `grep -niE 'propose_edit' supabase/functions/chat-with-jamo/context.ts` appears near the PRESERVE-PLACEHOLDERS rule (rule scoped to propose_edit).
    - `grep -ni 'default scope\|all sections' supabase/functions/chat-with-jamo/context.ts` confirms the scoping rule.
  </acceptance_criteria>
  <verify>grep confirms substitute_placeholders selection guidance, bias-to-skip language, default-scope rule, and PRESERVE rule now scoped to propose_edit.</verify>
  <done>System prompt teaches when to call substitute_placeholders, biases to skip on uncertainty, sets default scope to all matching sections, and no longer forbids the sanctioned fill path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → chat-with-jamo edge | Identity (user_id/org_id) is derived from the verified JWT via `_shared/auth.ts` (14.3), NOT the request body. |
| model tool output → client | `substitute_placeholders` payload is an untrusted CLAIM the client re-validates (D-04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.4-04 | Elevation of privilege | handleSubstitutePlaceholders reintroducing body-trusted identity | mitigate | The handler is a pure pass-through of `toolInput` (value+targets) and reads NO user_id/org_id; the surrounding handler still derives identity from the JWT per 14.3 (unchanged). Acceptance greps confirm no identity fields in the tool. |
| T-14.4-05 | Tampering | Cross-tenant section_key in targets | accept | The edge tool does not fetch or write by section_key; it only echoes routing. The client fan-out (Plan 04) resolves section_key against the caller's own in-browser editorRefs — a foreign section_key has no live handle → skip. No cross-tenant read/write introduced. |
| T-14.4-06 | Info disclosure | Tool description leaking secrets | accept | Description is static prompt text, no secrets; no new env vars, no VITE_ exposure. |
</threat_model>

<verification>
- Grep acceptance for all three files (Deno unavailable; `supabase/**` excluded from vitest — per 14.3 contingency).
- `npx vitest run` still green (no client files touched → suite unaffected).
- NOTE: registration runs stale in prod until Plan 05 deploys `chat-with-jamo` explicitly.
</verification>

<success_criteria>
- substitute_placeholders registered in the tools array + dispatched with an explicit pass-through case.
- Tool schema carries classification only (no after_html/changes/overall_summary).
- System prompt teaches tool selection, default scope, and bias-to-skip; PRESERVE rule scoped to propose_edit.
- propose_edit path byte-unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/14.4-deterministic-multi-section-substitution/14.4-02-SUMMARY.md`
</output>
