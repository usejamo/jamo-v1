---
phase: 14.4-deterministic-multi-section-substitution
plan: 03
type: execute
wave: 1
depends_on: [14.4-01]
files_modified:
  - src/components/chat/BulkSubstitutionSummaryCard.tsx
autonomous: true
requirements: [R6]
must_haves:
  truths:
    - "The aggregate card enumerates applied substitutions grouped by section AND all skipped items (section + placeholder label + reason)"
    - "Accept-all applies every proposed substitution across all affected sections; reject-all discards all of them"
    - "Counts render from the RECONCILED CLIENT OUTCOME (props), not a raw tool proposal (D-15)"
    - "The card composes EditSummaryCard's visual + accept/reject patterns — not a new design language (D-13)"
  artifacts:
    - path: "src/components/chat/BulkSubstitutionSummaryCard.tsx"
      provides: "Cross-section aggregate review card with accept-all / reject-all + skip enumeration"
      exports: ["BulkSubstitutionSummaryCard"]
      min_lines: 60
  key_links:
    - from: "BulkSubstitutionSummaryCard onAcceptAll"
      to: "parent handler (→ BATCH_ACCEPT_PENDING_EDITS per section, wired in Plan 04)"
      via: "onAcceptAll prop callback"
      pattern: "onAcceptAll"
    - from: "BulkSubstitutionSummaryCard skip list"
      to: "reconciled SkipEntry[] (from substitute.ts)"
      via: "skipped prop mapping section+label+reason"
      pattern: "skip"
---

<objective>
Build `BulkSubstitutionSummaryCard` (D-13) — the aggregate cross-section review control (R6). It composes `EditSummaryCard`'s existing visual language and accept/reject wiring (NOT a new design). It lists applied substitutions grouped by section (with a per-section applied count) and enumerates every skipped item (section + placeholder label + reason, the unified skip channel D-05/D-16), and exposes accept-all / reject-all across all affected sections. All counts come from the RECONCILED CLIENT OUTCOME passed as props (D-15) — this component renders; it does not compute the outcome (Plan 01 does) and does not dispatch (Plan 04 wires the callbacks).

This is a presentational + callback-prop component. Component RTL tests OOM on this dev machine (STATE: @testing-library/react pulls react-dom → OOM), so acceptance is tsc/grep-based; live behavior is verified in Plan 05 UAT.

Purpose: The aggregate review surface (SPEC R6); the "panel half" of R7's aggregate-panel + chat-line.
Output: one new component file consumed by AIChatPanel in Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.4-deterministic-multi-section-substitution/14.4-PATTERNS.md
@.planning/phases/14.4-deterministic-multi-section-substitution/14.4-SPEC.md
@src/components/chat/EditSummaryCard.tsx

<interfaces>
<!-- Reconciled-outcome types this card renders — exported from Plan 01's substitute.ts. -->
```ts
// src/editor/placeholders/substitute.ts
export interface SkipEntry { section_key: string; label: string; reason: string }
export interface ReconciledOutcome {
  applied: number
  skipped: SkipEntry[]
  outcome: 'full' | 'partial' | 'zero-match'
}
```
EditSummaryCard accept/reject prop contract to mirror (EditSummaryCard.tsx:4-14):
`onAcceptAll: () => void`, `onRejectAll: () => void`, `onReviewInEditor: () => void`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BulkSubstitutionSummaryCard component (applied-by-section + skip enumeration + accept-all/reject-all)</name>
  <read_first>
    - src/components/chat/BulkSubstitutionSummaryCard.tsx (file being created)
    - src/components/chat/EditSummaryCard.tsx (analog — reuse its container styling, header, accept-all/reject-all button treatment; do NOT invent a new design, D-13)
    - src/editor/placeholders/substitute.ts (Plan 01 — import ReconciledOutcome, SkipEntry types)
  </read_first>
  <action>
    Create `src/components/chat/BulkSubstitutionSummaryCard.tsx` exporting `BulkSubstitutionSummaryCard`. Props:
    ```ts
    interface BulkSubstitutionSummaryCardProps {
      value: string
      appliedBySection: Array<{ section_key: string; section_title?: string; count: number }>
      skipped: SkipEntry[]                       // from substitute.ts (section_key, label, reason)
      outcome: 'full' | 'partial'                // zero-match never renders this card (parent suppresses)
      onAcceptAll: () => void                    // parent fans BATCH_ACCEPT across sections (Plan 04)
      onRejectAll: () => void                    // parent rejects all across sections (Plan 04)
      resolved?: 'accepted' | 'rejected'         // when set, show terminal state, hide action buttons
    }
    ```
    Reuse EditSummaryCard's container/header/button classes (import shared bits or replicate the same Tailwind treatment). Render: (1) a header "Substituted \"{value}\"" with the total applied count = sum(appliedBySection.count); (2) an "Applied" section listing each section (section_title ?? section_key) + its count; (3) a "Skipped" section (only when skipped.length > 0) enumerating each SkipEntry as "{section} — {label}: {reason}" (D-16); (4) Accept all / Reject all buttons calling onAcceptAll / onRejectAll, hidden when `resolved` is set (show "Accepted"/"Rejected" badge instead). Keep it purely presentational — no workspace dispatch, no materialize calls here.
  </action>
  <acceptance_criteria>
    - `grep -n 'export function BulkSubstitutionSummaryCard\|export const BulkSubstitutionSummaryCard' src/components/chat/BulkSubstitutionSummaryCard.tsx` present.
    - `grep -nE 'onAcceptAll|onRejectAll' src/components/chat/BulkSubstitutionSummaryCard.tsx` shows both callbacks invoked.
    - `grep -nE 'skipped|SkipEntry|reason' src/components/chat/BulkSubstitutionSummaryCard.tsx` confirms skip enumeration.
    - `grep -n 'appliedBySection' src/components/chat/BulkSubstitutionSummaryCard.tsx` confirms per-section applied grouping.
    - `grep -n 'dispatch\|materializePendingEdits\|useSectionWorkspace' src/components/chat/BulkSubstitutionSummaryCard.tsx` returns NOTHING (presentational only — wiring is Plan 04).
    - `npx tsc --noEmit` clean for the new file (no type errors against imported ReconciledOutcome/SkipEntry).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i BulkSubstitutionSummaryCard || echo "no tsc errors for card"</automated>
  </verify>
  <done>Card renders applied-by-section + enumerated skips + accept-all/reject-all from props; contains no workspace dispatch or materialize; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconciled outcome (props) → rendered counts | Card trusts its props; the reconciliation (D-15) happens upstream in Plan 01/04, not here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.4-07 | Tampering (display integrity) | Card showing "applied" for an item the client couldn't resolve | mitigate | Card renders `appliedBySection` / `skipped` from the RECONCILED outcome (D-15) supplied by the parent; it has no access to the raw tool proposal, so it cannot over-report. Acceptance greps confirm no independent count source. |
| T-14.4-08 | Injection (XSS) | Rendering `value` / labels / reasons | mitigate | All strings rendered as React text children (auto-escaped); no `dangerouslySetInnerHTML` in this card. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npx vitest run` still green (no existing files modified; new file has no test but must not break compilation of importers — none yet until Plan 04).
</verification>

<success_criteria>
- BulkSubstitutionSummaryCard exists, composes EditSummaryCard patterns, enumerates applied-by-section + skips, exposes accept-all/reject-all callbacks, computes no counts of its own.
</success_criteria>

<output>
After completion, create `.planning/phases/14.4-deterministic-multi-section-substitution/14.4-03-SUMMARY.md`
</output>
