---
phase: 14.4-deterministic-multi-section-substitution
plan: 05
type: execute
wave: 3
depends_on: [14.4-04]
files_modified: []
autonomous: false
requirements: [R4, R5, R6, R7]
must_haves:
  truths:
    - "The chat-with-jamo edge function is DEPLOYED so the substitute_placeholders tool registration + system-prompt guidance run in prod (not stale)"
    - "A live bulk request substitutes a placeholder across ≥2 sections in one turn with independent per-section ghost review (R5)"
    - "The aggregate card shows reconciled applied (per section) + skipped (section+label+reason); accept-all/reject-all work across sections (R6)"
    - "Full, partial, and zero-match runs each produce a visible non-silent result — zero-match shows a chat message and NO empty panel (R7)"
    - "A known multi-part placeholder is skipped-and-reported, never force-filled (R4 bias-to-skip)"
  artifacts: []
  key_links:
    - from: "supabase functions deploy chat-with-jamo"
      to: "live prod tool registry"
      via: "explicit CLI deploy (execute-phase commits but does NOT deploy)"
      pattern: "chat-with-jamo"
---

<objective>
Ship and prove the phase. First DEPLOY `chat-with-jamo` explicitly (MEMORY: edge-functions-need-deploy — GSD execute-phase commits but never deploys edge functions; without this the new tool registration + bias-to-skip guidance run stale in prod). Then run the manual UAT that only a live editor + SSE + multiple sections can validate: multi-section fan-out (R5), aggregate accept-all/reject-all + reconciled counts (R6), the three honest-reporting outcomes full/partial/zero-match (R7), and classifier bias-to-skip on a multi-part placeholder (R4).

This is the final blocking gate. It is intentionally separate from Plan 04 (implementation) because it pairs a deploy with human verification (checkpoint) — no automated command can confirm the model's live routing or the cross-section review UX.

Purpose: Close R4/R5/R6/R7's behavioral acceptance and guarantee prod runs the shipped code.
Output: deployed edge function + a recorded UAT pass (or gap list for /gsd-plan-phase --gaps).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.4-deterministic-multi-section-substitution/14.4-VALIDATION.md
@.planning/phases/14.4-deterministic-multi-section-substitution/14.4-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy chat-with-jamo edge function</name>
  <read_first>
    - .planning/phases/14.4-deterministic-multi-section-substitution/14.4-RESEARCH.md (deploy note: Deno unavailable; deploy explicitly)
    - supabase/functions/chat-with-jamo/index.ts (confirm substitute_placeholders is registered before deploy)
  </read_first>
  <action>
    Deploy the edge function so prod runs the new tool registration + system-prompt guidance from Plan 02. Run `supabase functions deploy chat-with-jamo --project-ref fuuvdcvbliijffogjnwg`. If the CLI returns an auth error, STOP and raise an authentication checkpoint (user runs `npx supabase login`), then retry — do NOT skip the deploy. After deploy, capture the deployed version number from the CLI output and record it in the SUMMARY. NOTE: this phase introduces NO migration (placeholder data model unchanged per CONTEXT out-of-scope) — do NOT run `supabase db push` (it fails here anyway).
  </action>
  <acceptance_criteria>
    - `supabase functions deploy chat-with-jamo` completes successfully and prints a new version.
    - The deployed function serves the substitute_placeholders tool (confirmed in Task 2 UAT when the model can call it live).
  </acceptance_criteria>
  <verify>Deploy command exits 0 and reports a new deployed version; version recorded in SUMMARY.</verify>
  <done>chat-with-jamo deployed to project fuuvdcvbliijffogjnwg with substitute_placeholders live; deployed version recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live UAT — multi-section fan-out, aggregate control, full/partial/zero-match, bias-to-skip</name>
  <action>Pause for human verification. Do not proceed until the user confirms the 7 checks below pass (or reports gaps for /gsd-plan-phase 14.4 --gaps).</action>
  <what-built>
    Deterministic multi-section placeholder substitution end-to-end: chat classifies + routes (substitute_placeholders), the client deterministically locates/substitutes per section, fans out N reviewable changesets, shows the BulkSubstitutionSummaryCard, and reports full/partial/zero-match honestly.
  </what-built>
  <how-to-verify>
    Prereq: dev app at http://localhost:5173, login usera@jamo.com / password123. Open a proposal that has the SAME placeholder label in ≥2 sections (and, for the multi-part test, a multi-part placeholder like "Confirm name, mechanism of action, drug class, and regulatory precedent").

    1. MULTI-SECTION FAN-OUT (R5): In chat, "replace every section's <label> placeholder with albacore". Expect: ghost decorations appear in EACH matching section; accepting/rejecting one section's changeset does not affect another's.
    2. AGGREGATE CONTROL (R6): Confirm the BulkSubstitutionSummaryCard lists applied substitutions grouped by section + a per-section count. Click Accept all → every substitution applies across all sections as one undo step per section (Ctrl+Z reverts a section's batch). Re-run and click Reject all → all discarded.
    3. MULTI-PLACEHOLDER-IN-ONE-PARAGRAPH (D-09): On a paragraph containing two target placeholders, confirm BOTH fill correctly in ONE change (no duplicated/dropped value).
    4. PARTIAL + BIAS-TO-SKIP (R4/R7): Request a fill that matches a mix of single-value and a multi-part placeholder. Expect: single-value substituted; the multi-part one SKIPPED with a reason in the card; chat one-liner "Substituted in N sections, skipped M".
    5. ZERO-MATCH (R7): "replace any CRO name with Labcorp" where CRO name is NOT a placeholder. Expect: a clear chat message that it isn't a fillable placeholder, NO empty review panel, NO blank bubble.
    6. FULL-SUCCESS (R7): a request where all targets are single-value → all substituted + chat summary "Substituted \"albacore\" in N sections."
    7. Confirm the existing single-section propose_edit path still works (run a normal section edit).
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 pass, or list which checks failed (with the chat request + observed vs expected) so gaps can be planned via /gsd-plan-phase 14.4 --gaps.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy → prod runtime | Stale deployment is itself a correctness/security risk: prod could run without the bias-to-skip guidance. |
| live model routing → client | Confirmed end-to-end that the client remains authoritative (D-04) under real model output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.4-13 | Tampering (stale code) | prod running pre-14.4 chat-with-jamo | mitigate | Explicit deploy task (Task 1) is BLOCKING; deployed version recorded; UAT (Task 2) confirms the tool is live. |
| T-14.4-14 | Info disclosure (cross-tenant) | live fan-out under real model routing | mitigate | UAT step 1 uses a single authed user's own proposal; client authority (D-04) means a foreign/unresolvable target is a no-op skip. chat-with-jamo identity is JWT-derived (14.3), unchanged by this phase. |
</threat_model>

<verification>
- Task 1: deploy exits 0, new version recorded.
- Task 2: all 7 UAT checks pass (or gaps captured for --gaps replanning).
- Before sign-off: `npx vitest run` full suite green (regression guard).
</verification>

<success_criteria>
- chat-with-jamo deployed with substitute_placeholders live.
- Multi-section fan-out, aggregate accept-all/reject-all, multi-placeholder-per-paragraph, partial+bias-to-skip, zero-match, and full-success all verified live.
- Single-section propose_edit path unregressed.
</success_criteria>

<output>
After completion, create `.planning/phases/14.4-deterministic-multi-section-substitution/14.4-05-SUMMARY.md` recording the deployed version and UAT results.
</output>
