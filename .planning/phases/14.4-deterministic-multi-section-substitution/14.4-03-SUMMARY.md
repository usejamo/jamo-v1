---
phase: 14.4-deterministic-multi-section-substitution
plan: 03
subsystem: ui
tags: [react, typescript, chat, tailwind, presentational-component]

# Dependency graph
requires:
  - phase: 14.4-01
    provides: "ReconciledOutcome/SkipEntry types from substitute.ts (not yet landed at execution time — see Deviations)"
provides:
  - "BulkSubstitutionSummaryCard: cross-section aggregate review card (accept-all/reject-all + skip enumeration)"
affects: [14.4-04, 14.4-05]

# Tech tracking
tech-stack:
  added: []
  patterns: ["Presentational review card composing EditSummaryCard's container/header/button treatment (D-13)"]

key-files:
  created: [src/components/chat/BulkSubstitutionSummaryCard.tsx]
  modified: []

key-decisions:
  - "SkipEntry declared locally in the new component (not imported from substitute.ts) because Plan 01 had not landed in this worktree's base at execution time; shape matches the plan's documented interface contract exactly so Plan 04 wiring will be structurally compatible."
  - "Card computes zero counts of its own -- totalApplied is a pure sum reduction over appliedBySection props, satisfying D-15 (RECONCILED CLIENT OUTCOME as sole count source)."

patterns-established:
  - "Aggregate review cards reuse EditSummaryCard's exact Tailwind classes (border/rounded/p-3/bg-white container, text-sm font-semibold header, bg-gray-900 accept button, text-gray-400 reject button) rather than inventing new visual language."

requirements-completed: [R6]

# Metrics
duration: 12min
completed: 2026-07-06
---

# Phase 14.4 Plan 03: BulkSubstitutionSummaryCard Summary

**Cross-section aggregate review card composing EditSummaryCard's visual/accept-reject patterns, rendering applied-by-section counts and enumerated skips purely from reconciled-outcome props**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-06T23:02:00Z
- **Completed:** 2026-07-06T23:14:27Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Created `BulkSubstitutionSummaryCard` — the aggregate cross-section review surface for R6/R7
- Renders applied substitutions grouped by section (section_title fallback to section_key) with per-section counts
- Enumerates every skipped item as "{section} — {label}: {reason}" (D-16 unified skip channel)
- Accept-all / reject-all callback props with terminal `resolved` state (badge replaces buttons)
- Zero independent count computation — all totals derive from props (D-15)

## Task Commits

1. **Task 1: BulkSubstitutionSummaryCard component** - `ea3c83a` (feat)

**Plan metadata:** (this commit, docs: complete plan)

## Files Created/Modified
- `src/components/chat/BulkSubstitutionSummaryCard.tsx` - New presentational component: header with total applied count, Applied section (per-section counts), Skipped section (label + reason enumeration), accept-all/reject-all buttons, terminal resolved badge. 97 lines.

## Decisions Made
- **Local SkipEntry type declaration:** Plan 01's `src/editor/placeholders/substitute.ts` (this plan's stated dependency) had not landed in this worktree's base commit at execution time — the directory `src/editor/placeholders/` does not exist yet. Rather than block, `SkipEntry { section_key, label, reason }` was declared locally in the new component file, matching the exact shape documented in this plan's `<interfaces>` block. This keeps the component independently type-checkable now; when Plan 01 lands and Plan 04 wires the real import, the shapes are structurally identical so no consumer changes are needed on this file. A code comment documents the rationale for future readers.
- **Styling reuse:** Copied EditSummaryCard's exact Tailwind class strings (container, header, accept/reject buttons) rather than extracting a shared sub-component, since EditSummaryCard doesn't currently export reusable style primitives — consistent with "compose the same visual language" (D-13) without a refactor outside this plan's scope.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Declared SkipEntry type locally instead of importing from substitute.ts**
- **Found during:** Task 1 (BulkSubstitutionSummaryCard component)
- **Issue:** Plan's `<context>` and `<interfaces>` sections reference `src/editor/placeholders/substitute.ts` (Plan 01, this plan's `depends_on`) as the source of `SkipEntry`/`ReconciledOutcome` types. That file does not exist in this worktree's base commit — Plan 01 had not been executed/merged yet when this wave-1 worktree was created.
- **Fix:** Declared `SkipEntry` locally in `BulkSubstitutionSummaryCard.tsx` with the exact shape specified in the plan (`section_key`, `label`, `reason`), with an inline comment explaining the rationale and noting Plan 04 will thread the real import once Plan 01 lands.
- **Files modified:** `src/components/chat/BulkSubstitutionSummaryCard.tsx`
- **Verification:** `npx tsc --noEmit` clean for the new file; all acceptance-criteria greps pass (export, onAcceptAll/onRejectAll, skipped/SkipEntry/reason, appliedBySection, no dispatch/materialize/useSectionWorkspace).
- **Committed in:** `ea3c83a` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary to unblock execution given the missing upstream dependency file in this worktree; no scope creep — the component still surfaces zero logic of its own and the type shape is contractually identical to what Plan 01 will export. Plan 04 (which wires this card into AIChatPanel alongside Plan 01's real substitute.ts) should confirm the imported `SkipEntry` shape still matches this local declaration, or import it directly and remove the local one.

## Issues Encountered
- Full `npx tsc --noEmit` run shows pre-existing unrelated errors in `useResolvedItemsWriteOnTerminal.askThenFill.test.ts`, `document-upload.integration.test.ts`, `htmlToDocx.ts`, and `chatContext.test.ts` — none reference `BulkSubstitutionSummaryCard`; out of scope per SCOPE BOUNDARY rule, not fixed.
- `npm run test:run` shows 5 pre-existing failures in `src/hooks/__tests__/useGapAnalysisTrigger.spec.ts` (timing/mock-call-count flakiness unrelated to this plan's file) — 46/49 test files passed, 298/319 tests passed. Out of scope, not fixed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- `BulkSubstitutionSummaryCard` is ready for Plan 04 to wire into `AIChatPanel` alongside the real `ReconciledOutcome`/`SkipEntry` types once Plan 01 (`substitute.ts`) lands in the merged tree.
- Plan 04 should verify/replace the locally-declared `SkipEntry` with the Plan 01 export for a single source of truth.

---
*Phase: 14.4-deterministic-multi-section-substitution*
*Completed: 2026-07-06*

## Self-Check: PASSED

- FOUND: src/components/chat/BulkSubstitutionSummaryCard.tsx
- FOUND: commit ea3c83a
- FOUND: .planning/phases/14.4-deterministic-multi-section-substitution/14.4-03-SUMMARY.md
