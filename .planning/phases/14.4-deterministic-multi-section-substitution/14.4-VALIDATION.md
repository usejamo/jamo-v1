---
phase: 14.4
slug: deterministic-multi-section-substitution
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-06
---

# Phase 14.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (+ happy-dom) — already configured |
| **Config file** | `vitest.config.ts` (client); `supabase/**` excluded from vitest |
| **Quick run command** | `npx vitest run src/editor/placeholders` |
| **Full suite command** | `npx vitest run` |
| **Estimated runtime** | ~10–30 seconds (client unit tests) |

**Environment constraint:** Deno is unavailable in this dev environment (per Phase 14.3), and `supabase/**` is excluded from vitest. Edge-side changes (tool registration, system-prompt guidance) get grep/static acceptance, not runtime tests. All new *deterministic* logic (paragraph-HTML rewriter, target→paragraph grouping, reconciliation counts) lives client-side and IS pure-function unit-testable.

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run src/editor/placeholders`
- **After every plan wave:** Run `npx vitest run`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

> Populated by the planner. Each deterministic task maps to a unit test; edge/prompt tasks map to grep/static acceptance; UI fan-out + honest-reporting outcomes map to manual UAT.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 14.4-00-01 | 00 | 0 | R2,R3 | — | N/A | unit (stub) | `npx vitest run src/editor/placeholders` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `src/editor/placeholders/substitute.ts` — pure deterministic paragraph-HTML rewriter (locate span by `data-placeholder-id`, replace inner content with literal value, unwrap/strip placeholder mark, return full `<p data-id>` HTML) + target→paragraph grouping (one composed `after_html` per paragraph)
- [ ] `src/editor/placeholders/substitute.test.ts` — fixtures covering: single-value substitution; multiple placeholders in one paragraph → one composed edit; placeholder in a block lacking `data-id` → skip; unresolvable/absent `data-placeholder-id` → skip; mark-strip verified in output HTML
- [ ] Reconciliation helper test — proposed-substitute target the client can't resolve moves `applied → skipped(unresolvable)` in counts (D-15)

*Existing infrastructure (vitest + happy-dom) covers the framework; Wave 0 only adds the new pure-function module + fixtures.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Multi-section fan-out renders N per-section ghost changesets in one turn | R5 | Requires live editor + SSE + multiple sections | localhost:5173, login usera@jamo.com/password123; open a proposal with a placeholder in ≥2 sections; chat "replace every section's `<label>` with `<value>`"; confirm ghosts in each section |
| Aggregate accept-all / reject-all across sections; card shows reconciled applied/skipped counts | R6 | Cross-section batch UI + post-validation reconciliation | Trigger a bulk run; confirm BulkSubstitutionSummaryCard enumerates applied (per section) + skipped (section+label+reason); accept-all applies all as one undo step |
| Honest reporting: full / partial / zero-match | R4,R7 | Behavioral, spans model + client | Full: all matched substitute; Partial: mix of substitute + multi-part skip → both in panel + chat one-liner; Zero-match: "replace any CRO name with Labcorp" (no such placeholder) → clear chat message, NO empty panel, no blank bubble |
| Classifier bias-to-skip on a known multi-part placeholder | R4 | Model behavior | Target a multi-part placeholder ("Confirm name, mechanism of action, drug class, and regulatory precedent") → skipped+reported, never force-filled |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (substitute.ts + fixtures)
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
