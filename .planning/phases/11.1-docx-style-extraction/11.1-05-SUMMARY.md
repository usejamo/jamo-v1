---
phase: "11.1"
plan: "05"
subsystem: ui
tags: [react, localStorage, discoverability, nudge, docx-export]

# Dependency graph
requires:
  - phase: "11.1-03"
    provides: templateFilePath prop wired into ExportDropdown
provides:
  - One-time post-export discoverability nudge for DOCX template feature
affects: [11.1, settings, ProposalDetail]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - localStorage flag for one-time UI dismissal (template_nudge_dismissed)
    - Promotional nudge rendered inside ExportDropdown fragment alongside existing toast

key-files:
  created: []
  modified:
    - src/pages/ProposalDetail.tsx

key-decisions:
  - "localStorage chosen over DB persistence for MVP — no migration required, key stores only 'true' string (no PII)"
  - "Nudge rendered inside ExportDropdown component (not a separate component) to stay co-located with export state"
  - "Same nudge logic applied to both handleExport and handleForceExport paths for consistency"
  - "Promotional voice used ('Match your firm's branding') rather than error/warning voice per D-14"

patterns-established:
  - "One-time dismissible nudge: localStorage.getItem(key) !== 'true' guard + setItem on dismiss"
  - "Nudge show condition: feature flag absent (!templateFilePath) AND not previously dismissed AND action just succeeded"

requirements-completed: [REQ-10.4b]

# Metrics
duration: 15min
completed: 2026-05-01
---

# Phase 11.1 Plan 05: Post-Export Template Discoverability Nudge Summary

**One-time promotional nudge surfaces the DOCX template feature to users who export without a configured template, persisted via localStorage so it appears exactly once.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-05-01T09:44:00Z
- **Completed:** 2026-05-01T09:59:00Z
- **Tasks:** 1 completed
- **Files modified:** 1

## Accomplishments

- Added `nudgeVisible` state and `handleNudgeDismiss` handler to `ExportDropdown` in `ProposalDetail.tsx`
- Nudge shows after a successful export when `!templateFilePath` and `localStorage.getItem('template_nudge_dismissed') !== 'true'`
- Nudge dismissed via "Maybe later" (persists key, hides) or "Open Settings" (persists key + navigates to `/settings`)
- Applied identical nudge logic to both `handleExport` and `handleForceExport` paths

## Task Commits

1. **Task 1: Add discoverability nudge to ProposalDetail** - `0e08874` (feat)

**Plan metadata:** (docs commit — this file)

## Files Created/Modified

- `src/pages/ProposalDetail.tsx` — added nudge state, dismiss handler, localStorage persistence, and nudge JSX overlay inside ExportDropdown

## Deviations from Plan

None — plan executed exactly as written.

## Threat Surface

| Flag | File | Description |
|------|------|-------------|
| localStorage read/write | src/pages/ProposalDetail.tsx | `template_nudge_dismissed` key — stores only string `'true'`, no PII (accepted per T-11.1-12) |

## Self-Check: PASSED

- `0e08874` commit verified present in git log
- `src/pages/ProposalDetail.tsx` contains `template_nudge_dismissed`, `nudgeVisible`, `handleNudgeDismiss`, "Match your firm's branding", "Open Settings", "Maybe later"
