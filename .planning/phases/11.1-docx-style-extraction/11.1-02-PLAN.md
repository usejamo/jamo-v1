---
phase: "11.1"
plan: "02"
type: execute
wave: 2
depends_on:
  - "11.1-01"
files_modified:
  - supabase/migrations/20260429000025_style_inspection.sql
autonomous: false
requirements:
  - REQ-10.4b
must_haves:
  truths:
    - "supabase db push completes without error"
    - "style_inspection column exists on the live templates table"
  artifacts:
    - path: "supabase/migrations/20260429000025_style_inspection.sql"
      provides: "migration already written in Plan 01"
      contains: "ADD COLUMN style_inspection jsonb"
  key_links:
    - from: "supabase/migrations/20260429000025_style_inspection.sql"
      to: "live Supabase project fuuvdcvbliijffogjnwg"
      via: "supabase db push"
      pattern: "style_inspection"
---

<objective>
[BLOCKING] Push the style_inspection migration to the live Supabase database.

This plan contains a single task that is a hard prerequisite for Wave 3 (Edge Function) and Wave 2 (TemplatesTab query extension). The column must exist in the live database before any code reading or writing it can be verified end-to-end.

Purpose: Unblock all downstream plans that read/write templates.style_inspection.
Output: style_inspection jsonb column live on the templates table.
</objective>

<execution_context>
@C:/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/aaron/VS Code Projects/Jamo/jamo-v1/.planning/STATE.md
@C:/Users/aaron/VS Code Projects/Jamo/jamo-v1/.planning/phases/11.1-docx-style-extraction/11.1-CONTEXT.md

<interfaces>
<!-- Migration file written by Plan 01: -->
<!-- supabase/migrations/20260429000025_style_inspection.sql -->
<!-- ALTER TABLE templates ADD COLUMN style_inspection jsonb; -->

<!-- Supabase project ref: fuuvdcvbliijffogjnwg -->
<!-- Push command: supabase db push -->
<!-- Requires SUPABASE_ACCESS_TOKEN env var if running non-interactively -->
<!-- Or: npx supabase login first if running interactively -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Push migration to live database</name>
  <files>supabase/migrations/20260429000025_style_inspection.sql</files>
  <read_first>
    - supabase/migrations/20260429000025_style_inspection.sql — confirm it contains `ADD COLUMN style_inspection jsonb` before pushing
  </read_first>
  <action>
Run the database push:

```bash
supabase db push
```

If the command prompts interactively and cannot be suppressed, set SUPABASE_ACCESS_TOKEN first:
```bash
export SUPABASE_ACCESS_TOKEN="<token from Supabase dashboard → Account → Access Tokens>"
supabase db push
```

The command applies all pending migrations to project fuuvdcvbliijffogjnwg. Migration 20260429000025_style_inspection.sql is the only pending migration.

After push succeeds, verify the column exists:
```bash
supabase db query "SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name = 'templates' AND column_name = 'style_inspection';"
```

Expected output: one row with `column_name=style_inspection`, `data_type=jsonb`, `is_nullable=YES`.
  </action>
  <verify>
    <automated>supabase db query "SELECT column_name FROM information_schema.columns WHERE table_name = 'templates' AND column_name = 'style_inspection';" 2>&1 | grep style_inspection</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` exits 0 with no error output
    - `supabase db query` on information_schema.columns returns a row for `style_inspection`
    - Column is_nullable = YES (no NOT NULL constraint)
    - Column data_type = jsonb
  </acceptance_criteria>
  <done>style_inspection column is live on the templates table in project fuuvdcvbliijffogjnwg. Plans 03 and 04 can proceed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supabase db push | Migration applied to production database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.1-02 | Tampering | Migration DDL | accept | Migration is additive-only (ADD COLUMN); no data at risk; existing RLS policies unchanged |
| T-11.1-03 | Information Disclosure | SUPABASE_ACCESS_TOKEN | mitigate | Token used only in shell env var, not committed to repo; gitignore covers .env |
</threat_model>

<verification>
- `supabase db push` exits 0
- Column `style_inspection` present in `templates` table (jsonb, nullable)
</verification>

<success_criteria>
Live database has the style_inspection column. Downstream plans can write inspection results from the Edge Function and read them in TemplatesTab.
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-docx-style-extraction/11.1-02-SUMMARY.md`
</output>
