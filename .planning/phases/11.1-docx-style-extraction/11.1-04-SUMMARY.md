---
phase: "11.1"
plan: "04"
subsystem: docx-style-extraction
tags: [jszip, deno, edge-function, style-inspection, tdd, wave-3]
dependency_graph:
  requires:
    - "11.1-01 (jszip installed, test stubs, migration)"
    - "11.1-02 (templates table migration, storage policies)"
  provides:
    - supabase/functions/template-extract/index.ts (inspectDocxStyles exported, step 8.5, step 9 extended)
    - src/components/settings/TemplatesTab.tsx (style_inspection warnings)
  affects:
    - templates table (style_inspection column now populated on upload)
    - TemplatesTab UI (amber per-template warning for missing styles)
tech_stack:
  added: []
  patterns:
    - JSZip loadAsync in Deno Edge runtime for DOCX style XML parsing
    - Dual-match: w:styleId + w:name w:val for case-insensitive style detection (Pitfall 2)
    - D-06 null-on-error pattern: catch returns null, never false positive
    - TDD RED/GREEN cycle for inspectDocxStyles Deno tests
key_files:
  created: []
  modified:
    - supabase/functions/template-extract/index.ts
    - supabase/functions/template-extract/test.ts
    - src/components/settings/TemplatesTab.tsx
decisions:
  - "buffer reuse: fileData.arrayBuffer() called again for step 8.5 — Blob.arrayBuffer() is non-destructive and can be called multiple times"
  - "style_inspection warning placed inside the parse_status === 'ready' block alongside SectionDisclosure — only shown when extraction is complete"
  - "null style_inspection shows no warning per D-12 — consistent with unknown/non-DOCX cases"
metrics:
  duration: "~10 minutes"
  completed: "2026-04-29"
  tasks_completed: 2
  tasks_total: 2
  files_created: 0
  files_modified: 3
---

# Phase 11.1 Plan 04: Style Inspection & TemplatesTab Warnings Summary

**One-liner:** inspectDocxStyles() implemented in template-extract Edge Function (JSZip XML parse, dual styleId+name matching, null-on-error), persisted as style_inspection in step 9 UPDATE; TemplatesTab renders per-template amber warnings naming missing styles with a Word how-to-fix link.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add inspectDocxStyles to template-extract Edge Function | f2cf19f | supabase/functions/template-extract/index.ts, supabase/functions/template-extract/test.ts |
| 2 | Display style_inspection warnings in TemplatesTab | 385ee93 | src/components/settings/TemplatesTab.tsx |

## Verification Results

- `supabase/functions/template-extract/index.ts` exports `inspectDocxStyles`
- Function contains `REQUIRED_STYLES` with all 4 styles: `'Normal', 'Heading 1', 'Heading 2', 'Heading 3'`
- Function contains `return null` in catch block (D-06)
- Step 8.5 present: `styleInspection = await inspectDocxStyles(inspectBuffer)` inside `if (ext === 'docx')`
- Step 9 UPDATE contains `style_inspection: styleInspection`
- `test.ts` has 2 `Deno.test` blocks with `ignore: false`
- `src/components/settings/TemplatesTab.tsx` Template interface contains `style_inspection: { found: string[]; missing: string[] } | null`
- fetchTemplates `.select(` string contains `style_inspection`
- TemplatesTab contains `style_inspection?.missing` in JSX
- TemplatesTab contains `missing.join(', ')` in JSX
- TemplatesTab contains `How to fix in Word` link text
- `npm run test:run`: 3 files failed | 24 passed (27) — 4 failures are pre-existing SectionEditorBlock/SectionWorkspace/SectionStreamCard per Plan 01 SUMMARY; no new failures introduced

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None — all acceptance criteria met. style_inspection is populated end-to-end from Edge Function through to UI.

## Threat Surface Scan

No new threats beyond plan's threat register:
- T-11.1-09: ZIP bomb mitigated — 10MB cap from Plan 03 limits buffer size; JSZip errors caught returning null
- T-11.1-10: OOXML injection accepted — regex only reads style names, never executes XML
- T-11.1-11: style names in UI accepted — only standard heading names ('Heading 1' etc.) revealed

## Self-Check: PASSED

- `f2cf19f` exists: confirmed
- `385ee93` exists: confirmed
- `supabase/functions/template-extract/index.ts` exports `inspectDocxStyles`: confirmed
- `src/components/settings/TemplatesTab.tsx` contains `style_inspection`: confirmed
