---
phase: "11.1"
plan: "03"
subsystem: docx-style-extraction
tags: [jszip, export, template-styles, tdd, wave-3]
dependency_graph:
  requires:
    - "11.1-01 (jszip installed, test stubs)"
    - "11.1-02 (templates table migration, storage policies)"
  provides:
    - src/lib/applyTemplateStyles.ts (JSZip XML swap utility)
    - src/lib/exportDocx.ts extended with templateBlob param
    - src/pages/ProposalDetail.tsx with templateFilePath state + lazy signed URL fetch
    - src/components/settings/TemplatesTab.tsx with 10MB hard cap
  affects:
    - DOCX export pipeline (ProposalDetail → ExportDropdown → exportDocx → applyTemplateStyles)
    - TemplatesTab upload UX (new uploadError state + alert)
tech_stack:
  added: []
  patterns:
    - JSZip loadAsync/generateAsync for in-memory ZIP manipulation
    - Supabase Storage createSignedUrl for lazy template fetch
    - D-03 defensive pattern: always return original blob on any error
    - TDD RED/GREEN cycle for applyTemplateStyles
key_files:
  created:
    - src/lib/applyTemplateStyles.ts
  modified:
    - src/lib/applyTemplateStyles.test.ts
    - src/lib/exportDocx.ts
    - src/pages/ProposalDetail.tsx
    - src/components/settings/TemplatesTab.tsx
    - src/components/settings/TemplatesTab.test.tsx
decisions:
  - "fetchTemplateBlob placed inside ExportDropdown (not ProposalDetail) — fetch is lazy at click time, not on mount"
  - "templateBlob threaded through both handleExport and handleForceExport — force export also benefits from template styles"
  - "uploadError rendered after dropzone div (before hidden input) — visible without layout shift"
  - "TemplatesTab size cap test uses formula verification pattern — avoids OOM from rendering full component per existing test patterns"
metrics:
  duration: "~18 minutes"
  completed: "2026-04-29"
  tasks_completed: 2
  tasks_total: 2
  files_created: 1
  files_modified: 5
---

# Phase 11.1 Plan 03: Wave-3 Export Pipeline & Upload Cap Summary

**One-liner:** JSZip XML swap utility implemented and wired into the full export pipeline — ProposalDetail fetches template file_path, ExportDropdown lazily fetches a signed URL at click time, exportDocx applies style swap via applyTemplateStyles; TemplatesTab hard-rejects uploads over 10MB with an actionable compress-images message.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Create applyTemplateStyles.ts and fill test stubs | 2b496c7 | src/lib/applyTemplateStyles.ts, src/lib/applyTemplateStyles.test.ts |
| 2 | Wire export pipeline + TemplatesTab cap | 95f2696 | src/lib/exportDocx.ts, src/pages/ProposalDetail.tsx, src/components/settings/TemplatesTab.tsx, src/components/settings/TemplatesTab.test.tsx |

## Verification Results

- `src/lib/applyTemplateStyles.ts` exists and exports `applyTemplateStyles`
- Function signature: `export async function applyTemplateStyles(generated: Blob, template: Blob): Promise<Blob>`
- Contains `FILES_TO_SWAP` with all 3 paths: `word/styles.xml`, `word/theme/theme1.xml`, `word/numbering.xml`
- Contains `if (!tplZip.file('word/styles.xml')) { return generated }` D-03 guard
- Contains `catch (err)` block returning `generated`
- All 4 applyTemplateStyles tests pass (GREEN)
- `src/lib/exportDocx.ts` imports `applyTemplateStyles` and contains `const rawBlob = await Packer.toBlob(doc)` + `applyTemplateStyles(rawBlob, templateBlob)`
- `exportDocx` opts type contains `templateBlob?: Blob`
- `src/pages/ProposalDetail.tsx` contains `templateFilePath` state, selects `'name, file_path, source'`, contains `setTemplateFilePath(data.file_path)` inside `source === 'uploaded'` guard, contains `fetchTemplateBlob` with `createSignedUrl`
- `src/components/settings/TemplatesTab.tsx` contains `MAX_TEMPLATE_SIZE = 10 * 1024 * 1024`, `setUploadError(` inside `handleFileUpload`, `compress` in error message string
- TemplatesTab size cap test passes (was skipped before)
- Pre-existing 4 failures in SectionEditorBlock/SectionWorkspace/SectionStreamCard unchanged (confirmed pre-existing per Plan 01 SUMMARY)

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None — all stubs filled. The `applyTemplateStyles.test.ts` stubs (4) and `TemplatesTab.test.tsx` stub (1) are now real passing tests.

## Threat Surface Scan

No new network endpoints or auth paths introduced beyond what the plan's threat model covers:
- `fetch(signedUrl)` — covered by T-11.1-05 (TTL 3600s, org-scoped path)
- `JSZip.loadAsync(templateBlob)` — covered by T-11.1-04 (10MB cap limits ZIP bomb surface, catch returns original)
- `file.size` check — covered by T-11.1-07 (UX guard, not security boundary)

## Self-Check: PASSED

- `2b496c7` exists: confirmed
- `95f2696` exists: confirmed
- `src/lib/applyTemplateStyles.ts` exists: confirmed
- `src/lib/exportDocx.ts` contains `applyTemplateStyles`: confirmed
- `src/pages/ProposalDetail.tsx` contains `templateFilePath`: confirmed
- `src/components/settings/TemplatesTab.tsx` contains `MAX_TEMPLATE_SIZE`: confirmed
