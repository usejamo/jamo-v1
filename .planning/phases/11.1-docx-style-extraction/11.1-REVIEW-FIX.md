---
phase: 11.1-docx-style-extraction
fixed_at: 2026-05-01T00:00:00Z
review_path: .planning/phases/11.1-docx-style-extraction/11.1-REVIEW.md
fix_scope: critical_warning
findings_in_scope: 4
fixed: 4
skipped: 0
status: all_fixed
iteration: 1
---

# Phase 11.1: Code Review Fix Report

**Fixed at:** 2026-05-01T00:00:00Z
**Source review:** .planning/phases/11.1-docx-style-extraction/11.1-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 4
- Fixed: 4
- Skipped: 0

## Fixed Issues

### WR-01: Double `arrayBuffer()` call may drain the Blob stream before style inspection

**Files modified:** `supabase/functions/template-extract/index.ts`
**Commit:** 2c38ab6
**Applied fix:** Extracted a single `const fileBuffer = await fileData.arrayBuffer()` before the ext branch. Both the mammoth call (previously `{ buffer }`) and the PDF `extractText` call now reference `fileBuffer`. The step 8.5 `inspectDocxStyles` call was changed from `await fileData.arrayBuffer()` to `fileBuffer`, eliminating the second stream read entirely.

---

### WR-02: `parseSectionsFromText` `isShortNoPunct` heuristic over-matches

**Files modified:** `supabase/functions/template-extract/index.ts`
**Commit:** 2c38ab6
**Applied fix:** Added `const wordCount = trimmed.split(/\s+/).length` and extended `isShortNoPunct` with `wordCount >= 3` (excludes single words and fragments) and `!/^\d+$/.test(trimmed)` (excludes page numbers), matching the reviewer's suggested conditions exactly.

---

### WR-03: No deduplication guard on `template_sections` insert

**Files modified:** `supabase/functions/template-extract/index.ts`
**Commit:** 2c38ab6
**Applied fix:** Introduced a `seen = new Set<string>()` filter before `.map()` in the section insert block. Duplicate section names (same `name` value) are dropped from `dedupedSections` before the bulk insert, preventing multiple rows with the same `template_id` + `name`.

---

### WR-04: `TemplatesTab.test.tsx` tests a string literal, not the component

**Files modified:** `src/components/settings/TemplatesTab.test.tsx`
**Commit:** 1996633
**Applied fix:** Rewrote the test to render `<TemplatesTab />` with mocked `supabase` and `useAuth` dependencies, upload an 11 MB `File` via `userEvent.upload`, and assert that the rendered `role="alert"` element contains `'11.0 MB'` and `'compress'`. The test now exercises the actual `handleFileUpload` size-cap branch in the component rather than constructing and asserting on a string the test itself builds.

---

_Fixed: 2026-05-01T00:00:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
