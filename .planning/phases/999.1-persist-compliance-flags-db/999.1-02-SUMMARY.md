---
phase: 999.1-persist-compliance-flags-db
plan: "02"
subsystem: editor/compliance
tags: [compliance, persistence, read-path, supabase]
dependency_graph:
  requires: ["999.1-01"]
  provides: ["compliance-flags-read-path"]
  affects: ["src/pages/ProposalDetail.tsx", "src/components/editor/SectionWorkspace.tsx"]
tech_stack:
  added: []
  patterns: ["D-02 optimistic cache + background re-check", "compliance_flags JSONB read via select"]
key_files:
  modified:
    - src/pages/ProposalDetail.tsx
    - src/components/editor/SectionWorkspace.tsx
decisions:
  - "D-02: DB flags used as optimistic cache — displayed instantly, silently replaced by fresh re-check after 500ms"
  - "Background re-check scoped to proposalId dep only — runs once on mount, not on every state update"
metrics:
  duration: "~8 minutes"
  completed_date: "2026-04-02"
  tasks_completed: 1
  tasks_total: 1
  files_modified: 2
---

# Phase 999.1 Plan 02: Read Path — Load DB Compliance Flags on Mount

Compliance flags now load from DB on `ProposalDetail` mount (instant display) then silently refresh via background re-check 500ms later, implementing D-02 staleness policy.

## What Was Built

### Task 1: Add compliance_flags to ProposalDetail query + SectionWorkspace init

**ProposalDetail.tsx:**
- Added `ComplianceFlag` to the import from `../types/workspace`
- Added `compliance_flags` to both select strings: initial fetch and `refetchSections`
- Extended `proposalSections` state type to include `compliance_flags: ComplianceFlag[] | null`
- Passes `compliance_flags: s.compliance_flags ?? null` through to `SectionWorkspace` prop

**SectionWorkspace.tsx:**
- Added `ComplianceFlag` to type import from `../../types/workspace`
- Added `useComplianceCheck` import from `../../hooks/useComplianceCheck`
- Extended `SectionWorkspaceProps.sections` type to include `compliance_flags?: ComplianceFlag[] | null`
- Initialization `useEffect` now reads `Array.isArray(s.compliance_flags) ? s.compliance_flags : []` instead of hardcoded `[]`
- New D-02 background re-check `useEffect`: fires 500ms after mount (keyed on `proposalId`), calls `checkCompliance` for every section with non-empty content

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None — all data flows are wired end-to-end.

## Verification

- `grep -q "compliance_flags" src/pages/ProposalDetail.tsx` — PASS
- `grep "compliance_flags" src/components/editor/SectionWorkspace.tsx | grep -q "Array.isArray"` — PASS
- `grep -q "checkCompliance" src/components/editor/SectionWorkspace.tsx` — PASS
- `npm run test:run` — 137 passed, 9 skipped, 0 failures

## Commits

| Hash | Message |
|------|---------|
| 82758d1 | feat(999.1-02): wire read path — load DB compliance_flags on mount + background re-check |

## Self-Check: PASSED

- `src/pages/ProposalDetail.tsx` — modified, contains `compliance_flags` in both selects
- `src/components/editor/SectionWorkspace.tsx` — modified, reads `s.compliance_flags`, imports `useComplianceCheck`, has D-02 background re-check effect
- Commit `82758d1` — verified in git log
