---
phase: 14.6-regulatory-grounding-correctness
plan: 05
type: execute
wave: 4
depends_on: ["02", "03", "04"]
files_modified:
  - .planning/phases/14.6-regulatory-grounding-correctness/14.6-05-SUMMARY.md
autonomous: false
requirements:
  - brief-seeder-e2e
must_haves:
  truths:
    - "npm run seed:regulatory populates the regulatory corpus in supersedes-dependency order (live)"
    - "A US Phase-2 oncology 'Study Design' section retrieves the seeded ICH-E6(R3)"
    - "A Japan-scoped section legitimately retrieves zero regulatory chunks and surfaces the (b) no-grounding marker without asserting ICH-GCP compliance"
  artifacts:
    - path: ".planning/phases/14.6-regulatory-grounding-correctness/14.6-05-SUMMARY.md"
      provides: "Live seed + e2e acceptance record"
      contains: "E6"
  key_links:
    - from: "seeded ICH-E6R3 ({US,EU,UK})"
      to: "US oncology Study Design retrieval"
      via: "geography + phase filter match"
      pattern: "E6"
    - from: "Japan-scoped section"
      to: "(b) no-grounding marker"
      via: "legitimate zero-retrieval made visible"
      pattern: "no regulatory grounding"
---

<objective>
Run the seeder live against operator-supplied PDFs and prove the whole (b)+(a)+seeder chain end-to-end:
the corpus populates in dependency order, a US Phase-2 oncology "Study Design" section retrieves the
seeded ICH-E6(R3), and a Japan-scoped section legitimately retrieves zero regulatory chunks and surfaces
the (b) marker instead of a false compliance claim.

Purpose: This is the concrete case the (b) fix protects — a legitimate zero must be visible, not
swallowed. All steps are live (credit-spending / live retrieval) and gate on operator input.
Output: A live-seeded corpus and a recorded e2e acceptance in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.6-regulatory-grounding-correctness/14.6-BRIEF.md
@.planning/phases/14.6-regulatory-grounding-correctness/14.6-04-PLAN.md
@.planning/phases/14.6-regulatory-grounding-correctness/14.6-02-PLAN.md
@.planning/phases/14.6-regulatory-grounding-correctness/14.6-03-PLAN.md
</context>

<prerequisites>
Before this plan runs: Plan 02 (edge deployed live), Plan 03 (migration applied live), Plan 04 (seeder +
manifest merged) must all be complete. The manifest default set is a PROPOSAL — the operator confirms the
set to seed and supplies the PDFs.
</prerequisites>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Operator supplies PDFs + dry validate</name>
  <what-built>
    The seeder (Plan 04) with batch pre-validation and --validate-only. PDFs are operator-supplied and
    .gitignored; the seeder needs real files under regulatory-docs/&lt;documentKey&gt;/ before a live run.
  </what-built>
  <how-to-verify>
    1. Operator places curated PDFs into regulatory-docs/&lt;documentKey&gt;/*.pdf for each manifest entry to
       seed (at minimum the ICH-E6R3 folder for the US oncology retrieval smoke). Confirm the manifest set
       to seed is the operator-approved set (default list is a PROPOSAL pending clinical review).
    2. Dry validation (NO embedding spend):
         npm run seed:regulatory -- --validate-only
       EXPECT exit 0 and a success summary (all folders present with >=1 PDF, supersedes targets resolve,
       no dup keys, acyclic). If it exits non-zero, read the aggregated report and fix folders/manifest
       before proceeding.
  </how-to-verify>
  <verify>
    <automated>grep -n "validate-only" scripts/seed-regulatory.ts</automated>
  </verify>
  <acceptance_criteria>
    - regulatory-docs/&lt;documentKey&gt;/ folders exist with >=1 PDF for every entry being seeded.
    - `npm run seed:regulatory -- --validate-only` exits 0.
  </acceptance_criteria>
  <resume-signal>Type "validated" once --validate-only passes, or paste the aggregated failure report.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING — LIVE, credit-spending] Run the seeder</name>
  <what-built>
    A validated batch ready to embed and ingest. This run spends embedding credits and writes to the live
    regulatory corpus via the (a)-hardened ingest RPC.
  </what-built>
  <how-to-verify>
    1. Run the live seed (do NOT run until approved — credit-spending):
         npm run seed:regulatory
    2. EXPECT: entries ingest in supersedes-dependency order, all-or-nothing; the run reports per-doc
       success and a final summary. If the (a) raise fires on an unresolved supersedes, the run aborts
       non-zero naming the offending entry — fix and re-run.
    3. Verify rows landed: via Supabase MCP, confirm regulatory_documents has the seeded document_keys and
       chunks exist for ICH-E6R3 (chunk count > 0). Confirm ICH-E6R3 geography = {US,EU,UK} (NOT GLOBAL).
  </how-to-verify>
  <verify>
    <automated>grep -n "seed:regulatory" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm run seed:regulatory` completes successfully; corpus populated in dependency order.
    - regulatory_documents contains the seeded keys; ICH-E6R3 present with geography {US,EU,UK} and >=1 chunk.
  </acceptance_criteria>
  <resume-signal>Type "seeded" once the corpus is populated, or describe the run failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] E2E — US retrieves E6; Japan surfaces the (b) marker</name>
  <what-built>
    The full chain: (b) tier-separated retrieval + conditional compliance (deployed), (a) hardened ingest,
    and a live-seeded R3-only {US,EU,UK} corpus.
  </what-built>
  <how-to-verify>
    1. US positive case: generate/inspect a US, Phase-2, oncology "Study Design" section. CONFIRM the
       retrieved context includes the seeded ICH-E6(R3) under [REGULATORY CONTEXT], and the section prompt
       DOES assert ICH-GCP compliance (regulatoryCount > 0). (Use the local app localhost:5173 login
       usera@jamo.com / password123, or inspect retrieve-context output for the query directly.)
    2. Japan legitimate-zero case: generate/inspect a Japan-scoped section for the same area. CONFIRM
       retrieve-context returns regulatoryCount = 0 (R3 {US,EU,UK} filter excludes JP; graded relaxation
       keeps the geography filter), the prompt surfaces the "no regulatory grounding available" marker,
       [REGULATORY CONTEXT] shows "(No relevant regulatory context found)", proposal-history (if any)
       appears only under [PROPOSAL HISTORY], and the prompt does NOT assert ICH-GCP compliance.
    3. Record both outcomes in the SUMMARY.
  </how-to-verify>
  <verify>
    <automated>echo "manual e2e — record outcomes in 14.6-05-SUMMARY.md"</automated>
  </verify>
  <acceptance_criteria>
    - US Phase-2 oncology Study Design retrieves seeded E6(R3) and retains the compliance assertion.
    - Japan-scoped section retrieves zero regulatory chunks, surfaces the (b) no-grounding marker, and asserts NO ICH-GCP compliance.
    - No proposal-history chunk appears under [REGULATORY CONTEXT] in either case.
  </acceptance_criteria>
  <resume-signal>Type "accepted" once both cases are confirmed, or describe the discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator PDFs → live corpus | Live embedding + ingest of operator-supplied documents into the shared regulatory corpus |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.6-10 | DoS / cost | live embedding run | mitigate | Gate the run behind --validate-only (Task 1) so no spend on an invalid batch; run is operator-approved (Task 2 checkpoint). |
| T-14.6-11 | Integrity | wrong-geography seeding (E6 as GLOBAL) | mitigate | Task 2 verifies ICH-E6R3 geography = {US,EU,UK}; Task 3 confirms the Japan zero-retrieval behavior that GLOBAL scoping would have masked. |
</threat_model>

<verification>
- Live seed completes in dependency order (verification #5).
- US Phase-2 oncology Study Design retrieves E6(R3); Japan section legitimately retrieves none and surfaces the (b) marker (verification #5 + #1 live-smoke).
</verification>

<success_criteria>
The starter corpus is live, retrieval is correct for the covered geographies, and the legitimate Japan
zero-retrieval is VISIBLE (marker, no false compliance) — the concrete case the (b) fix exists to protect.
</success_criteria>

<output>
After completion, create `.planning/phases/14.6-regulatory-grounding-correctness/14.6-05-SUMMARY.md`
</output>
