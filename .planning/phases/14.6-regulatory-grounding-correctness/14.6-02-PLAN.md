---
phase: 14.6-regulatory-grounding-correctness
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - supabase/functions/generate-proposal-section/index.ts
  - supabase/functions/generate-proposal-section/promptAssembly.test.ts
autonomous: false
requirements:
  - brief-b-tier-separation
  - brief-b-conditional-compliance
  - brief-b-deploy
must_haves:
  truths:
    - "Regulatory chunks render under [REGULATORY CONTEXT]; proposal-history chunks under a separate [PROPOSAL HISTORY] header"
    - "When regulatoryCount === 0 the prompt emits an explicit 'no regulatory grounding' marker and contains NO ICH-GCP / E6 / FDA-EMA compliance reference anywhere — in EITHER builder, including buildSectionPromptV2's regulatory_strategy field"
    - "When regulatoryCount > 0 the compliance assertion and populated [REGULATORY CONTEXT] are present"
    - "generate-proposal-section is redeployed live and its inline payload types mirror GenerateSectionPayloadV2"
  artifacts:
    - path: "supabase/functions/generate-proposal-section/index.ts"
      provides: "Conditional compliance prompt + dual-header context assembly (both builders) + gated regulatory_strategy in V2"
      contains: "PROPOSAL HISTORY"
    - path: "supabase/functions/generate-proposal-section/promptAssembly.test.ts"
      provides: "Unit test of the ACTUAL V2 runtime builder branching on regulatoryCount, covering both builders"
      contains: "regulatoryCount"
  key_links:
    - from: "generate-proposal-section/index.ts"
      to: "regulatoryCount"
      via: "conditional compliance clause + gated regulatory_strategy + no-grounding marker"
      pattern: "regulatoryCount"
    - from: "buildSectionPrompt / buildSectionPromptV2"
      to: "[PROPOSAL HISTORY]"
      via: "separate header for proposal chunks"
      pattern: "\\[PROPOSAL HISTORY\\]"
---

<objective>
Fix the EDGE half of the (b) correctness bug and deploy it. Make EVERY ICH-GCP / E6 / FDA-EMA compliance
instruction conditional on `regulatoryCount > 0` — not only the base `CRO_PROPOSAL_SYSTEM_PROMPT`, but
also the `regulatory_strategy` field injected inside `buildSectionPromptV2` (the PRIMARY runtime path for
version-2 payloads). Separate regulatory chunks (`[REGULATORY CONTEXT]`) from proposal-history chunks
(`[PROPOSAL HISTORY]`) in BOTH builders, emit an explicit "no regulatory grounding" marker when
`regulatoryCount === 0`, unit-test the ACTUAL V2 builder on the branch, then redeploy
`generate-proposal-section` (new-to-14.6; committed != live).

Purpose: The conditional compliance behavior is the highest-value change — a not-found marker sitting
next to ANY instruction to reference ICH-GCP (E6 R2/R3) is contradictory input and the assertion wins.
In the legitimate Japan `regulatoryCount===0` case, the V2 path must NOT tell the model to reference
ICH-GCP. Output: corrected edge function (both prompt paths gated), a passing unit test on the V2
assembly branch, and a live deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.6-regulatory-grounding-correctness/14.6-BRIEF.md
@.planning/phases/14.6-regulatory-grounding-correctness/14.6-PATTERNS.md
@.planning/phases/14.6-regulatory-grounding-correctness/14.6-01-PLAN.md

<interfaces>
<!-- Client payload contract from Plan 01 (src/types/generation.ts). Edge re-declares inline types
     by hand (no shared import client↔edge) — keep them in lockstep. -->
  regulatoryChunks: { content: string; source: string; agency?: string; doc_type?: string }[]
  proposalChunks:   { content: string; source: string; agency?: string; doc_type?: string }[]
  regulatoryCount:  number

<!-- Copy this labeling pattern from retrieve-context/index.ts buildSystemPromptBlock (~132-146): -->
  const regSection  = regulatoryChunks.length > 0 ? regulatoryChunks.map(c => `[${c.source}] ${c.content}`).join('\n\n') : '(No relevant regulatory context found)'
  const propSection = proposalChunks.length  > 0 ? proposalChunks.map(c => `[${c.source}] ${c.content}`).join('\n\n')  : '(No relevant proposal history found)'

<!-- Current edge defect — generate-proposal-section/index.ts. There are THREE compliance-assertion
     sources, ALL must be gated on regulatoryCount:
     (1) CRO_PROPOSAL_SYSTEM_PROMPT (~27) asserts "compliant with ICH-GCP (E6 R2/R3)" UNCONDITIONALLY (also line 35).
     (2) buildSectionPromptV2 (~204) injects at ~line 221:
           regulatory_strategy: 'Reference specific ICH-GCP guidelines (E6 R2/R3), FDA/EMA guidance...'
         — NOT gated today. buildSectionPromptV2 is the PRIMARY path: the handler selects V2 for version-2
         payloads at ~line 443.
     (3) Both builders dump the merged array under a single [REGULATORY CONTEXT]:
           buildSectionPrompt (~140-142) AND buildSectionPromptV2 (~252-254).
     Handler passes ragChunks at ~397, 449, 457. CRITICAL RULE ([PLACEHOLDER], never fabricate) at ~134 and ~237. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate ALL compliance references + dual-header assembly in both builders</name>
  <files>supabase/functions/generate-proposal-section/index.ts</files>
  <read_first>
    - supabase/functions/generate-proposal-section/index.ts (the file being modified; CRO_PROPOSAL_SYSTEM_PROMPT ~25-35, buildSectionPrompt ~118-142, buildSectionPromptV2 param ~204 + regulatory_strategy ~221 + assembly ~252-254, handler V2 selection ~443, handler pass-throughs ~397/449/457)
    - supabase/functions/retrieve-context/index.ts (buildSystemPromptBlock ~132-146 — the exact labeling analog to copy)
    - .planning/phases/14.6-regulatory-grounding-correctness/14.6-PATTERNS.md (generate-proposal-section section + Shared Patterns)
  </read_first>
  <action>
    In supabase/functions/generate-proposal-section/index.ts. There are THREE compliance sources — gate ALL THREE on `regulatoryCount`:

    1. CONDITIONAL COMPLIANCE (base prompt): Remove the clause `, compliant with ICH-GCP (E6 R2/R3),` from
       the base `CRO_PROPOSAL_SYSTEM_PROMPT` (~line 27) and remove/neutralize the unconditional
       `- Compliant — reference ICH-GCP, FDA/EMA guidance naturally` bullet (~line 35). Define:
         const COMPLIANCE_CLAUSE = ' The proposal must be compliant with ICH-GCP (E6 R2/R3) and reference FDA/EMA guidance where grounded.'
       Build the effective system prompt per call as:
         const systemPrompt = CRO_PROPOSAL_SYSTEM_PROMPT + (regulatoryCount > 0 ? COMPLIANCE_CLAUSE : '')
       When regulatoryCount === 0 the base prompt (retaining the existing CRITICAL RULES: flag with
       [PLACEHOLDER], never fabricate) is used WITHOUT any compliance assertion.

    2. GATE THE regulatory_strategy FIELD IN buildSectionPromptV2 (~line 221) — this is the primary runtime
       path (handler selects V2 at ~443) and is the BLOCKER. Replace the static string literal
       `regulatory_strategy: 'Reference specific ICH-GCP guidelines (E6 R2/R3), FDA/EMA guidance...'`
       with an EXACTLY-conditional value (regulatoryCount is available from the destructured V2 param):
         regulatory_strategy: regulatoryCount > 0
           ? 'Reference specific ICH-GCP guidelines (E6 R2/R3) and FDA/EMA guidance only where grounded in the provided [REGULATORY CONTEXT].'
           : 'No regulatory grounding is available for this section/geography. Do NOT reference or assert ICH-GCP, E6, or FDA/EMA compliance for this section. Flag any unresolved regulatory items with [PLACEHOLDER] per the CRITICAL RULES; never fabricate.',
       The zero-grounding branch MUST contain no `ICH-GCP`, `E6`, or FDA/EMA-compliance instruction — keep
       it neutral and defer to the CRITICAL RULES. If buildSectionPrompt (the V1 builder) has an analogous
       compliance/strategy string, gate it the same way; otherwise only the base-prompt gating (step 1) applies to V1.

    3. DUAL-HEADER ASSEMBLY (patch BOTH builders identically): replace the inline param(s) that took the
       merged array with `regulatoryChunks`, `proposalChunks`, `regulatoryCount` (mirror the Plan 01
       contract in the inline types at ~118 and ~204). Replace the single `[REGULATORY CONTEXT]` block
       (~140-142 and ~252-254) with:
         const regSection  = regulatoryChunks.length > 0 ? regulatoryChunks.map(c => `[${c.source}] ${c.content}`).join('\n---\n') : '(No relevant regulatory context found)'
         const propSection = proposalChunks.length  > 0 ? proposalChunks.map(c => `[${c.source}] ${c.content}`).join('\n---\n')  : '(No relevant proposal history found)'
         system += `\n\n[REGULATORY CONTEXT]\n${regSection}\n[/REGULATORY CONTEXT]`
         system += `\n\n[PROPOSAL HISTORY]\n${propSection}\n[/PROPOSAL HISTORY]`
         if (regulatoryCount === 0) system += `\n\n[NOTE] No regulatory grounding available for this section/geography — do not assert regulatory compliance; flag unresolved items with [PLACEHOLDER].`

    4. Update the handler pass-throughs (~397, 449, 457) to read `regulatoryChunks`, `proposalChunks`,
       `regulatoryCount` off the request body and pass them into both builders (replace every `ragChunks`
       reference). Grep for `ragChunks` afterward — zero must remain.

    5. Export the V2 assembly for unit testing: `buildSectionPromptV2` is the primary path — export it
       (or a pure `assembleSectionPromptV2(args)` that returns the fully-assembled prompt string it feeds
       the model, given `{ regulatoryChunks, proposalChunks, regulatoryCount, ... }`) so Task 2 imports the
       ACTUAL runtime builder — NOT a divergent helper. Also export the V1 builder / its assembly so the
       test can cover both header paths. Ensure the pure assembly is importable without invoking Deno.serve
       (entry guard / export split).
  </action>
  <verify>
    <automated>grep -n "PROPOSAL HISTORY" supabase/functions/generate-proposal-section/index.ts && grep -n "regulatoryCount > 0 ? COMPLIANCE_CLAUSE" supabase/functions/generate-proposal-section/index.ts && grep -n "regulatory_strategy: regulatoryCount > 0" supabase/functions/generate-proposal-section/index.ts && grep -n "No regulatory grounding available" supabase/functions/generate-proposal-section/index.ts && ! grep -n "ragChunks" supabase/functions/generate-proposal-section/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ragChunks" supabase/functions/generate-proposal-section/index.ts` returns 0.
    - `grep "\[PROPOSAL HISTORY\]" ...index.ts` matches AND `grep "\[REGULATORY CONTEXT\]" ...index.ts` matches (separate headers).
    - `grep "regulatoryCount > 0 ? COMPLIANCE_CLAUSE" ...index.ts` matches (base compliance is conditional).
    - `grep "regulatory_strategy: regulatoryCount > 0" ...index.ts` matches (the line-221 V2 reference is gated — the BLOCKER fix).
    - The zero-grounding `regulatory_strategy` branch contains no `ICH-GCP` / `E6` / FDA-EMA compliance token: `grep -A2 "regulatory_strategy: regulatoryCount > 0" ...index.ts` shows the `: 'No regulatory grounding is available ... Do NOT reference or assert ICH-GCP...'` fallback.
    - `grep "No regulatory grounding available" ...index.ts` matches (the marker).
    - Base `CRO_PROPOSAL_SYSTEM_PROMPT` no longer contains the literal `compliant with ICH-GCP (E6 R2/R3)`: `grep -A6 "CRO_PROPOSAL_SYSTEM_PROMPT =" ...index.ts | grep -c "compliant with ICH-GCP"` returns 0.
    - `buildSectionPromptV2` (the primary path) and the V1 builder/assembly are exported for import by the test.
  </acceptance_criteria>
  <done>All three compliance sources gated on regulatoryCount (base clause, V2 regulatory_strategy, headers); both builders emit separate headers; no-grounding marker present when 0; no `ragChunks` remains; V2 (primary) + V1 assembly exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit-test the ACTUAL V2 builder (primary path) + V1, branching on regulatoryCount</name>
  <files>supabase/functions/generate-proposal-section/promptAssembly.test.ts</files>
  <read_first>
    - supabase/functions/generate-proposal-section/index.ts (the exported buildSectionPromptV2 / assembleSectionPromptV2 and V1 builder from Task 1; regulatory_strategy at ~221; handler V2 selection at ~443 confirming V2 is primary)
    - package.json (test:run / vitest convention)
  </read_first>
  <behavior>
    - V2 (primary), regulatoryCount === 0 with proposalChunks present: output contains "[PROPOSAL HISTORY]" with the proposal chunk content, contains the "No regulatory grounding available" marker, and contains ZERO occurrences of "ICH-GCP" (the gated regulatory_strategy fallback carries no ICH-GCP/E6/FDA-EMA reference).
    - V2, regulatoryCount === 0: proposal chunk content does NOT appear inside the [REGULATORY CONTEXT]...[/REGULATORY CONTEXT] block (regulatory section shows "(No relevant regulatory context found)").
    - V2, regulatoryCount > 0: output contains "compliant with ICH-GCP" (or the gated regulatory_strategy ICH-GCP reference), contains the regulatory chunk content inside [REGULATORY CONTEXT], and does NOT contain the "No regulatory grounding available" marker.
    - V1 builder (buildSectionPrompt): regulatoryCount === 0 → output has separate [REGULATORY CONTEXT] / [PROPOSAL HISTORY] headers, proposal content only under [PROPOSAL HISTORY], marker present; regulatoryCount > 0 → populated [REGULATORY CONTEXT].
  </behavior>
  <action>
    Create supabase/functions/generate-proposal-section/promptAssembly.test.ts as a Vitest spec importing
    the ACTUAL runtime builders from ./index.ts: `buildSectionPromptV2` (or `assembleSectionPromptV2`) — the
    PRIMARY path (handler selects V2 at ~443) — AND the V1 `buildSectionPrompt` assembly. Do NOT test a
    divergent helper. If index.ts imports Deno globals at module top-level that block a Node/Vitest import,
    extract the pure assemblies into a sibling `promptAssembly.ts` re-exported by index.ts and import from
    there (both index.ts and the test must use the same function — no drift). Write the <behavior> cases for
    BOTH builders. Use fixture chunks `{ content: 'PROP-CONTENT-XYZ', source: 'proposal:acme' }` and
    `{ content: 'REG-CONTENT-ABC', source: 'ICH-E6R3' }`. For the zero-grounding assertion, count ICH-GCP
    occurrences in the assembled prompt (must be 0) rather than only the `"compliant with ICH-GCP"` phrase —
    this is what catches the line-221 `regulatory_strategy` reference. Wire into test:run.
  </action>
  <verify>
    <automated>npm run test:run -- promptAssembly 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run -- promptAssembly` passes; cases cover BOTH buildSectionPromptV2 (primary) and buildSectionPrompt (V1).
    - The V2 regulatoryCount===0 case asserts the assembled prompt contains ZERO occurrences of "ICH-GCP" (e.g. `expect((prompt.match(/ICH-GCP/g) ?? []).length).toBe(0)`) AND contains the "No regulatory grounding available" marker — this catches the line-221 defect that a `"compliant with ICH-GCP"` grep would miss.
    - The V2 regulatoryCount>0 case asserts an ICH-GCP reference IS present and [REGULATORY CONTEXT] is populated with REG-CONTENT.
    - Both builders assert PROP-CONTENT appears only under [PROPOSAL HISTORY], never inside [REGULATORY CONTEXT].
    - The test imports the same exported symbol the handler uses for V2 (no divergent helper).
  </acceptance_criteria>
  <done>Vitest proves the ACTUAL V2 (primary) and V1 assemblies branch correctly on regulatoryCount, including zero ICH-GCP references in the ungrounded V2 branch; suite green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING — LIVE] Deploy generate-proposal-section</name>
  <what-built>
    Conditional-compliance (base clause + gated V2 regulatory_strategy) + dual-header edge function
    (Task 1) and its passing unit test (Task 2), plus the client contract from Plan 01.
    generate-proposal-section is new-to-14.6 (untouched by 14.5) — committed != live, so a redeploy is
    REQUIRED before (b) can be verified end-to-end. retrieve-context needs NO deploy.
  </what-built>
  <how-to-verify>
    1. Confirm Plan 01 (client contract) and Task 1/2 above are committed — client + edge ship together.
    2. Run the deploy (this is the live step — do not run until approved):
         npx supabase functions deploy generate-proposal-section --project-ref fuuvdcvbliijffogjnwg
    3. Confirm the deploy reports success (new version published).
    4. Contract check: the deployed edge reads regulatoryChunks / proposalChunks / regulatoryCount off the
       body and the client (Plan 01) sends exactly those — no silent shape drift. If the frontend is served
       from a separate deploy (Netlify), note that the client bundle must also be redeployed for the live
       contract to match; flag if not.
  </how-to-verify>
  <verify>
    <automated>grep -n "PROPOSAL HISTORY" supabase/functions/generate-proposal-section/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `supabase functions deploy generate-proposal-section` reports success for project-ref fuuvdcvbliijffogjnwg.
    - Client (Plan 01) and edge payload fields match: regulatoryChunks, proposalChunks, regulatoryCount.
  </acceptance_criteria>
  <resume-signal>Type "deployed" once the function is live, or describe deploy errors.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| retrieved chunk content → LLM prompt | Chunk text (regulatory + proposal history) is concatenated into the model system prompt |
| client → edge body | Untrusted payload fields (chunks, counts) cross into the edge function |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.6-02 | Tampering (prompt injection) | context-block assembly in index.ts | mitigate | Wrap both tiers in explicit delimited headers ([REGULATORY CONTEXT]/[/…], [PROPOSAL HISTORY]/[/…]); chunk content is quoted as reference material, and the no-grounding marker + CRITICAL RULES ([PLACEHOLDER], never fabricate) constrain the model. Do not let a client-supplied regulatoryCount fabricate a compliance claim — it only gates the clause + regulatory_strategy; the marker is emitted whenever regulatoryChunks is empty regardless. |
| T-14.6-03 | Spoofing/Info | client-supplied regulatoryCount | accept | regulatoryCount originates from retrieve-context retrievalMeta; a tampered count cannot manufacture regulatory chunks (empty array still yields the marker). Low risk; org-scoped auth unchanged. |
</threat_model>

<verification>
- Unit suite (Task 2) green, covering BOTH V2 (primary) and V1, asserting zero ICH-GCP references in the ungrounded V2 branch.
- grep confirms separate headers, conditional base clause, gated `regulatory_strategy` (line ~221), marker, and zero `ragChunks`.
- Edge redeployed to fuuvdcvbliijffogjnwg (Task 3). Build/typecheck passing is NOT sufficient — the deploy is mandatory for (b) verification.
</verification>

<success_criteria>
A section with regulatoryCount=0 + proposal chunks does not emit proposals under [REGULATORY CONTEXT],
carries the no-grounding marker, and asserts no ICH-GCP compliance ANYWHERE (base prompt AND V2
regulatory_strategy); a section with regulatoryCount>0 retains the assertion + populated
[REGULATORY CONTEXT]; and the corrected function is live.
</success_criteria>

<output>
After completion, create `.planning/phases/14.6-regulatory-grounding-correctness/14.6-02-SUMMARY.md`
</output>
