---
phase: 14.6-regulatory-grounding-correctness
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - supabase/functions/generate-proposal-section/index.ts
  - supabase/functions/generate-proposal-section/promptAssembly.test.ts
autonomous: false
requirements:
  - brief-b-tier-separation
  - brief-b-conditional-compliance
  - brief-b-deploy
must_haves:
  truths:
    - "Regulatory chunks render under [REGULATORY CONTEXT]; proposal-history chunks under a separate [PROPOSAL HISTORY] header"
    - "When regulatoryCount === 0 the prompt emits an explicit 'no regulatory grounding' marker and does NOT assert ICH-GCP compliance"
    - "When regulatoryCount > 0 the compliance assertion and populated [REGULATORY CONTEXT] are present"
    - "generate-proposal-section is redeployed live and its inline payload types mirror GenerateSectionPayloadV2"
  artifacts:
    - path: "supabase/functions/generate-proposal-section/index.ts"
      provides: "Conditional compliance prompt + dual-header context assembly (both builders)"
      contains: "PROPOSAL HISTORY"
    - path: "supabase/functions/generate-proposal-section/promptAssembly.test.ts"
      provides: "Unit test of prompt assembly branching on regulatoryCount"
      contains: "regulatoryCount"
  key_links:
    - from: "generate-proposal-section/index.ts"
      to: "regulatoryCount"
      via: "conditional compliance clause + no-grounding marker"
      pattern: "regulatoryCount"
    - from: "buildSectionPrompt / buildSectionPromptV2"
      to: "[PROPOSAL HISTORY]"
      via: "separate header for proposal chunks"
      pattern: "\\[PROPOSAL HISTORY\\]"
---

<objective>
Fix the EDGE half of the (b) correctness bug and deploy it. Make the ICH-GCP compliance assertion
conditional on `regulatoryCount > 0`, separate regulatory chunks (`[REGULATORY CONTEXT]`) from
proposal-history chunks (`[PROPOSAL HISTORY]`) in BOTH prompt builders, emit an explicit
"no regulatory grounding" marker when `regulatoryCount === 0`, unit-test the branch, then redeploy
`generate-proposal-section` (new-to-14.6; committed != live).

Purpose: The conditional compliance assertion is the highest-value change — a not-found marker
alongside an unconditional compliance claim is contradictory input and the assertion wins.
Output: Corrected edge function, a passing unit test on the assembly branch, and a live deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.6-regulatory-grounding-correctness/14.6-BRIEF.md
@.planning/phases/14.6-regulatory-grounding-correctness/14.6-PATTERNS.md
@.planning/phases/14.6-regulatory-grounding-correctness/14.6-01-PLAN.md

<interfaces>
<!-- Client payload contract from Plan 01 (src/types/generation.ts). Edge re-declares inline types
     by hand (no shared import client↔edge) — keep them in lockstep. -->
  regulatoryChunks: { content: string; source: string; agency?: string; doc_type?: string }[]
  proposalChunks:   { content: string; source: string; agency?: string; doc_type?: string }[]
  regulatoryCount:  number

<!-- Copy this labeling pattern from retrieve-context/index.ts buildSystemPromptBlock (~132-146): -->
  const regSection  = regulatoryChunks.length > 0 ? regulatoryChunks.map(c => `[${c.source}] ${c.content}`).join('\n\n') : '(No relevant regulatory context found)'
  const propSection = proposalChunks.length  > 0 ? proposalChunks.map(c => `[${c.source}] ${c.content}`).join('\n\n')  : '(No relevant proposal history found)'

<!-- Current edge defect — generate-proposal-section/index.ts:
     CRO_PROPOSAL_SYSTEM_PROMPT (~27) asserts "compliant with ICH-GCP (E6 R2/R3)" UNCONDITIONALLY (also line 35).
     buildSectionPrompt (~140-142) AND buildSectionPromptV2 (~252-254) both dump the merged array under a single [REGULATORY CONTEXT].
     Handler passes ragChunks at ~397, 449, 457. CRITICAL RULE ([PLACEHOLDER], never fabricate) at ~134 and ~237. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Conditional compliance + dual-header assembly in both prompt builders</name>
  <files>supabase/functions/generate-proposal-section/index.ts</files>
  <read_first>
    - supabase/functions/generate-proposal-section/index.ts (the file being modified; CRO_PROPOSAL_SYSTEM_PROMPT ~25-35, buildSectionPrompt ~118-142, buildSectionPromptV2 ~204-254, handler pass-throughs ~397/449/457)
    - supabase/functions/retrieve-context/index.ts (buildSystemPromptBlock ~132-146 — the exact labeling analog to copy)
    - .planning/phases/14.6-regulatory-grounding-correctness/14.6-PATTERNS.md (generate-proposal-section section + Shared Patterns)
  </read_first>
  <action>
    In supabase/functions/generate-proposal-section/index.ts:

    1. CONDITIONAL COMPLIANCE: Remove the clause `, compliant with ICH-GCP (E6 R2/R3),` from the base
       `CRO_PROPOSAL_SYSTEM_PROMPT` (~line 27) and remove/neutralize the unconditional
       `- Compliant — reference ICH-GCP, FDA/EMA guidance naturally` bullet (~line 35). Define:
         const COMPLIANCE_CLAUSE = ' The proposal must be compliant with ICH-GCP (E6 R2/R3) and reference FDA/EMA guidance where grounded.'
       Build the effective system prompt per call as:
         const systemPrompt = CRO_PROPOSAL_SYSTEM_PROMPT + (regulatoryCount > 0 ? COMPLIANCE_CLAUSE : '')
       When regulatoryCount === 0 the base prompt (retaining the existing CRITICAL RULES: flag with
       [PLACEHOLDER], never fabricate) is used WITHOUT any compliance assertion.

    2. DUAL-HEADER ASSEMBLY (patch BOTH builders identically): replace the inline param(s) that took the
       merged array with `regulatoryChunks`, `proposalChunks`, `regulatoryCount` (mirror the Plan 01
       contract in the inline types at ~118 and ~204). Replace the single `[REGULATORY CONTEXT]` block
       (~140-142 and ~252-254) with:
         const regSection  = regulatoryChunks.length > 0 ? regulatoryChunks.map(c => `[${c.source}] ${c.content}`).join('\n---\n') : '(No relevant regulatory context found)'
         const propSection = proposalChunks.length  > 0 ? proposalChunks.map(c => `[${c.source}] ${c.content}`).join('\n---\n')  : '(No relevant proposal history found)'
         system += `\n\n[REGULATORY CONTEXT]\n${regSection}\n[/REGULATORY CONTEXT]`
         system += `\n\n[PROPOSAL HISTORY]\n${propSection}\n[/PROPOSAL HISTORY]`
         if (regulatoryCount === 0) system += `\n\n[NOTE] No regulatory grounding available for this section/geography — do not assert regulatory compliance; flag unresolved items with [PLACEHOLDER].`

    3. Update the handler pass-throughs (~397, 449, 457) to read `regulatoryChunks`, `proposalChunks`,
       `regulatoryCount` off the request body and pass them into both builders (replace every `ragChunks`
       reference). Grep for `ragChunks` afterward — zero must remain.

    4. Export the assembly for unit testing: factor the prompt-assembly logic (system prompt + context
       block, given `{ regulatoryChunks, proposalChunks, regulatoryCount }`) into an exported pure
       function `export function assembleSectionPrompt(base, args)` (or export the existing builder) so
       Task 2 can import it without invoking the Deno serve handler.
  </action>
  <verify>
    <automated>grep -n "PROPOSAL HISTORY" supabase/functions/generate-proposal-section/index.ts && grep -n "regulatoryCount > 0 ? COMPLIANCE_CLAUSE" supabase/functions/generate-proposal-section/index.ts && grep -n "No regulatory grounding available" supabase/functions/generate-proposal-section/index.ts && ! grep -n "ragChunks" supabase/functions/generate-proposal-section/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ragChunks" supabase/functions/generate-proposal-section/index.ts` returns 0.
    - `grep "\[PROPOSAL HISTORY\]" ...index.ts` matches AND `grep "\[REGULATORY CONTEXT\]" ...index.ts` matches (separate headers).
    - `grep "regulatoryCount > 0 ? COMPLIANCE_CLAUSE" ...index.ts` matches (compliance is conditional).
    - `grep "No regulatory grounding available" ...index.ts` matches (the marker).
    - Base `CRO_PROPOSAL_SYSTEM_PROMPT` no longer contains the literal `compliant with ICH-GCP (E6 R2/R3)` (moved into COMPLIANCE_CLAUSE): `grep -A6 "CRO_PROPOSAL_SYSTEM_PROMPT =" ...index.ts | grep -c "compliant with ICH-GCP"` returns 0.
    - An exported `assembleSectionPrompt` (or equivalent exported builder) exists for import by the test.
  </acceptance_criteria>
  <done>Both builders emit separate headers; compliance clause appended only when regulatoryCount>0; no-grounding marker present when 0; no `ragChunks` remains; assembly is exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit-test prompt assembly branching on regulatoryCount</name>
  <files>supabase/functions/generate-proposal-section/promptAssembly.test.ts</files>
  <read_first>
    - supabase/functions/generate-proposal-section/index.ts (the exported assembleSectionPrompt from Task 1)
    - package.json (test:run / vitest convention)
  </read_first>
  <behavior>
    - regulatoryCount === 0 with proposalChunks present: output contains "[PROPOSAL HISTORY]" with the proposal chunk content, contains the "No regulatory grounding available" marker, and does NOT contain "compliant with ICH-GCP".
    - regulatoryCount === 0: proposal chunk content does NOT appear under/after "[REGULATORY CONTEXT]" before "[/REGULATORY CONTEXT]" (regulatory section shows "(No relevant regulatory context found)").
    - regulatoryCount > 0: output contains "compliant with ICH-GCP", contains the regulatory chunk content under "[REGULATORY CONTEXT]", and does NOT contain the "No regulatory grounding available" marker.
  </behavior>
  <action>
    Create supabase/functions/generate-proposal-section/promptAssembly.test.ts as a Vitest spec importing
    the exported `assembleSectionPrompt` from ./index.ts (import must not trigger Deno.serve — Task 1's
    entry guard / export split ensures the pure function is importable under Node/Vitest; if index.ts
    imports Deno globals at module top-level, extract the pure assembly into a sibling `promptAssembly.ts`
    and import from there instead). Write three `it()` cases matching <behavior>. Use fixture chunks like
    `{ content: 'PROP-CONTENT-XYZ', source: 'proposal:acme' }` and `{ content: 'REG-CONTENT-ABC', source: 'ICH-E6R3' }`
    so assertions can grep for distinctive tokens. Wire the spec into the existing vitest run (test:run).
  </action>
  <verify>
    <automated>npm run test:run -- promptAssembly 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run -- promptAssembly` passes with 3 passing cases.
    - Test asserts the regulatoryCount===0 branch omits "compliant with ICH-GCP" and includes the marker.
    - Test asserts the regulatoryCount>0 branch includes "compliant with ICH-GCP" and populated [REGULATORY CONTEXT].
    - Test asserts PROP-CONTENT token appears only under [PROPOSAL HISTORY], never under [REGULATORY CONTEXT].
  </acceptance_criteria>
  <done>Vitest proves the assembly branches correctly on regulatoryCount; suite green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING — LIVE] Deploy generate-proposal-section</name>
  <what-built>
    Conditional-compliance + dual-header edge function (Task 1) and its passing unit test (Task 2), plus
    the client contract from Plan 01. generate-proposal-section is new-to-14.6 (untouched by 14.5) —
    committed != live, so a redeploy is REQUIRED before (b) can be verified end-to-end. retrieve-context
    needs NO deploy.
  </what-built>
  <how-to-verify>
    1. Confirm Plan 01 (client contract) and Task 1/2 above are committed — client + edge ship together.
    2. Run the deploy (this is the live step — do not run until approved):
         npx supabase functions deploy generate-proposal-section --project-ref fuuvdcvbliijffogjnwg
    3. Confirm the deploy reports success (new version published).
    4. Contract check: the deployed edge reads regulatoryChunks / proposalChunks / regulatoryCount off the
       body and the client (Plan 01) sends exactly those — no silent shape drift. If the frontend is served
       from a separate deploy (Netlify), note that the client bundle must also be redeployed for the live
       contract to match; flag if not.
  </how-to-verify>
  <verify>
    <automated>grep -n "PROPOSAL HISTORY" supabase/functions/generate-proposal-section/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `supabase functions deploy generate-proposal-section` reports success for project-ref fuuvdcvbliijffogjnwg.
    - Client (Plan 01) and edge payload fields match: regulatoryChunks, proposalChunks, regulatoryCount.
  </acceptance_criteria>
  <resume-signal>Type "deployed" once the function is live, or describe deploy errors.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| retrieved chunk content → LLM prompt | Chunk text (regulatory + proposal history) is concatenated into the model system prompt |
| client → edge body | Untrusted payload fields (chunks, counts) cross into the edge function |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.6-02 | Tampering (prompt injection) | context-block assembly in index.ts | mitigate | Wrap both tiers in explicit delimited headers ([REGULATORY CONTEXT]/[/…], [PROPOSAL HISTORY]/[/…]); chunk content is quoted as reference material, and the no-grounding marker + CRITICAL RULES ([PLACEHOLDER], never fabricate) constrain the model. Do not let a client-supplied regulatoryCount fabricate a compliance claim — it only gates the clause; the marker is emitted whenever regulatoryChunks is empty regardless. |
| T-14.6-03 | Spoofing/Info | client-supplied regulatoryCount | accept | regulatoryCount originates from retrieve-context retrievalMeta; a tampered count cannot manufacture regulatory chunks (empty array still yields the marker). Low risk; org-scoped auth unchanged. |
</threat_model>

<verification>
- Unit suite (Task 2) green.
- grep confirms separate headers, conditional clause, marker, and zero `ragChunks`.
- Edge redeployed to fuuvdcvbliijffogjnwg (Task 3). Build/typecheck passing is NOT sufficient — the deploy is mandatory for (b) verification.
</verification>

<success_criteria>
A section with regulatoryCount=0 + proposal chunks does not emit proposals under [REGULATORY CONTEXT],
carries the no-grounding marker, and asserts no ICH-GCP compliance; a section with regulatoryCount>0
retains the assertion + populated [REGULATORY CONTEXT]; and the corrected function is live.
</success_criteria>

<output>
After completion, create `.planning/phases/14.6-regulatory-grounding-correctness/14.6-02-SUMMARY.md`
</output>
