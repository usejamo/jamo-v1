---
phase: 14.6-regulatory-grounding-correctness
plan: 04
subsystem: infra
tags: [seeder, cli, vitest, topo-sort, regulatory-corpus, tsx]

# Dependency graph
requires:
  - phase: 14.6-regulatory-grounding-correctness (plan 02/03)
    provides: hardened ingest_regulatory_document RPC (RAISEs on unresolved --supersedes) + regulatory tier-collapse fix, which this seeder depends on for a trustworthy all-or-nothing write
provides:
  - Typed, commented PROPOSAL starter manifest (scripts/regulatory-starter-manifest.ts) — 5 ICH docs, E6R3 scoped {US,EU,UK} NOT GLOBAL with load-bearing rationale comment
  - scripts/seed-regulatory.ts — validateManifest (aggregates dup-key/missing-folder/missing-supersedes-target/cycle failures, path-traversal guard), topoSortBySupersedes (Kahn's algorithm dependency ordering), and a full ingest loop reusing embedBatch/chunkDocument/ingest_regulatory_document RPC
  - npm run seed:regulatory / --validate-only CLI entry point
  - Vitest coverage proving pre-validation hard-fails before any embedding call
affects: [14.6-05 (live seed run + e2e verification)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Batch pre-validation aggregates ALL failures before any spend-sensitive call (never short-circuits on first error)"
    - "Kahn's algorithm topological sort for dependency ordering, with indegree-never-reaches-zero as the cycle signal"
    - "main() takes overridable (entries, docsRoot) params so tests exercise the CLI entrypoint against fixture manifests without touching the real STARTER_MANIFEST/regulatory-docs tree"

key-files:
  created:
    - scripts/regulatory-starter-manifest.ts
    - scripts/seed-regulatory.ts
    - scripts/seed-regulatory.test.ts
  modified:
    - package.json
    - .gitignore

key-decisions:
  - "extractPdfText duplicated verbatim from scripts/ingest-regulatory.ts (not exported there) rather than exporting it — matches the existing project convention of duplicating the loadEnv() minimal .env loader across CLI scripts rather than sharing a utils module"
  - "main(argv, entries?, docsRoot?) accepts overridable manifest/docsRoot params (anticipated by the plan's 'a manifest override if convenient' note) so the --validate-only Vitest case can exercise main() end-to-end against mkdtemp fixtures without mutating STARTER_MANIFEST or requiring env vars"
  - "Path-traversal guard (T-14.6-08) added to validateManifest: folder values containing '/', '\\\\', or '..' are rejected as a validation error before any filesystem read is attempted under docsRoot"

patterns-established:
  - "Seeder CLIs in scripts/ follow: pure validateManifest (no I/O side effects beyond fs existence checks) -> aggregated non-zero exit -> ingest loop only after 100% clean validation"

requirements-completed: []

# Metrics
duration: ~15min
completed: 2026-07-09
---

# Phase 14.6 Plan 04: Starter-Corpus Seeder (Manifest + Pre-Validation + Ingest Loop) Summary

**Manifest-driven `npm run seed:regulatory` CLI: batch pre-validation (dup-keys/missing-folders/missing-supersedes-targets/cycles/path-traversal) aggregated and exited non-zero before a single embedding call, Kahn's-algorithm dependency ordering, and an ingest loop reusing `embedBatch`/`chunkDocument`/`ingest_regulatory_document` — no live run performed.**

## Performance

- **Duration:** ~15 min (commit span 14:27–14:33 local, plus research/reading)
- **Started:** 2026-07-09T20:24:00Z (approx, session start)
- **Completed:** 2026-07-09T20:39:00Z (approx)
- **Tasks:** 3/3
- **Files modified:** 5 (3 created, 2 modified)

## Accomplishments
- Typed `STARTER_MANIFEST` with 5 ICH docs (E6R3, E8R1, E9, E3, M3R2), clearly marked PROPOSAL/pending-clinical-review, and the load-bearing E6R3 `{US,EU,UK}` NOT `GLOBAL` comment preserved verbatim from the BRIEF
- `validateManifest` aggregates all four required failure classes (duplicate keys, missing folder/PDF, missing supersedes target, cycle) into one non-zero-exit report — plus a path-traversal guard (T-14.6-08) not explicitly itemized in the four classes but required by the threat model
- Kahn's-algorithm `topoSortBySupersedes` computes a dependency-correct processing order independent of manifest array order, with cycle detection as a natural side effect (indegree never reaches 0)
- Full ingest loop (`ingestEntry`/`runIngest`) wired to the reused `embedBatch`, `chunkDocument`, and `ingest_regulatory_document` RPC — all-or-nothing, aborts and reports the failing entry on any RPC raise
- `scripts/seed-regulatory.test.ts`: 8 Vitest specs (6 `validateManifest`, 2 `main --validate-only`) using `mkdtemp` fixtures; `embedBatch` is mocked via `vi.mock('./ingest-regulatory', ...)` and asserted **not called** when an invalid manifest hits `--validate-only`

## Task Commits

1. **Task 1: Typed starter manifest + npm script + .gitignore** - `f5769fd` (feat)
2. **Task 2: Seeder pre-validation + dependency ordering + --validate-only** - `b18052b` (feat)
3. **Task 3: Ingest execution loop (reused exports) + pre-validation Vitest** - `86bd765` (feat)

**Plan metadata:** (this commit, pending) - `docs(14.6-04): complete starter-corpus seeder plan`

## Files Created/Modified
- `scripts/regulatory-starter-manifest.ts` - Typed `ManifestEntry`/`STARTER_MANIFEST`, PROPOSAL header comment, E6R3 load-bearing geography comment
- `scripts/seed-regulatory.ts` - `validateManifest`, `topoSortBySupersedes`, `ingestEntry`/`runIngest`, `main()` CLI entrypoint, `isMain` entry guard
- `scripts/seed-regulatory.test.ts` - Vitest coverage for pre-validation failure classes, ordering correctness, and the embedBatch-not-called invariant
- `package.json` - `"seed:regulatory": "npx tsx scripts/seed-regulatory.ts"` script added
- `.gitignore` - `regulatory-docs/**/*.pdf` (operator-supplied PDFs excluded, folder structure stays documented in the manifest)

## Decisions Made
- `extractPdfText` duplicated (not imported) from `scripts/ingest-regulatory.ts` since it isn't exported there — mirrors the existing codebase convention of duplicating small CLI-local helpers (e.g. `loadEnv`) rather than introducing a shared `scripts/` utils module.
- `main()` signature extended to `main(argv, entries?, docsRoot?)` with defaults of `STARTER_MANIFEST`/`DEFAULT_DOCS_ROOT` — enables the Vitest suite to exercise the real CLI entrypoint end-to-end (including the `process.exit` / embedBatch-not-called assertion) against `mkdtemp` fixtures instead of only unit-testing `validateManifest` in isolation.
- Added a path-traversal guard to `validateManifest` (folder values containing `/`, `\`, or `..` are rejected) per the plan's own threat model (T-14.6-08), even though it isn't one of the four explicitly-named failure classes in the acceptance criteria — this is a Rule 2 (missing critical functionality) auto-add since the threat register explicitly assigns a `mitigate` disposition to this exact risk for this exact file.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Added path-traversal guard to validateManifest**
- **Found during:** Task 2
- **Issue:** The plan's four named validation checks (dup keys, folder+PDF, supersedes target, cycle) don't explicitly mention rejecting unsafe `folder` values, but the plan's own `<threat_model>` assigns T-14.6-08 ("Tampering (arbitrary file read) — manifest `folder` → filesystem path") a `mitigate` disposition on this exact file, specifying "reject folder values containing path separators / `..` in validateManifest (treat as a validation error)".
- **Fix:** `validateManifest` now checks `folder.includes('/') || folder.includes('\\') || folder.includes('..')` before any `existsSync`/`readdirSync` call and pushes a validation error rather than attempting the read.
- **Files modified:** scripts/seed-regulatory.ts
- **Verification:** New Vitest case ("rejects a folder value containing path separators or '..' as a validation error") passes.
- **Committed in:** b18052b (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 missing critical / threat-model mitigation)
**Impact on plan:** Required by the plan's own threat register; no scope creep — implements an explicitly named mitigation for a file this plan creates.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required. No live seed run was performed in this plan (deferred to Plan 05 per objective).

## Next Phase Readiness
- Seeder is fully built and unit-tested; `npm run seed:regulatory --validate-only` correctly fails against the currently-empty `regulatory-docs/` tree (5 aggregated missing-folder errors, exit 1, zero embedding calls) — this is expected since no PDFs have been supplied yet.
- Plan 05 owns: supplying the operator PDFs under `regulatory-docs/<documentKey>/`, running the live `npm run seed:regulatory` (credit-spending embedding calls), and end-to-end verification (US Phase-2 oncology section retrieves ICH-E6R3; a JP-scoped section legitimately retrieves zero and surfaces the (b) marker).
- No blockers.

---
*Phase: 14.6-regulatory-grounding-correctness*
*Completed: 2026-07-09*

## Self-Check: PASSED

- FOUND: scripts/regulatory-starter-manifest.ts
- FOUND: scripts/seed-regulatory.ts
- FOUND: scripts/seed-regulatory.test.ts
- FOUND commit: f5769fd
- FOUND commit: b18052b
- FOUND commit: 86bd765
