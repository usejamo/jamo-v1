---
phase: 14.6-regulatory-grounding-correctness
plan: 03
subsystem: database
tags: [postgres, plpgsql, migration, ingest-rpc, supersession, security-definer]

# Dependency graph
requires:
  - phase: 14.5-rag-retrieval-overhaul
    provides: ingest_regulatory_document RPC (per-doc upsert + chunk replace), migration 20260708000005
provides:
  - ingest_regulatory_document raises on an unresolved active supersedes target (no more silent no-op)
  - NULL supersedes key path still ingests normally (GLOBAL starter-doc regression guard intact)
affects: [14.6-04-starter-seeder, 14.6-05-live-seed-e2e]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "SECURITY DEFINER RPC hardened to fail loudly (RAISE EXCEPTION) on an unresolvable reference rather than silently falling through a NULL guard — makes the seeder's all-or-nothing contract trustworthy"

key-files:
  created:
    - supabase/migrations/20260709000001_ingest_regulatory_rpc_supersedes_raise.sql
  modified: []

key-decisions:
  - "The RAISE is nested strictly inside the `IF p_supersedes_document_key IS NOT NULL THEN` branch. A top-level `IF v_supersedes_id IS NULL THEN RAISE` was explicitly avoided — it would fire on every non-superseding GLOBAL ingest and break seeding."
  - "Migration applied live via Supabase MCP apply_migration (NOT db push — diverged migration history per project memory)."
  - "REVOKE ... FROM PUBLIC, anon, authenticated + GRANT EXECUTE TO service_role footer re-emitted verbatim; omitting it silently re-grants EXECUTE to anon/authenticated on CREATE OR REPLACE."

patterns-established:
  - "Live-DB checkpoint (checkpoint:human-action, gate=blocking) resolved by the orchestrator via Supabase MCP after user approval, with post-apply regression checks (raise path + NULL-key regression guard + grant verification) rather than by the sequential executor (which lacks Supabase MCP tools)."

requirements-completed: [brief-a-supersession-raise]

# Metrics
duration: 12min
completed: 2026-07-09
---

# Phase 14.6 Plan 03: Ingest RPC Supersedes-Raise Summary

**Replaced `ingest_regulatory_document` with a `CREATE OR REPLACE` that RAISES `supersedes target % not found among active regulatory_documents` when a `p_supersedes_document_key` does not resolve to an active row, while leaving the NULL-key (non-superseding) path a normal ingest. Applied live to production and regression-verified.**

## Performance

- **Duration:** ~12 min (author + live apply + regression checks)
- **Completed:** 2026-07-09
- **Tasks:** 2 (1 auto, 1 blocking live-DB checkpoint)
- **Files created:** 1 migration

## Accomplishments
- Authored `supabase/migrations/20260709000001_ingest_regulatory_rpc_supersedes_raise.sql` — the full function body copied verbatim from `20260708000005` except the supersession branch, which now RAISES when `v_supersedes_id IS NULL` inside the `p_supersedes_document_key IS NOT NULL` guard.
- Preserved verbatim: the 11-arg signature, `SECURITY DEFINER SET search_path = public, extensions`, upsert-in-place (`document_key` never in the UPDATE SET), parent-scoped `DELETE FROM chunks WHERE regulatory_document_id = v_id`, jsonb chunk insert, and the REVOKE/GRANT footer.
- Applied the migration live to production project `fuuvdcvbliijffogjnwg` via Supabase MCP `apply_migration` (success).
- Regression-verified live (see below).

## Task Commits

1. **Task 1: Author CREATE OR REPLACE migration with the supersedes raise** — `eed4927` (feat)
2. **Task 2: [BLOCKING — LIVE DB] Apply migration via Supabase MCP + regression check** — orchestrator-driven live action (no code commit); this metadata commit records completion.

## Live Apply & Regression Evidence

Applied via Supabase MCP `apply_migration` (name `ingest_regulatory_rpc_supersedes_raise`) → `{"success":true}`.

1. **Raise path** — `ingest_regulatory_document(..., p_supersedes_document_key => 'NONEXISTENT-KEY-TEST', p_chunks => '[]')` raised:
   `ERROR: P0001: supersedes target NONEXISTENT-KEY-TEST not found among active regulatory_documents`.
   The statement rolled back — verified `TEST-RAISE-KEY-14-6-3` left 0 rows.
2. **NULL path (GLOBAL regression guard)** — `ingest_regulatory_document(..., p_supersedes_document_key => NULL, p_chunks => '[]')` succeeded, returning UUID `974b711a-96a5-4c05-82ea-b6c45a625118`. Test row cleaned up (0 leftover).
3. **Grants** — `EXECUTE` grantees on the function are `postgres, service_role` only; `anon` and `authenticated` are excluded (footer intact).

## Files Created/Modified
- `supabase/migrations/20260709000001_ingest_regulatory_rpc_supersedes_raise.sql` — CREATE OR REPLACE with the nested supersedes RAISE; footer + security clauses preserved.

## Deviations from Plan

None — plan executed as written. The live apply + regression checks were performed by the orchestrator (via Supabase MCP, after user approval) because the sequential executor agent lacks Supabase MCP tools; this matches the plan's `checkpoint:human-action` intent.

## Issues Encountered
None.

## User Setup Required
None — migration is live on production. No further action.

## Next Phase Readiness
- The ingest RPC now fails loudly on an unresolved supersedes, so the Plan 04/05 seeder's all-or-nothing contract is trustworthy — a bad `--supersedes` aborts the run instead of silently no-opping.
- All GLOBAL starter docs (NULL supersedes) ingest normally — no regression to the common path.

---
*Phase: 14.6-regulatory-grounding-correctness*
*Completed: 2026-07-09*

## Self-Check: PASSED

- FOUND: eed4927
- FOUND: supabase/migrations/20260709000001_ingest_regulatory_rpc_supersedes_raise.sql
- APPLIED LIVE: apply_migration success; raise path + NULL path + grants all verified
- FOUND: .planning/phases/14.6-regulatory-grounding-correctness/14.6-03-SUMMARY.md
