---
phase: 14.6-regulatory-grounding-correctness
verified: 2026-07-09T21:00:00Z
status: passed
score: 15/15 must-haves verified
overrides_applied: 0
---

# Phase 14.6: Regulatory Grounding Correctness + Ingest Hardening + Starter Corpus Seeder Verification Report

**Phase Goal:** Fix (b) the two-tier retrieval collapse + conditional compliance, (a) the ingest supersedes silent no-op, and deliver a starter-corpus seeder proven end-to-end.
**Verified:** 2026-07-09T21:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `fetchRagChunks` no longer merges the two retrieval tiers into one array | ✓ VERIFIED | `src/hooks/useProposalGeneration.ts:198-223` returns `{ regulatoryChunks, proposalChunks, regulatoryCount }`; zero `ragChunks` tokens remain in the file |
| 2 | `regulatoryChunks`, `proposalChunks`, `regulatoryCount` reach the edge payload separately | ✓ VERIFIED | `streamSection`/`generateAll`/`generateSection` thread `rag.*` into the three payload fields; `supabase/functions/generate-proposal-section/index.ts:179-181,233-245` destructures and passes all three to both builders |
| 3 | `GenerateSectionPayloadV2` no longer has a single merged `ragChunks` field | ✓ VERIFIED | `src/types/generation.ts:37,70-72` — `RagChunk` interface exported; `regulatoryChunks`/`proposalChunks`/`regulatoryCount` fields present; zero `ragChunks` matches |
| 4 | Regulatory chunks render under `[REGULATORY CONTEXT]`; proposal-history under separate `[PROPOSAL HISTORY]` (both builders) | ✓ VERIFIED | `promptAssembly.ts:107,232` — dual headers present in both V1 and V2 assembly functions |
| 5 | `regulatoryCount===0` emits "no regulatory grounding" marker and NO ICH-GCP/E6/FDA-EMA reference anywhere (incl. `regulatory_strategy`) | ✓ VERIFIED | `promptAssembly.ts:109,193,234` — marker text present; zero-grounding `regulatory_strategy` fallback reworded to contain no named-framework token (verified by grep + by passing unit test asserting 0 ICH-GCP occurrences) |
| 6 | `regulatoryCount>0` retains compliance assertion + populated `[REGULATORY CONTEXT]` | ✓ VERIFIED | `promptAssembly.ts:68,92,191-192,197` — `COMPLIANCE_CLAUSE` appended when `regulatoryCount>0`; live e2e confirmed ICH-GCP ×2 present with E6R3 populated |
| 7 | `generate-proposal-section` redeployed live; client/edge payload contract match | ✓ VERIFIED | Per task-provided live facts: version 18, verify_jwt=true, deployed; index.ts fields match Plan 01 client contract exactly (spot-checked) |
| 8 | `ingest_regulatory_document` RAISES when `p_supersedes_document_key` set but unresolved | ✓ VERIFIED | Migration `20260709000001_ingest_regulatory_rpc_supersedes_raise.sql:45` — `RAISE EXCEPTION 'supersedes target % not found...'` nested inside the `IS NOT NULL` branch; live regression-verified per task facts |
| 9 | `p_supersedes_document_key = NULL` ingests normally (no raise) | ✓ VERIFIED | Migration preserves the `IS NOT NULL` guard (branch skipped entirely when NULL); live regression-verified per task facts (NULL-path succeeded, row inserted) |
| 10 | Migration applied live via Supabase MCP `apply_migration`; EXECUTE grant service_role-only | ✓ VERIFIED | Migration footer (`REVOKE ... FROM PUBLIC, anon, authenticated; GRANT EXECUTE ... TO service_role`) present at lines 67-68; live grant confirmed per task facts |
| 11 | Typed `STARTER_MANIFEST`; E6R3 scoped `{US,EU,UK}` (NOT GLOBAL) with load-bearing comment; others GLOBAL | ✓ VERIFIED | `scripts/regulatory-starter-manifest.ts:24,29` — `STARTER_MANIFEST` exported; E6R3 load-bearing "NOT GLOBAL" comment present; live seed confirmed E6R3 geography `{US,EU,UK}` |
| 12 | `npm run seed:regulatory` runs the seeder; batch pre-validation fails before any embedding call; dependency-ordered processing; `--validate-only` mode | ✓ VERIFIED | `package.json:14` script wired; `scripts/seed-regulatory.ts` exports `validateManifest`, `topoSortBySupersedes`, `--validate-only` exits before embedding (8 passing Vitest specs incl. embedBatch-not-called assertion) |
| 13 | Live seed populates corpus in dependency order (5 ICH docs, 268 chunks) | ✓ VERIFIED | Per task-provided live facts and 05-SUMMARY: E3 55, E6R3 89, E8R1 31, E9 59, M3R2 34 chunks; order E3→E6R3→E8R1→E9→M3R2; all embedded, org_id NULL |
| 14 | US grounded query retrieves seeded E6R3 with compliance assertion; JP/genuine-zero surfaces marker with no ICH-GCP | ✓ VERIFIED | Per task facts: grounded query → regulatoryCount=5, E6R3-only, ICH-GCP×2, no marker; genuine-zero (Study Design/Japan) → regulatoryCount=0, marker present, 0 ICH-GCP references |
| 15 | Sub-threshold "Study Design" retrieval gap reasonably classified as backlog (not phase defect) | ✓ VERIFIED | `ROADMAP.md:684-701` — Backlog 999.3 filed with exact query + top-10 scores; query confirmed properly enriched via `buildRegulatoryQuery` (no collapse); shortfall is embedding-semantics, not a (b)/(a)/seeder defect; threshold correctly not lowered per user directive |

**Score:** 15/15 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/types/generation.ts` | Split payload contract | ✓ VERIFIED | `RagChunk` exported; 3-field split present; 0 `ragChunks` |
| `src/hooks/useProposalGeneration.ts` | Split threading through fetch/streamSection | ✓ VERIFIED | Confirmed by direct read (lines 198-223); 0 `ragChunks` |
| `src/hooks/useProposalGeneration.test.ts` | Test assertions updated to split shape | ✓ VERIFIED | Deferred item from Plan 02 (stale merged-array assertions) was fixed — file now asserts `result.regulatoryChunks`/`proposalChunks`/`regulatoryCount`; 0 `ragChunks` remain; suite green |
| `supabase/functions/generate-proposal-section/promptAssembly.ts` | Gated compliance + dual headers, both builders | ✓ VERIFIED | All grep + read checks pass; 5/5 unit tests green |
| `supabase/functions/generate-proposal-section/index.ts` | Handler reads split fields, re-exports builders | ✓ VERIFIED | Destructures `regulatoryChunks`/`proposalChunks`/`regulatoryCount`; 0 `ragChunks` |
| `supabase/functions/generate-proposal-section/promptAssembly.test.ts` | Unit test of actual V2/V1 builders | ✓ VERIFIED | 5 tests passing in full suite run |
| `supabase/migrations/20260709000001_ingest_regulatory_rpc_supersedes_raise.sql` | RAISE on unresolved supersedes | ✓ VERIFIED | RAISE + REVOKE/GRANT + SECURITY DEFINER all present; applied live |
| `scripts/regulatory-starter-manifest.ts` | Typed PROPOSED manifest | ✓ VERIFIED | STARTER_MANIFEST + load-bearing E6 comment present |
| `scripts/seed-regulatory.ts` | Pre-validation, ordering, ingest loop, --validate-only | ✓ VERIFIED | All functions present; live-run proven |
| `scripts/seed-regulatory.test.ts` | Pre-validation Vitest | ✓ VERIFIED | 8/8 tests passing |
| `package.json` | `seed:regulatory` script | ✓ VERIFIED | Present |
| `.gitignore` | `regulatory-docs/**/*.pdf` | ✓ VERIFIED | Present |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `useProposalGeneration.ts` | `generation.ts` types | `GenerateSectionPayloadV2` field types | ✓ WIRED | Direct read confirms exact field usage |
| `fetchRagChunks` | `streamSection` payload | threaded split, no merge | ✓ WIRED | `rag.regulatoryChunks`/`rag.proposalChunks`/`rag.regulatoryCount` spread into payload (per Plan 01 summary + no residual `ragChunks`) |
| `generate-proposal-section/index.ts` | `regulatoryCount` | conditional compliance + gated `regulatory_strategy` + marker | ✓ WIRED | Confirmed in promptAssembly.ts at all 3 gate sites |
| `buildSectionPrompt`/`buildSectionPromptV2` | `[PROPOSAL HISTORY]` | separate header | ✓ WIRED | Both builders contain the header (lines 107, 232) |
| `scripts/seed-regulatory.ts` | `embedBatch` + `chunkDocument` + `ingest_regulatory_document` RPC | reused exports | ✓ WIRED | grep confirms `embedBatch(`, `ingest_regulatory_document` calls; live run succeeded (268 chunks landed) |
| Pre-validation | process exit before embedding | aggregated report, exit non-zero | ✓ WIRED | `process.exit(1)` present pre-embedding; Vitest asserts embedBatch not called on invalid batch |
| Seeded ICH-E6R3 `{US,EU,UK}` | US oncology GCP retrieval | geography + phase filter match | ✓ WIRED | Live e2e: regulatoryCount=5, E6R3-only sources |
| Japan-scoped section | (b) no-grounding marker | legitimate zero-retrieval made visible | ✓ WIRED | Live e2e: regulatoryCount=0, marker present, 0 ICH-GCP |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite passes | `npm run test:run` | 344 passed, 16 skipped, 0 failed (51 files passed, 2 skipped) | ✓ PASS |
| No `ragChunks` token remains in client/edge/types | `grep -c ragChunks` across generation.ts, useProposalGeneration.ts, useProposalGeneration.test.ts, generate-proposal-section/index.ts | 0 matches in all four | ✓ PASS |
| Migration RAISE + grants intact | grep on migration file | RAISE EXCEPTION, REVOKE/GRANT, SECURITY DEFINER all present | ✓ PASS |
| Seeder --validate-only fails cleanly pre-embedding | `npm run test:run -- seed-regulatory` | 8/8 tests pass, includes embedBatch-not-called assertion | ✓ PASS |
| Live seed/deploy/migration state | Provided as established live facts by task (not re-run) | E6R3 89 chunks {US,EU,UK}, v18 deploy, migration applied+regression-verified | ✓ PASS (accepted per task-provided live-state facts) |

### Requirements Coverage

Phase 14.6 is a BRIEF-driven inserted phase (not present in the original `.planning/REQUIREMENTS.md`, which only tracks the MVP M1-* requirement set). Requirement IDs are locally defined in `14.6-BRIEF.md` and declared in plan frontmatter.

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|--------------|--------|----------|
| brief-b-tier-separation | 01, 02 | Split retrieval tiers client + edge | ✓ SATISFIED | Truths 1-4 |
| brief-b-conditional-compliance | 02 | Gate all 3 compliance sources on regulatoryCount | ✓ SATISFIED | Truths 5-6 |
| brief-b-deploy | 02 | Redeploy generate-proposal-section | ✓ SATISFIED | Truth 7 |
| brief-a-supersession-raise | 03 | RAISE on unresolved supersedes | ✓ SATISFIED | Truths 8-10 |
| brief-seeder-e2e | 05 | Live seed + e2e proof | ✓ SATISFIED | Truths 11-15 |

No orphaned requirements — Plan 04 (seeder build) declares no requirement ID in frontmatter (its `requirements-completed: []` in the SUMMARY is expected: it builds the seeder but the "seeder e2e" requirement is only satisfied once Plan 05 proves it live). All five BRIEF deliverable threads (b-tier-separation, b-conditional-compliance, b-deploy, a-supersession-raise, seeder-e2e) are claimed and satisfied by at least one plan.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `supabase/functions/generate-proposal-section/test.ts` | 1 | Stray unresolved `<<<<<<< HEAD` merge-conflict marker + stale `ragChunks`-signature calls | ℹ️ Info | Pre-existing (predates 14.6), logged in `deferred-items.md`. Deno-only suite excluded from `npm run test:run` and not imported by the deployed entrypoint — does not affect shipped behavior or this phase's goal. Should be cleaned up as housekeeping but is not a phase blocker. |
| `.planning/ROADMAP.md` | 606 | Plan 14.6-05 checkbox still `[ ]` unchecked despite SUMMARY recording all 3 tasks PASSED/ACCEPTED and live facts confirming completion | ℹ️ Info | Documentation staleness only — recommend the orchestrator flip this checkbox to `[x]` as part of phase close-out. Does not indicate incomplete work; SUMMARY, live DB state, and e2e evidence all confirm Plan 05 is done. |

No blocker-level anti-patterns found. No placeholder/stub code, no empty handlers, no hardcoded-empty data flows detected in the modified files.

### Human Verification Required

None. All truths are either verified directly against the current codebase state or accepted as already-confirmed live facts per the task's explicit live-state instructions (migration application, edge deployment, live seed, and e2e retrieval outcomes were pre-verified and supplied as ground truth, not re-derived here).

### Gaps Summary

No gaps found. All 15 derived observable truths (roadmap goal + BRIEF deliverables (b), (a), (third), each cross-checked against plan must_haves) are verified against the actual codebase: the tier-collapse is fixed end-to-end (client type → hook → edge handler → both prompt builders), all three compliance-assertion sources are conditionally gated on `regulatoryCount`, the ingest RPC now raises loudly on an unresolved supersedes while preserving the NULL-path regression, and the starter-corpus seeder is built, unit-tested, and has been proven live (5 docs / 268 chunks seeded in dependency order, with a live e2e run distinguishing a grounded US GCP retrieval from a genuine Japan/Study-Design zero-retrieval that correctly surfaces the no-grounding marker without a false compliance claim). The one open item (sub-threshold "Study Design" retrieval) was correctly diagnosed as an embedding-semantics gap rather than a defect in this phase's fixes and was filed as Backlog 999.3 with supporting evidence — this is a reasonable, well-evidenced deferral, not a phase gap. The previously-flagged regression in `useProposalGeneration.test.ts` (deferred from Plan 02) has since been fixed and the full suite is green (344 passed / 0 failed).

Two informational (non-blocking) housekeeping items were noted: a pre-existing stray Deno test file with a merge-conflict marker (unrelated to this phase, already logged in `deferred-items.md`), and a stale ROADMAP checkbox for Plan 05 that should be flipped to `[x]`.

---

*Verified: 2026-07-09T21:00:00Z*
*Verifier: Claude (gsd-verifier)*
