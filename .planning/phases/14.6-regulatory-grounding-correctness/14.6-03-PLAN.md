---
phase: 14.6-regulatory-grounding-correctness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260709000001_ingest_regulatory_rpc_supersedes_raise.sql
autonomous: false
requirements:
  - brief-a-supersession-raise
must_haves:
  truths:
    - "ingest_regulatory_document RAISES when p_supersedes_document_key IS NOT NULL and no active target resolves"
    - "ingest_regulatory_document with p_supersedes_document_key = NULL ingests normally (no raise)"
    - "The migration is applied live via Supabase MCP apply_migration"
  artifacts:
    - path: "supabase/migrations/20260709000001_ingest_regulatory_rpc_supersedes_raise.sql"
      provides: "CREATE OR REPLACE ingest_regulatory_document with supersedes raise"
      contains: "RAISE EXCEPTION"
  key_links:
    - from: "ingest_regulatory_document supersedes branch"
      to: "RAISE EXCEPTION"
      via: "v_supersedes_id IS NULL inside the p_supersedes_document_key IS NOT NULL branch"
      pattern: "RAISE EXCEPTION 'supersedes target"
---

<objective>
Harden `ingest_regulatory_document` so that passing a `--supersedes` key that does not resolve to an
active document RAISES instead of silently no-opping. New migration via `CREATE OR REPLACE`, applied
live through Supabase MCP `apply_migration` (NOT `db push` — diverged history). DB-only; no edge deploy.

Purpose: A trustworthy seeder (Plan 04/05) depends on the RPC failing loudly on an unresolved
supersedes rather than falling through the `IF v_supersedes_id IS NOT NULL` guard.
Output: Applied migration; regression-guarded raise behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.6-regulatory-grounding-correctness/14.6-BRIEF.md
@.planning/phases/14.6-regulatory-grounding-correctness/14.6-PATTERNS.md

<interfaces>
<!-- Current definition to copy verbatim except one branch:
     supabase/migrations/20260708000005_ingest_regulatory_rpc.sql
     Current supersession branch (~40-49) — the silent no-op:
       IF p_supersedes_document_key IS NOT NULL THEN
         SELECT id INTO v_supersedes_id FROM regulatory_documents
           WHERE document_key = p_supersedes_document_key AND status = 'active' AND id <> v_id;
         IF v_supersedes_id IS NOT NULL THEN   -- falls through silently when NULL
           UPDATE regulatory_documents SET status='superseded', superseded_by=v_id, updated_at=NOW() WHERE id=v_supersedes_id;
           UPDATE regulatory_documents SET supersedes=v_supersedes_id, updated_at=NOW() WHERE id=v_id;
         END IF;
       END IF;
     MUST preserve verbatim: full arg list (~8-20); LANGUAGE plpgsql SECURITY DEFINER SET search_path=public,extensions (~21);
     upsert-in-place (document_key NEVER in UPDATE SET, ~37); DELETE FROM chunks WHERE regulatory_document_id=v_id (~51);
     jsonb chunk insert (~53-57); REVOKE/GRANT footer (~63-66). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author CREATE OR REPLACE migration with the supersedes raise</name>
  <files>supabase/migrations/20260709000001_ingest_regulatory_rpc_supersedes_raise.sql</files>
  <read_first>
    - supabase/migrations/20260708000005_ingest_regulatory_rpc.sql (the current definition to copy verbatim except one branch, incl. REVOKE/GRANT footer)
    - .planning/phases/14.6-regulatory-grounding-correctness/14.6-PATTERNS.md (migration section + Shared Pattern: REVOKE/GRANT footer)
  </read_first>
  <action>
    Create supabase/migrations/20260709000001_ingest_regulatory_rpc_supersedes_raise.sql. Open with a
    header comment block `-- Migration: Phase 14.6 — ingest_regulatory_document supersedes raise` (mirror
    the source header 1-6). Copy the ENTIRE current function body from 20260708000005 VERBATIM, changing
    ONLY the supersession branch. New branch:
      IF p_supersedes_document_key IS NOT NULL THEN
        SELECT id INTO v_supersedes_id FROM regulatory_documents
          WHERE document_key = p_supersedes_document_key AND status = 'active' AND id <> v_id;
        IF v_supersedes_id IS NULL THEN
          RAISE EXCEPTION 'supersedes target % not found among active regulatory_documents', p_supersedes_document_key;
        END IF;
        UPDATE regulatory_documents SET status='superseded', superseded_by=v_id, updated_at=NOW() WHERE id=v_supersedes_id;
        UPDATE regulatory_documents SET supersedes=v_supersedes_id, updated_at=NOW() WHERE id=v_id;
      END IF;
    CRITICAL: the RAISE is INSIDE the `p_supersedes_document_key IS NOT NULL` branch. Do NOT add any
    top-level `IF v_supersedes_id IS NULL THEN RAISE` — that would fire on every non-superseding ingest
    (all GLOBAL starter docs) and break seeding. When p_supersedes_document_key IS NULL the whole block
    is skipped (correct). Re-emit the REVOKE/GRANT footer (source ~63-66) IDENTICALLY, or EXECUTE is
    silently re-granted to anon/authenticated. Preserve the arg list, SECURITY DEFINER + search_path,
    upsert-in-place, parent-scoped chunk DELETE, and jsonb chunk insert exactly.
  </action>
  <verify>
    <automated>grep -n "RAISE EXCEPTION 'supersedes target" supabase/migrations/20260709000001_ingest_regulatory_rpc_supersedes_raise.sql && grep -n "REVOKE\|GRANT" supabase/migrations/20260709000001_ingest_regulatory_rpc_supersedes_raise.sql && grep -n "SECURITY DEFINER" supabase/migrations/20260709000001_ingest_regulatory_rpc_supersedes_raise.sql</automated>
  </verify>
  <acceptance_criteria>
    - File contains `RAISE EXCEPTION 'supersedes target % not found among active regulatory_documents'`.
    - The RAISE sits inside the `IF p_supersedes_document_key IS NOT NULL THEN` block (grep shows the outer IF above the RAISE; no RAISE outside it).
    - `grep -c "REVOKE" file` >= 1 and the GRANT footer is present (matches source ~63-66).
    - `grep "SECURITY DEFINER" file` matches and `grep "search_path = public, extensions" file` matches.
    - No `IF v_supersedes_id IS NOT NULL THEN` remains as the sole guard (the old silent path is gone).
  </acceptance_criteria>
  <done>Migration file created; only the supersedes branch changed; footer + security clauses preserved verbatim.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING — LIVE DB] Apply migration via Supabase MCP + regression check</name>
  <what-built>
    New CREATE OR REPLACE migration (Task 1) that makes the supersedes branch RAISE on an unresolved
    active target while leaving the NULL-key path a normal ingest.
  </what-built>
  <how-to-verify>
    1. Apply via Supabase MCP `apply_migration` (name: `ingest_regulatory_rpc_supersedes_raise`, contents
       = Task 1 file). DO NOT use `db push` (diverged history — see project memory).
    2. Regression / raise checks (via Supabase MCP execute_sql against project fuuvdcvbliijffogjnwg):
       a. RAISE path: call ingest_regulatory_document with p_supersedes_document_key = a key that does not
          exist (e.g. 'NONEXISTENT-KEY-TEST') and minimal valid args → EXPECT an error
          "supersedes target NONEXISTENT-KEY-TEST not found among active regulatory_documents".
       b. NULL path (GLOBAL-doc regression guard): call ingest_regulatory_document with
          p_supersedes_document_key = NULL and minimal valid args → EXPECT success (row inserted, no raise).
          Clean up the test row afterward.
    3. Confirm EXECUTE grant is service_role-only (not anon/authenticated) after apply.
  </how-to-verify>
  <verify>
    <automated>grep -n "RAISE EXCEPTION 'supersedes target" supabase/migrations/20260709000001_ingest_regulatory_rpc_supersedes_raise.sql</automated>
  </verify>
  <acceptance_criteria>
    - apply_migration reports success (function replaced).
    - Non-existent supersedes key call RAISES the expected message.
    - NULL supersedes key call succeeds (regression guard for GLOBAL docs).
  </acceptance_criteria>
  <resume-signal>Type "applied" once the migration is live and both raise/NULL checks pass, or describe errors.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| seeder/CLI → ingest RPC | Caller-supplied document_key, supersedes key, chunk payload cross into a SECURITY DEFINER function |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.6-04 | Elevation of Privilege | SECURITY DEFINER RPC grants | mitigate | Re-emit REVOKE ... FROM anon, authenticated + GRANT EXECUTE TO service_role footer verbatim; omitting it silently re-grants EXECUTE. Verify grants post-apply. |
| T-14.6-05 | Injection (SQLi) | parameterized RPC args | accept | All inputs are plpgsql function parameters bound by value (not string-concatenated SQL); RAISE uses the `%` format placeholder, not string interpolation. No dynamic SQL introduced. |
| T-14.6-06 | Tampering | search_path hijack | mitigate | Preserve `SET search_path = public, extensions` on the SECURITY DEFINER function (carried verbatim). |
</threat_model>

<verification>
- Migration file contains the raise inside the correct branch and the intact REVOKE/GRANT + security clauses.
- Live apply via MCP succeeds; non-existent key raises; NULL key ingests normally.
</verification>

<success_criteria>
Passing a `--supersedes` key that isn't an active document fails loudly; plain non-superseding ingests
(all GLOBAL starter docs) still succeed. Function is live on the project.
</success_criteria>

<output>
After completion, create `.planning/phases/14.6-regulatory-grounding-correctness/14.6-03-SUMMARY.md`
</output>
