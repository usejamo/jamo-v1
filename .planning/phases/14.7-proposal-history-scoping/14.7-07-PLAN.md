---
phase: 14.7-proposal-history-scoping
plan: 07
type: execute
wave: 5
depends_on: [05, 06]
files_modified:
  - .planning/phases/14.7-proposal-history-scoping/14.7-VERIFICATION.md
  - supabase/migrations/20260710000005_drop_backfill_audit_table.sql
autonomous: false
requirements: [R6, R7, R8, R9]

must_haves:
  truths:
    - "All 7 BRIEF verification cases pass live against the deployed project"
    - "The 3 fail-closed edge cases (unknown status, override-NULL fallthrough, overload count) pass"
    - "The admin write-gate (non-admin denied, admin allowed, cross-org denied) is confirmed live"
    - "A fresh proposal-document upload produces chunks with the correct proposal_id (R2 live)"
    - "The 14.6 regulatory e2e still passes unchanged after deploy (R9)"
    - "The Settings UI live UAT (3 toggles persist, per-proposal control non-draft-only, no draft include-path, non-admin cannot mutate) passes"
    - "The one-time _backfill_unresolved_proposal_chunks audit table is dropped once its count is confirmed 0 (D-05b)"
  artifacts:
    - path: ".planning/phases/14.7-proposal-history-scoping/14.7-VERIFICATION.md"
      provides: "Pass/fail record of all cases with the actual query results"
      contains: "BRIEF case"
    - path: "supabase/migrations/20260710000005_drop_backfill_audit_table.sql"
      provides: "Disposable audit-table cleanup (D-05b) after count confirmed 0"
      contains: "DROP TABLE"
  key_links:
    - from: "verify-eligibility.sql"
      to: "live RPCs"
      via: "Supabase MCP execute_sql"
      pattern: "match_chunks"
    - from: "14.6 e2e"
      to: "regulatory tier unchanged"
      via: "re-run existing e2e"
      pattern: "regulatory"
---

<objective>
Verify the deployed fix live: the BRIEF's 7 cases + 3 fail-closed edge cases, the admin write-gate, a fresh-ingest proposal_id check (R7/R2), the FULL Settings UI live UAT (R8 — folded here from Plan 05 since the RPCs are only live after Plan 06), and the 14.6 regulatory regression (R9). Then drop the one-time backfill audit table (D-05b). Deploy-first means automated build/CI is insufficient — this is the live gate before phase completion.

Purpose: prove the within-org bleed is closed, drafts are a hard floor, NULL/unknown-status fail closed, only admins can mutate the controls, nothing regressed in the regulatory tier, and no disposable artifact lingers.

Output: 14.7-VERIFICATION.md (pass/fail + actual results) + an audit-table-drop migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.7-proposal-history-scoping/14.7-BRIEF.md
@.planning/phases/14.7-proposal-history-scoping/14.7-RESEARCH.md
@.planning/phases/14.7-proposal-history-scoping/verify-eligibility.sql
@.planning/phases/14.7-proposal-history-scoping/verify-write-gating.sql

<facts>
- Live fixtures (RESEARCH): 2 won, 1 submitted, 1 lost, 56 draft proposals across 2 orgs — sufficient for every case.
- Case 6 (NULL proposal_id) has NO live rows → requires a synthetic insert in a BEGIN...ROLLBACK.
- 14.6 e2e (STATE.md): US-GCP grounded → E6R3 + ICH-GCP; Japan/zero → empty-marker. Re-run, do NOT recreate.
- MCP execute_sql runs as service_role — for the admin write-gate negative case, simulate a non-admin JWT claim or fall back to app-level UAT (per Plan 03 Task 2).
- localhost:5173 talks to the SAME live project (no local Supabase stack) — so the Settings/per-proposal live UAT is only meaningful here, AFTER Plan 06 applied the RPCs/columns. This is why Plan 05's live checkpoint moved here.
- D-05b: `_backfill_unresolved_proposal_chunks` is a disposable one-time artifact — drop it once inspected/confirmed 0 (not a permanent schema addition). D-05a: do NOT purge unresolved CHUNKS themselves (that's different from dropping the audit TABLE).
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the eligibility + write-gating SQL suites live; record pass/fail</name>
  <read_first>
    - .planning/phases/14.7-proposal-history-scoping/verify-eligibility.sql (Plan 02 Task 2 — the 7 cases + 3 edge cases).
    - .planning/phases/14.7-proposal-history-scoping/verify-write-gating.sql (Plan 03 Task 2 — allow/deny/cross-org).
    - .planning/phases/14.7-proposal-history-scoping/14.7-BRIEF.md § VERIFICATION (the authoritative 7 cases).
    - 14.7-06-DEPLOY-LOG.md — confirm the deploy actually happened before running (else results are meaningless).
  </read_first>
  <files>.planning/phases/14.7-proposal-history-scoping/14.7-VERIFICATION.md</files>
  <action>
Using Supabase MCP `execute_sql` against `fuuvdcvbliijffogjnwg`, run each block of verify-eligibility.sql and verify-write-gating.sql, substituting real live proposal/org UUIDs (SELECT them first). For EACH, record in 14.7-VERIFICATION.md: the case id, the query, the actual result, and PASS/FAIL against the expected outcome. Cover:
- BRIEF case 1: own chunks retrieved at any status incl. draft.
- BRIEF case 2: other-proposal drafts NEVER retrieved (even with reference_override=true forced in a rollback).
- BRIEF case 3: won (other) retrieved by default.
- BRIEF case 4: submitted+lost excluded by default; included after flipping the org switch; excluded after flipping back (use set_org_learning_switches or a direct rollback UPDATE).
- BRIEF case 5: reference_override=false excludes a won; =true includes a lost; =true on a draft is a no-op.
- BRIEF case 6: synthetic NULL proposal_id chunk not returned by either RPC (BEGIN...ROLLBACK).
- BRIEF case 7: `_backfill_unresolved_proposal_chunks` count matches the deploy-log headline.
- Edge A: unknown status ('archived') with all switches on → excluded (ELSE false).
- Edge B: reference_override IS NULL falls through exactly to the master switch.
- Edge C: exactly 2 proposal RPC overloads.
- Write-gate: admin allowed; non-admin denied ('not authorized'); cross-org write affects 0 rows (or documented app-UAT fallback for the non-admin path).
Any FAIL → record it and mark the phase gate not met (feeds a gap-closure plan). Leave NO live test state behind (all mutations rolled back).
  </action>
  <verify>
    <automated>grep -qi "BRIEF case 1" .planning/phases/14.7-proposal-history-scoping/14.7-VERIFICATION.md && grep -qi "BRIEF case 7" .planning/phases/14.7-proposal-history-scoping/14.7-VERIFICATION.md && grep -Eqi "PASS|FAIL" .planning/phases/14.7-proposal-history-scoping/14.7-VERIFICATION.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 7 BRIEF cases + 3 edge cases + write-gate cases recorded with actual results and PASS/FAIL in 14.7-VERIFICATION.md.
    - Case 2 explicitly confirms no toggle/override path retrieves another proposal's draft.
    - Case 6 uses a synthetic NULL chunk (rolled back); case 7 matches the deploy-log count.
    - Edge C confirms exactly 2 overloads.
    - No residual live test state (mutations rolled back).
  </acceptance_criteria>
  <done>All SQL verification cases run live and recorded PASS (or FAILs flagged for gap closure).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live e2e UAT — fresh ingest proposal_id, generation threading, FULL Settings UI, 14.6 regression</name>
  <what-built>The full 14.7 fix deployed live: proposal-scoped RAG history, ingest proposal_id, admin-gated controls (3 org toggles + per-proposal tri-state), all against the live project.</what-built>
  <how-to-verify>
    Local app http://localhost:5173 (login usera@jamo.com / password123, per MEMORY local-app-debug-setup) against the deployed prod project:
    1. R2 fresh ingest: upload a document to a known proposal; after extraction, via Supabase MCP query `SELECT DISTINCT proposal_id FROM chunks WHERE metadata->>'document_id' = '<new doc id>'` → equals that proposal's id (non-null, no backfill involved).
    2. R7 threading: generate a section in proposal A; confirm the [PROPOSAL HISTORY] tier surfaces A's own content and eligible non-draft others, but NOT other proposals' drafts. (Spot-check via generation output or retrieve-context response.)
    3. R8 Settings UI (folded from Plan 05 — first live run of these controls):
       a. As an admin, open Settings → the new "Reference Library"/"AI grounding" section. Confirm 3 toggles with labels "Learn from won proposals" (on), "Learn from submitted (in-flight) proposals" (off), "Learn from lost proposals" (off). Flip one, reload — it PERSISTS (confirms set_org_learning_switches wrote live).
       b. Open a NON-DRAFT proposal → the "use as reference" tri-state control appears near the status selector; set always/never/follow — reload PERSISTS (confirms set_reference_override wrote live).
       c. Open a DRAFT proposal → confirm there is NO "use as reference" include affordance.
       d. If a non-admin (role='user') test user is available, confirm the toggles/control are disabled AND a mutation attempt is refused ('not authorized') — the server gate, not just the UI hide.
    4. R9 regression: re-run the existing 14.6 live e2e (US-GCP grounded → E6R3 + ICH-GCP; Japan/zero → empty-marker). Confirm it is UNCHANGED. Do NOT recreate it — re-run.
    Record each result (with the actual proposal_id, the toggle-persistence outcome, and the 14.6 outcome) in 14.7-VERIFICATION.md.
  </how-to-verify>
  <resume-signal>Type "approved" when all four (incl. 3a-3d) confirm, or describe the failure(s).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Drop the disposable backfill audit table once its count is confirmed 0 (D-05b)</name>
  <read_first>
    - .planning/phases/14.7-proposal-history-scoping/14.7-CONTEXT.md D-05b (disposable lifecycle) + D-05a (do NOT purge the unresolved CHUNKS — only the audit TABLE is dropped) + D-05c (expected 0).
    - .planning/phases/14.7-proposal-history-scoping/14.7-VERIFICATION.md — BRIEF case 7 result must show the audit table count.
    - Deferred Ideas (CONTEXT.md): if the count is NON-ZERO and a recoverable-mismigration inspection is wanted first, DEFER the drop to backlog instead of running it.
  </read_first>
  <files>supabase/migrations/20260710000005_drop_backfill_audit_table.sql</files>
  <action>
GATE on BRIEF case 7: only proceed if `_backfill_unresolved_proposal_chunks` count == 0 (per D-05c/deploy-log). 
- If count == 0: author + apply `20260710000005_drop_backfill_audit_table.sql` = `DROP TABLE IF EXISTS public._backfill_unresolved_proposal_chunks;`. Apply it live via Supabase MCP `apply_migration` (NOT db push). Record the drop in 14.7-VERIFICATION.md.
- If count > 0: DO NOT drop. Instead record in 14.7-VERIFICATION.md that the table is retained for recoverable-mismigration inspection, and add a backlog note (per CONTEXT.md Deferred Ideas) to inspect + backfill recoverable rows, then drop later. Still author the migration file but leave it un-applied with a header comment explaining it is gated on count==0.
IMPORTANT: this drops the AUDIT TABLE only. Never DELETE the unresolved chunk rows themselves (D-05a — NULL already fails closed at retrieval).
  </action>
  <verify>
    <automated>grep -q "DROP TABLE IF EXISTS public._backfill_unresolved_proposal_chunks" supabase/migrations/20260710000005_drop_backfill_audit_table.sql && ! grep -Eqi "DELETE FROM public.chunks" supabase/migrations/20260710000005_drop_backfill_audit_table.sql && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Migration `20260710000005_drop_backfill_audit_table.sql` contains `DROP TABLE IF EXISTS public._backfill_unresolved_proposal_chunks;` and no DELETE on chunks.
    - If count==0: the table is dropped live (confirm via MCP `SELECT to_regclass('public._backfill_unresolved_proposal_chunks')` returns NULL) and recorded in VERIFICATION.md.
    - If count>0: drop NOT applied; retention + backlog note recorded; migration file present but gated.
  </acceptance_criteria>
  <done>Audit table dropped (count 0) or explicitly retained with a backlog note (count>0); unresolved chunks untouched; grep verify prints PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live prod behavior → confidentiality claim | Only a live run proves the bleed is closed; build/CI cannot (types come from config) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.7-23 | Information Disclosure | residual bleed undetected by static checks | mitigate | Live RPC calls against real fixtures for all 7 BRIEF cases + draft-floor negative case |
| T-14.7-24 | Information Disclosure | regulatory regression from the change | mitigate | 14.6 e2e re-run unchanged (R9 guard) |
| T-14.7-25 | Elevation of Privilege | write-gate not actually enforced live | mitigate | Non-admin-denied + cross-org-denied checks run live (SQL + app UAT step 3d) |
| T-14.7-26 | Information Disclosure | forensic audit table lingers with document_id ↔ proposal linkage | mitigate | Disposable audit table dropped once count confirmed 0 (D-05b); retained only if non-zero for recovery, then dropped |
</threat_model>

<verification>
- 14.7-VERIFICATION.md records PASS for all 7 BRIEF cases, 3 edge cases, write-gate, fresh-ingest proposal_id, threading, the full Settings UI live UAT (3a-3d), and the 14.6 regression.
- The audit table is dropped (count 0) or explicitly retained-with-backlog (count>0).
- Any FAIL is flagged for a `/gsd-plan-phase 14.7 --gaps` closure pass.
</verification>

<success_criteria>
- Live-verified: within-org proposal-history bleed closed; draft floor absolute; NULL/unknown-status fail closed; controls admin-only (server-enforced); regulatory tier unchanged; Settings UI works live; disposable audit artifact cleaned up.
</success_criteria>

<output>
After completion, create `.planning/phases/14.7-proposal-history-scoping/14.7-07-SUMMARY.md` summarizing the verification outcomes (all cases PASS or the list of FAILs), the audit-table disposition, and update `.planning/STATE.md` with the phase-14.7 completion status.
</output>
