# Phase 14.7: Proposal-History Scoping - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-10
**Phase:** 14.7-proposal-history-scoping
**Areas discussed:** Org-switch storage, RPC reads eligibility, Backfill reporting + orphans, Settings UI placement

---

## Org master-switch storage

| Option | Description | Selected |
|--------|-------------|----------|
| Dedicated columns | 3 boolean NOT NULL columns on `organizations` (won=true, submitted/lost=false) | ✓ |
| feature_flags jsonb keys | Store switches as keys in existing `organizations.feature_flags` jsonb | |

**User's choice:** Dedicated columns.
**Notes:** User added a blocker to VERIFY (not assume): confirm that `ADD COLUMN NOT NULL DEFAULT` backfills existing rows to the default. Verified — PG 17.6, metadata-only default, 2 existing orgs auto-get the safe state (won=on/submitted=off/lost=off); no separate backfill UPDATE. Migration must use constant defaults.

## RPC reads eligibility

| Option | Description | Selected |
|--------|-------------|----------|
| RPC self-joins + fail-closed | RPCs (SECURITY DEFINER) join organizations+proposals, read switches/override; unknown status = not eligible | ✓ |
| Edge passes as params | retrieve-context reads settings, passes booleans as RPC params | |
| Self-join, unknown = eligible | Self-join but unknown non-draft status defaults to eligible | |

**User's choice:** RPC self-joins + fail-closed (option 1).
**Notes:** Reject edge-passes-params — splits a confidentiality rule across edge + SQL (lockstep drift risk); express the rule exactly once in the RPC. Unknown/future non-draft status → NOT eligible; a new status (archived, on_hold) must not silently become retrievable. Accept visible under-include, never silent over-include. Implementation note: write eligibility as ORDERED branch evaluation with the draft-floor gate FIRST, not a flat boolean — makes fail-closed structural.

## Backfill reporting + orphans

| Option | Description | Selected |
|--------|-------------|----------|
| RAISE NOTICE + leave NULL | Log count, leave rows NULL | |
| Audit table + leave NULL | Record unresolvable ids in a disposable audit table, leave NULL; also emit count | ✓ |
| Report + purge orphans | Report count and delete orphaned/unresolvable chunks | |

**User's choice:** Audit table + leave NULL (option 2); explicitly NOT purge.
**Notes:** Do not purge — deletion irreversible; "orphaned" is an assumption (some may be recoverable mis-migrations); NULL already fails closed so purging gains nothing at retrieval while destroying evidence. Audit table `_backfill_unresolved_proposal_chunks` (chunk id + document_id, joinable to affected proposals) turns a bare count into inspectable state — each unresolved chunk is a proposal whose own uploaded content silently dropped from its retrievable history. Make it an explicitly disposable one-time artifact (drop after inspection/recovery). Still emit the count as the headline. Verified scope: 171 proposal chunks, 0 missing document_id.

## Settings UI placement

| Option | Description | Selected |
|--------|-------------|----------|
| Org Settings + proposal detail | Org toggles in Settings; per-proposal control on proposal detail | partial ✓ (permission model) |
| /admin + dashboard lifecycle | Org toggles on platform /admin; per-proposal in lifecycle menu | ✗ (rejected) |
| Defer placement to planner | Lock behavior, planner picks exact component after scout | ✓ (exact component) |

**User's choice:** Split — permission model (self-serve org Settings, NOT /admin) is locked; exact component deferred to planner scout.
**Notes:** These are the customer's own confidentiality/strategy call and the feature exists to give users self-serve toggles — so reject /admin placement. Org toggles in a customer-accessible Settings surface; per-proposal control with the proposal. Confirm during scout: which org role can flip toggles → gate mutations to org-admin (match/tighten existing org Settings role-gating), not every authenticated member; report the existing pattern before wiring. Exact Settings section ("AI grounding"/"Reference Library" reasonable) + per-proposal placement chosen after scouting real components.

## Claude's Discretion

- Exact UI component placement (Settings section name + per-proposal control location) — deferred to planner after scouting real components; behavior fully locked.

## Deferred Ideas

- Recovering audit-flagged unresolvable proposal chunks (recoverable mis-migrations) within the backfill step; wholesale re-ingestion of truly-dead orphans is separate cleanup.
- Proposal-retrieval ranking/threshold tuning — backlog 999.3.
