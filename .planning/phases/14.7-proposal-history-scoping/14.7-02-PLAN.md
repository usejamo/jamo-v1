---
phase: 14.7-proposal-history-scoping
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - supabase/migrations/20260710000003_proposal_rpc_eligibility.sql
  - .planning/phases/14.7-proposal-history-scoping/verify-eligibility.sql
autonomous: true
requirements: [R6]

must_haves:
  truths:
    - "Both match_chunks_vector_proposals and match_chunks_fts_proposals accept current_proposal_id uuid"
    - "A chunk with proposal_id IS NULL is never returned by either RPC (fail-closed)"
    - "The current proposal's own chunks are returned at ANY status, including draft"
    - "Another proposal in status='draft' is never returned — no override or switch makes it retrievable"
    - "A non-draft OTHER proposal is returned only when reference_override=true, or (reference_override IS NULL AND its status master switch is on)"
    - "reference_override=false excludes an otherwise-eligible non-draft proposal"
    - "An unknown/new status defaults to NOT eligible (CASE ELSE false)"
    - "Exactly one overload of each proposal RPC exists after the migration (old signature dropped)"
  artifacts:
    - path: "supabase/migrations/20260710000003_proposal_rpc_eligibility.sql"
      provides: "DROP old sigs + CREATE both proposal RPCs with eligibility WHERE clause"
      contains: "current_proposal_id"
    - path: ".planning/phases/14.7-proposal-history-scoping/verify-eligibility.sql"
      provides: "Runnable live-verification queries for the 7 BRIEF cases + 3 fail-closed edge cases"
      contains: "match_chunks_fts_proposals"
  key_links:
    - from: "chunks.proposal_id"
      to: "proposals.id"
      via: "JOIN proposals p ON p.id = c.proposal_id"
      pattern: "JOIN\\s+.*proposals\\s+p\\s+ON\\s+p.id\\s*=\\s*c.proposal_id"
    - from: "proposals.status"
      to: "organizations.learn_from_*"
      via: "CASE p.status WHEN ... master switch, ELSE false"
      pattern: "ELSE false"
    - from: "draft floor"
      to: "eligibility"
      via: "p.status <> 'draft' as first tier-B conjunct"
      pattern: "status <> 'draft'"
---

<objective>
Rewrite both proposal retrieval RPCs so the `[PROPOSAL HISTORY]` tier is scoped: own chunks (any status) ∪ eligible OTHER non-draft chunks. This is the confidentiality heart of the phase (R6).

Purpose: implement the BRIEF's draft-floor invariant `own_proposal OR (draft-floor AND category/toggle AND per-proposal logic)` as ONE `LANGUAGE sql` WHERE clause, with the draft floor first-evaluated, fail-closed NULL, and fail-closed unknown status.

Output: One migration (DROP-before-CREATE both RPCs) + a runnable verify-eligibility.sql encoding all verification cases. APPLIED live in Plan 06, asserted in Plan 07 — not applied here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.7-proposal-history-scoping/14.7-BRIEF.md
@.planning/phases/14.7-proposal-history-scoping/14.7-RESEARCH.md
@.planning/phases/14.7-proposal-history-scoping/14.7-PATTERNS.md

<facts>
- Both RPCs are `LANGUAGE sql SECURITY DEFINER SET search_path TO 'public','extensions'` — NO plpgsql branching; eligibility MUST be one WHERE-clause boolean (RESEARCH.md § Live RPC Definitions).
- LIVE current signatures (pg_get_functiondef, 2026-07-10 — the on-disk 20260320000016_match_chunks_functions.sql is STALE, 14.5 rewrote them live):
  - match_chunks_fts_proposals(query_text text, org_id_filter uuid, match_count integer) RETURNS TABLE(id uuid, content text, source text, agency text, therapeutic_area text, doc_type text, text_score double precision)
  - match_chunks_vector_proposals(query_embedding vector, org_id_filter uuid, similarity_threshold double precision, match_count integer) RETURNS TABLE(id uuid, content text, source text, agency text, therapeutic_area text, doc_type text, vector_score double precision)
- Adding current_proposal_id CHANGES the signature → CREATE OR REPLACE registers a NEW overload and leaves the OLD unscoped one live+callable. MUST DROP first (RESEARCH Pitfall 1; 14.5 precedent 20260708000004 hit this exact bug).
- Do NOT REVOKE/tighten grants on these two RPCs — pre-existing anon/PUBLIC grant is out of scope (RESEARCH Pitfall 5 / Open Q1). Leave grants as-is.
</facts>

<interfaces>
Analog to copy (supabase/migrations/20260708000004_regulatory_match_rpcs.sql): DROP FUNCTION IF EXISTS <sig>; then CREATE OR REPLACE ... LANGUAGE sql SECURITY DEFINER SET search_path = public, extensions.

BRIEF-exact eligibility WHERE clause (14.7-BRIEF.md lines 80-95, adapted for LANGUAGE sql per PATTERNS.md lines 61-79):
  WHERE c.org_id = org_id_filter
    AND c.doc_type = 'proposal'
    AND c.proposal_id IS NOT NULL
    AND (
      c.proposal_id = current_proposal_id
      OR (
        p.status <> 'draft'
        AND ( p.reference_override = true
              OR ( p.reference_override IS NULL
                   AND CASE p.status
                         WHEN 'won'       THEN o.learn_from_won
                         WHEN 'submitted' THEN o.learn_from_submitted
                         WHEN 'lost'      THEN o.learn_from_lost
                         ELSE false
                       END ) )
        AND p.reference_override IS DISTINCT FROM false
      )
    )
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DROP + CREATE both proposal RPCs with the eligibility WHERE clause</name>
  <read_first>
    - .planning/phases/14.7-proposal-history-scoping/14.7-BRIEF.md § CORE DESIGN (lines 19-43) + § RPC CHANGES (lines 73-97) — GOVERNS the eligibility rule and NULL semantics.
    - .planning/phases/14.7-proposal-history-scoping/14.7-CONTEXT.md D-02/D-03/D-04 — self-join reads switches, fail-closed unknown status, ordered draft-floor-first.
    - .planning/phases/14.7-proposal-history-scoping/14.7-PATTERNS.md § proposal_rpc_eligibility — DROP-before-CREATE + LANGUAGE sql shape + exact WHERE clause.
    - supabase/migrations/20260708000004_regulatory_match_rpcs.sql — the in-repo DROP-before-CREATE + LANGUAGE sql SECURITY DEFINER precedent.
    - MANDATORY LIVE FETCH before editing: run the Supabase MCP `execute_sql` with `SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname IN ('match_chunks_vector_proposals','match_chunks_fts_proposals');` to confirm the current signatures/return columns match the <facts> above (the on-disk migration is stale). If they differ, adapt the DROP signatures to the live ones.
  </read_first>
  <files>supabase/migrations/20260710000003_proposal_rpc_eligibility.sql</files>
  <action>
Create migration `20260710000003_proposal_rpc_eligibility.sql`.

1) DROP the exact CURRENT signatures FIRST (mandatory — Pitfall 1):
```sql
DROP FUNCTION IF EXISTS public.match_chunks_vector_proposals(extensions.vector(1536), uuid, double precision, integer);
DROP FUNCTION IF EXISTS public.match_chunks_fts_proposals(text, uuid, integer);
```

2) CREATE both with `current_proposal_id uuid` added to the signature, joining proposals + organizations, using the BRIEF WHERE clause. Keep the EXACT existing RETURNS TABLE column lists + ORDER BY + LIMIT. Vector version:
```sql
CREATE OR REPLACE FUNCTION public.match_chunks_vector_proposals(
  query_embedding extensions.vector(1536),
  org_id_filter uuid,
  similarity_threshold double precision,
  match_count integer,
  current_proposal_id uuid
)
RETURNS TABLE(id uuid, content text, source text, agency text, therapeutic_area text, doc_type text, vector_score double precision)
LANGUAGE sql SECURITY DEFINER SET search_path = public, extensions
AS $$
  SELECT c.id, c.content, c.source, c.agency, c.therapeutic_area, c.doc_type,
         1 - (c.embedding <=> query_embedding) AS vector_score
  FROM chunks c
  JOIN proposals p ON p.id = c.proposal_id
  JOIN organizations o ON o.id = org_id_filter
  WHERE c.org_id = org_id_filter
    AND c.doc_type = 'proposal'
    AND c.proposal_id IS NOT NULL
    AND 1 - (c.embedding <=> query_embedding) >= similarity_threshold
    AND (
      c.proposal_id = current_proposal_id
      OR (
        p.status <> 'draft'
        AND ( p.reference_override = true
              OR ( p.reference_override IS NULL
                   AND CASE p.status
                         WHEN 'won'       THEN o.learn_from_won
                         WHEN 'submitted' THEN o.learn_from_submitted
                         WHEN 'lost'      THEN o.learn_from_lost
                         ELSE false
                       END ) )
        AND p.reference_override IS DISTINCT FROM false
      )
    )
  ORDER BY vector_score DESC
  LIMIT match_count;
$$;
```
FTS version is identical in structure — swap the select-list/score to `ts_rank(c.search_vector, plainto_tsquery('english', query_text))::float AS text_score`, drop the similarity_threshold line, add `AND c.search_vector @@ plainto_tsquery('english', query_text)`, ORDER BY text_score DESC. Signature: `(query_text text, org_id_filter uuid, match_count integer, current_proposal_id uuid)`.

Notes locked by the BRIEF/CONTEXT — do NOT deviate:
- `current_proposal_id` goes LAST in the param list (append, don't reorder existing params).
- `JOIN organizations o ON o.id = org_id_filter` reads the master switches (D-02: RPC self-joins, switches are NOT passed as params).
- `c.proposal_id IS NOT NULL` fail-closed gate stays BEFORE the tier logic (a JOIN to proposals already drops NULL proposal_id, but keep the explicit gate — it is load-bearing documentation of the fail-closed intent).
- `ELSE false` is mandatory (D-03 fail-closed unknown status). Never add COALESCE(..., true).
- Keep the existing `c.org_id = org_id_filter` clause (defense-in-depth, D-02).
- Do NOT add REVOKE/GRANT lines (out of scope, RESEARCH Pitfall 5).

3) Append the overload-count verification query as a trailing comment or SELECT:
```sql
-- POST-APPLY CHECK: must return exactly 2 rows (one per function name).
SELECT proname, pg_get_function_identity_arguments(oid) FROM pg_proc WHERE proname LIKE 'match_chunks_%_proposals';
```
  </action>
  <verify>
    <automated>f=supabase/migrations/20260710000003_proposal_rpc_eligibility.sql; grep -q "DROP FUNCTION IF EXISTS public.match_chunks_vector_proposals(extensions.vector(1536), uuid, double precision, integer)" $f && grep -q "DROP FUNCTION IF EXISTS public.match_chunks_fts_proposals(text, uuid, integer)" $f && grep -c "current_proposal_id uuid" $f | grep -q 2 && grep -q "status <> 'draft'" $f && grep -q "ELSE false" $f && grep -q "reference_override IS DISTINCT FROM false" $f && grep -q "JOIN organizations o ON o.id = org_id_filter" $f && ! grep -qi "COALESCE" $f && ! grep -qi "REVOKE" $f && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Both DROP FUNCTION statements match the live signatures verified via pg_get_functiondef in read_first.
    - Both CREATE functions append `current_proposal_id uuid` as the last param; RETURNS TABLE column lists are byte-identical to the live originals.
    - The WHERE clause contains, in order: `c.proposal_id IS NOT NULL`, then the `c.proposal_id = current_proposal_id OR (...)` union, with `p.status <> 'draft'` as the first tier-B conjunct and `p.reference_override IS DISTINCT FROM false` present.
    - The master-switch CASE has an explicit `ELSE false`; no COALESCE; no REVOKE/GRANT.
    - Trailing overload-count check query present.
    - Live-apply acceptance (Plan 07): `SELECT proname, pg_get_function_identity_arguments(oid) FROM pg_proc WHERE proname LIKE 'match_chunks_%_proposals'` returns exactly 2 rows.
  </acceptance_criteria>
  <done>Migration authors both DROP+CREATE per the BRIEF WHERE clause; grep verify prints PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Author runnable live-verification SQL script (7 BRIEF cases + 3 fail-closed edge cases)</name>
  <read_first>
    - .planning/phases/14.7-proposal-history-scoping/14.7-BRIEF.md § VERIFICATION (lines 117-126) — the 7 cases.
    - .planning/phases/14.7-proposal-history-scoping/14.7-RESEARCH.md § BRIEF's 7 Verification Cases + § Fail-closed edge cases — live fixtures (2 won, 1 submitted, 1 lost, 56 draft) and the synthetic-NULL/unknown-status approaches.
  </read_first>
  <files>.planning/phases/14.7-proposal-history-scoping/verify-eligibility.sql</files>
  <action>
Create `verify-eligibility.sql`: a documented, runnable set of SELECT/transactional test blocks (each wrapped in BEGIN ... ROLLBACK where it mutates fixtures) that Plan 07 runs via Supabase MCP `execute_sql` against the LIVE project. Encode, each with an expected result comment:
- Case 1: call `match_chunks_fts_proposals('<any term>', <org>, 50, <a draft proposal's own id>)` → asserts the draft's own chunks appear.
- Case 2: with current_proposal_id = proposal A, assert NO chunk of any OTHER draft appears; then inside a BEGIN...ROLLBACK, set `reference_override=true` on one other draft and re-run → still absent (floor wins).
- Case 3: default switches → the 2 won proposals' chunks ARE eligible for a querying proposal.
- Case 4: submitted + lost excluded by default; BEGIN...ROLLBACK flip `learn_from_submitted=true` / `learn_from_lost=true` → included; revert → excluded.
- Case 5: BEGIN...ROLLBACK set won→reference_override=false (excluded) and lost→reference_override=true (included); separately set a draft→reference_override=true and assert unchanged.
- Case 6 (synthetic, fail-closed NULL): BEGIN; insert a chunk with `doc_type='proposal', proposal_id=NULL, org_id=<org>`; assert neither RPC returns it; ROLLBACK.
- Case 7: `SELECT count(*) FROM _backfill_unresolved_proposal_chunks;` → expect 0 (D-05c) — the backfill headline.
- Edge case A (unknown status): BEGIN...ROLLBACK set a test proposal's status to 'archived' with all switches ON → assert excluded (ELSE false).
- Edge case B (override NULL falls through to switch): assert a won proposal with reference_override IS NULL toggles exactly with learn_from_won.
- Edge case C (overload count): `SELECT proname, pg_get_function_identity_arguments(oid) FROM pg_proc WHERE proname LIKE 'match_chunks_%_proposals';` → expect exactly 2 rows.

Use real live ids by SELECTing them at the top (e.g. a CTE or a leading `SELECT id, status FROM proposals ORDER BY status;`) so the runner substitutes concrete UUIDs. Every mutating block MUST be BEGIN ... ROLLBACK so the script is idempotent and never leaves test state.
  </action>
  <verify>
    <automated>f=.planning/phases/14.7-proposal-history-scoping/verify-eligibility.sql; grep -q "match_chunks_fts_proposals" $f && grep -q "match_chunks_vector_proposals" $f && grep -q "_backfill_unresolved_proposal_chunks" $f && grep -qi "ROLLBACK" $f && grep -qi "archived" $f && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Script references both RPCs by name and covers all 7 BRIEF cases + the 3 edge cases (unknown-status, override-NULL-fallthrough, overload-count) with expected-result comments.
    - Every fixture-mutating block is wrapped BEGIN ... ROLLBACK (no permanent live mutation).
    - The synthetic NULL-proposal_id chunk case is present (no live NULL rows exist per D-05c).
  </acceptance_criteria>
  <done>verify-eligibility.sql exists with all 10 cases runnable and rollback-safe; grep verify prints PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retrieve-context → RPC | `current_proposal_id` + `org_id_filter` cross into SECURITY DEFINER SQL; the RPC is the sole enforcement point of the eligibility rule (D-02) |
| chunk row → result set | Every chunk must pass draft-floor + NULL + status/override gates before leaving the RPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.7-04 | Information Disclosure | within-org proposal-history bleed (core) | mitigate | Union WHERE clause returns only own-proposal OR eligible non-draft OTHER; org-wide return path removed. Verified by verify-eligibility.sql cases 1-5 |
| T-14.7-05 | Information Disclosure | draft-floor bypass | mitigate | `p.status <> 'draft'` is the first-evaluated tier-B conjunct; no override/switch precedes it. Verified case 2 (override=true on a draft is a no-op) |
| T-14.7-06 | Information Disclosure | NULL proposal_id over-include | mitigate | `c.proposal_id IS NOT NULL` gate + JOIN proposals drops NULL rows; never defaults to org-wide. Verified case 6 (synthetic NULL) |
| T-14.7-07 | Information Disclosure | unknown/new status over-include | mitigate | Master-switch CASE `ELSE false`; unmapped status is never eligible. Verified edge case A ('archived') |
| T-14.7-08 | Elevation of Privilege | stale unscoped RPC overload left callable | mitigate | Explicit DROP of live signatures before CREATE; post-apply overload-count check must return exactly 2 rows. Verified edge case C |
| T-14.7-09 | Tampering | switches passed as edge params drifting from SQL | accept→mitigate by design | D-02: switches read via RPC self-join to organizations, never passed by retrieve-context — the rule lives once, in SQL |
</threat_model>

<verification>
- Migration authors DROP-before-CREATE for BOTH live signatures and both CREATEs use the BRIEF WHERE clause with ELSE false + IS DISTINCT FROM false + draft-floor-first.
- Static grep checks pass. Live verification (all 10 cases + overload count) runs in Plan 07 after Plan 06 applies the migration.
</verification>

<success_criteria>
- Both proposal RPCs, after apply, accept current_proposal_id and enforce `own_proposal OR (draft-floor AND category/toggle AND per-proposal logic)`.
- Exactly one overload of each RPC exists post-apply.
- verify-eligibility.sql encodes all 7 BRIEF cases + 3 edge cases, rollback-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/14.7-proposal-history-scoping/14.7-02-SUMMARY.md` recording the final RPC signatures, the exact WHERE clause shipped, the DROP signatures used (and whether the live pg_get_functiondef matched <facts>), and the verify-eligibility.sql case list.
</output>
