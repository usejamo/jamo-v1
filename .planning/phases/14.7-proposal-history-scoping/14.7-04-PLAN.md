---
phase: 14.7-proposal-history-scoping
plan: 04
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - supabase/functions/extract-document/index.ts
  - supabase/functions/retrieve-context/index.ts
  - src/hooks/useProposalGeneration.ts
  - supabase/functions/extract-document/test.ts
  - supabase/functions/retrieve-context/test.ts
autonomous: true
requirements: [R2, R7]

must_haves:
  truths:
    - "A freshly extracted proposal document writes chunks whose proposal_id equals the owning proposal's id"
    - "fetchRagChunks sends the current proposalId to retrieve-context"
    - "retrieve-context threads current_proposal_id into BOTH match_chunks_*_proposals RPC calls"
    - "The regulatory retrieval path in retrieve-context is byte-for-byte unchanged"
    - "The internal service-role caller path in retrieve-context is preserved (no new auth requirement)"
  artifacts:
    - path: "supabase/functions/extract-document/index.ts"
      provides: "proposal_id written on every inserted proposal chunk at ingest"
      contains: "proposal_id: params.proposalId"
    - path: "supabase/functions/retrieve-context/index.ts"
      provides: "proposalId accepted and passed as current_proposal_id to both proposal RPCs"
      contains: "current_proposal_id"
    - path: "src/hooks/useProposalGeneration.ts"
      provides: "fetchRagChunks sends proposalId in the retrieve-context body"
      contains: "proposalId"
  key_links:
    - from: "src/hooks/useProposalGeneration.ts fetchRagChunks"
      to: "retrieve-context"
      via: "body: { orgId, proposalId, query, ... }"
      pattern: "proposalId"
    - from: "retrieve-context"
      to: "match_chunks_vector_proposals / match_chunks_fts_proposals"
      via: "current_proposal_id: proposalId ?? null"
      pattern: "current_proposal_id"
    - from: "extract-document caller"
      to: "chunkAndEmbedProposal"
      via: "proposalId: doc.proposal_id ?? null"
      pattern: "doc.proposal_id"
---

<objective>
Thread the proposal association end-to-end: set `proposal_id` at ingest (R2) and carry `current_proposal_id` client → edge → both RPCs (R7). Without this thread, Plan 02's scoping has no proposal id to scope by — this is the wiring the whole fix depends on.

Purpose: new proposal chunks get their association at write time (never relying on backfill); the current proposalId reaches the RPCs at generation. Regulatory path and the internal service-role caller are untouched.

Output: three code edits (edge x2 + client) + test assertions. Deployed live in Plan 06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.7-proposal-history-scoping/14.7-BRIEF.md
@.planning/phases/14.7-proposal-history-scoping/14.7-PATTERNS.md
@.planning/phases/14.7-proposal-history-scoping/14.7-CONTEXT.md

<facts>
- extract-document already fetches the `doc` row via `.from('proposal_documents').select('*')` — `doc.proposal_id` is in scope with NO new lookup (PATTERNS lines 211-226).
- retrieve-context trust boundary (14.3, live): `isInternalServiceRoleCall(req)` branch is checked FIRST; internal caller (chat-with-jamo/rag.ts) trusts body orgId unchanged; user caller derives effectiveOrgId from JWT and 403s a mismatched body orgId. Do NOT break this. `proposalId` is NOT an identity claim — it needs no JWT cross-check, just pass it through (BRIEF § EDGE FUNCTIONS).
- retrieve-context proposal RPC call sites ~ lines 279-295; regulatory relaxation loop ~ lines 226-276 is GUARDED — do not touch.
- fetchRagChunks (useProposalGeneration.ts ~198-223); call sites ~ line 479 and ~547 are inside `useProposalGeneration(proposalId)` closure — proposalId is already in scope, zero new plumbing.
- Plan 02's new RPC signatures append `current_proposal_id uuid` LAST. Passing `current_proposal_id: proposalId ?? null` matches.
</facts>

<interfaces>
extract-document (PATTERNS lines 189-226): add `proposalId: string | null` to chunkAndEmbedProposal params; add `proposal_id: params.proposalId` to the row map; caller passes `proposalId: doc.proposal_id ?? null`.
retrieve-context (PATTERNS lines 134-181): add `proposalId?: string` to RetrieveRequest; destructure alongside orgId; add `current_proposal_id: proposalId ?? null` to both `.rpc('match_chunks_vector_proposals', {...})` and `.rpc('match_chunks_fts_proposals', {...})`.
fetchRagChunks (PATTERNS lines 234-263): add a `proposalId: string` param, include it in the invoke body `{ orgId, proposalId, query, therapeuticArea, studyPhase, geography }`; pass `proposalId` at both call sites.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: extract-document sets proposal_id on every inserted proposal chunk (R2)</name>
  <read_first>
    - supabase/functions/extract-document/index.ts — `chunkAndEmbedProposal` (~45-86), the `doc` fetch (~181-189), the call site (~243-248).
    - .planning/phases/14.7-proposal-history-scoping/14.7-PATTERNS.md § extract-document — exact edit points.
    - .planning/phases/14.7-proposal-history-scoping/14.7-SPEC.md R2 acceptance.
  </read_first>
  <files>supabase/functions/extract-document/index.ts, supabase/functions/extract-document/test.ts</files>
  <behavior>
    - A chunk row built by chunkAndEmbedProposal carries `proposal_id` equal to the passed proposalId.
    - When the document's proposal_id is null, chunks are written with proposal_id null (does not throw).
  </behavior>
  <action>
1) Extend `chunkAndEmbedProposal` params type with `proposalId: string | null`.
2) In the `rows` map (PATTERNS lines 199-207), add `proposal_id: params.proposalId,` alongside `org_id`, `doc_type: 'proposal'`, `metadata`, etc. Keep `metadata.document_id` as-is (still written; backfill join is retired but metadata stays for provenance).
3) At the call site, pass `proposalId: doc.proposal_id ?? null` (doc is already fetched via select('*'); no new query).
4) In test.ts add a Deno test (may be `ignore:true` per the repo's Deno-unavailable contingency — name Plan 07 as live-validation owner) asserting the inserted row object includes `proposal_id` equal to the input proposalId. Follow the existing test.ts scaffolding convention (14.3/14.6 used ignore:true + grep acceptance).
  </action>
  <verify>
    <automated>grep -q "proposal_id: params.proposalId" supabase/functions/extract-document/index.ts && grep -q "proposalId: doc.proposal_id ?? null" supabase/functions/extract-document/index.ts && grep -q "proposalId" supabase/functions/extract-document/test.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `chunkAndEmbedProposal` params include `proposalId: string | null`.
    - The inserted chunk row object contains `proposal_id: params.proposalId`.
    - The caller passes `doc.proposal_id ?? null`; no additional proposal_documents query added.
    - test.ts has a proposal_id-on-ingest assertion (ignore:true acceptable, naming Plan 07).
    - Live acceptance (Plan 07): upload+extract a doc to a known proposal → its chunks have proposal_id = that proposal.
  </acceptance_criteria>
  <done>extract-document writes proposal_id at ingest; test asserts it; grep verify prints PASS.</done>
</task>

<task type="auto">
  <name>Task 2: retrieve-context accepts + threads current_proposal_id into both proposal RPCs (R7)</name>
  <read_first>
    - supabase/functions/retrieve-context/index.ts — RetrieveRequest (~18-26), body parse + effectiveOrgId (~165, 185-198), proposal RPC call sites (~279-295), regulatory loop (~226-276, DO NOT TOUCH).
    - .planning/phases/14.7-proposal-history-scoping/14.7-PATTERNS.md § retrieve-context — exact edit points + the trust-boundary block to preserve.
    - .planning/STATE.md Active Decisions 14.3-03 (retrieve-context branch on caller shape) — must not be broken.
  </read_first>
  <files>supabase/functions/retrieve-context/index.ts, supabase/functions/retrieve-context/test.ts</files>
  <action>
1) Add `proposalId?: string` to the `RetrieveRequest` interface with a `// NEW (14.7) — proposal-history scoping` comment (matches the existing "NEW (phase)" convention).
2) Destructure `proposalId` alongside `orgId` at the body-parse line (~165). Do NOT run any JWT cross-check on it (it is a filter param, not an identity claim) — leave the entire `isInternalServiceRoleCall` / `effectiveOrgId` block EXACTLY as-is.
3) At BOTH proposal RPC calls (~279 vector, ~291 fts), add `current_proposal_id: proposalId ?? null` to the params object. Do NOT touch the regulatory relaxation loop (~226-276) or its `.rpc('match_chunks_vector'/'match_chunks_fts')` calls.
4) In test.ts add assertions (ignore:true acceptable) that (a) proposalId is destructured and passed as current_proposal_id, (b) the regulatory calls are unchanged. Name Plan 07 as live-validation owner.
  </action>
  <verify>
    <automated>f=supabase/functions/retrieve-context/index.ts; grep -q "proposalId?: string" $f && grep -c "current_proposal_id: proposalId ?? null" $f | grep -q 2 && grep -q "proposalId" supabase/functions/retrieve-context/test.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - RetrieveRequest has `proposalId?: string`; it is destructured at body parse.
    - BOTH proposal `.rpc()` calls include `current_proposal_id: proposalId ?? null` (exactly 2 occurrences).
    - The `isInternalServiceRoleCall`/`effectiveOrgId` trust-boundary block is unchanged; the regulatory relaxation loop is unchanged (no diff in those line ranges).
    - test.ts asserts threading + regulatory-unchanged (ignore:true ok).
  </acceptance_criteria>
  <done>retrieve-context threads current_proposal_id into both proposal RPCs only; regulatory + trust boundary untouched; grep verify prints PASS.</done>
</task>

<task type="auto">
  <name>Task 3: fetchRagChunks sends proposalId (client thread) (R7)</name>
  <read_first>
    - src/hooks/useProposalGeneration.ts — fetchRagChunks (~198-223) + the two call sites (~479, ~547) inside the `useProposalGeneration(proposalId)` closure.
    - .planning/phases/14.7-proposal-history-scoping/14.7-PATTERNS.md § fetchRagChunks — exact signature + body + call-site edits.
    - .planning/STATE.md Active Decision 14.6-01 (split RAG payload contract) — the return shape must stay `{ regulatoryChunks, proposalChunks, regulatoryCount }`; do NOT change it.
  </read_first>
  <files>src/hooks/useProposalGeneration.ts</files>
  <action>
1) Add a `proposalId: string` parameter to `fetchRagChunks`. Placement: prefer adding it as the 2nd positional arg (right after `orgId`) OR convert to an options object — planner's discretion, but pick the lower-churn option and update BOTH call sites consistently.
2) Include `proposalId` in the `supabase.functions.invoke('retrieve-context', { body: { orgId, proposalId, query, therapeuticArea, studyPhase, geography } })` body.
3) At both call sites (~479, ~547), pass the enclosing closure's `proposalId` (already in scope from `useProposalGeneration(proposalId)`). Do NOT change the split return shape (14.6-01) or the non-blocking `[]`/zeroed error paths.
  </action>
  <verify>
    <automated>f=src/hooks/useProposalGeneration.ts; grep -q "proposalId" $f && grep -Eq "invoke\\('retrieve-context'|body: \\{[^}]*proposalId" $f && npm run build >/dev/null 2>&1 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `fetchRagChunks` accepts `proposalId` and includes it in the retrieve-context invoke body.
    - Both call sites pass the closure `proposalId`.
    - The split return contract (`regulatoryChunks`/`proposalChunks`/`regulatoryCount`) and non-blocking error returns are unchanged.
    - `npm run build` succeeds (no type errors).
  </acceptance_criteria>
  <done>Client sends proposalId to retrieve-context at both generation call sites; build clean; grep verify prints PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → retrieve-context | proposalId crosses as a filter param (not identity); orgId identity boundary from 14.3 stays intact |
| ingest → chunks | proposal_id written at the trust point where the association is known (upload) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.7-14 | Information Disclosure | proposalId spoofed to read another proposal | accept | proposalId only WIDENS to "own proposal, any status" within the caller's already-JWT-verified org (14.3); it cannot cross org (org_id_filter = effectiveOrgId) and cannot un-gate other proposals' drafts (Plan 02 floor). A caller passing someone else's proposalId in their own org only surfaces that proposal's own chunks — same-org, already permitted at the org tier |
| T-14.7-15 | Tampering | new chunks written without proposal_id (silent backfill reliance) | mitigate | extract-document writes proposal_id at ingest; Plan 07 live-verifies a fresh upload's chunks are non-null |
| T-14.7-16 | Information Disclosure | regulatory path collateral change | mitigate | Only the two proposal RPC call param objects change; regulatory loop + trust-boundary block untouched (verified by test.ts + Plan 07 14.6 regression) |
</threat_model>

<verification>
- extract-document writes proposal_id at ingest; retrieve-context threads current_proposal_id into both proposal RPCs only; client sends proposalId at both call sites.
- `npm run build` clean. Live e2e (fresh upload → proposal_id; generation carries proposalId; regulatory unchanged) runs in Plan 07 after Plan 06 deploy.
</verification>

<success_criteria>
- current_proposal_id reaches both proposal RPCs from a real generation request; new proposal chunks carry proposal_id; regulatory + internal-caller paths unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/14.7-proposal-history-scoping/14.7-04-SUMMARY.md` recording the fetchRagChunks signature choice (positional vs options), the exact edit lines, and confirmation the regulatory loop + 14.3 trust boundary were left untouched.
</output>
