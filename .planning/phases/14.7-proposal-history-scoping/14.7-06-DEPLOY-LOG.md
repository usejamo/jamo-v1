# 14.7-06 Deploy Log — Proposal History Scoping (deploy-first gate)

**Project:** `fuuvdcvbliijffogjnwg`
**Date:** 2026-07-10
**Operator:** developer-approved (Wave 4 human-action checkpoint, "Deploy now")
**Method:** Supabase MCP `apply_migration` (NOT `supabase db push` — history diverged) + Supabase CLI `functions deploy`

---

## Task 1 — Migrations applied to live DB (strict order)

| # | Migration | Method | Result |
|---|-----------|--------|--------|
| 1 | `20260710000001_chunks_proposal_scope_columns` | MCP apply_migration | ✅ success |
| 2 | `20260710000002_backfill_proposal_chunks` | MCP apply_migration | ✅ success |
| 3 | `20260710000003_proposal_rpc_eligibility` | MCP apply_migration | ✅ success |
| 4 | `20260710000004_eligibility_write_gating` | MCP apply_migration | ✅ success |

### Post-000001 — schema columns
5 columns confirmed live (n=5):
`chunks.proposal_id, organizations.learn_from_lost, organizations.learn_from_submitted, organizations.learn_from_won, proposals.reference_override`

Org defaults (both existing orgs on the safe state):
- `00000000-…-0001`: won=true, submitted=false, lost=false
- `00000000-…-0002`: won=true, submitted=false, lost=false

### Post-000002 — fail-closed backfill
| Metric | Value |
|--------|-------|
| Total proposal chunks | 171 |
| Resolved (proposal_id NOT NULL) | 171 |
| Still NULL (`doc_type='proposal' AND proposal_id IS NULL`) | 0 |
| `_backfill_unresolved_proposal_chunks` rows | 0 |

**Unresolvable proposal chunks: 0.** The two counts are equal (0 == 0) — matches expected D-05c outcome. No fail-closed exclusions; audit table left in place (dropped in Plan 07, D-05b).

### Post-000003 — RPC overload check (T-14.7-08 / Edge case C)
`SELECT proname, pg_get_function_identity_arguments(oid) FROM pg_proc WHERE proname LIKE 'match_chunks_%_proposals';` → **exactly 2 rows** (not 4 — old unscoped overloads dropped cleanly):

- `match_chunks_fts_proposals(query_text text, org_id_filter uuid, match_count integer, current_proposal_id uuid)`
- `match_chunks_vector_proposals(query_embedding vector, org_id_filter uuid, similarity_threshold double precision, match_count integer, current_proposal_id uuid)`

Each includes `current_proposal_id uuid`. ✅ No confidentiality bypass.

### Post-000004 — write-gating RPCs
Both present, grants correct (no anon/PUBLIC):
- `set_org_learning_switches(p_learn_from_won boolean, p_learn_from_submitted boolean, p_learn_from_lost boolean)` → EXECUTE: authenticated, service_role
- `set_reference_override(p_proposal_id uuid, p_value boolean)` → EXECUTE: authenticated, service_role

---

## Task 2 — Edge functions deployed

| Function | Before | After | Status |
|----------|--------|-------|--------|
| `retrieve-context` | v9 | **v10** | ACTIVE ✅ |
| `extract-document` | v7 | **v8** | ACTIVE ✅ |

Deployed via `npx supabase functions deploy <fn> --project-ref fuuvdcvbliijffogjnwg` using the first 44-char `sbp_` PAT from `.env`. Both returned `{"message":"Deployed Functions."}` (exit 0). No other edge function was redeployed (all others retain prior versions).

---

## Structural gate: PASS
The full 14.7 schema + scoped RPCs + write-gating + edge code are **LIVE** on `fuuvdcvbliijffogjnwg`. Behavioral verification proceeds in Plan 07.
