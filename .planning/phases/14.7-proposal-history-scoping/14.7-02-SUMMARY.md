---
phase: 14.7-proposal-history-scoping
plan: 02
subsystem: database
tags: [postgres, sql, rpc, security-definer, rag, confidentiality]

# Dependency graph
requires:
  - phase: 14.7-proposal-history-scoping (Plan 01)
    provides: chunks.proposal_id column, proposals.reference_override, organizations.learn_from_won/submitted/lost columns
provides:
  - Scoped match_chunks_vector_proposals/match_chunks_fts_proposals RPCs (own-proposal OR eligible-non-draft-other WHERE clause)
  - verify-eligibility.sql runnable live-verification script (7 BRIEF cases + 3 fail-closed edge cases)
affects: [14.7-05-retrieve-context-threading, 14.7-06-deploy-gate, 14.7-07-live-verification]

# Tech tracking
tech-stack:
  added: []
  patterns: [DROP-before-CREATE for signature-changing RPC rewrites, LANGUAGE sql single-WHERE-clause eligibility boolean, RPC self-join to read org master switches instead of passing them as params]

key-files:
  created:
    - supabase/migrations/20260710000003_proposal_rpc_eligibility.sql
    - .planning/phases/14.7-proposal-history-scoping/verify-eligibility.sql
  modified: []

key-decisions:
  - "Draft floor (p.status <> 'draft') is the first-evaluated tier-B conjunct — no override or master switch can precede or bypass it for OTHER proposals"
  - "current_proposal_id appended as the LAST param on both RPCs (not inserted mid-signature) to keep param order stable for the existing callers being migrated in later plans"
  - "JOIN organizations o ON o.id = org_id_filter reads the 3 master switches inside the RPC itself — retrieve-context never passes switches as params, so the eligibility rule lives exactly once, in SQL (D-02)"
  - "Unknown/new proposal status defaults to CASE ... ELSE false (never COALESCE(...,true)) — fail-closed for any future status value not yet mapped"

patterns-established:
  - "Signature-changing RPC rewrite: DROP FUNCTION IF EXISTS <exact live signature> before CREATE OR REPLACE, to avoid leaving a stale unscoped overload live+callable (precedent: 20260708000004, this plan)"

requirements-completed: [R6]

# Metrics
duration: 12min
completed: 2026-07-10
---

# Phase 14.7 Plan 02: Proposal RPC Eligibility Summary

**Rewrote both proposal retrieval RPCs (`match_chunks_vector_proposals`, `match_chunks_fts_proposals`) with a single `LANGUAGE sql` WHERE clause enforcing own-proposal-any-status OR draft-floor-gated-eligible-other, plus a rollback-safe live-verification script covering all 7 BRIEF cases and 3 fail-closed edge cases.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-10T21:09:00Z
- **Completed:** 2026-07-10T21:21:01Z
- **Tasks:** 2
- **Files modified:** 2 (both new files)

## Accomplishments
- Authored `20260710000003_proposal_rpc_eligibility.sql`: DROP-before-CREATE for both live RPC signatures, adding `current_proposal_id uuid` as the final param, with the BRIEF-exact eligibility WHERE clause (draft-floor-first, `ELSE false` fail-closed unknown status, `IS DISTINCT FROM false` override gate, `proposal_id IS NOT NULL` fail-closed NULL gate)
- Authored `verify-eligibility.sql`: fixture-discovery query + all 7 BRIEF verification cases + 3 fail-closed edge cases (unknown status, override-NULL-fallthrough, overload count), every mutating block wrapped `BEGIN...ROLLBACK`
- Confirmed (per plan `<facts>`, already live-verified 2026-07-10 via `pg_get_functiondef`) that the on-disk `20260320000016_match_chunks_functions.sql` is stale — 14.5 rewrote these two RPCs live with the 4-param unscoped signatures this migration's DROP statements target exactly

## Task Commits

Each task was committed atomically:

1. **Task 1: DROP + CREATE both proposal RPCs with the eligibility WHERE clause** - `3d597d4` (feat)
2. **Task 2: Author runnable live-verification SQL script** - `c4428b9` (docs)

**Plan metadata:** (this summary's own commit, made by orchestrator/worktree-merge — not a separate metadata commit in this worktree per parallel-executor instructions)

## Files Created/Modified
- `supabase/migrations/20260710000003_proposal_rpc_eligibility.sql` - DROP-before-CREATE migration for both proposal RPCs with the scoped eligibility WHERE clause; authored only, applies live in Plan 06
- `.planning/phases/14.7-proposal-history-scoping/verify-eligibility.sql` - Runnable, rollback-safe live-verification script for all 7 BRIEF cases + 3 fail-closed edge cases; authored only, runs live in Plan 07

## Decisions Made
- Reused the plan's `<facts>`-provided live signature confirmation (already fetched via Supabase MCP `pg_get_functiondef` by the planner on 2026-07-10) rather than re-fetching in this worktree — no live Supabase MCP tool was available in this execution context, and the plan's read_first note explicitly supplies the confirmed live signatures as an authoritative fact. The DROP statements target those exact confirmed signatures.
- Followed the BRIEF/CONTEXT locked WHERE-clause shape verbatim (draft-floor-first, self-join for switches, `ELSE false`, no COALESCE, no REVOKE/GRANT changes) — no interpretation needed, plan's `<interfaces>` block was copy-ready.

## Deviations from Plan

None - plan executed exactly as written. Both grep-based automated verifications (Task 1 and Task 2) printed PASS on first attempt.

## Issues Encountered
None. No live Supabase MCP `execute_sql` tool was available in this worktree's tool set to independently re-verify the live RPC signatures beyond what the plan's `<facts>` already supplied — this is documented above as a decision, not a blocker, since the plan explicitly pre-supplied the confirmed live signatures for this exact reason (read_first note: "If they differ, adapt the DROP signatures to the live ones" — no divergence signal was available to act on, so the plan's stated live signatures were used as-is).

## User Setup Required

None - no external service configuration required. This plan only authors SQL files; nothing is applied to the live database until Plan 06 (deploy gate).

## Next Phase Readiness
- Migration `20260710000003_proposal_rpc_eligibility.sql` is ready for Plan 06 to apply via Supabase MCP `apply_migration`.
- `verify-eligibility.sql` is ready for Plan 07 to run live via Supabase MCP `execute_sql`, substituting concrete `<ORG_ID>`/`<DRAFT_ID>`/`<OTHER_DRAFT_ID>`/`<SUBMITTED_ID>`/`<LOST_ID>` placeholders with ids from the script's own leading fixture-discovery SELECT.
- No blockers. Plan 05 (retrieve-context threading of `current_proposal_id`) can proceed independently of live apply, since it only needs the RPC signature (now fixed) to build its call sites against.

---
*Phase: 14.7-proposal-history-scoping*
*Completed: 2026-07-10*

## Self-Check: PASSED

- FOUND: supabase/migrations/20260710000003_proposal_rpc_eligibility.sql
- FOUND: .planning/phases/14.7-proposal-history-scoping/verify-eligibility.sql
- FOUND commit: 3d597d4
- FOUND commit: c4428b9
