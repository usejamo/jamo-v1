---
phase: 14.7-proposal-history-scoping
plan: 05
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - src/components/settings/ReferenceLibraryTab.tsx
  - src/pages/Settings.tsx
  - src/components/ProposalReferenceControl.tsx
  - src/pages/ProposalDetail.tsx
  - src/components/settings/ReferenceLibraryTab.test.tsx
autonomous: false
requirements: [R4, R5, R8]

must_haves:
  truths:
    - "Org settings render 3 toggles: Learn from won (default on), submitted/in-flight (default off), lost (default off)"
    - "The 3 toggles persist via set_org_learning_switches and reflect the org's stored values on load"
    - "The toggle section is visible/mutable only to org-admins (disabled for role='user')"
    - "Non-draft proposals show a tri-state 'use as reference' control (follow settings / always include / never include)"
    - "The per-proposal control writes reference_override via set_reference_override (admin-gated)"
    - "No draft proposal exposes any UI affordance that would make it includable as another proposal's reference"
  artifacts:
    - path: "src/components/settings/ReferenceLibraryTab.tsx"
      provides: "3 org master toggles, admin-gated, persisted via RPC"
      contains: "set_org_learning_switches"
    - path: "src/components/ProposalReferenceControl.tsx"
      provides: "Per-proposal tri-state reference control, non-draft only, admin-gated"
      contains: "set_reference_override"
  key_links:
    - from: "ReferenceLibraryTab"
      to: "set_org_learning_switches RPC"
      via: "supabase.rpc()"
      pattern: "set_org_learning_switches"
    - from: "ProposalReferenceControl"
      to: "set_reference_override RPC"
      via: "supabase.rpc()"
      pattern: "set_reference_override"
    - from: "control render guard"
      to: "proposal.status !== 'draft'"
      via: "no draft include-path"
      pattern: "status\\s*!==\\s*'draft'"
---

<objective>
Build the customer-facing controls: 3 org master toggles + a per-proposal tri-state reference control (R8), backed by the admin-gated RPCs from Plan 03 (writes to R4/R5). Behavior is fully locked; the component/placement is chosen AFTER scouting the real Settings + ProposalDetail (D-08).

Purpose: give org-admins self-serve control of proposal-history eligibility, with drafts structurally offering NO include-path.

Output: a Settings section/tab + a per-proposal control, both admin-gated (client-hide backed by the server RPC gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.7-proposal-history-scoping/14.7-CONTEXT.md
@.planning/phases/14.7-proposal-history-scoping/14.7-PATTERNS.md
@.planning/phases/14.7-proposal-history-scoping/14.7-BRIEF.md

<facts>
- D-06: controls live in the CUSTOMER-accessible org Settings surface (NOT platform /admin). D-07: mutation is org-admin-gated; reads may be broader. D-08: "AI grounding" / "Reference Library" is a reasonable section name (not locked); per-proposal placement near the status control is reasonable — planner scouts and decides.
- Labels VERBATIM (CONTEXT.md D-08 / Specifics): "Learn from won proposals" (on), "Learn from submitted (in-flight) proposals" (off — label clearly as live/in-flight), "Learn from lost proposals" (off). Per-proposal tri-state: "use as reference" = follow-settings / always-include / never-include.
- Server enforcement is Plan 03's `set_org_learning_switches` + `set_reference_override`. Client hide is UX only — the RPC is the real boundary (RESEARCH Pitfalls 2/3).
- reference_override tri-state maps to: follow-settings=NULL, always-include=true, never-include=false.
- No Settings*test* file exists in the repo today (Wave 0 gap).
</facts>

<interfaces>
Reusable (PATTERNS §§ Settings UI + per-proposal control):
- src/pages/Settings.tsx `Toggle` primitive (lines 138-154) — div role="switch", copy it.
- Admin tab-gating: `profile?.role === 'admin' || profile?.role === 'super_admin'` (Settings.tsx line 464); SUB_TABS filter for admin-only tabs (lines 463-465, 520-521).
- Tab component precedent: src/components/settings/TemplatesTab.tsx — standalone, owns its supabase calls (`import { supabase } from '../../lib/supabase'`, `import { useAuth } from '../../context/AuthContext'`).
- Per-proposal analog: src/components/StatusSelector.tsx (dropdown + async onChange + pending state); mount point ProposalDetail.tsx line 567 (`<StatusSelector status={proposal.status} .../>`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SCOUT Settings + ProposalDetail, then build the 3 org master toggles (ReferenceLibraryTab)</name>
  <read_first>
    - src/pages/Settings.tsx — READ the tab structure, SUB_TABS array, the Toggle primitive (138-154), and the admin-gate pattern (463-465, 520-521). REPORT in the SUMMARY the exact role-gating pattern existing Settings controls use (D-07 requires confirming + matching/tightening it).
    - src/components/settings/TemplatesTab.tsx — the standalone admin-gated tab structure to mirror.
    - .planning/phases/14.7-proposal-history-scoping/14.7-PATTERNS.md § Settings UI.
    - .planning/phases/14.7-proposal-history-scoping/14.7-CONTEXT.md D-06/D-07/D-08 + Specifics (verbatim labels).
  </read_first>
  <files>src/components/settings/ReferenceLibraryTab.tsx, src/pages/Settings.tsx, src/components/settings/ReferenceLibraryTab.test.tsx</files>
  <action>
1) Create `ReferenceLibraryTab.tsx` (mirror TemplatesTab.tsx structure): owns its supabase calls + useAuth. On mount, load the org's `learn_from_won, learn_from_submitted, learn_from_lost` from `organizations` (select scoped by profile.org_id). Render three rows using the copied `Toggle` primitive with the VERBATIM labels:
   - "Learn from won proposals"
   - "Learn from submitted (in-flight) proposals"  (add a small sub-label making the live/in-flight meaning explicit)
   - "Learn from lost proposals"
2) On any toggle change, call `supabase.rpc('set_org_learning_switches', { p_learn_from_won, p_learn_from_submitted, p_learn_from_lost })` with the full current trio (the RPC sets all three). Use the StatusSelector-style pending state; on RPC error (e.g. non-admin 'not authorized') revert the optimistic toggle and surface a message.
3) Admin gate: disable (not just hide) the toggles when `profile?.role === 'user'`; expose the tab following the existing SUB_TABS admin-filter pattern (match the Templates tab gating exactly per D-07). Reads may render for all; only admins can mutate — and the RPC is the true enforcement.
4) Wire the tab into `Settings.tsx` SUB_TABS + render switch (section name "Reference Library" or "AI grounding" per D-08 — pick one, matching existing tab naming style).
5) Create `ReferenceLibraryTab.test.tsx` (Wave 0 gap): assert 3 toggles render with correct default reflection, the RPC is called on change, and non-admin renders disabled controls. Use the repo's Vitest mock conventions (STATE.md: inline `vi.mock('../../lib/supabase', ...)`, no renderHook, happy-dom).
  </action>
  <verify>
    <automated>grep -q "set_org_learning_switches" src/components/settings/ReferenceLibraryTab.tsx && grep -q "Learn from won proposals" src/components/settings/ReferenceLibraryTab.tsx && grep -q "Learn from submitted (in-flight) proposals" src/components/settings/ReferenceLibraryTab.tsx && grep -q "Learn from lost proposals" src/components/settings/ReferenceLibraryTab.tsx && grep -q "ReferenceLibraryTab" src/pages/Settings.tsx && npm run test:run >/dev/null 2>&1 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 3 toggles render with the verbatim labels + correct default reflection from the org row (won=on/submitted=off/lost=off for a fresh org).
    - Changes call `set_org_learning_switches` with all three values; error path reverts.
    - Non-admin (`role='user'`) sees disabled toggles; tab gating matches the existing Templates admin pattern (reported in SUMMARY).
    - Tab wired into Settings.tsx; `npm run test:run` passes including new ReferenceLibraryTab.test.tsx.
  </acceptance_criteria>
  <done>3 org toggles live in Settings, admin-gated, persisted via RPC, tested; grep+test verify prints PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Per-proposal tri-state "use as reference" control (non-draft only)</name>
  <read_first>
    - src/components/StatusSelector.tsx — dropdown + async onChange + pending + disabled prop shape to model.
    - src/pages/ProposalDetail.tsx — line ~567 StatusSelector mount point; confirm `proposal.status` + `profile?.role` are in scope.
    - .planning/phases/14.7-proposal-history-scoping/14.7-PATTERNS.md § per-proposal control.
    - .planning/phases/14.7-proposal-history-scoping/14.7-BRIEF.md § SETTINGS UI (lines 111-114) — tri-state, NO draft include-path.
  </read_first>
  <files>src/components/ProposalReferenceControl.tsx, src/pages/ProposalDetail.tsx</files>
  <action>
1) Create `ProposalReferenceControl.tsx` modeled on StatusSelector: a small tri-state control (dropdown or segmented) with options mapping to reference_override: follow-settings=NULL, always-include=true, never-include=false. Label "use as reference". Async onChange with pending state.
2) onChange calls `supabase.rpc('set_reference_override', { p_proposal_id: proposal.id, p_value })` where p_value ∈ {null, true, false}. On 'not authorized' error, revert and message.
3) RENDER GUARD (load-bearing, BRIEF): render the control ONLY when `proposal.status !== 'draft'`. On a draft, render nothing (or an explicit disabled "not available for drafts" note) — there must be NO affordance that sets reference_override on a draft. Disable for `profile?.role === 'user'` (D-07).
4) Mount in ProposalDetail.tsx adjacent to `<StatusSelector .../>` (~line 567). Initialize from `proposal.reference_override`.
5) Extend an existing ProposalDetail/StatusSelector test OR add a focused test asserting: control hidden on draft, visible+tri-state on non-draft, calls set_reference_override, disabled for non-admin. (If a suitable test file is impractical due to ProposalDetail's mount weight, document a manual UAT item for Plan 07 — but prefer a component test on the isolated ProposalReferenceControl.)
  </action>
  <verify>
    <automated>grep -q "set_reference_override" src/components/ProposalReferenceControl.tsx && grep -Eq "status\\s*!==\\s*'draft'" src/components/ProposalReferenceControl.tsx && grep -q "ProposalReferenceControl" src/pages/ProposalDetail.tsx && npm run build >/dev/null 2>&1 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Control renders only for non-draft proposals; drafts get no include affordance (verified by the `status !== 'draft'` guard).
    - Tri-state maps follow-settings=NULL / always-include=true / never-include=false and writes via `set_reference_override`.
    - Disabled for role='user'; error path reverts.
    - Mounted in ProposalDetail near StatusSelector, initialized from proposal.reference_override; `npm run build` clean.
  </acceptance_criteria>
  <done>Per-proposal tri-state control writes reference_override via RPC, non-draft-only, admin-gated, mounted; grep+build verify prints PASS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the Settings toggles + per-proposal control (visual/behavioral)</name>
  <what-built>3 org master toggles in Settings (Reference Library / AI grounding section) + a per-proposal tri-state "use as reference" control on non-draft proposals, both admin-gated and RPC-backed.</what-built>
  <how-to-verify>
    Local app: http://localhost:5173 (login usera@jamo.com / password123, per MEMORY local-app-debug-setup).
    1. As an admin, open Settings → the new section. Confirm 3 toggles with labels "Learn from won proposals" (on), "Learn from submitted (in-flight) proposals" (off), "Learn from lost proposals" (off). Flip one, reload — it persists.
    2. Open a NON-DRAFT proposal → confirm the "use as reference" tri-state control appears near the status selector; set it to always/never/follow — reloads persist.
    3. Open a DRAFT proposal → confirm there is NO "use as reference" include affordance.
    4. (If a non-admin test user is available) confirm the toggles/control are disabled and a mutation attempt is refused.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser UI → RPC | Client-side role hide is UX only; the admin gate is enforced by Plan 03's RPCs |
| draft proposal → include-path | UI must offer NO affordance to include a draft as a reference |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.7-17 | Elevation of Privilege | non-admin mutates controls via UI | mitigate | Controls disabled for role='user'; all writes go through admin-gated RPCs (Plan 03) which RAISE for non-admin — client hide is defense-in-depth, not the boundary |
| T-14.7-18 | Information Disclosure | draft include-path via UI | mitigate | ProposalReferenceControl renders only when `status !== 'draft'`; no draft affordance. Backed by Plan 02 draft floor (even a forced reference_override on a draft is a no-op) |
| T-14.7-19 | Tampering | raw table update instead of RPC | mitigate | UI writes reference_override / switches EXCLUSIVELY via the RPCs, never `supabase.from(...).update()` — keeps the single admin-gated write path |
</threat_model>

<verification>
- 3 toggles + per-proposal control render with correct defaults/labels, persist via RPCs, admin-gated, no draft include-path.
- `npm run test:run` + `npm run build` clean. Human-verify checkpoint passes.
</verification>

<success_criteria>
- Org-admins can self-serve the 3 master switches and per-proposal override; non-admins cannot mutate; drafts offer no include-path.
</success_criteria>

<output>
After completion, create `.planning/phases/14.7-proposal-history-scoping/14.7-05-SUMMARY.md` recording the chosen section name + placement, the EXACT existing Settings admin-gating pattern found during scout (D-07 report), the tri-state→reference_override mapping, and the test coverage / any deferred UAT.
</output>
