---
phase: 14.7-proposal-history-scoping
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - supabase/migrations/20260710000004_eligibility_write_gating.sql
  - .planning/phases/14.7-proposal-history-scoping/verify-write-gating.sql
autonomous: true
requirements: [R4, R5, R8]

must_haves:
  truths:
    - "Only org-admin (role IN ('admin','super_admin')) users can change the 3 org master switches"
    - "Only org-admin users can change proposals.reference_override"
    - "A non-admin org member cannot flip either control even via a direct table update / RPC call"
    - "Writes are scoped to the caller's own org (no cross-org write)"
    - "Reads of the switches/override remain broad (RPC self-join in Plan 02 is unaffected)"
  artifacts:
    - path: "supabase/migrations/20260710000004_eligibility_write_gating.sql"
      provides: "set_org_learning_switches + set_reference_override SECURITY DEFINER RPCs (admin-gated)"
      contains: "NOT IN ('admin','super_admin')"
    - path: ".planning/phases/14.7-proposal-history-scoping/verify-write-gating.sql"
      provides: "Live checks: non-admin denied, admin allowed, cross-org denied"
      contains: "set_reference_override"
  key_links:
    - from: "Settings UI (Plan 05)"
      to: "set_org_learning_switches RPC"
      via: "supabase.rpc()"
      pattern: "set_org_learning_switches"
    - from: "per-proposal control (Plan 05)"
      to: "set_reference_override RPC"
      via: "supabase.rpc()"
      pattern: "set_reference_override"
---

<objective>
Build the server-side admin-only write path for the confidentiality controls before any UI is wired (R4/R5 write path; R8 enforcement half).

Purpose: The controls change a confidentiality boundary, so mutation MUST be org-admin-gated server-side — a hidden UI control is not enforcement (RESEARCH Pitfalls 2 & 3). `organizations` has NO org-admin write path today (only super_admin ALL), and `proposals_update` RLS has NO role check — so any org member could flip `reference_override` directly. Both gaps must be closed here.

Output: One migration adding two admin-gated SECURITY DEFINER RPCs + a verify script. Applied in Plan 06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.7-proposal-history-scoping/14.7-RESEARCH.md
@.planning/phases/14.7-proposal-history-scoping/14.7-PATTERNS.md
@.planning/phases/14.7-proposal-history-scoping/14.7-CONTEXT.md

<facts>
- RESEARCH Pitfall 2: the ONLY write policy on `organizations` is `orgs_super_admin` (ALL, role='super_admin'). A regular org `admin` has NO write path — a direct `.update()` silently affects 0 rows. A NEW admin write path is required.
- RESEARCH Pitfall 3: `proposals_update` RLS is `org_id = get_user_org_id()` with NO role clause — any org member can already `.update({reference_override})` directly. Hiding the UI does not stop this.
- No existing UPDATE-with-role-check RLS precedent in this codebase (`templates_insert_admin`/`templates_delete_admin` are INSERT/DELETE only). RESEARCH Open Q2 + D-02 "express the rule once" convention → prefer dedicated SECURITY DEFINER RPCs over broad RLS UPDATE policies (avoids incidentally opening plan/slug/is_active to admins).
- user_profiles.role is free text; observed values 'user','admin','super_admin'. Admin role-check subquery (PATTERNS.md): `(SELECT role FROM user_profiles WHERE user_id = auth.uid()) IN ('admin','super_admin')`.
- REVOKE/GRANT footer precedent (PATTERNS.md § REVOKE/GRANT): mutation RPCs should NOT be granted to PUBLIC/anon.
</facts>

<interfaces>
Admin-gated mutation RPC shape (PATTERNS.md lines 111-126), plpgsql (single mutation, not the constrained read path):
  CREATE OR REPLACE FUNCTION set_reference_override(p_proposal_id uuid, p_value boolean)
  RETURNS void LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$
  BEGIN
    IF (SELECT role FROM user_profiles WHERE user_id = auth.uid()) NOT IN ('admin','super_admin') THEN
      RAISE EXCEPTION 'not authorized';
    END IF;
    UPDATE proposals SET reference_override = p_value
      WHERE id = p_proposal_id
        AND org_id = (SELECT org_id FROM user_profiles WHERE user_id = auth.uid());
  END; $$;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: set_reference_override + set_org_learning_switches admin-gated RPCs</name>
  <read_first>
    - .planning/phases/14.7-proposal-history-scoping/14.7-CONTEXT.md D-06/D-07 — self-serve org settings, org-admin gating (match or tighten existing pattern), reads broad / writes admin-only.
    - .planning/phases/14.7-proposal-history-scoping/14.7-PATTERNS.md § RLS/RPC admin-gated write + § REVOKE/GRANT footer + § Admin role-check subquery.
    - .planning/phases/14.7-proposal-history-scoping/14.7-RESEARCH.md Pitfalls 2 & 3 + § Don't Hand-Roll.
    - supabase/migrations/20260418000023_templates.sql — the live admin role-check subquery shape to copy.
    - MANDATORY LIVE FETCH: run Supabase MCP `execute_sql` → `SELECT polname, cmd, qual, with_check FROM pg_policies WHERE tablename IN ('organizations','proposals');` to confirm Pitfalls 2 & 3 still hold before choosing the mechanism.
  </read_first>
  <files>supabase/migrations/20260710000004_eligibility_write_gating.sql</files>
  <action>
Create migration `20260710000004_eligibility_write_gating.sql` with TWO plpgsql SECURITY DEFINER RPCs (D-02-consistent "express the rule once"; RPC chosen over broad RLS UPDATE per RESEARCH Don't-Hand-Roll to avoid opening plan/slug/is_active).

1) `set_reference_override(p_proposal_id uuid, p_value boolean) RETURNS void` — exactly the <interfaces> shape. `p_value` may be NULL (tri-state: NULL/true/false), so type it `boolean` and pass NULL through. Admin check RAISEs 'not authorized' before the UPDATE; UPDATE is org-scoped to the caller's org.

2) `set_org_learning_switches(p_learn_from_won boolean, p_learn_from_submitted boolean, p_learn_from_lost boolean) RETURNS void`:
```sql
CREATE OR REPLACE FUNCTION public.set_org_learning_switches(
  p_learn_from_won boolean, p_learn_from_submitted boolean, p_learn_from_lost boolean
) RETURNS void LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$
BEGIN
  IF (SELECT role FROM user_profiles WHERE user_id = auth.uid()) NOT IN ('admin','super_admin') THEN
    RAISE EXCEPTION 'not authorized';
  END IF;
  UPDATE organizations
     SET learn_from_won = p_learn_from_won,
         learn_from_submitted = p_learn_from_submitted,
         learn_from_lost = p_learn_from_lost
   WHERE id = (SELECT org_id FROM user_profiles WHERE user_id = auth.uid());
END; $$;
```
Only the three switch columns are updated — plan/slug/is_active are untouched (column-scoped by construction).

3) REVOKE/GRANT footer for BOTH (mutation RPCs must not be PUBLIC/anon):
```sql
REVOKE ALL ON FUNCTION public.set_reference_override(uuid, boolean) FROM PUBLIC, anon;
GRANT EXECUTE ON FUNCTION public.set_reference_override(uuid, boolean) TO authenticated, service_role;
REVOKE ALL ON FUNCTION public.set_org_learning_switches(boolean, boolean, boolean) FROM PUBLIC, anon;
GRANT EXECUTE ON FUNCTION public.set_org_learning_switches(boolean, boolean, boolean) TO authenticated, service_role;
```
Do NOT alter the existing `proposals_update` or `organizations` policies (leaving `proposals_update` broad for status/title edits is intended — the sensitive field is gated at the RPC, and the UI writes reference_override ONLY via this RPC, never a raw table update).
  </action>
  <verify>
    <automated>f=supabase/migrations/20260710000004_eligibility_write_gating.sql; grep -q "FUNCTION public.set_reference_override(p_proposal_id uuid, p_value boolean)" $f && grep -q "FUNCTION public.set_org_learning_switches" $f && grep -c "NOT IN ('admin','super_admin')" $f | grep -q 2 && grep -q "RAISE EXCEPTION 'not authorized'" $f && grep -q "REVOKE ALL ON FUNCTION public.set_reference_override" $f && grep -q "REVOKE ALL ON FUNCTION public.set_org_learning_switches" $f && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Both RPCs are SECURITY DEFINER, plpgsql, `SET search_path = public`.
    - Each RAISEs 'not authorized' when the caller's role is not in ('admin','super_admin') BEFORE any UPDATE.
    - `set_reference_override` UPDATE is scoped `AND org_id = (SELECT org_id FROM user_profiles WHERE user_id = auth.uid())` and accepts a NULL p_value (tri-state).
    - `set_org_learning_switches` updates ONLY the 3 learn_from_* columns, scoped to the caller's org.
    - Both have REVOKE FROM PUBLIC, anon + GRANT TO authenticated, service_role.
    - No change to existing `proposals_update` / `organizations` RLS policies.
  </acceptance_criteria>
  <done>Migration authors both admin-gated RPCs with role check + org scope + grants; grep verify prints PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Author live write-gating verification script</name>
  <read_first>
    - .planning/phases/14.7-proposal-history-scoping/14.7-RESEARCH.md Pitfalls 2 & 3 (the exact bypass being closed).
    - Local app debug note (MEMORY): test users exist (e.g. usera@jamo.com). Identify a non-admin and an admin user_profiles row for the checks.
  </read_first>
  <files>.planning/phases/14.7-proposal-history-scoping/verify-write-gating.sql</files>
  <action>
Create `verify-write-gating.sql`: runnable blocks (Plan 07 runs via Supabase MCP `execute_sql`) proving the gate. Since MCP execute_sql runs as service_role (bypasses RLS and `auth.uid()` is NULL), the RPC role-check must be validated by SETting the JWT claim context OR by asserting the function BODY contains the guard AND running a role-simulated check. Include, each with expected outcome:
- Positive: as an admin context, `SELECT set_org_learning_switches(true, true, false);` then `SELECT learn_from_won, learn_from_submitted, learn_from_lost FROM organizations WHERE id = <org>` → matches; wrap BEGIN...ROLLBACK to revert.
- Negative (non-admin denied): simulate a non-admin caller (e.g. `SET LOCAL role authenticated; SELECT set_config('request.jwt.claims', json_build_object('sub', '<non-admin user_id>')::text, true);` then call the RPC) → expect `not authorized` error; BEGIN...ROLLBACK.
- Negative (reference_override direct-update bypass is closed at the UI layer, not RLS): note explicitly that `proposals_update` RLS remains broad by design and the enforcement is that the UI calls `set_reference_override` only — assert the RPC denies a non-admin caller the same way.
- Cross-org: as an admin of org A, `SELECT set_reference_override('<proposal in org B>', true);` → 0 rows affected (org-scoped WHERE); assert org B's proposal unchanged.
If the JWT-claim simulation is not feasible via MCP, fall back to a documented manual UAT step in Plan 07 (log in as a non-admin in the app and confirm the mutation errors), and keep the positive/cross-org SQL checks here.
  </action>
  <verify>
    <automated>f=.planning/phases/14.7-proposal-history-scoping/verify-write-gating.sql; grep -q "set_org_learning_switches" $f && grep -q "set_reference_override" $f && grep -qi "not authorized" $f && grep -qi "ROLLBACK" $f && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Script exercises both positive (admin allowed) and negative (non-admin denied → 'not authorized') paths for at least one RPC.
    - Includes a cross-org write attempt asserting 0-row effect.
    - Mutating blocks are BEGIN ... ROLLBACK; if JWT simulation is infeasible, a manual UAT fallback is documented for Plan 07.
  </acceptance_criteria>
  <done>verify-write-gating.sql exists covering admin-allow, non-admin-deny, cross-org-deny; grep verify prints PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| org member → eligibility controls | Any authenticated org member could reach the switches/override; only org-admins may mutate |
| client `.update()` → proposals/organizations | Direct table writes bypass UI; the RPC + grant hygiene is the real boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.7-10 | Elevation of Privilege | non-admin flips org master switch | mitigate | `set_org_learning_switches` RAISEs 'not authorized' for non-admin; organizations has no other org-level write path (super_admin ALL unchanged). Verified negative case |
| T-14.7-11 | Tampering | non-admin flips reference_override via direct .update() | mitigate | UI writes reference_override ONLY via `set_reference_override` (admin-gated); RPC RAISEs for non-admin. proposals_update stays broad for legitimate status/title edits (does not touch the sensitive field once UI routes through the RPC) |
| T-14.7-12 | Information Disclosure | cross-org write to another tenant's control | mitigate | Both RPC UPDATEs are scoped to `org_id = (caller's org)`; a cross-org id affects 0 rows. Verified cross-org case |
| T-14.7-13 | Elevation of Privilege | mutation RPC callable by anon/PUBLIC | mitigate | REVOKE FROM PUBLIC, anon + GRANT TO authenticated, service_role on both |
</threat_model>

<verification>
- Migration authors both admin-gated RPCs with role check + org scope + REVOKE/GRANT.
- verify-write-gating.sql covers allow/deny/cross-org. Live assertions run in Plan 07 after apply.
</verification>

<success_criteria>
- Org master switches and reference_override are mutable ONLY by org-admins, only within the caller's org, only via the two RPCs.
- Mutation RPCs are not executable by anon/PUBLIC.
</success_criteria>

<output>
After completion, create `.planning/phases/14.7-proposal-history-scoping/14.7-03-SUMMARY.md` recording the two RPC signatures, the chosen mechanism (RPC vs policy) and why, whether the live pg_policies check confirmed Pitfalls 2 & 3, and the verify approach for Plan 07.
</output>
