---
phase: 14.7-proposal-history-scoping
plan: 06
status: complete
completed: 2026-07-10
requirements: [R1, R2, R3, R4, R5, R6, R7]
---

# 14.7-06 Summary — [BLOCKING] Deploy-first gate

Applied all four 14.7 migrations to the live project `fuuvdcvbliijffogjnwg` and deployed the two changed edge functions. Developer-approved production deploy (Wave 4 human-action checkpoint). Full detail in [14.7-06-DEPLOY-LOG.md](./14.7-06-DEPLOY-LOG.md).

## What was deployed

- **Migrations (in order, via Supabase MCP `apply_migration`, not `db push`):**
  1. `20260710000001` schema columns — 5 new columns confirmed live; both orgs default won=true/submitted=false/lost=false.
  2. `20260710000002` fail-closed backfill — **unresolvable proposal chunks: 0** (171/171 resolved; audit-table count == still-NULL count == 0).
  3. `20260710000003` scoped proposal RPCs — **exactly 2** `match_chunks_*_proposals` overloads live, each with `current_proposal_id uuid` (old unscoped overloads dropped; T-14.7-08 mitigated).
  4. `20260710000004` write-gating — `set_reference_override` + `set_org_learning_switches` present, EXECUTE granted to authenticated/service_role only.
- **Edge functions (via `supabase functions deploy`, first `sbp_` token from .env):**
  - `retrieve-context` v9 → **v10** (ACTIVE)
  - `extract-document` v7 → **v8** (ACTIVE)
  - No other function redeployed.

## Self-Check: PASSED
Structural gate green. Automated verify (grep DEPLOY-LOG for "unresolvable" + RPC overload check + edge versions) passes. Behavioral verification is Plan 07.

## Key files
- `.planning/phases/14.7-proposal-history-scoping/14.7-06-DEPLOY-LOG.md` (created)

## Notes / deviations
- Edge functions deployed via the Supabase CLI (bundles shared deps automatically) rather than MCP `deploy_edge_function` — both are sanctioned by the plan; CLI avoids manually enumerating shared imports. Docker-not-running warning is benign (CLI used remote bundling).
