---
phase: 14.7-proposal-history-scoping
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - .planning/phases/14.7-proposal-history-scoping/14.7-06-DEPLOY-LOG.md
autonomous: false
requirements: [R1, R2, R3, R4, R5, R6, R7]

must_haves:
  truths:
    - "All four 14.7 migrations are applied to the live project fuuvdcvbliijffogjnwg"
    - "chunks.proposal_id, proposals.reference_override, and the 3 organizations.learn_from_* columns exist live"
    - "Exactly one overload of each match_chunks_*_proposals RPC exists live (old signatures dropped)"
    - "The backfill unresolvable count is recorded and equals _backfill_unresolved_proposal_chunks row count"
    - "retrieve-context and extract-document edge functions are deployed with the 14.7 code"
  artifacts:
    - path: ".planning/phases/14.7-proposal-history-scoping/14.7-06-DEPLOY-LOG.md"
      provides: "Record of applied migrations, backfill count, RPC overload check, edge deploy versions"
      contains: "unresolvable"
  key_links:
    - from: "migration files (Plans 01-03)"
      to: "live project fuuvdcvbliijffogjnwg"
      via: "Supabase MCP apply_migration"
      pattern: "apply_migration"
    - from: "edge code (Plan 04)"
      to: "live edge runtime"
      via: "supabase functions deploy"
      pattern: "functions deploy"
---

<objective>
[BLOCKING] Apply all 14.7 schema/RPC migrations to the live DB and deploy the two changed edge functions BEFORE any verification. This is the deploy-first gate — build/type checks pass WITHOUT this (TS types come from config, not the live DB), so skipping it produces a false-positive verification state.

Purpose: make the confidentiality fix actually live. Nothing in Plan 07 is meaningful until this completes.

Output: a deploy log capturing applied migrations, the backfill count, the RPC overload check, and edge deploy versions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.7-proposal-history-scoping/14.7-RESEARCH.md
@.planning/phases/14.7-proposal-history-scoping/14.7-CONTEXT.md

<facts>
- `supabase db push` is DIVERGED for this project — DO NOT use it. Apply migrations via Supabase MCP `apply_migration` (or Management API database/query), one migration at a time, in order (RESEARCH § Deploy mechanics; MEMORY supabase-migration-history-diverged).
- Migration apply ORDER (dependencies): 20260710000001 (schema) → 20260710000002 (backfill) → 20260710000003 (RPC eligibility) → 20260710000004 (write-gating). 000002 needs 000001's columns; 000003 needs 000001's columns.
- Edge deploy: `npx supabase functions deploy retrieve-context --project-ref fuuvdcvbliijffogjnwg` and same for `extract-document`, using the FIRST 44-char `sbp_` token in `.env` (line 9 — later duplicates are malformed/401). Ambient env token is legacy/invalid (MEMORY edge-deploy-token-from-env). Edge fns do NOT auto-deploy (MEMORY edge-functions-need-deploy).
- Current live versions: retrieve-context v9, extract-document v7 → expect v10 / v8 after deploy.
</facts>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply the four 14.7 migrations live, in order, via Supabase MCP</name>
  <read_first>
    - supabase/migrations/20260710000001_chunks_proposal_scope_columns.sql
    - supabase/migrations/20260710000002_backfill_proposal_chunks.sql
    - supabase/migrations/20260710000003_proposal_rpc_eligibility.sql
    - supabase/migrations/20260710000004_eligibility_write_gating.sql
    - .planning/phases/14.7-proposal-history-scoping/14.7-RESEARCH.md § Deploy mechanics (db push diverged; use MCP apply_migration).
  </read_first>
  <files>.planning/phases/14.7-proposal-history-scoping/14.7-06-DEPLOY-LOG.md</files>
  <action>
Apply each migration to project `fuuvdcvbliijffogjnwg` via the Supabase MCP `apply_migration` tool (name each migration by its filename), STRICTLY in this order: 000001 → 000002 → 000003 → 000004. Do NOT run `supabase db push`.

After each apply, capture the result. Specifically:
1. After 000001: confirm columns exist — `SELECT column_name FROM information_schema.columns WHERE (table_name='chunks' AND column_name='proposal_id') OR (table_name='proposals' AND column_name='reference_override') OR (table_name='organizations' AND column_name LIKE 'learn_from_%');` → expect 5 rows. Confirm both existing orgs read won=true/submitted=false/lost=false.
2. After 000002: capture the backfill NOTICE/return count and `SELECT count(*) FROM _backfill_unresolved_proposal_chunks;` and `SELECT count(*) FROM chunks WHERE doc_type='proposal' AND proposal_id IS NULL;` — the two counts MUST be equal (expected 0 per D-05c). If NON-ZERO, record the count + reasons from the audit table (do NOT purge; D-05a) and flag it in the log — a non-zero count is an acceptable fail-closed outcome but must be surfaced.
3. After 000003: run `SELECT proname, pg_get_function_identity_arguments(oid) FROM pg_proc WHERE proname LIKE 'match_chunks_%_proposals';` → MUST be exactly 2 rows, each including `current_proposal_id uuid`. If 4 rows appear, the old overload survived — STOP and fix the DROP in Plan 02's migration before proceeding.
4. After 000004: confirm both RPCs exist — `SELECT proname FROM pg_proc WHERE proname IN ('set_reference_override','set_org_learning_switches');` → 2 rows.

Write all captured results into `14.7-06-DEPLOY-LOG.md`.

This task is checkpoint:human-action because applying schema to the live prod DB is a confidentiality-sensitive, irreversible-ish operation that should be confirmed by the developer before running (per the phase's deploy-first, same-rigor-as-14.6 mandate).
  </action>
  <verify>
    <automated>grep -qi "unresolvable" .planning/phases/14.7-proposal-history-scoping/14.7-06-DEPLOY-LOG.md && grep -Eqi "2 rows|exactly 2|match_chunks_.*_proposals" .planning/phases/14.7-proposal-history-scoping/14.7-06-DEPLOY-LOG.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All four migrations applied via MCP apply_migration, in order; no `supabase db push` used.
    - 5 new columns confirmed live; both orgs on won=true/submitted=false/lost=false.
    - Backfill: `chunks WHERE doc_type='proposal' AND proposal_id IS NULL` count == `_backfill_unresolved_proposal_chunks` count, and both logged (expected 0).
    - Exactly 2 proposal RPC overloads live, each with current_proposal_id; both write-gating RPCs present.
    - DEPLOY-LOG.md records every result.
  </acceptance_criteria>
  <done>Four migrations live, columns + RPC overload count + backfill count verified and logged.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Deploy retrieve-context + extract-document edge functions</name>
  <read_first>
    - supabase/functions/retrieve-context/index.ts, supabase/functions/extract-document/index.ts (the Plan 04 edits must be committed).
    - .planning/phases/14.7-proposal-history-scoping/14.7-RESEARCH.md § Deploy mechanics (sbp_ token, first occurrence only).
    - MEMORY: edge-deploy-token-from-env, edge-functions-need-deploy.
  </read_first>
  <files>.planning/phases/14.7-proposal-history-scoping/14.7-06-DEPLOY-LOG.md</files>
  <action>
Deploy BOTH changed edge functions to `fuuvdcvbliijffogjnwg` using the FIRST 44-char `sbp_` PAT from `.env` (line 9; ignore the 6 malformed duplicates):
```
npx supabase functions deploy retrieve-context --project-ref fuuvdcvbliijffogjnwg
npx supabase functions deploy extract-document --project-ref fuuvdcvbliijffogjnwg
```
(Or use the Supabase MCP `deploy_edge_function` tool.) Capture the resulting versions (expect retrieve-context v10, extract-document v8) and append to DEPLOY-LOG.md. If deploy prompts for auth, this is the auth gate — supply the sbp_ token; do not skip. Do NOT deploy any other function (chat-with-jamo, generate-proposal-section, etc. are unchanged — leaving them avoids collateral regression).
  </action>
  <verify>
    <automated>grep -Eqi "retrieve-context.*v1[0-9]|extract-document.*v[89]" .planning/phases/14.7-proposal-history-scoping/14.7-06-DEPLOY-LOG.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - retrieve-context and extract-document redeployed with the 14.7 code; new versions logged (expect v10 / v8).
    - No other edge function deployed.
    - The sbp_ token used was the first .env occurrence (not a malformed duplicate).
  </acceptance_criteria>
  <done>Both edge functions deployed live with 14.7 code; versions logged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored migration → live prod DB | Applying confidentiality logic to prod; wrong order or a surviving old overload leaves the bleed un-fixed |
| committed edge code → live runtime | Stale deployment = code looks fixed but prod runs the old unscoped path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.7-20 | Information Disclosure | old unscoped RPC overload survives apply | mitigate | Post-000003 overload check must be exactly 2 rows; STOP + fix if 4 |
| T-14.7-21 | Information Disclosure | edge fns not deployed → prod runs stale unscoped path | mitigate | Explicit deploy of both fns with version capture; false-positive avoided (build passes without deploy) |
| T-14.7-22 | Tampering | wrong apply order breaks dependencies | mitigate | Strict 000001→000002→000003→000004 order documented + enforced |
</threat_model>

<verification>
- Four migrations applied in order; 5 columns live; exactly 2 proposal RPC overloads; backfill count logged; both write-gating RPCs present; both edge fns redeployed with versions logged.
</verification>

<success_criteria>
- The full 14.7 schema + RPC + edge changes are LIVE on fuuvdcvbliijffogjnwg, verified structurally, before any behavioral verification (Plan 07).
</success_criteria>

<output>
After completion, create `.planning/phases/14.7-proposal-history-scoping/14.7-06-SUMMARY.md` referencing 14.7-06-DEPLOY-LOG.md and recording: migrations applied, backfill unresolvable count, RPC overload-check result, and the two new edge versions.
</output>
