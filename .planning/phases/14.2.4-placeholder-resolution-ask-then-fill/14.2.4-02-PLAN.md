---
phase: 14.2.4-placeholder-resolution-ask-then-fill
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - supabase/functions/analyze-proposal-gaps/index.ts
  - src/types/chat.ts
  - supabase/functions/analyze-proposal-gaps/__tests__/enum-sync.test.ts
autonomous: true
requirements: [D-02, D-03, D-06, AC-3, AC-6]
must_haves:
  truths:
    - "A finding with cta_tool:'ask_user' validates on the edge PendingActionSchema (AC-3 edge half)"
    - "The client ActionItemCtaTool union accepts 'ask_user' (AC-3 client half)"
    - "All 4 analyzer few-shot OUTPUT blocks (placeholder + missing families) emit cta_tool:'ask_user' with a Provide info / Fill in label (D-06, D-03)"
    - "Needs-expansion / conflict / compliance findings keep propose_edit (AC-6 regression guard at the few-shot level)"
    - "ActiveTask gains an optional originating_snapshot field of type OriginatingActionSnapshot (foundation for Risk B)"
  artifacts:
    - path: "supabase/functions/analyze-proposal-gaps/index.ts"
      provides: "Widened PendingActionSchema enum (exported), updated system-prompt valid-values lines, 4 updated few-shot OUTPUT blocks"
      contains: "export const PendingActionSchema"
    - path: "src/types/chat.ts"
      provides: "Widened ActionItemCtaTool union + ActiveTask.originating_snapshot field"
      contains: "originating_snapshot"
  key_links:
    - from: "analyze-proposal-gaps/index.ts PendingActionSchema"
      to: "src/types/chat.ts ActionItemCtaTool"
      via: "enum-sync — both contain 'ask_user'"
      pattern: "'ask_user'"
---

<objective>
The foundation wave: widen the `cta_tool` enum in BOTH sync sites (edge Zod schema +
client TS union — Risk A / Pitfall 4), add the `ActiveTask.originating_snapshot` field
(foundation for Risk B), and rewrite the analyzer's 4 placeholder/missing few-shot OUTPUT
blocks so all placeholder + missing findings emit `cta_tool:'ask_user'` (D-06) with a
"Provide info" / "Fill in" label (D-03). Needs-expansion stays `propose_edit` (AC-6).

This is the #1 silent-break risk plan — both enum sites widen TOGETHER, with the Wave 0
enum-sync test un-skipped to prove a needs-value finding validates on the edge.

Purpose: a single data-change that re-routes detection; no control-flow change.
Output: synced enums, extended ActiveTask type, ask-routing few-shots, green enum-sync test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-CONTEXT.md
@.planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-PATTERNS.md

<interfaces>
Edge enum site — analyze-proposal-gaps/index.ts:27 (CONFIRMED):
  cta_tool: z.enum(['propose_edit', 'check_regulatory_compliance', 'answer_with_citations']),
PendingActionSchema declared at :19 as `const` — change to `export const` (test imports it).

Client enum site — src/types/chat.ts:190 (CONFIRMED):
  export type ActionItemCtaTool = 'propose_edit' | 'check_regulatory_compliance' | 'answer_with_citations'

ActiveTask interface — src/types/chat.ts:229, last field source_action_item_id at :248.
OriginatingActionSnapshot type alias — already declared in same file at :273 (no import needed;
type aliases hoist in TS, so referencing it inside the :229 interface is safe).

System prompt valid-values lines — analyze-proposal-gaps/index.ts ~:97-98.
Four few-shot OUTPUT blocks — ~:121 (Family 1 gap/bracket), :126 (Family 2 gap/descriptive-noun),
:131 (Family 3 missing/contact), :136 (Family 4 missing/TBD). All currently emit propose_edit.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen both cta_tool enums + add ActiveTask.originating_snapshot (D-02, AC-3)</name>
  <files>src/types/chat.ts, supabase/functions/analyze-proposal-gaps/index.ts, supabase/functions/analyze-proposal-gaps/__tests__/enum-sync.test.ts</files>
  <behavior>
    - enum-sync test 14.2.4-AC3-01: PendingActionSchema.safeParse({valid finding, cta_tool:'ask_user'}) → success true.
    - enum-sync test 14.2.4-AC3-02: PendingActionSchema.safeParse({..., cta_tool:'unknown_tool'}) → success false.
    - tsc: a value typed ActionItemCtaTool may be assigned 'ask_user' (no error).
  </behavior>
  <read_first>
    - supabase/functions/analyze-proposal-gaps/index.ts (lines 19-29 — PendingActionSchema + enum at :27)
    - src/types/chat.ts (lines 190, 229-250, 273 — ActionItemCtaTool, ActiveTask, OriginatingActionSnapshot)
    - .planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-PATTERNS.md (lines 31-47, 79-102, 358-389)
  </read_first>
  <action>
    1. EDGE (analyze-proposal-gaps/index.ts):
       - Line 19: change `const PendingActionSchema` → `export const PendingActionSchema` (so the
         Wave 0 Deno test can import it — required by PATTERNS.md note line 389).
       - Line 27: add `'ask_user'` to the z.enum array:
         `cta_tool: z.enum(['propose_edit', 'check_regulatory_compliance', 'answer_with_citations', 'ask_user']),`
    2. CLIENT (src/types/chat.ts):
       - Line 190: append ` | 'ask_user'` to ActionItemCtaTool union.
       - In the ActiveTask interface (:229-248), AFTER `source_action_item_id?: string`, add:
         `  /** Originating finding snapshot — persisted for ask-then-fill attribution across ask hop + reload (Risk B / D-01) */`
         `  originating_snapshot?: OriginatingActionSnapshot`
         (OriginatingActionSnapshot is declared at :273 in the same file — no new import.)
    3. TEST (enum-sync.test.ts): un-skip both stubs from Plan 01. Add the real import
       `import { PendingActionSchema } from '../index.ts'` and the safeParse bodies per PATTERNS.md
       lines 364-386 (valid finding fields: id, type:'gap', section_key, title, description,
       priority, cta_label:'Provide info', cta_tool:'ask_user', cta_payload:{}).
    Do NOT touch any other enum consumer; this is the minimal two-site widen.
  </action>
  <acceptance_criteria>
    - grep `index.ts:27` enum array contains `'ask_user'`; grep `chat.ts:190` union contains `'ask_user'`.
    - grep `export const PendingActionSchema` in analyze-proposal-gaps/index.ts matches (was `const`).
    - grep `originating_snapshot?: OriginatingActionSnapshot` in chat.ts matches inside the ActiveTask interface (before/after source_action_item_id).
    - `npm run test:run` exits 0 with 14.2.4-AC3-01 / AC3-02 now PASSING (not skipped).
    - `npx tsc --noEmit` clean for src/types/chat.ts (no unresolved OriginatingActionSnapshot).
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run</automated>
  </verify>
  <done>Both enum sites contain 'ask_user'; ActiveTask has originating_snapshot; enum-sync test green.</done>
</task>

<task type="auto">
  <name>Task 2: Re-route placeholder + missing few-shots to ask_user (D-06, D-03, AC-6)</name>
  <files>supabase/functions/analyze-proposal-gaps/index.ts</files>
  <read_first>
    - supabase/functions/analyze-proposal-gaps/index.ts (lines 86-137 — ANALYSIS_SYSTEM_PROMPT, valid-values at ~:97-98, four OUTPUT blocks at ~:121/:126/:131/:136)
    - .planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-PATTERNS.md (lines 39-75 — exact before/after for prompt lines + 4 OUTPUT blocks)
    - .planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-CONTEXT.md (D-06 forward-compat: rule lives in ONE place — the few-shots; D-07 watch-item 1: descriptions must stay enumerative)
  </read_first>
  <action>
    All edits are inside ANALYSIS_SYSTEM_PROMPT (no code branch — D-06 says the few-shot rewrite
    IS the detection rule; do NOT add an isNeedsValue() code function):
    1. Valid-values lines (~:97-98): widen to include the new label + tool values:
       `"cta_label": "Fix it" | "Draft it" | "Check it" | "Provide info" | "Fill in",`
       `"cta_tool": "propose_edit" | "check_regulatory_compliance" | "answer_with_citations" | "ask_user",`
    2. Family 1 OUTPUT (~:121, gap + bracket placeholder): `"cta_label":"Provide info","cta_tool":"ask_user"`.
    3. Family 2 OUTPUT (~:126, gap + descriptive-noun placeholder): `"cta_label":"Provide info","cta_tool":"ask_user"`.
    4. Family 3 OUTPUT (~:131, missing + contact/address): `"cta_label":"Fill in","cta_tool":"ask_user"`.
    5. Family 4 OUTPUT (~:136, missing + TBD/marker): `"cta_label":"Fill in","cta_tool":"ask_user"`.
    6. Add ONE prompt instruction line near the valid-values block stating the routing rule
       (D-06): "Emit cta_tool 'ask_user' for ALL gap and missing findings (unfilled
       placeholders / unstarted content — only the user has the value). Use 'propose_edit' only
       for conflict and compliance findings the AI can resolve from regulatory context. This is
       the deterministic ask-vs-fill decision (no model discretion)."
    AC-6 regression guard: do NOT change any conflict/compliance example or the propose_edit
    value for them. D-07 watch-item 1: confirm each of the 4 placeholder example DESCRIPTIONS
    enumerates the specific needed values (e.g. lists "'CRO legal entity name'"); if any is vague,
    tighten it to be enumerative (a vague description yields a vague ask).
  </action>
  <acceptance_criteria>
    - grep within ANALYSIS_SYSTEM_PROMPT: exactly the 4 placeholder/missing OUTPUT blocks contain `"cta_tool":"ask_user"`; zero `propose_edit` remains in those 4 blocks.
    - Valid-values `cta_tool` line lists all four tools including `ask_user`; `cta_label` line lists `Provide info` and `Fill in`.
    - Conflict/compliance examples (if present) still reference their original tool — grep shows no `ask_user` leaked onto a conflict/compliance example (AC-6).
    - The added D-06 routing instruction line is present (grep: `Emit cta_tool 'ask_user' for ALL gap and missing`).
    - `npm run test:run` exits 0 (prompt is a string; no client tests break).
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run</automated>
  </verify>
  <done>All 4 placeholder/missing few-shots emit ask_user with Provide info/Fill in; conflict/compliance unchanged; routing rule documented in prompt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Haiku analyzer output → edge Zod validation | Model-emitted findings cross into PendingActionSchema.parse; widened enum must still reject unknown tools. |
| Edge enum ↔ client enum | Two copies of the cta_tool vocabulary; desync makes a valid edge finding `unknown` on the client (Pitfall 4). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.2.4-02 | T (Tampering) | Enum desync — only one site widened | mitigate | Both sites widened in THIS plan in one commit; enum-sync test (AC3-01/02) asserts edge accepts ask_user and rejects unknown; AC3-02 proves the schema is not loosened to accept arbitrary strings. |
| T-14.2.4-03 | E (Elevation) | Widened enum accidentally accepts arbitrary cta_tool | mitigate | z.enum stays a closed set; AC3-02 (`unknown_tool` → success:false) is the guard. |
| T-14.2.4-04 | I (Info disclosure) | originating_snapshot stored in active_task JSONB | accept | Field holds only finding metadata (id, section_key, type, title, description) already visible to the same user; chat_sessions is org/user-scoped (RLS, user_id filter D-45). No new PII surface. |
</threat_model>

<verification>
- `npm run test:run` green; AC3-01/AC3-02 pass (not skipped).
- Both enum sites and the ActiveTask field grep-confirmed.
- Manual read of the 4 few-shot blocks confirms ask_user + correct labels, conflict/compliance untouched.
</verification>

<success_criteria>
Enums synced with 'ask_user' on both sides; ActiveTask carries originating_snapshot; analyzer
routes all placeholder/missing findings to ask_user with actor-correct labels; AC-6 regression
guard holds at the few-shot level; enum-sync test green.
</success_criteria>

<output>
After completion, create `.planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-02-SUMMARY.md`
</output>
