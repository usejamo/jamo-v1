---
phase: 14.2.4-placeholder-resolution-ask-then-fill
plan: "03"
subsystem: edge
tags: [wave-2, ask-user, active-task, durable-resume, attribution, D-01, D-10, AC-4, AC-5, risk-b]
dependency_graph:
  requires:
    - "Plan 02 widened cta_tool enum + ActiveTask.originating_snapshot field"
  provides:
    - "Awaited ask_user needs-value active_task write (durable resume — D-01 condition 3)"
    - "originating_snapshot + source_action_item_id persisted into active_task (Risk B write half)"
    - "Pure, vitest-tested buildNeedsValueActiveTask + resolveSectionTitle (src/chat/activeTaskBuilder.ts)"
    - "set_focus section_title TODO fixed to real resolved title (D-10 bonus)"
  affects:
    - "supabase/functions/chat-with-jamo/index.ts"
    - "src/chat/activeTaskBuilder.ts"
    - "src/chat/__tests__/activeTaskShape.test.ts"
tech_stack:
  added: []
  patterns:
    - "dual-source pure helper: single tested src/chat/ builder + field-identical Deno-local mirror (edge cannot import src/)"
    - "presence-as-flag: cta_payload.originating_snapshot existence IS the needs-value condition (no separate boolean)"
    - "awaited durable write vs set_focus fire-and-forget (D-01 condition 3)"
key_files:
  created:
    - src/chat/activeTaskBuilder.ts
  modified:
    - supabase/functions/chat-with-jamo/index.ts
    - src/chat/__tests__/activeTaskShape.test.ts
decisions:
  - "Dual-source builder: pure logic lives in src/chat/activeTaskBuilder.ts (vitest-tested, 14.2.4-shape-01) AND is mirrored field-identically inside chat-with-jamo/index.ts because the Deno edge runtime cannot resolve src/ imports at deploy time (per STATE.md constraint). Plan explicitly permitted this when single-source is not viable."
  - "Needs-value flag is the presence of cta_payload.originating_snapshot — no separate boolean threaded from the client."
  - "set_focus write intentionally left fire-and-forget (void); only the new ask_user write is awaited (D-01 condition 3 scope)."
recovery_note: "Original executor agent completed Task 1 (committed 8c1c4d4 RED + 2ad5272 GREEN) then died mid-Task-2 with chat-with-jamo/index.ts modified-but-uncommitted (last message cut off at 'verify... run tests'). Orchestrator verified the uncommitted Task 2 work against all acceptance greps + full test suite, then committed it (87867e7). No work lost."
metrics:
  completed_date: "2026-06-11"
  tasks_completed: 2
  tasks_total: 2
  files_created: 1
  files_modified: 2
---

# Phase 14.2.4 Plan 03: Edge ask_user Needs-Value Dispatch (D-01 + Risk B)

**One-liner:** The forced `ask_user` needs-value dispatch now performs one awaited `active_task` write with a 12-field shape carrying the real resolved section title (D-10) and the originating snapshot + action id (Risk B attribution) — one write doing both the durable-resume and attribution jobs.

---

## Tasks Completed

| Task | Name | Commits | Files |
|------|------|---------|-------|
| 1 | Extract pure shape-builder + un-skip shape-identity test (D-01 cond 1, D-10) | 8c1c4d4 (RED), 2ad5272 (GREEN) | src/chat/activeTaskBuilder.ts, src/chat/__tests__/activeTaskShape.test.ts |
| 2 | Wire awaited ask_user needs-value dispatch + fix set_focus title TODO (D-01 cond 3, D-10, Risk B) | 87867e7 | supabase/functions/chat-with-jamo/index.ts |

---

## Verification

- `npm run test:run` — 280 passed / 21 skipped / 0 failed (main suite green)
- `src/chat/__tests__/activeTaskShape.test.ts` — 4 tests pass (un-skipped; 14.2.4-shape-01 asserts status:'active', stage:'gathering_inputs', section_title !== section_key, originating_snapshot present)
- grep: ask_user case contains `await supabase.from('chat_sessions')` (awaited, not void)
- grep: write reads `cta_payload.originating_snapshot` and passes it to `buildNeedsValueActiveTask`
- grep: write filters by BOTH `.eq('proposal_id'` and `.eq('user_id'` (D-45)
- grep: 0 residual `section_title: toolInput.section_key` (set_focus TODO fixed → resolveSectionTitle); set_focus still uses `void` (fire-and-forget preserved)
- tool_choice forcing + propose_edit advance paths untouched (diff scope = new helpers + ask_user case + set_focus title only)

---

## Deviations from Plan

- **Dual-source builder (anticipated by plan).** `buildNeedsValueActiveTask` + `resolveSectionTitle` exist as a vitest-tested pure module at `src/chat/activeTaskBuilder.ts` AND as a field-identical local mirror inside `chat-with-jamo/index.ts`. The Deno edge runtime cannot import `src/` at deploy time, so a single source is not viable; the plan explicitly allowed replicating the builder while keeping the shape field-identical. The src/chat/ copy is the tested source of truth for shape correctness.

---

## Deferred / Downstream

- **Live verification deferred to Plan 05.** The edge functions are not auto-deployed; AC-1/AC-2/AC-4/AC-5 live behavior (the awaited write actually landing in chat_sessions, ask→answer→propose_edit hop, mid-walkthrough reload) is validated in Plan 05 after `supabase functions deploy`.
- **Client snapshot threading (Plan 04).** The edge reads `cta_payload.originating_snapshot`; the client embeds it at CTA-click in Plan 04. Until Plan 04 lands, no production caller sends the snapshot, so the new branch is dormant (correctly guarded).

---

## Threat Flags

- **T-14.2.4-06 (write targets another user's row)** — mitigated: D-45 double filter (`.eq('proposal_id')` + `.eq('user_id')`) preserved on the awaited write.
- **T-14.2.4-05 (forged snapshot)** — accepted: snapshot holds only finding metadata the same authed user already sees; `.eq('user_id')` constrains writes to the caller's own session row.

---

## Self-Check: PASSED

- [x] src/chat/activeTaskBuilder.ts — EXISTS (pure builder + title resolver)
- [x] src/chat/__tests__/activeTaskShape.test.ts — un-skipped, 4 tests pass
- [x] supabase/functions/chat-with-jamo/index.ts — awaited ask_user write + set_focus title fix
- [x] Commit 8c1c4d4 (RED), 2ad5272 (GREEN), 87867e7 (Task 2) — all EXIST
- [x] `npm run test:run` — 0 failures
