---
phase: 14.2.4-placeholder-resolution-ask-then-fill
plan: "02"
subsystem: edge+types
tags: [wave-1, enum-sync, cta-tool, ask-user, few-shot, D-02, D-03, D-06, AC-3, AC-6]
dependency_graph:
  requires:
    - "Plan 01 Wave 0 enum-sync stub (un-skipped here)"
  provides:
    - "cta_tool 'ask_user' accepted on BOTH sync sites (edge Zod + client union)"
    - "ActiveTask.originating_snapshot field (Risk B foundation)"
    - "Analyzer few-shots route all placeholder/missing findings to ask_user (D-06)"
  affects:
    - "supabase/functions/analyze-proposal-gaps/index.ts"
    - "src/types/chat.ts"
    - "supabase/functions/analyze-proposal-gaps/__tests__/enum-sync.test.ts"
tech_stack:
  added: []
  patterns:
    - "two-site enum widen in one wave (Pitfall 4 — edge Zod + client TS union together)"
    - "detection rule encoded entirely in system-prompt few-shots (D-06 — no isNeedsValue() code branch)"
    - "exported Zod schema imported directly by sidecar vitest suite"
key_files:
  created: []
  modified:
    - supabase/functions/analyze-proposal-gaps/index.ts
    - src/types/chat.ts
    - supabase/functions/analyze-proposal-gaps/__tests__/enum-sync.test.ts
    - vitest.supabase.config.ts
decisions:
  - "vitest.supabase.config.ts widened (test include glob + npm:zod passthrough) to let the AC-3 enum-sync test import the live edge PendingActionSchema — necessary deviation, not in declared files_modified"
  - "All 4 few-shot descriptions confirmed already enumerative (D-07 watch-item 1 satisfied) — no tightening needed"
  - "No conflict/compliance few-shot examples exist in the prompt, so AC-6 leak-guard is structurally satisfied (only the 4 placeholder/missing families present)"
  - "pre-existing prompt-construction.test.ts failures (3, Phase 14.2.2/14.2.3) confirmed unrelated — present at Plan 01 commit before any Plan 02 change"
recovery_note: "Original executor agent died mid-Task-1 (stuck resolving npm:zod under the sidecar vitest config). Orchestrator verified Task 1 work on disk, committed it, then implemented Task 2 inline. Both tasks complete and verified."
metrics:
  completed_date: "2026-06-11"
  tasks_completed: 2
  tasks_total: 2
  files_created: 0
  files_modified: 4
---

# Phase 14.2.4 Plan 02: cta_tool Enum Widen + ask_user Few-Shot Routing

**One-liner:** Widened the `cta_tool` vocabulary to include `'ask_user'` on both sync sites, added `ActiveTask.originating_snapshot`, and rewrote the analyzer's 4 placeholder/missing few-shots so every gap/missing finding routes to `ask_user` — the single data-change that re-routes detection with no control-flow change.

---

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Widen both cta_tool enums + ActiveTask.originating_snapshot + un-skip enum-sync test (D-02, AC-3) | 33c3cd5 | src/types/chat.ts, supabase/functions/analyze-proposal-gaps/index.ts, .../enum-sync.test.ts, vitest.supabase.config.ts |
| 2 | Re-route placeholder + missing few-shots to ask_user (D-06, D-03, AC-6) | dacc064 | supabase/functions/analyze-proposal-gaps/index.ts |

---

## Verification

- `npm run test:run` — 276 passed / 22 skipped / 0 failed (main suite green)
- Sidecar supabase suite — enum-sync AC3-01 (accepts ask_user) + AC3-02 (rejects unknown) both PASS (un-skipped). Remaining 3 failures are pre-existing prompt-construction.test.ts (Phase 14.2.2/14.2.3), confirmed identical at Plan 01 commit.
- grep `index.ts:27` enum contains `'ask_user'`; `chat.ts:190` union contains `'ask_user'`; `export const PendingActionSchema` present
- grep `originating_snapshot?: OriginatingActionSnapshot` present inside ActiveTask
- grep: exactly 4 `"cta_tool":"ask_user"` in few-shot OUTPUT blocks; 0 `propose_edit` remaining in those blocks; D-06 routing line present; valid-values line widened with `"ask_user"`

---

## Deviations from Plan

- **`vitest.supabase.config.ts` modified** (not in declared `files_modified`). The un-skipped AC-3 test imports the live edge `PendingActionSchema` from `index.ts`, which transitively imports `npm:zod`. The sidecar config's deno-specifier stub plugin was stubbing out `npm:zod`; added a passthrough resolving `npm:zod*` to the real `node_modules/zod`, and widened the test `include` glob to cover nested `__tests__/` dirs (`analyze-proposal-gaps/__tests__/**`). Required to execute AC-3 — justified.

---

## Recovery Note

The original `gsd-executor` subagent died mid-Task-1 while resolving the `npm:zod` module-resolution issue above (its final message was cut off mid-debug, no SUMMARY/commits). The orchestrator: (1) spot-checked and found Task 1's substantive edits correct on disk and the enum-sync tests green, (2) committed Task 1, (3) confirmed Task 2 was never started, (4) implemented Task 2 inline per plan + PATTERNS.md, (5) verified and committed. No work lost.

---

## Threat Flags

- **T-14.2.4-02 (enum desync)** — mitigated: both sites widened in this plan; AC3-01/02 assert edge accepts `ask_user` and rejects unknown.
- **T-14.2.4-03 (arbitrary cta_tool)** — mitigated: `z.enum` stays a closed set; AC3-02 (`unknown_tool` → success:false) is the guard.

---

## Self-Check: PASSED

- [x] supabase/functions/analyze-proposal-gaps/index.ts — enum widened, schema exported, 4 few-shots + valid-values + D-06 line updated
- [x] src/types/chat.ts — union widened, ActiveTask.originating_snapshot added
- [x] .../enum-sync.test.ts — AC3-01/02 un-skipped and green
- [x] Commit 33c3cd5 — EXISTS (Task 1)
- [x] Commit dacc064 — EXISTS (Task 2)
- [x] `npm run test:run` — 0 failures
