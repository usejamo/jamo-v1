---
phase: 14.2.4-placeholder-resolution-ask-then-fill
plan: 05
type: execute
wave: 4
depends_on: [02, 03, 04]
files_modified: []
autonomous: false
requirements: [AC-1, AC-2, AC-5, AC-7, AC-8, AC-9]
must_haves:
  truths:
    - "analyze-proposal-gaps and chat-with-jamo are deployed live (the new enum + ask routing + active_task write run in prod, not stale code)"
    - "A placeholder finding's 'Provide info' CTA opens an ask-then-fill walkthrough (asks for the value), not a silent rewrite (AC-1/AC-2)"
    - "A half-completed walkthrough survives reload via active_task / ResumeTaskBanner (AC-5)"
    - "An addressed placeholder does not re-surface; a partial answer fills mapped values and re-surfaces the rest (AC-7/AC-8)"
    - "Deferring (card skip / stop walkthrough) writes no resolved_items and the finding re-surfaces (AC-9 live)"
  artifacts: []
  key_links:
    - from: "local edge code (Plans 02-03)"
      to: "Supabase prod edge functions"
      via: "supabase functions deploy"
      pattern: "functions deploy"
---

<objective>
Deploy the two edge functions and run the manual smoke that only a live deployed edge can
verify. GSD commits edge code but NEVER deploys it (STATE.md: 14.2.3's "no findings" symptom
was a STALE DEPLOYMENT). Without this wave, prod runs the OLD propose_edit routing and every
live AC silently fails. This plan deploys, then walks the human through AC-1/2/5/7/8/9.

Purpose: make the phase real in prod + close the integration-level acceptance criteria that
are not unit-testable (forced ask_user → AskUserCard → answer → propose_edit → ghost accept →
reload → re-analysis).
Output: deployed functions + a completed live smoke checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-VALIDATION.md
@.planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-RESEARCH.md

<interfaces>
Deploy commands (RESEARCH Deployment Note + CONTEXT critical constraint):
  supabase functions deploy analyze-proposal-gaps chat-with-jamo
Supabase project ref (STATE.md): fuuvdcvbliijffogjnwg. Needs SUPABASE_ACCESS_TOKEN or an
interactive `supabase login` — hence autonomous:false. Per MEMORY: `supabase db push` history
diverged here (migrations go via MCP), but FUNCTION deploy is unaffected — use functions deploy.
Manual-only verifications (VALIDATION.md): AC-1/2, AC-5, AC-7, AC-8 require a deployed edge +
live chat on a proposal that has unfilled placeholders.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deploy edge functions (interactive auth)</name>
  <what-built>
    Plans 02-03 changed analyze-proposal-gaps (enum + ask few-shots) and chat-with-jamo
    (ask_user active_task write + title fix). These are committed but NOT deployed — prod runs
    stale code until deployed.
  </what-built>
  <action>
    Operator runs `supabase functions deploy analyze-proposal-gaps chat-with-jamo` (project ref
    fuuvdcvbliijffogjnwg). Deploy is interactive auth — run `supabase login` or export
    SUPABASE_ACCESS_TOKEN if it errors. Deploy BOTH in the single command (Pitfall 4 — prevents a
    prod enum/routing desync). GSD does not auto-deploy edge functions; this is the explicit step.
  </action>
  <how-to-verify>
    Run from the repo root:
    1. `supabase functions deploy analyze-proposal-gaps chat-with-jamo`
       (if it errors with auth: `supabase login` or export SUPABASE_ACCESS_TOKEN, then retry).
    2. Confirm both functions report a new version / "Deployed" with project ref
       fuuvdcvbliijffogjnwg.
    3. Sanity: in the Supabase dashboard (Edge Functions), confirm the updated_at timestamps
       for both functions are now (today).
  </how-to-verify>
  <resume-signal>Type "deployed" once both functions show a fresh deploy, or paste any deploy error.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live ask-then-fill smoke (AC-1/2/5/7/8/9)</name>
  <what-built>
    The full ask-then-fill loop: needs-value CTA forces ask_user, AskUserCard asks for the value
    (with a defer button), the answer drives a propose_edit that fills the placeholder, ghost
    edits accept, resolved_items attributes correctly, and the finding does not re-surface.
  </what-built>
  <action>
    Operator drives the live UI smoke below on a proposal containing unfilled placeholders and
    records pass/fail per AC. No code changes — this task is verification only. Capture any failing
    AC with the observed behavior so a `--gaps` plan can be derived if needed.
  </action>
  <how-to-verify>
    Open a proposal that has unfilled placeholders (e.g. "CRO legal entity name",
    "investigational product name") and run gap analysis so findings appear. Then:
    1. AC-1/AC-2: Click the placeholder finding's CTA — it now reads "Provide info" / "Fill in"
       (not "Fix it"). Confirm Jamo ASKS for the value (a question card appears) rather than
       silently rewriting. Answer it; confirm a propose_edit ghost edit appears with your value;
       Accept; confirm the placeholder is replaced with your actual text.
    2. AC-5 (durable resume): Start another placeholder CTA, get the question, then RELOAD the
       page BEFORE answering. Confirm the ResumeTaskBanner / walkthrough state restores and you
       can answer and complete it.
    3. AC-7 (no re-surface): After accepting a fill, re-run gap analysis; confirm that placeholder
       finding is GONE (resolved_items attribution survived the ask hop).
    4. AC-8 (batched + partial): On a section with multiple placeholders, confirm ONE question
       lists all needed values. Answer only SOME of them (e.g. 3 of 5). Confirm the answered ones
       fill and, on the next analysis, the unanswered ones re-surface.
    5. AC-9 (defer ≠ dismiss): Start a placeholder CTA, then click the card's "I don't have this
       yet" defer button (or Stop walkthrough). Confirm NO "fixed" resolution is written and the
       finding re-surfaces next analysis. Separately confirm queue-Dismiss still suppresses a
       finding (the two paths stay distinct).
  </how-to-verify>
  <resume-signal>Type "verified" if AC-1/2/5/7/8/9 all behave as described, or list any AC that failed with the observed behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Supabase prod | Deploy uses an authenticated Supabase token; pushes new edge code to the live project. |
| Live user input → prod edge | The smoke exercises real user free-text answers through the deployed ask-then-fill path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.2.4-11 | T (Tampering) | Deploying only one of the two functions → enum/routing desync in prod | mitigate | Deploy BOTH in a single `functions deploy analyze-proposal-gaps chat-with-jamo` command; Task 1 verifies both timestamps refreshed before the smoke runs. |
| T-14.2.4-12 | I (Info disclosure) | Smoke run on a real proposal exposes org data | accept | Operator uses their own org's proposal under existing RLS; no new data path. The smoke is read/write within the authed user's scope. |
</threat_model>

<verification>
- Both edge functions show a fresh deploy on project fuuvdcvbliijffogjnwg.
- All six live ACs (AC-1/2/5/7/8/9) confirmed by the human-verify checkpoint.
- Unit ACs (AC-3/4/6/9-unit) were already green in Plans 02-04.
</verification>

<success_criteria>
analyze-proposal-gaps and chat-with-jamo deployed live; the full ask-then-fill loop verified
end-to-end including durable resume, no-re-surface, batched/partial answers, and defer-without-fix.
</success_criteria>

<output>
After completion, create `.planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-05-SUMMARY.md`
</output>
