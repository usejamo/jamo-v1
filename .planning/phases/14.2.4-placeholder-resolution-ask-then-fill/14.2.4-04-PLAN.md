---
phase: 14.2.4-placeholder-resolution-ask-then-fill
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - src/components/AIChatPanel.tsx
  - src/components/chat/AskUserCard.tsx
  - src/hooks/__tests__/useResolvedItemsWriteOnTerminal.askThenFill.test.ts
  - src/components/chat/__tests__/AskUserCard.skip.test.tsx
autonomous: true
requirements: [D-01, D-03, D-07, D-08, D-09, AC-1, AC-2, AC-4, AC-7, AC-8, AC-9]
must_haves:
  truths:
    - "A needs-value (ask_user) CTA forces ask_user AND sets activeSectionKey = action.section_key client-side (D-01 condition 2)"
    - "The enumerative finding description is threaded to the ask turn via message text, not payload (D-07 watch-item 2)"
    - "The originating snapshot is embedded in cta_payload at CTA-click so the edge can persist it (Risk B client half)"
    - "At propose_edit arrival, attribution does a two-step lookup: in-memory Map first (direct propose_edit unchanged), then active_task.originating_snapshot fallback (Risk B read half, AC-4/AC-7)"
    - "AskUserCard renders a defer/skip button ('I don't have this yet') wired to onStopWalkthrough — discards active_task, writes NO resolved_items (D-09, AC-9)"
    - "The propose_edit auto-dismiss guard (:553-557) is NOT changed — it still excludes ask_user (D-09 / Pitfall 3, AC-6 visibility)"
  artifacts:
    - path: "src/components/AIChatPanel.tsx"
      provides: "onCtaClick ask_user branch (focus+snapshot+description), two-step attribution lookup, AskUserCard onSkip wiring"
      contains: "originating_snapshot"
    - path: "src/components/chat/AskUserCard.tsx"
      provides: "optional onSkip prop + defer button"
      contains: "onSkip"
  key_links:
    - from: "AIChatPanel onCtaClick (ask_user branch)"
      to: "handleSendMessage forcedTool='ask_user' + cta_payload.originating_snapshot"
      via: "embed snapshot in payload + append description to message text"
      pattern: "originating_snapshot"
    - from: "AIChatPanel propose_edit arrival"
      to: "activeTask.originating_snapshot"
      via: "two-step fallback when Map returns null"
      pattern: "activeTask\\?\\.originating_snapshot"
    - from: "AskUserCard onSkip"
      to: "onStopWalkthrough (active_task discarded, no resolved_items)"
      via: "reuse existing stop-walkthrough body"
      pattern: "status: 'discarded'"
---

<objective>
The client wave: route needs-value CTAs to ask_user with focus + description + snapshot
(D-01 cond 2, D-07, Risk B client half), read attribution back from active_task at fill time
(Risk B read half — AC-4/AC-7), and add the AskUserCard defer affordance (D-09, AC-9). The
batched-ask (D-08) and partial-resolution (AC-8) behaviors are inherited unchanged from the
existing machinery (verified in RESEARCH Priority 1) — this plan VERIFIES, not rebuilds them.

Purpose: complete the ask→answer→fill→attribute loop on the client; preserve the distinct
defer-vs-dismiss terminal states.
Output: three edit sites in AIChatPanel + one prop in AskUserCard; AC-4/AC-9 unit tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-CONTEXT.md
@.planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-PATTERNS.md
@.planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-RESEARCH.md

<interfaces>
Site 1 onCtaClick — AIChatPanel.tsx:713-718 (CONFIRMED). Current:
  onCtaClick={(action) => { captureSnapshot(ctaSnapshotRef.current, action);
    handleSendMessage(`[Action: ${action.cta_tool}] ${action.title}`, action.cta_payload, action.cta_tool) }}
  Request body field at :435 carries cta_payload; forcedTool is action.cta_tool.

Site 2 attribution at propose_edit arrival — AIChatPanel.tsx:501-507 (CONFIRMED). Current uses
  takeSnapshot(ctaSnapshotRef.current, section_key, 'propose_edit') only.
  takeSnapshot/captureSnapshot/makeCtaKey live in src/chat/ctaSnapshotMap.ts (key = section_key:cta_tool).

Site 3 AskUserCard render — AIChatPanel.tsx:932-963 (CONFIRMED). onStopWalkthrough body at :684-690:
  setActiveTask(null); void supabase.from('chat_sessions').update({active_task:{...activeTask,
    status:'discarded', stage:'discarded', completed_at:...}}).eq('proposal_id',proposalId).eq('user_id',userId)

DO NOT CHANGE — auto-dismiss guard AIChatPanel.tsx:553-557 (predicate cta_tool === 'propose_edit'
already excludes ask_user — Pitfall 3 / D-09). Verify unchanged.

AskUserCard.tsx (45 lines, CONFIRMED) props: { payload, onAnswer, answered }. Single free-text
input + Reply button (D-08: one batched answer → one propose_edit).

OriginatingActionSnapshot shape (ctaSnapshotMap.ts:27-33 / chat.ts:273):
  { id, section_key, finding_type, title, description }.
ActiveTask.originating_snapshot field added in Plan 02.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: onCtaClick ask_user branch + two-step attribution (D-01 cond 2, D-03/D-07, Risk B, AC-4/AC-7)</name>
  <files>src/components/AIChatPanel.tsx, src/hooks/__tests__/useResolvedItemsWriteOnTerminal.askThenFill.test.ts</files>
  <behavior>
    - 14.2.4-AC4: when active_task.originating_snapshot is present and the in-memory Map lookup
      returns null, computeTerminalWrites (or the assembled toolData.originating_action) produces a
      resolved_items entry whose originating_action references the originating finding (non-null,
      correct id/section_key). This is the Risk B read path.
    - Direct propose_edit CTA (Map hit) path is UNCHANGED — Map lookup still wins; fallback only
      fires when Map returns null.
  </behavior>
  <read_first>
    - src/components/AIChatPanel.tsx (lines 495-557 attribution + auto-dismiss, 713-719 onCtaClick, 435 body field)
    - src/chat/ctaSnapshotMap.ts (key shape; captureSnapshot/takeSnapshot)
    - .planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-PATTERNS.md (lines 184-256 — Site 1 + Site 2 exact diffs, 256 invariant)
    - .planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-CONTEXT.md (D-01 cond 2, D-07 watch-item 2, Risk B)
  </read_first>
  <action>
    SITE 1 — onCtaClick (:713-718), add an `action.cta_tool === 'ask_user'` branch (PATTERNS.md 196-221):
    - Keep `captureSnapshot(ctaSnapshotRef.current, action)` (harmless; Map key will be …:ask_user).
    - D-01 condition 2: `setActiveSectionKey(action.section_key)` (set focus client-side — set_focus
      normally does this; use the existing activeSectionKey setter the panel already owns).
    - Risk B client half: build `payloadWithSnapshot = { ...action.cta_payload, originating_snapshot:
      { id: action.id, section_key: action.section_key, finding_type: action.type, title: action.title,
        description: action.description ?? '' } }`.
    - D-07 watch-item 2: thread the enumerative description via MESSAGE TEXT (not payload — the edge
      reads history, not payload, into model context):
      `handleSendMessage(\`[Action: ask_user] ${action.title}: ${action.description ?? ''}\`,
        payloadWithSnapshot, 'ask_user')`.
    - else branch: keep the existing exact call for all non-ask_user tools (unchanged).
    SITE 2 — attribution at propose_edit arrival (:501-507), two-step lookup (PATTERNS.md 240-253):
    - First: `let originatingSnapshot = takeSnapshot(ctaSnapshotRef.current, section_key, 'propose_edit')`
      (UNCHANGED for direct propose_edit CTAs — invariant per PATTERNS.md 256).
    - Then: `if (!originatingSnapshot && activeTask?.originating_snapshot) originatingSnapshot =
      activeTask.originating_snapshot as OriginatingActionSnapshot`.
    - `toolData.originating_action = originatingSnapshot`.
    - Use whatever the panel's active-task state variable is actually named (confirm via read; the
      Site 3 code at :684 references `activeTask`).
    DO NOT modify the auto-dismiss block at :553-557 (it correctly excludes ask_user via its
    `cta_tool === 'propose_edit'` predicate — D-09 / Pitfall 3).
    TEST: un-skip 14.2.4-AC4 in useResolvedItemsWriteOnTerminal.askThenFill.test.ts. Assert that
    with an active_task carrying originating_snapshot and a null Map result, the assembled
    originating_action is non-null and references the finding. Use the existing snap() helper +
    inline supabase mock (STATE.md mock pattern; no renderHook — pure function assertions).
  </action>
  <acceptance_criteria>
    - grep AIChatPanel.tsx: `action.cta_tool === 'ask_user'` branch present in onCtaClick; calls setActiveSectionKey(action.section_key).
    - grep: `originating_snapshot:` embedded in the payload spread; message text uses `${action.description`.
    - grep at :501-507 region: two-step lookup — `takeSnapshot(` first, then `activeTask?.originating_snapshot` fallback guarded by `!originatingSnapshot`.
    - grep: auto-dismiss block still reads `cta_tool === 'propose_edit'` and has NOT added 'ask_user' (AC-6/D-09 guard).
    - 14.2.4-AC4 PASSES (not skipped); direct-propose_edit path test (existing) still green (Map hit unchanged).
    - `npm run test:run` exits 0; `npx tsc --noEmit` clean for AIChatPanel.tsx.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run</automated>
  </verify>
  <done>Needs-value CTA forces ask_user with focus+snapshot+description; attribution falls back to active_task; auto-dismiss untouched; AC-4 green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AskUserCard defer/skip affordance + AC-9 no-write (D-09, AC-9)</name>
  <files>src/components/chat/AskUserCard.tsx, src/components/AIChatPanel.tsx, src/components/chat/__tests__/AskUserCard.skip.test.tsx, src/hooks/__tests__/useResolvedItemsWriteOnTerminal.askThenFill.test.ts</files>
  <behavior>
    - 14.2.4-AC9-card-01: skip button renders when onSkip prop provided.
    - 14.2.4-AC9-card-02: clicking the skip button calls onSkip exactly once.
    - 14.2.4-AC9-card-03: skip button does NOT render when onSkip is undefined.
    - 14.2.4-AC9 (hook): the skip/defer path creates NO pending_edits, so computeTerminalWrites
      produces zero resolved_items writes (no 'fixed' resolution ever written on defer).
  </behavior>
  <read_first>
    - src/components/chat/AskUserCard.tsx (full file — props interface lines 4-8, unanswered branch + Reply button)
    - src/components/AIChatPanel.tsx (lines 684-690 onStopWalkthrough body, 932-963 AskUserCard render site)
    - .planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-PATTERNS.md (lines 258-339, 454-477, 497-515 — onSkip prop, render wiring, discard pattern, card test stubs)
    - .planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-CONTEXT.md (D-09 — defer wording, defer≠dismiss, never write 'fixed')
  </read_first>
  <action>
    1. AskUserCard.tsx — add `onSkip?: () => void` to AskUserCardProps (PATTERNS.md 318-339).
       In the UNANSWERED branch, after the Reply button, render the defer button ONLY when onSkip
       is provided:
       `{onSkip && (<button onClick={onSkip} className="mt-1 text-[10px] text-gray-400
         hover:text-gray-600">I don&apos;t have this yet</button>)}`
       D-09 wording: "I don't have this yet" = DEFER (stays on list). Do NOT use "Skip"/"Dismiss"
       alone (blurs the defer/dismiss distinction).
    2. AIChatPanel.tsx (AskUserCard render :932-963) — pass `onSkip` wired to the existing
       stop-walkthrough behavior (PATTERNS.md 271-287). If onStopWalkthrough is already a named
       function/callback in the panel, pass it directly: `onSkip={activeTask ? onStopWalkthrough :
       undefined}`. Otherwise inline the EXACT discard body from :684-690 (setActiveTask(null) +
       discarded/discarded/completed_at update filtered by proposal_id + user_id). NO resolved_items
       write (D-09 load-bearing invariant). Pass undefined when there is no active_task so the button
       hides outside a walkthrough.
    3. Un-skip the three AskUserCard.skip.test.tsx stubs (render with onSkip → button present;
       fireEvent.click → onSkip called once; no onSkip → button absent). Use @testing-library/react
       render (component test — allowed; the OOM constraint is about renderHook in CONTEXT tests).
    4. Un-skip 14.2.4-AC9 in the hook test: assert computeTerminalWrites with empty pending_edits
       (the defer path creates none) returns []. This is invariant (c).
    Defer vs dismiss stays distinct: do NOT route onSkip through the queue-dismiss handler
    (:720-775); it must use the stop-walkthrough/discard path (no resolved_items, finding re-surfaces).
  </action>
  <acceptance_criteria>
    - grep AskUserCard.tsx: `onSkip?: () => void` in props; `{onSkip &&` guarded button with text `I don&apos;t have this yet`.
    - grep AIChatPanel.tsx AskUserCard render: `onSkip=` wired to the discard/stop-walkthrough path (status: 'discarded'); no resolved_items call in that path.
    - 14.2.4-AC9-card-01/02/03 PASS; 14.2.4-AC9 (hook) PASSES — zero writes on empty edits.
    - grep confirms onSkip path does NOT call the queue-dismiss handler (defer≠dismiss).
    - `npm run test:run` exits 0; `npx tsc --noEmit` clean for both touched components.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run</automated>
  </verify>
  <done>AskUserCard shows a defer button wired to the discard path; no resolved_items written on defer; AC-9 unit tests green; defer≠dismiss preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User free-text answer → propose_edit content | The batched answer becomes proposal content via the existing propose_edit/ghost-accept path (already sanitized + user-reviewed before apply). |
| Client → edge (cta_payload.originating_snapshot) | Snapshot embedded client-side travels to the edge (threat handled in Plan 03's model; here it is constructed). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.2.4-08 | T (Tampering) | User answer injects malicious markup into the section | accept | Answer flows through the existing propose_edit → materializePendingEdits → ghost-accept path which already sanitizes and requires explicit user Accept before apply (RESEARCH fact #5). No new ingestion path. |
| T-14.2.4-09 | R (Repudiation) | Defer silently writes a 'fixed' resolution → "marked done but empty" | mitigate | onSkip uses the discard path (status:'discarded', NO resolved_items); AC-9 hook test asserts zero writes on empty edits — the load-bearing D-09 invariant is unit-guarded. |
| T-14.2.4-10 | I (Info disclosure) | originating_snapshot description leaks across users | accept | Snapshot is built from the user's own finding already rendered in their panel; embedded in their own cta_payload; edge write is user-scoped (D-45, Plan 03). No cross-tenant exposure. |
</threat_model>

<verification>
- `npm run test:run` green; AC-4, AC-9-card-01/02/03, AC-9-hook all pass.
- grep confirms: ask_user branch, two-step attribution, untouched auto-dismiss guard, defer button + discard wiring, defer≠dismiss.
- AC-8 (batched/partial) is inherited (RESEARCH Priority 1) — verified live in Plan 05, no code here.
</verification>

<success_criteria>
Needs-value CTAs force ask_user with client focus, threaded description, and an embedded snapshot;
attribution at fill time falls back to active_task.originating_snapshot; AskUserCard offers a
defer affordance that discards the walkthrough without writing resolved_items; defer and dismiss
remain distinct terminal states.
</success_criteria>

<output>
After completion, create `.planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-04-SUMMARY.md`
</output>
