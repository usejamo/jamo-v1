---
phase: 14.2.4-placeholder-resolution-ask-then-fill
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - supabase/functions/chat-with-jamo/index.ts
  - src/chat/__tests__/activeTaskShape.test.ts
autonomous: true
requirements: [D-01, D-10, AC-4, AC-5]
must_haves:
  truths:
    - "A forced ask_user needs-value dispatch writes active_task with a structurally identical 10-field shape to set_focus (D-01 condition 1)"
    - "The needs-value active_task write is AWAITED, not fire-and-forget (D-01 condition 3)"
    - "active_task.section_title is the REAL resolved title from target_section/other_sections, not section_key (D-10)"
    - "The originating snapshot + source_action_item_id from cta_payload are persisted into active_task.originating_snapshot (Risk B write half)"
    - "set_focus's existing section_title=section_key TODO is fixed on the same pass (D-10 bonus)"
  artifacts:
    - path: "supabase/functions/chat-with-jamo/index.ts"
      provides: "Needs-value ask_user dispatch block mirroring set_focus's active_task write + awaited + snapshot + resolved title"
      contains: "originating_snapshot"
    - path: "src/chat/__tests__/activeTaskShape.test.ts"
      provides: "Un-skipped shape-identity test against an extracted pure shape-builder"
      contains: "14.2.4-shape-01"
  key_links:
    - from: "chat-with-jamo ask_user dispatch"
      to: "chat_sessions.active_task column"
      via: "awaited supabase update filtered by proposal_id + user_id (D-45)"
      pattern: "await supabase\\.from\\('chat_sessions'\\)"
    - from: "cta_payload.originating_snapshot"
      to: "active_task.originating_snapshot"
      via: "read from request body at ask dispatch"
      pattern: "originating_snapshot"
---

<objective>
The edge wave (D-01 + Risk B "one write, both jobs"). When the forced tool is `ask_user`
AND the request carries a needs-value snapshot, mirror `set_focus`'s `active_task` write —
structurally identical 10-field shape (condition 1) — but with three deltas: (a) AWAIT the
write (condition 3 — load-bearing durable resume), (b) `section_title` = the REAL resolved
title (D-10), (c) carry `source_action_item_id` + `originating_snapshot` from `cta_payload`
into the same write (Risk B). Also fix set_focus's existing `section_title = section_key`
TODO at :216 on the same pass (D-10 bonus).

Purpose: one awaited write establishes durable resume state AND attribution that survives
the ask→answer→propose_edit hop and a mid-walkthrough reload.
Output: a new ask_user dispatch block; the existing answer/resume path is reused unchanged
(provenance-agnostic — the edge re-reads active_task identically per fact #3/#7).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-CONTEXT.md
@.planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-PATTERNS.md
@.planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-RESEARCH.md

<interfaces>
set_focus block to MIRROR — chat-with-jamo/index.ts:203-230 (CONFIRMED shape, PATTERNS.md 110-140):
  newTask = { type:'walkthrough', status:'active', section_key, section_title (BUG: =section_key),
    stage:'gathering_inputs', collected_inputs:{}, pending_paragraph_ids:[], accepted_paragraph_ids:[],
    content_hash:'', started_at, last_updated }
  void supabase.from('chat_sessions').update({active_task:newTask}).eq('proposal_id',proposal_id).eq('user_id',user_id)

ask_user dispatch case — chat-with-jamo/index.ts ~:144 (handleAskUser already registered, :16-22).
tool_choice forcing logic — :112-115 (DO NOT CHANGE).
propose_edit advances gathering_inputs→drafting — :177-184 (DO NOT CHANGE).
active_task read (7-day expiry) + prompt injection — :50-82, :90 (reused unchanged).

D-10 title source (CONFIRMED, no new threading) — edge request body already has target_section.title
and other_sections[].title (populated by chatContext.ts:124-125,138). Resolve by matching
toolInput.section_key against target_section then other_sections; fallback to section_key.

Snapshot threading (PATTERNS.md 178, 551-557): client embeds originating_snapshot + action.id in
cta_payload at CTA-click (Plan 04). The edge reads them from the request body's cta_payload at ask
dispatch. OriginatingActionSnapshot shape: { id, section_key, finding_type, title, description }.

ActiveTask type (Plan 02 extended it with originating_snapshot?: OriginatingActionSnapshot).
D-45 pattern: always filter update by BOTH proposal_id AND user_id.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract pure active_task shape-builder + un-skip shape-identity test (D-01 cond 1, D-10)</name>
  <files>supabase/functions/chat-with-jamo/index.ts, src/chat/__tests__/activeTaskShape.test.ts</files>
  <behavior>
    - 14.2.4-shape-01: buildNeedsValueActiveTask({section_key, section_title, action_id, snapshot})
      returns an object with status:'active', stage:'gathering_inputs', and all 12 fields
      (type, status, section_key, section_title, stage, collected_inputs, pending_paragraph_ids,
      accepted_paragraph_ids, content_hash, started_at, last_updated, originating_snapshot).
    - The returned shape is structurally identical to set_focus's 10-field write plus the two
      attribution fields (source_action_item_id, originating_snapshot).
    - section_title equals the passed resolved title, NOT the section_key.
  </behavior>
  <read_first>
    - supabase/functions/chat-with-jamo/index.ts (lines 203-230 — set_focus newTask shape + :216 TODO; lines 50-82 active_task read)
    - src/types/chat.ts (lines 229-248, 273 — ActiveTask + OriginatingActionSnapshot; originating_snapshot field added in Plan 02)
    - .planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-PATTERNS.md (lines 142-179, 432-450 — new block + shape test)
    - .planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-RESEARCH.md (lines 113-181 — field-by-field shape, Priority 3)
  </read_first>
  <action>
    1. In chat-with-jamo/index.ts, extract a PURE helper so the shape is unit-testable
       (VALIDATION invariant b requires an extracted pure shape-builder):
       `export function buildNeedsValueActiveTask(args: { section_key: string; section_title: string;
         action_id?: string; snapshot?: OriginatingActionSnapshot }): ActiveTask` returning the
       12-field object per RESEARCH.md lines 162-178 (status:'active', stage:'gathering_inputs',
       collected_inputs:{}, empty paragraph arrays, content_hash:'', started_at/last_updated = now,
       section_title = args.section_title, source_action_item_id = args.action_id,
       originating_snapshot = args.snapshot). Import ActiveTask + OriginatingActionSnapshot types.
       Note Deno import path constraints (STATE.md: Edge runtime cannot resolve src/lib/ imports at
       deploy time) — keep this helper LOCAL to chat-with-jamo (do not import from src/). The test
       imports the type from src/types/chat for the assertion; replicate the type locally in the
       edge file if a cross-runtime import is not viable, but keep the shape field-identical.
    2. Add a `resolveSectionTitle(sectionKey, target_section, other_sections)` helper:
       returns target_section.title if keys match, else other_sections.find(...).title, else sectionKey.
    3. Un-skip src/chat/__tests__/activeTaskShape.test.ts (14.2.4-shape-01). Assert the 12 fields
       on the builder output. If buildNeedsValueActiveTask cannot be imported into vitest from the
       Deno edge file, replicate the builder as a tested pure function in src/chat/ and have the edge
       call the same logic — but PREFER a single source. Document the chosen approach in the SUMMARY.
  </action>
  <acceptance_criteria>
    - grep `buildNeedsValueActiveTask` in chat-with-jamo/index.ts matches; returns ActiveTask with all 12 fields.
    - grep `resolveSectionTitle` matches.
    - 14.2.4-shape-01 PASSES (not skipped): asserts status:'active', stage:'gathering_inputs', section_title !== section_key when a real title is passed, originating_snapshot present.
    - `npm run test:run` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run</automated>
  </verify>
  <done>Pure shape-builder + title resolver exist; shape-identity test passes; section_title is the real title.</done>
</task>

<task type="auto">
  <name>Task 2: Wire awaited ask_user needs-value dispatch + fix set_focus title TODO (D-01 cond 3, D-10, Risk B)</name>
  <files>supabase/functions/chat-with-jamo/index.ts</files>
  <read_first>
    - supabase/functions/chat-with-jamo/index.ts (lines 144 ask_user case region, 203-230 set_focus block incl :216 TODO, :112-115 forcing logic — read-only)
    - .planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-PATTERNS.md (lines 142-179, 484-495 — await vs void, snapshot threading)
    - .planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-CONTEXT.md (D-01 four conditions, D-10, Risk B "one write both jobs")
  </read_first>
  <action>
    In the `case "ask_user":` dispatch (after `toolResult = handleAskUser(...)`), add the
    needs-value active_task write mirroring set_focus (PATTERNS.md lines 144-173):
    1. Guard: only when `proposal_id && user_id` AND the request carries a needs-value snapshot.
       Read the snapshot + action id from the request body's `cta_payload` (field
       `cta_payload.originating_snapshot` and `cta_payload.originating_snapshot.id`). The PRESENCE
       of `cta_payload.originating_snapshot` IS the needs-value flag — no separate boolean needed.
    2. Resolve the title: `const resolvedTitle = resolveSectionTitle(toolInput.section_key,
       target_section, other_sections)` (D-10 — real title, never section_key).
    3. Build: `const newTask = buildNeedsValueActiveTask({ section_key: toolInput.section_key,
       section_title: resolvedTitle, action_id: snapshot.id, snapshot })`.
    4. AWAIT the write (D-01 condition 3 — NOT void):
       `await supabase.from('chat_sessions').update({ active_task: newTask as unknown as Json })
         .eq('proposal_id', proposal_id).eq('user_id', user_id)` (D-45 — both filters).
    5. `break` (do not fall through). Do NOT change the tool_choice forcing logic (:112-115) or the
       propose_edit gathering_inputs→drafting advance (:177-184).
    6. D-10 BONUS on the same pass: fix set_focus's `section_title: toolInput.section_key` at :216 →
       `section_title: resolveSectionTitle(toolInput.section_key, target_section, other_sections)`.
       Leave set_focus's write fire-and-forget (`void`) — do NOT inherit await there (out of scope
       per D-01 condition 3 / RESEARCH Pitfall 2). Only the new ask_user write is awaited.
    Provenance-agnostic guarantee: do NOT add any continuation-trigger wiring — the answer click
    calls handleSendMessage(text) with tool_choice auto and the edge re-reads active_task per :50-82
    (fact #3/#7). This plan only WRITES active_task; the read path is untouched.
  </action>
  <acceptance_criteria>
    - grep in the ask_user case: `await supabase.from('chat_sessions')` present (awaited, not `void`).
    - grep: the ask_user write reads `cta_payload` / `originating_snapshot` from the request body and passes it to buildNeedsValueActiveTask.
    - grep: the ask_user write filters by BOTH `.eq('proposal_id'` and `.eq('user_id'` (D-45).
    - set_focus block (:216 region) no longer contains `section_title: toolInput.section_key`; now calls resolveSectionTitle; set_focus still uses `void` (not await).
    - tool_choice forcing (:112-115) and propose_edit advance (:177-184) are byte-unchanged (grep diff scope = ask_user case + :216 only).
    - `npm run test:run` exits 0; `deno check supabase/functions/chat-with-jamo/index.ts` (or `deno test`) type-clean.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run</automated>
  </verify>
  <done>Needs-value ask_user dispatch awaits an active_task write with the resolved title + snapshot; set_focus title TODO fixed; read/forcing paths untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client cta_payload → edge | Untrusted JSON from the client carries originating_snapshot + action id; it is written into chat_sessions.active_task JSONB. |
| Edge → chat_sessions DB | active_task update is RLS/user-scoped; must filter by proposal_id AND user_id (D-45). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.2.4-05 | T (Tampering) | Client supplies a forged originating_snapshot for another finding | accept | Snapshot holds only finding metadata the same authed user already sees; worst case is mis-attributing the user's OWN resolved_items entry. No cross-user impact — the write is constrained by `.eq('user_id', user_id)` (D-45) so it can only touch the caller's own session row. |
| T-14.2.4-06 | E (Elevation) | active_task write targets another user's/proposal's row | mitigate | D-45 double filter (proposal_id + user_id) preserved on the new awaited write; acceptance criterion greps both .eq filters. |
| T-14.2.4-07 | D (DoS) | Awaited write adds latency / could hang the ask turn | accept | Single indexed UPDATE on chat_sessions by (proposal_id,user_id); bounded, sub-100ms; await is required for the durability guarantee (D-01 cond 3) and replaces a race, not adds unbounded work. |
</threat_model>

<verification>
- `npm run test:run` green; 14.2.4-shape-01 passes.
- grep confirms: awaited write, D-45 double filter, resolved title in both ask_user and set_focus, forcing/advance logic untouched.
- DEPLOY + live AC-1/2/4/5 verification happens in Plan 05 (edge not auto-deployed).
</verification>

<success_criteria>
The forced ask_user needs-value dispatch performs one awaited active_task write with an
identical-shape 12-field object carrying the real section title and the originating snapshot;
set_focus's title TODO is fixed; the read/resume/forcing paths are unchanged (provenance-agnostic).
</success_criteria>

<output>
After completion, create `.planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-03-SUMMARY.md`
</output>
