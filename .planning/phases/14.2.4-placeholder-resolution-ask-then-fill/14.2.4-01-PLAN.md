---
phase: 14.2.4-placeholder-resolution-ask-then-fill
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/functions/analyze-proposal-gaps/__tests__/enum-sync.test.ts
  - src/hooks/__tests__/useResolvedItemsWriteOnTerminal.askThenFill.test.ts
  - src/chat/__tests__/activeTaskShape.test.ts
  - src/components/chat/__tests__/AskUserCard.skip.test.tsx
autonomous: true
requirements: [AC-3, AC-4, AC-9, D-01, D-02, D-09]
must_haves:
  truths:
    - "Four Wave 0 test stub files exist as named verify targets for AC-3, AC-4, AC-9, and the active_task shape invariant"
    - "npm run test:run exits 0 with the new stubs present (it.skip, no behavior asserted yet)"
  artifacts:
    - path: "supabase/functions/analyze-proposal-gaps/__tests__/enum-sync.test.ts"
      provides: "AC-3 enum-sync test scaffold (edge PendingActionSchema)"
      contains: "14.2.4-AC3"
    - path: "src/hooks/__tests__/useResolvedItemsWriteOnTerminal.askThenFill.test.ts"
      provides: "AC-4 + AC-9 attribution/no-write scaffolds"
      contains: "14.2.4-AC4"
    - path: "src/chat/__tests__/activeTaskShape.test.ts"
      provides: "active_task shape-identity scaffold (invariant b)"
      contains: "14.2.4-shape-01"
    - path: "src/components/chat/__tests__/AskUserCard.skip.test.tsx"
      provides: "AC-9 skip-button render/click scaffold"
      contains: "14.2.4-AC9-card"
  key_links:
    - from: "all four stub files"
      to: "npm run test:run / deno test"
      via: "vitest + deno test discovery"
      pattern: "it\\.skip|describe"
---

<objective>
Create the four Wave 0 Nyquist test stub files from 14.2.4-VALIDATION.md so every
acceptance criterion that is unit-testable has a named verify target BEFORE any
production code changes. Stubs use `it.skip` (the project's mandated stub pattern —
Vite resolves all imports at transform time, so stubs must NOT import not-yet-exported
symbols at module scope) and assert nothing yet.

Purpose: Nyquist compliance — later waves convert these `it.skip` tests to real
assertions. This is the sampling floor for the phase.
Output: 4 test files, `npm run test:run` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-VALIDATION.md
@.planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-PATTERNS.md

<interfaces>
Stub test pattern (STATE.md decision): "Use it.skip (not dynamic imports) for tests
targeting files not yet created — Vite resolves all imports at transform time."
Mock pattern (STATE.md): inline `vi.mock('../../lib/supabase', ...)` — never `() => import(...)`.
No `renderHook` in context tests (OOMs); but `@testing-library/react render` IS used in
AskUserCard component tests (see analog AskUserCard pattern). Keep the skip stub minimal.
Test ID convention (existing): `14.2-B2-01: ...` → use `14.2.4-<AC>-NN: ...`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edge enum-sync stub (AC-3, Deno/vitest)</name>
  <files>supabase/functions/analyze-proposal-gaps/__tests__/enum-sync.test.ts</files>
  <read_first>
    - .planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-PATTERNS.md (lines 343-389 — enum-sync.test.ts exact pattern)
    - supabase/functions/analyze-proposal-gaps/__tests__/analyzeGaps.spec.ts (analog: describe/it Deno-vitest pattern)
  </read_first>
  <action>
    Create the file with a `describe('analyze-proposal-gaps — enum sync (AC-3)', ...)` block
    containing two `it.skip` stubs (NOT yet asserting), exactly per PATTERNS.md lines 358-387:
    - `14.2.4-AC3-01: PendingActionSchema accepts cta_tool: ask_user`
    - `14.2.4-AC3-02: PendingActionSchema rejects unknown cta_tool value`
    Use `it.skip` and do NOT import `PendingActionSchema` at module scope yet (it is not
    exported until Plan 02 — importing it now would fail transform-time resolution). Put the
    intended import + safeParse body in a comment so Plan 02 can un-skip and wire it.
    Header comment: "Wave 0 stub — un-skipped + wired in Plan 02 (enum widen + export const)."
  </action>
  <acceptance_criteria>
    - File exists at the exact path; grep `14.2.4-AC3-01` and `14.2.4-AC3-02` both match.
    - No top-level import of `PendingActionSchema` (grep: zero `import.*PendingActionSchema` outside comments).
    - `npm run test:run` exits 0 (stubs counted as skipped, suite green).
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run</automated>
  </verify>
  <done>enum-sync.test.ts exists with 2 it.skip stubs; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Hook + shape + card stubs (AC-4, AC-9, shape invariant)</name>
  <files>src/hooks/__tests__/useResolvedItemsWriteOnTerminal.askThenFill.test.ts, src/chat/__tests__/activeTaskShape.test.ts, src/components/chat/__tests__/AskUserCard.skip.test.tsx</files>
  <read_first>
    - .planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-PATTERNS.md (lines 393-477 — exact stub bodies for all three files)
    - src/hooks/__tests__/useResolvedItemsWriteOnTerminal.test.ts (analog: vitest + snap() helper pattern)
  </read_first>
  <action>
    Create all three files using `it.skip` stubs exactly as listed in PATTERNS.md:
    1. useResolvedItemsWriteOnTerminal.askThenFill.test.ts — describe
       'useResolvedItemsWriteOnTerminal — ask-then-fill (14.2.4)' with two it.skip:
       `14.2.4-AC4: resolved_items entry has originating_action set when active_task.originating_snapshot present`
       and `14.2.4-AC9: no resolved_items write when onSkip called (no pending_edits created)`.
       You MAY import `computeTerminalWrites` and `OriginatingActionSnapshot` (both already
       exist today) since those imports resolve now.
    2. activeTaskShape.test.ts — describe 'activeTask shape — needs-value ask_user path (14.2.4 D-01)'
       with one it.skip: `14.2.4-shape-01: needs-value ask_user dispatch produces ActiveTask
       with status:active, stage:gathering_inputs, originating_snapshot set`. Import only the
       `ActiveTask` type (type-only, resolves now). Comment lists the 12 required fields to assert
       in Plan 03 against the extracted pure shape-builder.
    3. AskUserCard.skip.test.tsx — describe 'AskUserCard — skip affordance (14.2.4 D-09)' with
       three it.skip: `14.2.4-AC9-card-01` (renders when onSkip provided),
       `14.2.4-AC9-card-02` (calls onSkip on click), `14.2.4-AC9-card-03` (does NOT render when
       onSkip undefined). Import `{ render, screen, fireEvent }` from '@testing-library/react'
       and `{ AskUserCard }` (exists today) — these resolve now; the `onSkip` prop does not exist
       yet, so the assertions stay skipped until Plan 04.
    Each file gets a header comment naming the wiring plan (Plan 03 / Plan 04).
  </action>
  <acceptance_criteria>
    - All three files exist at exact paths.
    - grep `14.2.4-AC4`, `14.2.4-shape-01`, `14.2.4-AC9-card-01` each match.
    - No reference to `originating_snapshot` field access on a typed ActiveTask at module scope
      that would fail tsc (the field arrives in Plan 02) — keep such references inside it.skip bodies/comments.
    - `npm run test:run` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run</automated>
  </verify>
  <done>Three stub files exist; all new tests show as skipped; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only plan; no runtime trust boundary crossed. Tests do not touch network, auth, or user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.2.4-01 | T (Tampering) | Test stubs assert nothing (it.skip) — risk a stub silently never gets wired | accept | Later-wave plans explicitly un-skip each stub; VALIDATION sign-off gates `nyquist_compliant: true`. No runtime surface. |
</threat_model>

<verification>
- `npm run test:run` exits 0 with 4 new files discovered, all new tests skipped.
- All four VALIDATION.md "Wave 0 Requirements" checkboxes have a corresponding file.
</verification>

<success_criteria>
Four stub files exist at the exact VALIDATION.md paths; suite green; no production code touched.
</success_criteria>

<output>
After completion, create `.planning/phases/14.2.4-placeholder-resolution-ask-then-fill/14.2.4-01-SUMMARY.md`
</output>
