---
phase: 16-token-free-demo-mode
plan: 09
type: execute
wave: 5
depends_on: [16-05, 16-08]
files_modified:
  - src/components/demo/DemoResetControl.tsx
  - src/components/demo/DemoRunSurface.tsx
  - src/hooks/useDemoRun.ts
  - src/components/__tests__/DemoResetControl.test.tsx
autonomous: false
requirements: [SPEC-R8]
must_haves:
  truths:
    - "The demo reset control calls demo-reset with the session's demo_run_id"
    - "After reset the surface returns to the 'Add demo RFP' start in-session with NO page reload"
    - "The control is modeless — one button, one behavior (reset THIS run)"
    - "The reset control appears only inside the demo run surface for the demo-org super_admin"
  artifacts:
    - path: "src/components/demo/DemoResetControl.tsx"
      provides: "modeless in-session reset button wired to demo-reset"
      contains: "demo-reset"
  key_links:
    - from: "DemoResetControl"
      to: "demo-reset edge fn"
      via: "supabase.functions.invoke with demo_run_id"
      pattern: "functions.invoke\\('demo-reset'"
    - from: "reset success"
      to: "wizard step 1 (add-RFP)"
      via: "in-session state reset, no reload"
      pattern: "SET_STEP|RESET"
---

<objective>
Wire the real reset (D-10/D-11): a modeless control inside the demo run surface that calls `demo-reset` with the session's `demo_run_id` (from `useDemoRun`), and on success returns the presenter to the "Add demo RFP" start entirely in-session — NO `window.location.reload()`. One button, one behavior. Then run the end-to-end presenter human-verify (the full add-RFP → extract → assumptions → locked template → populate → chat/rewrite → reset loop) since Deno/live E2E is unavailable in this dev sandbox.

Purpose: Presenters can re-run mid-call without a stale/half-deleted proposal or a page reload; the control is unambiguous under the shared login (run-scoped, not account-scoped).
Output: reset control + in-session return-to-start + component test + presenter verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-token-free-demo-mode/16-SPEC.md

<interfaces>
From src/hooks/useDemoRun.ts (Plan 16-08): exposes `demo_run_id` for the active run and a way to reset local state back to step 1 (add-RFP). Add/confirm a `resetToStart()` that dispatches the wizard back to step 1 and clears the current proposal_id/demo_run_id WITHOUT reloading.
From src/lib/supabase: `supabase.functions.invoke('demo-reset', { body: { demo_run_id } })` → `{ data: { ok }, error }`.
D-11: drop the old `window.location.reload()` pattern entirely. D-10: pass the session's demo_run_id; do NOT infer "current run" from the account.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DemoResetControl wired to demo-reset (run-scoped, modeless)</name>
  <read_first>
    - src/hooks/useDemoRun.ts (demo_run_id + resetToStart from Plan 16-08)
    - .planning/phases/16-token-free-demo-mode/16-CONTEXT.md D-10/D-11/D-12 (run-scoped id, no reload, no bulk cleanup here)
    - src/components/demo/DemoRunSurface.tsx (where to mount the control)
  </read_first>
  <action>
    Create src/components/demo/DemoResetControl.tsx: a single button ("Reset demo") that, on click, calls `supabase.functions.invoke('demo-reset', { body: { demo_run_id } })` with the active run's `demo_run_id` from useDemoRun. On success, call `resetToStart()` (in-session return to the "Add demo RFP" step) — NO `window.location.reload()`, NO `sessionStorage.clear()` reset-by-reload. Disable while in flight; surface any error. The control has ONE behavior (reset THIS run) — no modes, no bulk-cleanup option (D-12: bulk cleanup is the sweep only).
    Mount DemoResetControl inside DemoRunSurface (only rendered there, for the demo-org super_admin) — never in the global Sidebar.
    In useDemoRun, ensure `resetToStart()` exists and clears proposal_id + demo_run_id and dispatches the wizard to step 1 without a reload.
  </action>
  <verify>
    <automated>grep -q "functions.invoke('demo-reset'" src/components/demo/DemoResetControl.tsx && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "functions.invoke('demo-reset'" src/components/demo/DemoResetControl.tsx` succeeds
    - `grep -q "demo_run_id" src/components/demo/DemoResetControl.tsx` succeeds (run-scoped id passed)
    - `grep -q "window.location.reload" src/components/demo/DemoResetControl.tsx src/hooks/useDemoRun.ts` returns nothing (D-11)
    - `grep -q "resetToStart" src/hooks/useDemoRun.ts` succeeds
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>A modeless reset control calls demo-reset with the session demo_run_id and returns to add-RFP in-session with no reload.</done>
</task>

<task type="auto">
  <name>Task 2: Component test for reset behavior</name>
  <read_first>
    - src/components/__tests__/ (inline supabase mock pattern per STATE.md)
    - .planning/STATE.md "Test mock pattern" + "No renderHook in context tests" active decisions
  </read_first>
  <action>
    Create src/components/__tests__/DemoResetControl.test.tsx (Vitest, inline `vi.mock('../../lib/supabase', ...)`): assert (a) clicking the control invokes `functions.invoke` with `'demo-reset'` and the provided `demo_run_id`; (b) on a successful mock response, `resetToStart` is called (pass a spy) and no `window.location.reload` occurs; (c) the button disables during the in-flight call. Avoid renderHook (OOM per STATE.md) — test the component with a mocked useDemoRun or props.
  </action>
  <verify>
    <automated>npm run test:run -- DemoResetControl</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run -- DemoResetControl` exits 0
    - Test asserts the invoke target is `'demo-reset'` and the body carries `demo_run_id`
    - Test asserts resetToStart is called and reload is not
  </acceptance_criteria>
  <done>Passing test covers the invoke target, run-scoped id, in-session reset, and no reload.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Presenter end-to-end verification (live)</name>
  <read_first>
    - .planning/phases/16-token-free-demo-mode/16-SPEC.md §"Acceptance Criteria" (full list)
    - .planning/STATE.md memory local-app-debug-setup (localhost:5173 login) + demo-deploy-signups-disabled
  </read_first>
  <what-built>The complete demo path: seed (16-02), edge fns (16-03/04/05/06), capture UI (16-07), run surface (16-08), and this reset control — all deployed/applied.</what-built>
  <how-to-verify>
    Log in as the demo-org super_admin presenter (from 16-02). Then:
    1. On a demo-org proposal, click "Save as demo fixture" → confirm a version is returned (capture, Req 2).
    2. Open the demo run surface → "Add demo RFP" → confirm the fixture's RFP fields appear (extraction step), then assumptions appear (Req 4), the standard template is pre-selected + locked (Req 4/D-02), and Generate populates sections one-by-one with a visible per-section delay and NO token streaming (Req 4/D-03).
    3. In the populated proposal, run a chat query and a rewrite → confirm real RFP + regulatory citations appear (Req 3/6) and behavior matches a real draft.
    4. Click "Reset demo" → confirm the proposal + rows are gone (no orphaned document rows), the shared RFP file remains, and the UI returns to "Add demo RFP" with NO page reload (Req 8/D-11).
    5. Confirm two concurrent runs (two tabs) get independent proposals (Req 5).
    Verify via Supabase MCP after reset: `select count(*) from proposal_documents where proposal_id is null and org_id = <demo org>` did not grow (Pitfall 1).
  </how-to-verify>
  <verify>
    <automated>echo "MISSING — live presenter E2E (Deno/live unavailable in dev sandbox per repo contingency); human-verify checkpoint"</automated>
  </verify>
  <acceptance_criteria>
    - Capture → run → populate → chat/rewrite → reset loop works end-to-end
    - Reset leaves no orphaned proposal_documents/document_extracts and retains the shared RFP file
    - No page reload on reset; template locked; no fake streaming; concurrent runs isolated
  </acceptance_criteria>
  <resume-signal>Type "verified" once the full presenter loop passes, or describe any failing acceptance item.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| reset control → demo-reset | untrusted demo_run_id (client-held, server-verified — D-10) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-31 | Tampering | reset control passing an arbitrary demo_run_id | mitigate | server triple-guard in demo-reset (16-05 T-16-18) verifies demo org + draft + demo_runs membership; UI just forwards the session id |
| T-16-32 | Denial of Service | old sessionStorage.clear()+reload pattern re-introduced | mitigate | acceptance grep asserts no window.location.reload in the control/hook (D-11) |
| T-16-33 | Elevation of Privilege | reset control exposed outside the demo surface | mitigate | rendered only inside DemoRunSurface for the demo-org super_admin; global Sidebar control was removed in 16-07 |
</threat_model>

<verification>
- `npm run test:run -- DemoResetControl` green
- Grep: demo-reset invoke with demo_run_id; no window.location.reload
- Live presenter E2E (Task 3) passes all SPEC acceptance items
</verification>

<success_criteria>
The presenter has a modeless, run-scoped reset that hard-deletes the current run and returns to add-RFP in-session with no reload; the full demo loop verifies live against the SPEC acceptance criteria.
</success_criteria>

<output>
After completion, create `.planning/phases/16-token-free-demo-mode/16-09-SUMMARY.md`.
</output>
