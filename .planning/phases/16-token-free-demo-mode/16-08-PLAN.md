---
phase: 16-token-free-demo-mode
plan: 08
type: execute
wave: 4
depends_on: [16-04]
files_modified:
  - src/components/demo/DemoRunSurface.tsx
  - src/hooks/useDemoRun.ts
  - src/components/wizard/Step4Generate.tsx
  - src/components/__tests__/DemoRunSurface.test.tsx
  - src/__tests__/no-demo-branch-below-population.test.ts
autonomous: true
requirements: [SPEC-R4, SPEC-R6]
must_haves:
  truths:
    - "A super_admin/demo-org presenter can drive add-RFP → extract fields → extract assumptions → template-locked → populate reusing the real wizard"
    - "The standard template card is pre-selected AND locked (cannot pick another) in demo mode"
    - "Sections fill one-by-one from the fixture-populated rows with a fixed per-section delay, no fake token streaming"
    - "No demo-aware conditional exists in generate-proposal-section / retrieve-context / chat / rewrite / regenerate / export"
  artifacts:
    - path: "src/hooks/useDemoRun.ts"
      provides: "demo driver: calls demo-run-start, holds proposal_id + demo_run_id, paces reveal"
      contains: "demo-run-start"
    - path: "src/__tests__/no-demo-branch-below-population.test.ts"
      provides: "Req 6 negative guard test"
      contains: "is_demo"
  key_links:
    - from: "useDemoRun"
      to: "demo-run-start edge fn"
      via: "supabase.functions.invoke"
      pattern: "functions.invoke\\('demo-run-start'"
    - from: "Step4Generate (demo mode)"
      to: "standard template card"
      via: "pre-select + disabled lock"
      pattern: "demoMode|disabled"
---

<objective>
Build the presenter run surface (D-01/D-02/D-03): a demo driver that reuses the real `ProposalCreationWizard` + Step2/3/4, sourcing everything from `demo-run-start`. It presents add-RFP, surfaces the fixture's extracted RFP fields + assumptions (already materialized server-side, no live extract calls), pre-selects and LOCKS the standard template (cannot choose another / cannot pick a template with no fixture), and populates sections one-by-one from the already-written `proposal_sections` rows with a fixed per-section delay and NO fake token streaming. All demo branching lives ABOVE the population step. Also lands the Req 6 invariant guard: an automated negative test asserting no `is_demo`/demo branch exists below the population boundary.

Purpose: The demo is visually indistinguishable from a live run but deterministic and near-instant; below population everything is unmodified production code.
Output: demo run surface + driver hook + template lock + paced reveal + Req 6 guard test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-token-free-demo-mode/16-SPEC.md
@.planning/phases/16-token-free-demo-mode/16-RESEARCH.md

<interfaces>
From src/hooks/useProposalGeneration.ts §322-349: a Supabase Realtime subscription on `proposal_sections` dispatches `SECTION_COMPLETE` when a row is `status='complete'` with content. Since demo-run-start writes all rows `status='complete'` up front, the demo reveal should dispatch `SECTION_COMPLETE` per section on a fixed timer sourcing content from those rows — NOT stream from generate-proposal-section.
From src/components/ProposalCreationWizard.tsx: reducer actions incl. `SET_STEP`, `SET_PROPOSAL_ID`, `SET_ASSUMPTIONS`, `START_GENERATION`, `SECTION_COMPLETE`; wizard eagerly creates a draft on step 1 (demo mode must instead adopt the demo-run-start proposal_id, not create a second draft).
From src/components/wizard/Step4Generate.tsx §26-100: `TemplateSelector` renders cards; `onSelect(id)` toggles selection; `is_default` marks the standard template. Add a `demoMode` prop that pre-selects the `is_default` template and renders every card `disabled`/non-clickable (pointer-events-none + aria-disabled) so only the standard template shows selected and none can be changed (D-02).
From src/lib/supabase: `supabase.functions.invoke('demo-run-start', { body: { template_id } })` → `{ data: { proposal_id, demo_run_id } }`.
Standard template UUID: `00000000-0000-0000-0000-000000000001`.
Per-section reveal delay (D-03, Claude's discretion): a few hundred ms (e.g. `DEMO_SECTION_DELAY_MS = 350`), tunable; no token streaming.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Req 6 negative-branch guard test (write first — invariant)</name>
  <read_first>
    - .planning/phases/16-token-free-demo-mode/16-SPEC.md Req 6 + §"What is FAKED vs REAL" (population-step boundary)
    - .planning/phases/16-token-free-demo-mode/16-RESEARCH.md §"Validation Architecture" Req 6 row (negative grep)
  </read_first>
  <action>
    Create src/__tests__/no-demo-branch-below-population.test.ts (Vitest) that reads the source of the below-population production paths and asserts NONE contain a demo-aware conditional. Read each of: supabase/functions/generate-proposal-section/index.ts, supabase/functions/retrieve-context/index.ts, supabase/functions/chat-with-jamo/index.ts, supabase/functions/section-ai-action/index.ts, src/lib/exportDocx.ts. For each, assert the content does NOT match `/\b(is_demo|demo_run|demo_fixture|demoMode)\b/`. Use `fs.readFileSync` (no Deno). This should PASS immediately (no branches exist today) and acts as a regression fence for the whole phase.
  </action>
  <verify>
    <automated>npm run test:run -- no-demo-branch-below-population</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run -- no-demo-branch-below-population` exits 0
    - The test file references all five below-population paths (generate-proposal-section, retrieve-context, chat-with-jamo, section-ai-action, exportDocx)
    - The forbidden-pattern set includes `is_demo`, `demo_run`, `demo_fixture`, `demoMode`
  </acceptance_criteria>
  <done>An automated guard proves no demo branch exists below the population step; passes on current code.</done>
</task>

<task type="auto">
  <name>Task 2: useDemoRun driver + DemoRunSurface (reuse wizard, paced reveal)</name>
  <read_first>
    - src/components/ProposalCreationWizard.tsx §1-90 (reducer + step flow) and §139-159 (eager draft creation to bypass in demo mode)
    - src/hooks/useProposalGeneration.ts §315-349 (SECTION_COMPLETE dispatch + realtime shape to mirror for the paced fill)
    - src/components/wizard/Step2DocumentUpload.tsx §1-40 (DB-backed polling — demo relies on the server-materialized proposal_documents row)
    - .planning/phases/16-token-free-demo-mode/16-CONTEXT.md D-01/D-03
  </read_first>
  <action>
    Create src/hooks/useDemoRun.ts: a driver that (a) starts a run via `supabase.functions.invoke('demo-run-start', { body: { template_id: '00000000-0000-0000-0000-000000000001' } })`, storing `proposal_id` + `demo_run_id` in state (expose both; `demo_run_id` is consumed by the reset control in Plan 16-09); (b) drives the wizard steps (add-RFP → the fixture-backed Step2 doc-extraction view → Step3 assumptions → Step4) using the returned proposal_id — do NOT create a second draft (demo mode adopts the server proposal_id instead of the wizard's eager step-1 draft); (c) paces the populate: for each section (ordered by position), after `DEMO_SECTION_DELAY_MS` dispatch `SECTION_COMPLETE` with the section's already-written content, reusing the existing reducer/reveal plumbing — NO call to generate-proposal-section, NO simulated token streaming.
    Create src/components/demo/DemoRunSurface.tsx: the entry surface (rendered behind a super_admin/demo-org gate — reuse `useAuth().profile.role === 'super_admin'`) that mounts the reused wizard components wired to useDemoRun. Keep ALL demo-specific logic in useDemoRun/DemoRunSurface — never inside the shared generation/retrieval/chat code.
  </action>
  <verify>
    <automated>grep -q "functions.invoke('demo-run-start'" src/hooks/useDemoRun.ts && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "functions.invoke('demo-run-start'" src/hooks/useDemoRun.ts` succeeds
    - `grep -q "demo_run_id" src/hooks/useDemoRun.ts` succeeds (exposed for reset)
    - `grep -q "SECTION_COMPLETE" src/hooks/useDemoRun.ts` succeeds AND `grep -qi "generate-proposal-section\|streamSection\|token" src/hooks/useDemoRun.ts` returns nothing (no fake streaming / no real generation call)
    - `grep -qi "DEMO_SECTION_DELAY" src/hooks/useDemoRun.ts` succeeds (fixed per-section delay)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>The demo driver starts a run, adopts the server proposal_id, and paces section reveal from pre-written rows with no real generation or fake streaming.</done>
</task>

<task type="auto">
  <name>Task 3: Standard-template pre-select + lock in Step4Generate (D-02) + tests</name>
  <read_first>
    - src/components/wizard/Step4Generate.tsx §26-100 (TemplateSelector, renderCard, onSelect)
    - .planning/phases/16-token-free-demo-mode/16-CONTEXT.md D-02 (pre-select + lock)
  </read_first>
  <action>
    Add a `demoMode?: boolean` prop threaded to `Step4Generate` / `TemplateSelector`. When `demoMode` is true: force `selectedTemplateId` to the `is_default` template, render every card with `aria-disabled`, `pointer-events-none`, and no `onClick` (or an onClick no-op), and visually mark the standard card selected. The presenter can point at it but cannot change it, and cannot select a template that has no fixture. Do not alter the non-demo (default) behavior.
    Create src/components/__tests__/DemoRunSurface.test.tsx (Vitest, inline supabase mock per STATE.md pattern): (a) in demoMode the standard template card is rendered selected and disabled and clicking another card does NOT change selection; (b) mounting the surface calls `functions.invoke('demo-run-start')`. Use `it.skip` for any piece requiring components not yet importable, per repo convention.
  </action>
  <verify>
    <automated>npm run test:run -- DemoRunSurface</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "demoMode" src/components/wizard/Step4Generate.tsx` succeeds
    - `grep -qi "pointer-events-none\|aria-disabled\|disabled" src/components/wizard/Step4Generate.tsx` succeeds (lock)
    - `npm run test:run -- DemoRunSurface` exits 0 with the lock + invoke assertions
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>In demo mode the standard template is pre-selected and locked; tests cover the lock and the run-start invocation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| presenter UI → demo-run-start | UI gate cosmetic; server authoritative |
| demo driver ↔ shared generation/retrieval code | must stay isolated (no demo branch crosses the population boundary) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-28 | Tampering | demo-aware conditional leaking into shared generation/retrieval/chat/export | mitigate | Req 6 negative-branch guard test (Task 1) fences all five below-population paths; all demo logic isolated in useDemoRun/DemoRunSurface |
| T-16-29 | Information Disclosure | demo presenter picking a non-standard template with no fixture → blank/undefined run | mitigate | template pre-select + hard lock (D-02); demo-run-start also rejects non-default template server-side (16-04) |
| T-16-30 | Elevation of Privilege | non-super_admin reaching the demo surface | mitigate | surface gated on `profile.role==='super_admin'` (cosmetic) + demo-run-start server gate (authoritative) |
</threat_model>

<verification>
- `npm run test:run -- no-demo-branch-below-population` green (Req 6)
- `npm run test:run -- DemoRunSurface` green (template lock + run-start invoke)
- Grep: no fake streaming / no generate-proposal-section call in the driver
- `npm run build` succeeds
</verification>

<success_criteria>
The presenter surface reuses the real wizard, starts a real isolated run via demo-run-start, locks the standard template, and reveals sections on a fixed delay with no fake streaming — and an automated guard proves no demo branch exists below the population step.
</success_criteria>

<output>
After completion, create `.planning/phases/16-token-free-demo-mode/16-08-SUMMARY.md`.
</output>
