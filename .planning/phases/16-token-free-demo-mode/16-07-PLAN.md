---
phase: 16-token-free-demo-mode
plan: 07
type: execute
wave: 3
depends_on: [16-03]
files_modified:
  - src/pages/ProposalDetail.tsx
  - src/components/SaveAsDemoFixtureButton.tsx
  - src/components/Sidebar.tsx
  - src/components/ProposalContentsSidebar.tsx
  - src/components/__tests__/SaveAsDemoFixture.test.tsx
autonomous: true
requirements: [SPEC-R2]
must_haves:
  truths:
    - "A super_admin sees a 'Save as demo fixture' action on the proposal detail page; non-super_admins do not"
    - "The action invokes demo-capture-fixture with the current proposal id"
    - "The vestigial 'Reset Demo' button and both 'jamo Demo v0.1.0' labels are gone for all users"
  artifacts:
    # Execution deviation (16-07 SUMMARY): the capture action was extracted into
    # its own component per Task 3's escape hatch — ProposalDetail cannot be
    # mounted in a test. ProposalDetail renders it behind the super_admin gate.
    - path: "src/components/SaveAsDemoFixtureButton.tsx"
      provides: "super_admin + demo-org-gated capture action wired to demo-capture-fixture"
      contains: "demo-capture-fixture"
    - path: "src/pages/ProposalDetail.tsx"
      provides: "renders the capture action behind the super_admin gate"
      contains: "SaveAsDemoFixtureButton"
  key_links:
    - from: "SaveAsDemoFixtureButton (rendered by ProposalDetail)"
      to: "demo-capture-fixture edge fn"
      via: "supabase.functions.invoke"
      pattern: "functions.invoke\\(\\s*'demo-capture-fixture'"
---

<objective>
Add the capture entry point (D-04): a super_admin-only "Save as demo fixture" action on the proposal detail page that invokes `demo-capture-fixture` with the current proposal id, surfacing the returned version. Simultaneously remove the vestigial pre-Supabase demo controls (D-09): the "Reset Demo" button + "jamo Demo v0.1.0" label in `Sidebar.tsx` and the duplicate "jamo Demo v0.1.0" label in `ProposalContentsSidebar.tsx`, for all users. The real reset lives only inside the demo run surface (Plan 16-09).

Purpose: Capture is how fixtures are created from a demo-org proposal; the old sessionStorage-clearing "Reset Demo" control (zero DB deletes) is dead code that must not be confused with the new run-scoped reset.
Output: capture action wired + 3 vestigial controls removed + component test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-token-free-demo-mode/16-SPEC.md

<interfaces>
From src/context/AuthContext: `useAuth()` exposes `profile` with `profile.role` ('super_admin'|'admin'|'user'). Gate the capture action on `profile?.role === 'super_admin'` (cosmetic — the edge fn is authoritative).
From src/lib/supabase: `supabase.functions.invoke('demo-capture-fixture', { body: { source_proposal_id } })` returns `{ data: { fixture_id, version }, error }`.
From ProposalDetail.tsx: the proposal id comes from `useParams()`; `useProposals()` provides the current proposal. Place the action near the existing header/StatusSelector area (StatusSelector imported at line 41).
Removal targets:
- src/components/Sidebar.tsx §119-132: the entire `{!collapsed && (<>...jamo Demo v0.1.0... Reset Demo button...</>)}` block (sessionStorage.clear()+reload).
- src/components/ProposalContentsSidebar.tsx §99-102: the footer `<div>` containing `<p>jamo Demo v0.1.0</p>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Super_admin-only "Save as demo fixture" action on ProposalDetail</name>
  <read_first>
    - src/pages/ProposalDetail.tsx §1-70 (imports, useAuth, useParams, header area) and the StatusSelector render site
    - src/context/AuthContext.tsx (profile.role shape)
    - .planning/phases/16-token-free-demo-mode/16-CONTEXT.md D-04
  </read_first>
  <action>
    In src/pages/ProposalDetail.tsx, add a "Save as demo fixture" button rendered only when `profile?.role === 'super_admin'` (use the existing `useAuth()`), placed in the proposal header/actions area near StatusSelector. On click: call `supabase.functions.invoke('demo-capture-fixture', { body: { source_proposal_id: <current proposal id from useParams/proposal> } })`; on success show the returned `version` (a toast or inline confirmation, matching existing toast patterns in the file); on error surface the message. Disable the button while in flight. Do not add any capture logic to non-super_admin render paths.
  </action>
  <verify>
    <automated>grep -q "functions.invoke('demo-capture-fixture'" src/pages/ProposalDetail.tsx && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "functions.invoke('demo-capture-fixture'" src/pages/ProposalDetail.tsx` succeeds
    - `grep -q "role === 'super_admin'" src/pages/ProposalDetail.tsx` succeeds (cosmetic gate on the action)
    - `grep -q "Save as demo fixture" src/pages/ProposalDetail.tsx` succeeds
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>A super_admin-only capture action on ProposalDetail invokes demo-capture-fixture with the proposal id and reports the new version.</done>
</task>

<task type="auto">
  <name>Task 2: Remove vestigial "Reset Demo" button + both demo labels (D-09)</name>
  <read_first>
    - src/components/Sidebar.tsx §108-135 (the `{!collapsed && (<>...Reset Demo...</>)}` block)
    - src/components/ProposalContentsSidebar.tsx §99-102 (footer label div)
  </read_first>
  <action>
    In src/components/Sidebar.tsx, delete the entire block at §119-132 (the `jamo Demo v0.1.0` label + the "Reset Demo" button whose onClick is `sessionStorage.clear(); window.location.reload()`). Remove any now-unused imports/vars it referenced.
    In src/components/ProposalContentsSidebar.tsx, delete the footer `<div className="p-4 border-t border-gray-200 shrink-0">` at §99-102 containing `<p>jamo Demo v0.1.0</p>` (or, if the border-t footer is structurally needed elsewhere, remove just the demo-label `<p>` — prefer removing the whole vestigial footer if it holds nothing else).
    Do not introduce a replacement reset control here (the real one is Plan 16-09, inside the run surface only).
  </action>
  <verify>
    <automated>grep -rq "Reset Demo\|jamo Demo v0.1.0" src/components/Sidebar.tsx src/components/ProposalContentsSidebar.tsx && echo FOUND || echo CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "Reset Demo" src/components/Sidebar.tsx` returns 0
    - `grep -rc "jamo Demo v0.1.0" src/components/Sidebar.tsx src/components/ProposalContentsSidebar.tsx` returns 0
    - `grep -q "sessionStorage.clear(); window.location.reload()" src/components/Sidebar.tsx` returns nothing
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Both vestigial demo labels and the sessionStorage-clearing "Reset Demo" button are removed; build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Component test for the capture action gate</name>
  <read_first>
    - src/components/__tests__/ (existing component-test conventions; happy-dom, inline supabase mock per STATE.md test-mock pattern)
    - .planning/STATE.md "Test mock pattern" + "vitest pool" active decisions
  </read_first>
  <action>
    Create src/components/__tests__/SaveAsDemoFixture.test.tsx (Vitest) asserting: (a) the "Save as demo fixture" action is NOT rendered for a `profile.role='user'` / `'admin'`; (b) it IS rendered for `role='super_admin'`; (c) clicking it calls `supabase.functions.invoke` with `'demo-capture-fixture'` and the proposal id. Use the repo's inline `vi.mock('../../lib/supabase', ...)` pattern (NOT dynamic import — OOM per STATE.md). If ProposalDetail is too heavy to mount in isolation, extract the capture action into a small `SaveAsDemoFixtureButton` component and test that.
  </action>
  <verify>
    <automated>npm run test:run -- SaveAsDemoFixture</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run -- SaveAsDemoFixture` exits 0
    - Test asserts non-super_admin does NOT see the action and super_admin does
    - Test asserts the invoke target is `'demo-capture-fixture'`
  </acceptance_criteria>
  <done>Passing component test covers the role gate and the invoke target for the capture action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser UI → demo-capture-fixture | UI gate is cosmetic; server is authoritative |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-26 | Elevation of Privilege | non-super_admin seeing/using capture affordance | mitigate | render only for `role==='super_admin'`; the demo-capture-fixture server gate (16-03) is the real enforcement (Access-control point 5) |
| T-16-27 | Information Disclosure | capturing a real-client proposal via the UI | mitigate | server asserts demo-org source (16-03 T-16-09); UI does not bypass it |
</threat_model>

<verification>
- `npm run test:run -- SaveAsDemoFixture` green
- Grep: capture invoke present + gated; zero "Reset Demo"/"jamo Demo v0.1.0" remaining
- `npm run build` succeeds
</verification>

<success_criteria>
Super_admins have a working capture action on ProposalDetail; the vestigial demo controls are fully removed; build + tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/16-token-free-demo-mode/16-07-SUMMARY.md`.
</output>
