---
phase: 16-token-free-demo-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260721000001_demo_fixture_tables.sql
  - supabase/migrations/20260721000002_clone_demo_fixture_chunks_rpc.sql
  - src/types/database.types.ts
autonomous: false
requirements: [SPEC-R2, SPEC-R3, SPEC-R5, SPEC-R8]
must_haves:
  truths:
    - "The 5 demo tables exist in the live DB with super_admin-only RLS"
    - "A fixture can hold one active version per template (partial unique index)"
    - "clone_demo_fixture_chunks copies fixture RFP chunks into chunks under a fresh proposal_id with no model call"
    - "Deleting a demo proposal cascade-removes its cloned chunks (no orphaned embeddings)"
  artifacts:
    - path: "supabase/migrations/20260721000001_demo_fixture_tables.sql"
      provides: "demo_fixtures, demo_fixture_sections, demo_fixture_assumptions, demo_fixture_rfp_chunks, demo_runs + RLS"
      contains: "create table"
    - path: "supabase/migrations/20260721000002_clone_demo_fixture_chunks_rpc.sql"
      provides: "clone_demo_fixture_chunks SECURITY DEFINER RPC"
      contains: "insert into chunks"
  key_links:
    - from: "clone_demo_fixture_chunks"
      to: "chunks"
      via: "INSERT ... SELECT from demo_fixture_rfp_chunks"
      pattern: "insert into chunks.*select"
    - from: "demo_runs.proposal_id"
      to: "proposals.id"
      via: "FK ON DELETE CASCADE"
      pattern: "references proposals\\(id\\) on delete cascade"
---

<objective>
Create the 5 demo-mode tables (all demo-org-scoped, super_admin-only RLS), the versioned-fixture constraints, and the `clone_demo_fixture_chunks` RPC that replays pre-computed RFP embeddings into `chunks` under a fresh `proposal_id` at run time with zero model calls. Then apply both migrations to the live hosted project via the Supabase MCP (`supabase db push` is diverged here) and regenerate committed TS types.

Purpose: This is the data foundation every other Phase 16 plan builds on — capture writes fixtures, run-start reads them + clones chunks, reset/sweep delete by `demo_runs`.
Output: 2 committed migration files applied live + regenerated `database.types.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-token-free-demo-mode/16-SPEC.md
@.planning/STATE.md

<interfaces>
<!-- Exact column/constraint facts extracted from the repo. Executor: use these verbatim; no exploration needed. -->

From supabase/migrations/20260320000015_chunks_table.sql + 20260710000001_chunks_proposal_scope_columns.sql:
- `chunks` columns for clone: `org_id uuid, doc_type text CHECK IN ('regulatory','proposal'), proposal_id uuid REFERENCES proposals(id) ON DELETE CASCADE, source text, content text, embedding extensions.vector(1536), metadata jsonb`.
- `chunks.search_vector` is maintained by a BEFORE INSERT/UPDATE trigger — MUST NOT be in the clone INSERT column list.
- pgvector type notation in this repo is `extensions.vector(1536)` (extension installed in `extensions` schema), NOT `vector(1536)`.

From supabase/migrations/20260305000012_rls_helper_functions.sql:
- `private.get_user_role()` returns the caller's role; `private.get_user_org_id()` returns their org.

From SPEC §Schema changes (verbatim column lists) — implement exactly:
- demo_fixtures(id uuid PK default gen_random_uuid(), template_id uuid NOT NULL REFERENCES templates(id), version int NOT NULL, label text, status text CHECK (status IN ('active','archived')) DEFAULT 'active', source_proposal_id uuid, rfp_fields jsonb NOT NULL, rfp_extract_text text, org_id uuid NOT NULL, captured_by uuid REFERENCES user_profiles(id), created_at timestamptz DEFAULT now()); UNIQUE(template_id, version); partial unique index one active per template_id.
- demo_fixture_sections(id uuid PK, fixture_id uuid NOT NULL REFERENCES demo_fixtures(id) ON DELETE CASCADE, role text NOT NULL, position int NOT NULL, section_name text NOT NULL, content text NOT NULL, compliance_flags jsonb); UNIQUE(fixture_id, position).
- demo_fixture_assumptions(id uuid PK, fixture_id uuid REFERENCES demo_fixtures(id) ON DELETE CASCADE, category text, content text NOT NULL, confidence text, status text DEFAULT 'approved', user_edited boolean DEFAULT false).
- demo_fixture_rfp_chunks(id uuid PK, fixture_id uuid REFERENCES demo_fixtures(id) ON DELETE CASCADE, source text, content text NOT NULL, embedding extensions.vector(1536), metadata jsonb).
- demo_runs(id uuid PK, proposal_id uuid NOT NULL REFERENCES proposals(id) ON DELETE CASCADE, fixture_id uuid REFERENCES demo_fixtures(id), started_by uuid REFERENCES user_profiles(id), org_id uuid NOT NULL, created_at timestamptz DEFAULT now()).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the 5 demo tables + RLS migration</name>
  <read_first>
    - .planning/phases/16-token-free-demo-mode/16-SPEC.md §"Schema changes (new)" and §"Access-control enforcement points" item 2
    - supabase/migrations/20260305000013_rls_policies.sql (RLS policy shape, private.get_user_role() usage)
    - supabase/migrations/20260714000000_harden_profile_role_rls.sql (role-integrity RLS convention)
  </read_first>
  <action>
    Create supabase/migrations/20260721000001_demo_fixture_tables.sql. Define exactly the 5 tables from the `<interfaces>` block above (verbatim column lists), each with `id uuid primary key default gen_random_uuid()`.
    Constraints: `demo_fixtures` gets `unique(template_id, version)` AND `create unique index demo_fixtures_one_active_per_template on demo_fixtures(template_id) where status = 'active';`. `demo_fixture_sections` gets `unique(fixture_id, position)`.
    RLS: `alter table ... enable row level security` on all 5. For each table add a single policy `using (private.get_user_role() = 'super_admin')` for `select`, and NO insert/update/delete policy for anon/authenticated (all mutations happen via service-role edge fns which bypass RLS). Name policies `<table>_super_admin_select`.
    Do NOT add any `is_demo`/feature_flag-aware cross-org bypass policy (RESEARCH anti-pattern: Decision A needs zero new cross-org surface).
    Add table/column comments noting these are demo-org-scoped, super_admin-only.
  </action>
  <verify>
    <automated>grep -c "create table" supabase/migrations/20260721000001_demo_fixture_tables.sql  # expect 5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "enable row level security" supabase/migrations/20260721000001_demo_fixture_tables.sql` returns 5
    - `grep -c "private.get_user_role() = 'super_admin'" supabase/migrations/20260721000001_demo_fixture_tables.sql` returns >= 5
    - `grep -q "demo_fixtures_one_active_per_template" ...` succeeds (partial unique index present)
    - `grep -q "unique(template_id, version)" ...` succeeds
    - `grep -q "references proposals(id) on delete cascade" ...` succeeds (demo_runs.proposal_id)
    - `grep -q "extensions.vector(1536)" ...` succeeds and `grep -qi "vector(1536)" ...` never appears without the `extensions.` prefix
    - `grep -qi "is_demo\|feature_flags" supabase/migrations/20260721000001_demo_fixture_tables.sql` returns nothing (no cross-org bypass)
  </acceptance_criteria>
  <done>Migration file defines all 5 tables, both fixture constraints, super_admin-only RLS on each, and no cross-org bypass policy.</done>
</task>

<task type="auto">
  <name>Task 2: Write clone_demo_fixture_chunks RPC migration</name>
  <read_first>
    - .planning/phases/16-token-free-demo-mode/16-RESEARCH.md §"Pattern 3: Clone-per-run chunk copy" (the exact function body)
    - .planning/phases/16-token-free-demo-mode/16-SPEC.md Decision C (search_vector trigger-maintained; column list constraints)
    - supabase/migrations/20260713000001_reap_stuck_document_extractions.sql (SECURITY DEFINER + REVOKE/GRANT convention)
  </read_first>
  <action>
    Create supabase/migrations/20260721000002_clone_demo_fixture_chunks_rpc.sql with `public.clone_demo_fixture_chunks(p_fixture_id uuid, p_proposal_id uuid, p_org_id uuid) returns integer` — `language sql security definer set search_path = public`. Body: `insert into chunks (org_id, doc_type, proposal_id, source, content, embedding, metadata) select p_org_id, 'proposal', p_proposal_id, source, content, embedding, metadata from demo_fixture_rfp_chunks where fixture_id = p_fixture_id` wrapped in a CTE returning `count(*)::int`.
    The INSERT column list MUST be exactly `org_id, doc_type, proposal_id, source, content, embedding, metadata` — NEVER include `search_vector` (trigger-maintained) or `id`/`created_at` (defaulted). No OpenAI/embedding call anywhere.
    Then `revoke all on function public.clone_demo_fixture_chunks(uuid,uuid,uuid) from public;` and `grant execute on function public.clone_demo_fixture_chunks(uuid,uuid,uuid) to service_role;`.
  </action>
  <verify>
    <automated>grep -q "insert into chunks (org_id, doc_type, proposal_id, source, content, embedding, metadata)" supabase/migrations/20260721000002_clone_demo_fixture_chunks_rpc.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qi "search_vector" supabase/migrations/20260721000002_clone_demo_fixture_chunks_rpc.sql` returns nothing (never hand-copied)
    - `grep -q "security definer" ...` succeeds
    - `grep -q "grant execute on function public.clone_demo_fixture_chunks" ... && grep -q "to service_role" ...` succeeds
    - `grep -qi "openai\|embedding.*create\|text-embedding" ...` returns nothing (no run-time model call)
  </acceptance_criteria>
  <done>Clone RPC does a pure INSERT...SELECT of the 7 columns, is service_role-only, and never computes search_vector or embeddings.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Apply both migrations via Supabase MCP + regenerate types</name>
  <read_first>
    - .planning/STATE.md active decision "Live migration apply deferred to orchestrator (15-01)" (MCP apply_migration is the only path here)
    - .claude memory supabase-migration-history-diverged (db push fails; use Supabase MCP)
  </read_first>
  <what-built>Two committed migration files (Tasks 1-2) that must be applied to the live hosted project `fuuvdcvbliijffogjnwg`.</what-built>
  <action>
    Apply both migrations to the live DB via Supabase MCP `apply_migration` (project ref `fuuvdcvbliijffogjnwg`), in order: 20260721000001 then 20260721000002. Do NOT use `supabase db push` (diverged — will fail). After apply, regenerate committed TS types (this repo commits generated types) so the 5 new tables + RPC appear in `src/types/database.types.ts`.
    This task is [BLOCKING]: TS build/type checks pass WITHOUT it because generated types come from config, not the live DB — creating a false-positive verification state. It must run before any DB-dependent verification in later waves.
  </action>
  <how-to-verify>
    Via Supabase MCP run: `select table_name from information_schema.tables where table_name in ('demo_fixtures','demo_fixture_sections','demo_fixture_assumptions','demo_fixture_rfp_chunks','demo_runs');` (expect 5 rows) and `select proname from pg_proc where proname = 'clone_demo_fixture_chunks';` (expect 1 row). Confirm `src/types/database.types.ts` now contains `demo_fixtures`.
  </how-to-verify>
  <verify>
    <automated>grep -q "demo_fixtures" src/types/database.types.ts && echo "types regenerated"</automated>
  </verify>
  <acceptance_criteria>
    - MCP query confirms all 5 tables exist live
    - MCP query confirms `clone_demo_fixture_chunks` exists live
    - `grep -q "demo_runs" src/types/database.types.ts` succeeds
  </acceptance_criteria>
  <resume-signal>Type "applied" once the 5 tables + RPC are confirmed live and types are regenerated, or describe the apply error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| authenticated client → demo tables (direct) | RLS is the only guard for direct table reads |
| service-role edge fn → demo tables | trusted; bypasses RLS |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | demo_fixtures/* direct table read by admin/user | mitigate | RLS `select using (private.get_user_role() = 'super_admin')` on all 5 tables; no authenticated write policy |
| T-16-02 | Elevation of Privilege | clone RPC callable by non-service-role | mitigate | `revoke all from public` + `grant execute to service_role` only |
| T-16-03 | Tampering | cross-org corpus leak via new bypass policy | mitigate | Decision A: add NO is_demo/cross-org bypass; existing org-scoped RLS isolates the dedicated demo org unmodified |
| T-16-04 | Denial of Service | orphaned cloned embeddings after delete | accept (structurally prevented) | `chunks.proposal_id` FK ON DELETE CASCADE removes clones automatically |
</threat_model>

<verification>
- `grep -c "create table" .../20260721000001_demo_fixture_tables.sql` == 5
- Clone RPC column list assertion (acceptance criteria Task 2)
- MCP: 5 tables + RPC exist live; `database.types.ts` contains `demo_fixtures`
</verification>

<success_criteria>
All 5 demo tables + clone RPC exist in the live project with super_admin-only RLS and service_role-only RPC execute; committed TS types include them; no cross-org bypass policy was added.
</success_criteria>

<output>
After completion, create `.planning/phases/16-token-free-demo-mode/16-01-SUMMARY.md`.
</output>
