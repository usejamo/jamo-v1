---
phase: 16-token-free-demo-mode
plan: 06
type: execute
wave: 3
depends_on: [16-01, 16-05]
files_modified:
  - supabase/functions/demo-sweep/index.ts
  - supabase/functions/demo-sweep/deno.json
  - supabase/functions/demo-sweep/test.ts
  - supabase/migrations/20260721000004_demo_sweep_cron.sql
autonomous: false
requirements: [SPEC-R1, SPEC-R9]
must_haves:
  truths:
    - "demo-sweep is callable only by an internal service-role/Vault-secret caller (isInternalServiceRoleCall), not a user JWT"
    - "The sweep removes demo-org draft proposals whose demo_runs are older than 24h, using the SAME cleanup as demo-reset"
    - "A pg_cron job invokes demo-sweep hourly via net.http_post"
    - "No demo-path code sets a proposal to a non-draft status"
  artifacts:
    - path: "supabase/functions/demo-sweep/index.ts"
      provides: "scheduled batch cleanup reusing cleanupDemoRun"
      contains: "cleanupDemoRun"
    - path: "supabase/migrations/20260721000004_demo_sweep_cron.sql"
      provides: "pg_net enable + cron.schedule → net.http_post to demo-sweep"
      contains: "cron.schedule"
  key_links:
    - from: "pg_cron job"
      to: "demo-sweep edge fn"
      via: "net.http_post with Vault-sourced auth"
      pattern: "net.http_post"
---

<objective>
Build `demo-sweep`: a scheduled edge function (Req 9) that catches abandoned demo runs — demo-org proposals still `draft` whose `demo_runs` row is older than 24h — and applies the exact same cleanup as `demo-reset` (reusing `_shared/demoRunCleanup.ts`, one implementation not two). Wire it via a `pg_cron` job that fires `net.http_post` to the deployed function URL hourly, authenticated by a Vault-stored secret so the function's `isInternalServiceRoleCall` check passes without a user JWT. This is the first `pg_net` usage in the repo — verify the extension + `net.http_post` argument shape against the live project before finalizing.

Purpose: Demo proposals stay draft (so they are structurally excluded from cross-proposal retrieval) and are auto-swept, preventing accumulation without touching the general abandoned-draft defect (Backlog 999.6).
Output: 1 deployed edge fn + 1 committed+applied cron migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-token-free-demo-mode/16-SPEC.md
@.planning/phases/16-token-free-demo-mode/16-RESEARCH.md

<interfaces>
From _shared/auth.ts: `isInternalServiceRoleCall(req): boolean` — compares the bearer token to `SUPABASE_SERVICE_ROLE_KEY`; unspoofable from the browser. Use THIS (not getAuthedUserAndOrg) to gate demo-sweep — it is triggered by pg_net, not a user JWT.
From _shared/demoRunCleanup.ts (16-05): `cleanupDemoRun(admin, { id, proposal_id, org_id })` — reuse verbatim.

pg_cron precedent (supabase/migrations/20260713000001): `create extension if not exists pg_cron;` + idempotent `do $$ ... cron.unschedule ... $$;` guard + `select cron.schedule(name, schedule, $$ sql $$);`.

pg_net (NEW to this repo — RESEARCH Pattern 4 / Assumption A1 / Open Question 2): `create extension if not exists pg_net;` then inside the cron body `select net.http_post(url := 'https://fuuvdcvbliijffogjnwg.supabase.co/functions/v1/demo-sweep', headers := jsonb_build_object('Content-Type','application/json','Authorization','Bearer '||(select decrypted_secret from vault.decrypted_secrets where name = 'demo_sweep_service_key')), body := '{}'::jsonb);`. VERIFY the exact net.http_post arg names against the live pg_net version via Supabase MCP before committing (RESEARCH Pitfall 3 / Open Question 2). Response log table for debugging: `net._http_response`.

Vault convention (20260506000026): store the secret via a `private.*` SECURITY DEFINER wrapper / `vault.create_secret`; REVOKE ALL FROM PUBLIC; GRANT to service_role.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: demo-sweep edge fn (internal-only gate + batch cleanup)</name>
  <read_first>
    - supabase/functions/_shared/auth.ts §14-17 (isInternalServiceRoleCall)
    - supabase/functions/_shared/demoRunCleanup.ts (reuse cleanupDemoRun)
    - .planning/phases/16-token-free-demo-mode/16-SPEC.md Req 9 (hourly, >24h, must mirror demo-reset's full cleanup)
    - supabase/functions/retrieve-context/index.ts (isInternalServiceRoleCall usage precedent)
  </read_first>
  <action>
    Create supabase/functions/demo-sweep/index.ts + deno.json. Handler:
    1. OPTIONS preflight. Gate: `if (!isInternalServiceRoleCall(req)) return jsonError(401, 'internal only', corsHeaders)` — NO getAuthedUserAndOrg (no user JWT). This is the Spoofing mitigation.
    2. service-role `admin` client. Identify the demo org id (`select id from organizations where feature_flags->>'is_demo' = 'true'` — or where slug='jamo-demo').
    3. Select abandoned runs: `select dr.id, dr.proposal_id, dr.org_id from demo_runs dr join proposals p on p.id = dr.proposal_id where dr.org_id = <demo org> and p.status = 'draft' and dr.created_at < now() - interval '24 hours'`.
    4. For each, call `cleanupDemoRun(admin, run)` (the exact demo-reset path). Never flip any status; never call StatusSelector-equivalent logic.
    5. Return `{ swept: <count> }`.
  </action>
  <verify>
    <automated>grep -q "isInternalServiceRoleCall" supabase/functions/demo-sweep/index.ts && grep -q "cleanupDemoRun" supabase/functions/demo-sweep/index.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "isInternalServiceRoleCall" supabase/functions/demo-sweep/index.ts` succeeds AND `grep -q "getAuthedUserAndOrg" ...` returns nothing (no user-JWT path)
    - `grep -q "cleanupDemoRun" ...` succeeds (single shared cleanup, not a re-implementation)
    - `grep -q "24 hours\|interval '24" ...` succeeds
    - `grep -q "status = 'draft'\|status='draft'" ...` succeeds
    - `grep -qi "update proposals set status\|StatusSelector" ...` returns nothing (never flips status)
  </acceptance_criteria>
  <done>demo-sweep is internal-only, batch-cleans >24h draft demo runs via the shared cleanup, and never mutates status.</done>
</task>

<task type="auto">
  <name>Task 2: pg_net + cron migration (deferred live apply)</name>
  <read_first>
    - supabase/migrations/20260713000001_reap_stuck_document_extractions.sql (idempotent cron.unschedule/schedule guard)
    - supabase/migrations/20260506000026_salesforce_integration.sql (private.* Vault wrapper convention)
    - .planning/phases/16-token-free-demo-mode/16-RESEARCH.md Pattern 4 + Pitfall 3 + Open Question 2
  </read_first>
  <action>
    Create supabase/migrations/20260721000004_demo_sweep_cron.sql:
    1. `create extension if not exists pg_net;` (idempotent — first pg_net use in the repo).
    2. Ensure the auth secret exists in Vault: create `demo_sweep_service_key` via the repo's Vault wrapper convention (or reference an existing service-role secret). Do NOT write the literal key into the migration SQL.
    3. Idempotent job (drop-if-exists guard like the reaper): `select cron.schedule('demo-run-sweep', '0 * * * *', $$ select net.http_post(url := 'https://fuuvdcvbliijffogjnwg.supabase.co/functions/v1/demo-sweep', headers := jsonb_build_object('Content-Type','application/json','Authorization','Bearer '||(select decrypted_secret from vault.decrypted_secrets where name='demo_sweep_service_key')), body := '{}'::jsonb) $$);`
    Add a top comment: VERIFY net.http_post arg names against the live pg_net version before apply (Open Question 2). Use the hardcoded hosted URL (single-project repo; no local/prod divergence).
  </action>
  <verify>
    <automated>grep -q "create extension if not exists pg_net" supabase/migrations/20260721000004_demo_sweep_cron.sql && grep -q "cron.schedule('demo-run-sweep'" supabase/migrations/20260721000004_demo_sweep_cron.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "pg_net" supabase/migrations/20260721000004_demo_sweep_cron.sql` succeeds
    - `grep -q "net.http_post" ...` succeeds with the demo-sweep function URL
    - `grep -q "'0 \* \* \* \*'" ...` succeeds (hourly)
    - `grep -q "cron.unschedule" ...` succeeds (idempotent re-apply guard)
    - `grep -qiE "sbp_|service_role_key.*=.*ey|Bearer [A-Za-z0-9._-]{20,}" ...` returns nothing (no literal secret in SQL)
  </acceptance_criteria>
  <done>Migration enables pg_net, references a Vault-stored secret (no literal), and schedules an idempotent hourly net.http_post to demo-sweep.</done>
</task>

<task type="auto">
  <name>Task 3: Grep/predicate test for demo-sweep (Req 9)</name>
  <read_first>
    - supabase/functions/demo-reset/test.ts (Deno.test + ignore:true grep-acceptance convention to mirror)
    - .planning/phases/16-token-free-demo-mode/16-RESEARCH.md §"Validation Architecture" Req 9 row (threshold predicate)
    - supabase/functions/demo-sweep/index.ts (the 24h-threshold + internal-only gate to assert against)
  </read_first>
  <action>
    Create supabase/functions/demo-sweep/test.ts mirroring demo-reset/test.ts. Export a pure threshold helper `isSweepable(createdAt: string | Date, now: Date): boolean` from demo-sweep/index.ts (returns true only when `createdAt < now - 24h`) — mirrors the isResettableRun-exported-for-testing convention in demo-reset — and use the SQL `interval '24 hours'` cutoff as its source of truth. In test.ts: a pure `Deno.test` asserting `isSweepable` is true at 24h01m and false at 23h59m, plus a `Deno.test({ ignore: true, ... })` integration stub for the internal-only-gate rejection (non-service-role caller → 401) and the batch-cleanup path (>24h draft demo runs removed via cleanupDemoRun).
  </action>
  <verify>
    <automated>grep -q "ignore: true" supabase/functions/demo-sweep/test.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Deno.test" supabase/functions/demo-sweep/test.ts` returns >= 2
    - `grep -q "ignore: true" supabase/functions/demo-sweep/test.ts` succeeds (internal-only-gate/batch-cleanup integration stub present)
    - `grep -q "isSweepable" supabase/functions/demo-sweep/test.ts` AND `grep -q "isSweepable" supabase/functions/demo-sweep/index.ts` both succeed (threshold predicate exported + unit-tested)
  </acceptance_criteria>
  <done>demo-sweep/test.ts exists with a pure 24h-threshold predicate + ignore:true integration stub, matching demo-reset's convention.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: [BLOCKING] Verify pg_net, deploy demo-sweep, apply cron, confirm first run</name>
  <read_first>
    - .planning/phases/16-token-free-demo-mode/16-RESEARCH.md Open Question 2 + Pitfall 3 (verify net.http_post shape + net._http_response log)
    - .claude memory edge-functions-need-deploy + edge-deploy-token-from-env + supabase-migration-history-diverged
  </read_first>
  <what-built>demo-sweep edge fn (Task 1) + the pg_net/cron migration (Task 2).</what-built>
  <action>
    1. Via Supabase MCP, confirm pg_net availability: `select * from pg_available_extensions where name='pg_net';` and enable if needed. Confirm the live `net.http_post` argument names match the migration (adjust the migration SQL if the installed version differs — Open Question 2).
    2. Store the `demo_sweep_service_key` Vault secret (service-role key or dedicated internal secret) if not already present.
    3. Deploy: `supabase functions deploy demo-sweep` with the `.env` sbp_ token. If Supabase requires it for a pg_net-invoked function, set `verify_jwt = false` for demo-sweep in supabase/config.toml (the internal secret check inside the function is the real gate — Assumption A2).
    4. Apply 20260721000004_demo_sweep_cron.sql via Supabase MCP `apply_migration`.
    5. Confirm the first scheduled (or manually triggered) call logged a row in `net._http_response`.
  </action>
  <how-to-verify>
    MCP: `select extname from pg_extension where extname='pg_net';` (present); `select jobname, schedule from cron.job where jobname='demo-run-sweep';` (present, hourly); `select status_code from net._http_response order by created desc limit 3;` (a 2xx after a trigger).
  </how-to-verify>
  <verify>
    <automated>echo "MISSING — live verification via Supabase MCP (pg_net + cron.job + net._http_response); no local automated command"</automated>
  </verify>
  <acceptance_criteria>
    - pg_net is enabled live and net.http_post arg names match the committed migration
    - demo-sweep is deployed and the `demo-run-sweep` cron job exists live (hourly)
    - net._http_response shows a successful (2xx) invocation of demo-sweep
  </acceptance_criteria>
  <resume-signal>Type "swept" once pg_net is verified, demo-sweep is deployed, the cron job exists, and a successful net._http_response row is confirmed — or describe the blocker.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| pg_cron (pg_net) → demo-sweep | internal HTTP call; must carry the Vault secret |
| external caller → demo-sweep URL | must be rejected (no user path) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-22 | Spoofing | forged external POST to demo-sweep triggering mass deletes | mitigate | `isInternalServiceRoleCall(req)` — bearer must equal service-role/Vault secret; 401 otherwise; no user-JWT path |
| T-16-23 | Information Disclosure | service-role key literal committed in the cron migration | mitigate | secret stored in Vault, referenced via `vault.decrypted_secrets`; acceptance grep asserts no literal key |
| T-16-24 | Tampering | sweep flipping proposal status or deleting non-demo/non-draft rows | mitigate | query scoped to demo org + status='draft'; reuses cleanupDemoRun's guards; grep asserts no status update |
| T-16-25 | Denial of Service | wrong-environment / malformed net.http_post silently failing | mitigate | verify pg_net + arg shape live before apply; confirm net._http_response 2xx (Pitfall 3) |
</threat_model>

<verification>
- Grep acceptance (Tasks 1-2): internal-only gate, shared cleanup, hourly cron, no literal secret, no status flip
- Live (Task 3): pg_net enabled, cron job present, net._http_response 2xx, a >24h draft demo run gets removed
</verification>

<success_criteria>
demo-sweep is deployed and internal-only; an hourly pg_cron→pg_net job invokes it; it removes >24h draft demo runs via the same cleanup as reset, never flipping status, and no secret is committed in SQL.
</success_criteria>

<output>
After completion, create `.planning/phases/16-token-free-demo-mode/16-06-SUMMARY.md`.
</output>
