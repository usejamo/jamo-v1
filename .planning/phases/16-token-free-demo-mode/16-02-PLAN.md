---
phase: 16-token-free-demo-mode
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260721000003_demo_org.sql
  - scripts/seed-demo-org.ts
  - scripts/seed-demo-org.test.ts
  - package.json
autonomous: false
requirements: [SPEC-R5]
user_setup:
  - service: supabase
    why: "Create the demo presenter super_admin auth user via admin API"
    env_vars:
      - name: DEMO_PRESENTER_EMAIL
        source: "Chosen internal demo login email (e.g. demo@usejamo.com)"
      - name: DEMO_PRESENTER_PASSWORD
        source: "Chosen strong password for the shared demo login (never hardcoded)"
must_haves:
  truths:
    - "A dedicated organizations row exists with feature_flags.is_demo = true"
    - "A super_admin presenter account exists whose user_profiles.org_id is the demo org"
    - "Re-running the seed is idempotent (no duplicate org or account)"
  artifacts:
    - path: "supabase/migrations/20260721000003_demo_org.sql"
      provides: "idempotent upsert of the demo organizations row with is_demo flag"
      contains: "feature_flags"
    - path: "scripts/seed-demo-org.ts"
      provides: "idempotent presenter super_admin account seed (invite → createUser → accept)"
      contains: "auth.admin.createUser"
  key_links:
    - from: "scripts/seed-demo-org.ts"
      to: "organizations (is_demo)"
      via: "upsert onConflict slug + invite bound to that org_id"
      pattern: "onConflict"
---

<objective>
Provision the dedicated demo org (Decision A) and its shared super_admin presenter account (D-07/D-08) reproducibly and idempotently, mirroring the Phase 15 `scripts/bootstrap-super-admin.ts` sequence exactly. The org row goes in via a committed SQL migration; the presenter account goes through the invite → `auth.admin.createUser` → accept path (never a raw `auth.users` insert), matching every other org+user pair in this codebase.

Purpose: Every demo run's proposal lives in this org; retrieval is org-scoped, so the isolated demo org is what makes the confidentiality guarantee (Decision A) hold with zero new RLS surface.
Output: 1 committed migration (org row) + 1 committed idempotent seed script (+ test) + npm entry, run live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-token-free-demo-mode/16-SPEC.md
@.planning/phases/15-client-onboarding-provisioning/15-SPEC.md

<interfaces>
From scripts/bootstrap-super-admin.ts (mirror this exact 5-step sequence):
- `loadEnv()` — .env loader (duplicate verbatim).
- `bootstrapSuperAdmin(admin, email, password)`: (1) idempotency guard — skip if a matching account already exists; (2) `admin.from('organizations').upsert(ORG, { onConflict: 'slug' })`; (3) insert PENDING `invites` row `{ email, org_id, role: 'super_admin', status: 'pending' }`; (4) `admin.auth.admin.createUser({ email, password, email_confirm: true })` (handle_new_user trigger binds org/role from the pending invite); (5) flip invite to `accepted`.
- Exported `bootstrapSuperAdmin` + `main(env?)` + `import.meta.main`-style CLI guard for Vitest importability.
- INTERNAL_ORG const shape: `{ name, slug, plan }`.

The demo org constant for this plan:
- `DEMO_ORG = { name: 'Jamo Demo', slug: 'jamo-demo', plan: 'internal' }`, feature_flags: `{ is_demo: true }`.

From supabase/migrations/20260305000002_organizations.sql: `organizations.feature_flags jsonb` already exists (no schema change needed to mark the org).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Demo org migration (idempotent upsert with is_demo)</name>
  <read_first>
    - supabase/migrations/20260305000002_organizations.sql (feature_flags column + slug unique)
    - .planning/phases/16-token-free-demo-mode/16-CONTEXT.md D-08 (committed, idempotent, safe to re-run)
  </read_first>
  <action>
    Create supabase/migrations/20260721000003_demo_org.sql that idempotently upserts the demo org:
    `insert into organizations (name, slug, plan, feature_flags) values ('Jamo Demo', 'jamo-demo', 'internal', '{"is_demo": true}'::jsonb) on conflict (slug) do update set feature_flags = organizations.feature_flags || '{"is_demo": true}'::jsonb;`
    Use a jsonb merge (`||`) so re-applying never clobbers other flags. Do NOT create the presenter auth user here (that requires the admin API — Task 2).
  </action>
  <verify>
    <automated>grep -q "'{\"is_demo\": true}'::jsonb" supabase/migrations/20260721000003_demo_org.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "on conflict (slug)" supabase/migrations/20260721000003_demo_org.sql` succeeds (idempotent)
    - `grep -q "feature_flags = organizations.feature_flags ||" ...` succeeds (merge, not clobber)
    - `grep -q "'jamo-demo'" ...` succeeds
    - `grep -qi "auth.users\|createUser" ...` returns nothing (no auth-user insert in SQL)
  </acceptance_criteria>
  <done>Migration upserts the demo org with is_demo=true idempotently via jsonb merge.</done>
</task>

<task type="auto">
  <name>Task 2: seed-demo-org.ts script + test + npm entry</name>
  <read_first>
    - scripts/bootstrap-super-admin.ts (byte-level pattern to mirror — the full 5-step sequence)
    - scripts/bootstrap-super-admin.test.ts (mocked-client call-ordering test convention)
    - .planning/phases/16-token-free-demo-mode/16-RESEARCH.md §"Pattern 2: Idempotent seed script"
  </read_first>
  <action>
    Create scripts/seed-demo-org.ts mirroring bootstrap-super-admin.ts's exported shape: `DEMO_ORG = { name: 'Jamo Demo', slug: 'jamo-demo', plan: 'internal' }`, an exported `seedDemoOrg(admin, email, password)` running the identical 5-step sequence but (a) the org upsert sets `feature_flags: { is_demo: true }`, (b) the invite `role` is `'super_admin'` (per D-07/Decision A — presenter needs the demo gate to pass), (c) idempotency guard checks for an existing super_admin whose `org_id` matches the demo org (query the demo org id first, then `user_profiles` for a super_admin in that org) so it skips cleanly on re-run without clobbering the Phase-15 internal super_admin.
    Env vars: `DEMO_PRESENTER_EMAIL`, `DEMO_PRESENTER_PASSWORD` (env-sourced, NEVER hardcoded). Duplicate `loadEnv()` verbatim. Add the `main(env?)` + direct-run guard so Vitest can import without executing.
    Add `scripts/seed-demo-org.test.ts` mirroring bootstrap-super-admin.test.ts: mock the Supabase client, assert the call order (org upsert → pending invite with role super_admin → createUser → invite accepted) and that the idempotency guard short-circuits when a demo super_admin already exists.
    Add an npm script `"seed:demo-org": "tsx scripts/seed-demo-org.ts"` to package.json.
  </action>
  <verify>
    <automated>npm run test:run -- seed-demo-org</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "is_demo: true" scripts/seed-demo-org.ts` succeeds
    - `grep -q "role: 'super_admin'" scripts/seed-demo-org.ts` succeeds (invite role)
    - `grep -qi "password.*=.*['\"][^'\"]" scripts/seed-demo-org.ts` returns nothing (no hardcoded password literal)
    - `grep -q "seed:demo-org" package.json` succeeds
    - `npm run test:run -- seed-demo-org` exits 0 with the call-order + idempotency assertions passing
  </acceptance_criteria>
  <done>Idempotent seed script + passing Vitest call-order/idempotency test + npm entry, no hardcoded secrets.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Apply org migration + run seed live</name>
  <read_first>
    - .planning/STATE.md active decision "Live migration apply deferred to orchestrator (15-01)"
    - .claude memory edge-deploy-token-from-env (sbp_ token / service-role key from .env)
  </read_first>
  <what-built>The demo org migration (Task 1) and idempotent presenter seed (Task 2), which must be applied/run against the live project.</what-built>
  <action>
    Apply supabase/migrations/20260721000003_demo_org.sql to the live project via Supabase MCP `apply_migration` (NOT `supabase db push`). Then set `DEMO_PRESENTER_EMAIL` / `DEMO_PRESENTER_PASSWORD` in `.env` and run `npm run seed:demo-org` against the live project to create the presenter super_admin bound to the demo org.
  </action>
  <how-to-verify>
    Via Supabase MCP: `select id, feature_flags from organizations where slug = 'jamo-demo';` (feature_flags contains is_demo=true) and `select role, org_id from user_profiles where org_id = (select id from organizations where slug='jamo-demo');` (a super_admin row exists). Confirm the presenter can log in at the app with the chosen credentials.
  </how-to-verify>
  <verify>
    <automated>echo "MISSING — live verification via Supabase MCP query (see how-to-verify); no local automated command (requires live project + credentials)"</automated>
  </verify>
  <acceptance_criteria>
    - MCP query returns the demo org with `feature_flags` containing `is_demo=true`
    - MCP query returns exactly one super_admin `user_profiles` row in the demo org
    - Re-running `npm run seed:demo-org` prints a skip message (idempotent) and creates nothing new
  </acceptance_criteria>
  <resume-signal>Type "seeded" once the demo org + presenter account are confirmed live, or describe the error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| seed script (service-role) → auth.users / organizations | privileged provisioning; secrets must be env-sourced |
| presenter account → all orgs (super_admin) | global super_admin can list org metadata (accepted per Decision A) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Information Disclosure | hardcoded demo password/service key in committed script | mitigate | env-sourced only (`DEMO_PRESENTER_PASSWORD`), verified by acceptance grep; mirrors T-15-26 |
| T-16-06 | Elevation of Privilege | seed accidentally minting a 2nd platform super_admin / clobbering internal org | mitigate | idempotency guard scoped to the demo org; org upsert uses jsonb merge, never overwrites |
| T-16-07 | Tampering | demo presenter reading other orgs' proposal content | accept | Decision A: `orgs_super_admin` bypass exposes org metadata only; proposal tables have no super_admin cross-org bypass |
</threat_model>

<verification>
- `npm run test:run -- seed-demo-org` green (call-order + idempotency)
- Migration grep acceptance (Task 1)
- Live MCP confirms demo org + presenter (Task 3)
</verification>

<success_criteria>
The demo org exists live with is_demo=true and a single super_admin presenter account bound to it; the seed is idempotent and carries no hardcoded secrets.
</success_criteria>

<output>
After completion, create `.planning/phases/16-token-free-demo-mode/16-02-SUMMARY.md`.
</output>
