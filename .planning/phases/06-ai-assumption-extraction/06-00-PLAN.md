---
phase: 06-ai-assumption-extraction
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/__tests__/Step3AssumptionReview.test.tsx
  - src/components/__tests__/AssumptionCard.test.tsx
  - supabase/functions/extract-assumptions/test.ts
autonomous: true
requirements:
  - REQ-3.1
  - REQ-3.2
  - REQ-3.3
  - REQ-3.4
  - REQ-3.5
  - REQ-3.6
  - REQ-3.7
must_haves:
  truths:
    - "npm run test:run exits 0 after Wave 0 plan"
    - "All new test files contain only it.skip stubs — no real imports of unbuilt files"
    - "Every requirement REQ-3.1 through REQ-3.7 has at least one stub test"
  artifacts:
    - path: "src/components/__tests__/Step3AssumptionReview.test.tsx"
      provides: "Stubs for REQ-3.4, REQ-3.5, REQ-3.7"
    - path: "src/components/__tests__/AssumptionCard.test.tsx"
      provides: "Stubs for REQ-3.3, REQ-3.5"
    - path: "supabase/functions/extract-assumptions/test.ts"
      provides: "Stubs for REQ-3.1, REQ-3.2, REQ-3.6"
  key_links: []
---

<objective>
Create Nyquist-compliant Wave 0 test stub files for Phase 6 so every requirement has an automated verify target before any implementation begins.

Purpose: Nyquist rule — every <verify> must reference an existing file. All Phase 6 tasks verify against files created here.
Output: 3 test stub files with it.skip tests. npm run test:run exits 0.
</objective>

<execution_context>
@C:/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai-assumption-extraction/06-CONTEXT.md
@.planning/phases/06-ai-assumption-extraction/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Step3AssumptionReview and AssumptionCard test stubs</name>
  <files>src/components/__tests__/Step3AssumptionReview.test.tsx, src/components/__tests__/AssumptionCard.test.tsx</files>
  <action>
Create two Vitest stub files using ONLY it.skip — do NOT import the components under test (Vite resolves imports at transform time; importing non-existent files causes build failures per active STATE.md decision "Stub test pattern").

`src/components/__tests__/Step3AssumptionReview.test.tsx`:
- it.skip('REQ-3.4: renders assumption cards for each extracted assumption')
- it.skip('REQ-3.5: approve button sets assumption status to approved with green border')
- it.skip('REQ-3.5: reject button sets assumption status to rejected with strikethrough')
- it.skip('REQ-3.5: clicking value text enables inline editing')
- it.skip('REQ-3.5: Add assumption button appends blank card')
- it.skip('REQ-3.7: missing fields section renders amber section above assumptions list')
- it.skip('REQ-3.7: filling missing field and saving converts it to an approved assumption card')
- it.skip('REQ-3.7: Next button shows count badge when missing fields remain unfilled')

`src/components/__tests__/AssumptionCard.test.tsx`:
- it.skip('REQ-3.3: renders High confidence badge in green for confidence >= 0.8')
- it.skip('REQ-3.3: renders Medium confidence badge in yellow for confidence 0.5-0.79')
- it.skip('REQ-3.3: renders Low confidence badge in red for confidence < 0.5')
- it.skip('REQ-3.5: approve toggles card to green border state')
- it.skip('REQ-3.5: reject toggles card to gray strikethrough state')
- it.skip('REQ-3.5: rejected card can be un-rejected by clicking approve again')

Both files: `import { describe, it } from 'vitest'` only — no component imports.
  </action>
  <verify>
    <automated>cd "C:/Users/aaron/VS Code Projects/Jamo/jamo-v1" && npm run test:run</automated>
  </verify>
  <done>Both files exist, npm run test:run exits 0, all new tests show as skipped not errored</done>
</task>

<task type="auto">
  <name>Task 2: Create extract-assumptions edge function test stub</name>
  <files>supabase/functions/extract-assumptions/test.ts</files>
  <action>
Create a Deno test stub file at `supabase/functions/extract-assumptions/test.ts`.

Pattern: use `Deno.test({ name: '...', ignore: true, fn: () => {} })` — matches the existing pattern in `supabase/functions/retrieve-context/test.ts` and `supabase/functions/extract-document/test.ts`. The `denoSpecifierStubPlugin` in vitest.config.ts already skips Deno test files under Vitest.

Stubs to include:
- Deno.test({ name: 'REQ-3.1: extract-assumptions invoked when all docs complete — returns { assumptions, missing }', ignore: true, fn: () => {} })
- Deno.test({ name: 'REQ-3.2: extraction identifies sponsor_metadata, scope, timeline, budget, criteria categories', ignore: true, fn: () => {} })
- Deno.test({ name: 'REQ-3.2: missing fields array returned for fields not found in documents', ignore: true, fn: () => {} })
- Deno.test({ name: 'REQ-3.6: approved assumptions written to proposal_assumptions with correct schema mapping (value→content)', ignore: true, fn: () => {} })
- Deno.test({ name: 'REQ-3.6: confidence float from AI maps to high/medium/low string before DB insert', ignore: true, fn: () => {} })

No imports of the (not-yet-existing) index.ts.
  </action>
  <verify>
    <automated>cd "C:/Users/aaron/VS Code Projects/Jamo/jamo-v1" && npm run test:run</automated>
  </verify>
  <done>File exists at supabase/functions/extract-assumptions/test.ts, npm run test:run still exits 0</done>
</task>

</tasks>

<verification>
npm run test:run exits 0. All new it.skip / Deno.test(ignore:true) entries appear in test output as skipped, not errored. No existing passing tests regressed.
</verification>

<success_criteria>
- 3 new stub files created
- All 13+ stubs use it.skip or Deno ignore:true (no real component imports)
- npm run test:run exits 0 in under 15 seconds
- REQ-3.1 through REQ-3.7 each have at least one named stub
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-assumption-extraction/06-00-SUMMARY.md`
</output>
