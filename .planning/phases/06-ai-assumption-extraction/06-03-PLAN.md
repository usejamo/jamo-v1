---
phase: 06-ai-assumption-extraction
plan: "03"
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - src/components/wizard/Step3AssumptionReview.tsx
  - src/components/__tests__/Step3AssumptionReview.test.tsx
  - src/components/__tests__/AssumptionCard.test.tsx
autonomous: true
requirements:
  - REQ-3.3
  - REQ-3.4
  - REQ-3.5
  - REQ-3.7
must_haves:
  truths:
    - "Step 3 renders assumption cards with approve/reject/edit controls"
    - "ConfidenceBadge shows green/yellow/red for high/medium/low"
    - "Rejected cards stay visible with gray + strikethrough; re-approvable"
    - "Approved cards show green border"
    - "Clicking value text enables inline editing"
    - "Add assumption button appends blank editable card"
    - "Missing fields amber section renders above assumptions list"
    - "Filling a missing field converts it to an approved assumption card"
    - "Next button shows count badge when unfilled missing fields remain"
    - "Spinner shown when extractionStatus === 'extracting'"
    - "npm run test:run exits 0"
  artifacts:
    - path: "src/components/wizard/Step3AssumptionReview.tsx"
      provides: "Full assumption review UI with AssumptionCard, ConfidenceBadge, MissingFieldItem, Add assumption, Next button"
  key_links:
    - from: "src/components/wizard/Step3AssumptionReview.tsx"
      to: "src/components/ProposalCreationWizard.tsx"
      via: "dispatch UPDATE_ASSUMPTION, ADD_ASSUMPTION, FILL_MISSING, SET_STEP"
      pattern: "dispatch\\(\\{"
---

<objective>
Build the Step3AssumptionReview component and convert all Wave 0 stubs in Step3AssumptionReview.test.tsx and AssumptionCard.test.tsx from it.skip to passing tests.

Purpose: The visible face of Phase 6 — where users review, edit, approve/reject AI-extracted assumptions before generation.
Output: Step3AssumptionReview.tsx with full UI; all REQ-3.3 through REQ-3.7 tests passing.
</objective>

<execution_context>
@C:/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-ai-assumption-extraction/06-CONTEXT.md
@.planning/phases/06-ai-assumption-extraction/06-RESEARCH.md
@src/types/wizard.ts
@src/components/DocumentList.tsx
@src/components/__tests__/Step3AssumptionReview.test.tsx
@src/components/__tests__/AssumptionCard.test.tsx
@.planning/phases/06-ai-assumption-extraction/06-01-SUMMARY.md

<interfaces>
<!-- WizardAssumption type (from Plan 01 output) -->
interface WizardAssumption {
  id: string
  category: string   // 'sponsor_metadata'|'scope'|'timeline'|'budget'|'criteria'
  value: string      // editable display text
  confidence: 'high' | 'medium' | 'low'
  source: string     // filename or 'user-provided'
  status: 'pending' | 'approved' | 'rejected'
}

interface MissingField {
  field: string
  description: string
  filledValue?: string
}

<!-- Dispatcher actions available -->
dispatch({ type: 'UPDATE_ASSUMPTION', id, updates: Partial<WizardAssumption> })
dispatch({ type: 'ADD_ASSUMPTION' })
dispatch({ type: 'FILL_MISSING', field, value })
dispatch({ type: 'SET_STEP', step: 3 })  // Next button

<!-- ConfidenceBadge color pattern (from DocumentList.tsx StatusBadge) -->
high   → bg-green-100 text-green-600
medium → bg-yellow-100 text-yellow-600
low    → bg-red-100 text-red-600
All: px-2 py-1 text-xs rounded-full
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Step3AssumptionReview component</name>
  <files>src/components/wizard/Step3AssumptionReview.tsx</files>
  <behavior>
    - When extractionStatus === 'extracting': renders a centered spinner with "Analyzing documents..." text; no assumption cards yet
    - When extractionStatus === 'idle' and assumptions is empty: renders "No documents uploaded. You can add assumptions manually." with Add assumption button
    - MissingFields section: renders above assumption list, amber background (bg-amber-50 border border-amber-200), heading "Missing Information (N)" where N is count of unfilled fields
    - Each MissingField renders: label (field name humanized), description text, inline text input, Save button; clicking Save dispatches FILL_MISSING
    - AssumptionCard approved: border-green-500 border-2
    - AssumptionCard rejected: opacity-50, value text has line-through class
    - AssumptionCard approve button: dispatches UPDATE_ASSUMPTION with status:'approved'; if already approved, clicking again does nothing (idempotent)
    - AssumptionCard reject button: dispatches UPDATE_ASSUMPTION with status:'rejected'; if already rejected, clicking approve dispatches UPDATE_ASSUMPTION with status:'approved' (un-reject)
    - AssumptionCard value click: toggles to input[type=text] controlled by local state; onBlur dispatches UPDATE_ASSUMPTION with updated value
    - ConfidenceBadge: high=green pill, medium=yellow pill, low=red pill (exact DocumentList StatusBadge pattern)
    - Add assumption button: dispatches ADD_ASSUMPTION; new blank card appears at bottom with status:'pending'
    - Next button: label is "Next" when no unfilled missing fields; "Next (N missing)" when N > 0; dispatches SET_STEP step:3
    - Back button: dispatches SET_STEP step:1
  </behavior>
  <action>
Create `src/components/wizard/Step3AssumptionReview.tsx` as a React functional component.

Props: `{ state: WizardState, dispatch: React.Dispatch<WizardAction> }`

Structure:
1. **Loading state** — if `state.extractionStatus === 'extracting'`, return spinner div

2. **ConfidenceBadge** sub-component (inline in same file): replicates StatusBadge pattern from DocumentList exactly with high/medium/low → green/yellow/red pills

3. **AssumptionCard** sub-component (inline in same file):
   - Props: `{ assumption: WizardAssumption, dispatch }`
   - Local state: `isEditing: boolean`
   - Container class: `border-2 rounded-lg p-3 mb-2` + conditional border color (approved=border-green-500, rejected=border-gray-200 opacity-50, pending=border-gray-200)
   - Header row: category badge (text-xs text-gray-500 uppercase), ConfidenceBadge, source text
   - Value row: if isEditing → `<input>` controlled by local `editValue` state, onBlur saves and sets isEditing=false; if not editing → `<span onClick={() => setIsEditing(true)}>` with rejected adding `line-through` class
   - Controls: Approve button (checkmark ✓, green when approved), Reject button (✗, red when rejected)
   - Approve: if status !== 'approved' → dispatch UPDATE_ASSUMPTION status:'approved'; if status === 'rejected' → dispatch UPDATE_ASSUMPTION status:'approved' (un-reject path)
   - Reject: if status !== 'rejected' → dispatch UPDATE_ASSUMPTION status:'rejected'

4. **MissingFieldItem** sub-component: field label, description, input with local state, Save button → dispatches FILL_MISSING; after save the item should visually indicate it was saved (disable input, change Save to checkmark)

5. **Main render:**
   - MissingFields section (amber) — only if `state.missingFields.length > 0`
   - Assumptions list — `state.assumptions.map(a => <AssumptionCard>)`
   - Add assumption button at bottom
   - Navigation row: Back button (left) + Next button (right) with missing count badge

Tailwind classes: jamo-500 for primary actions, amber-50/amber-200 for missing section, standard gray card borders.
  </action>
  <verify>
    <automated>cd "C:/Users/aaron/VS Code Projects/Jamo/jamo-v1" && npm run test:run</automated>
  </verify>
  <done>Step3AssumptionReview.tsx created; npm run test:run exits 0</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert Wave 0 stubs to passing tests</name>
  <files>src/components/__tests__/Step3AssumptionReview.test.tsx, src/components/__tests__/AssumptionCard.test.tsx</files>
  <behavior>
    - REQ-3.4: renderering Step3AssumptionReview with 2 assumptions in state shows 2 cards
    - REQ-3.5: clicking approve button on a card calls dispatch with UPDATE_ASSUMPTION status:'approved'
    - REQ-3.5: clicking reject button on a card calls dispatch with UPDATE_ASSUMPTION status:'rejected'
    - REQ-3.5: clicking value span shows an input; changing it and blurring dispatches UPDATE_ASSUMPTION with new value
    - REQ-3.5: clicking Add assumption button dispatches ADD_ASSUMPTION
    - REQ-3.7: component with missingFields renders amber section with "Missing Information" heading
    - REQ-3.7: clicking Save on a missing field item dispatches FILL_MISSING with correct field and value
    - REQ-3.7: Next button label includes unfilled count when missingFields have no filledValue
    - REQ-3.3: ConfidenceBadge with 'high' renders text "High" and has bg-green-100 class
    - REQ-3.3: ConfidenceBadge with 'medium' renders text "Medium" and has bg-yellow-100 class
    - REQ-3.3: ConfidenceBadge with 'low' renders text "Low" and has bg-red-100 class
    - REQ-3.5: rejected card approve button dispatch sets status to 'approved' (un-reject)
  </behavior>
  <action>
Convert all it.skip stubs in both test files to real tests. Use `@testing-library/react` render + fireEvent/userEvent pattern matching existing tests in ProposalCreationWizard.test.tsx and DocumentList.test.tsx.

For Step3AssumptionReview.test.tsx:
- Import Step3AssumptionReview from the component
- Create mock WizardState and mock dispatch (vi.fn())
- Render with testing-library render()
- Assert DOM output and dispatch calls

For AssumptionCard.test.tsx:
- Since AssumptionCard is an inline sub-component of Step3AssumptionReview, test it by rendering Step3AssumptionReview with specific assumptions and asserting card behavior
- OR if AssumptionCard is exported separately, import and test directly
- Keep all 6 confidence badge tests and approve/reject/un-reject tests

State mock helper:
```typescript
const makeState = (overrides: Partial<WizardState> = {}): WizardState => ({
  step: 2,
  stateVersion: 6,
  assumptions: [],
  missingFields: [],
  extractionStatus: 'idle',
  // ... all other required WizardState fields with defaults
  ...overrides,
})
```

Run `npm run test:run` after each test file conversion.
  </action>
  <verify>
    <automated>cd "C:/Users/aaron/VS Code Projects/Jamo/jamo-v1" && npm run test:run</automated>
  </verify>
  <done>All REQ-3.3, REQ-3.4, REQ-3.5, REQ-3.7 stubs converted to passing tests; no it.skip remaining in these two files; npm run test:run exits 0</done>
</task>

</tasks>

<verification>
npm run test:run exits 0. All stubs in Step3AssumptionReview.test.tsx and AssumptionCard.test.tsx are passing (not skipped). Step3AssumptionReview component renders without runtime errors with empty and populated state.
</verification>

<success_criteria>
- Step3AssumptionReview.tsx created with AssumptionCard, ConfidenceBadge, MissingFieldItem sub-components
- Approve/reject/edit/add controls dispatch correct actions
- Missing fields amber section renders above assumptions
- Next button shows count badge for unfilled missing fields
- All REQ-3.3, 3.4, 3.5, 3.7 tests passing
- npm run test:run exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-assumption-extraction/06-03-SUMMARY.md`
</output>
