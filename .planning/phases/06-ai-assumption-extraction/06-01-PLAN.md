---
phase: 06-ai-assumption-extraction
plan: "01"
type: execute
wave: 2
depends_on:
  - "06-00"
files_modified:
  - src/types/wizard.ts
  - src/components/wizard/Step3Generate.tsx
  - src/components/wizard/Step4Generate.tsx
  - src/components/ProposalCreationWizard.tsx
  - src/components/wizard/WizardStepIndicator.tsx
  - src/components/__tests__/ProposalCreationWizard.test.tsx
autonomous: true
requirements:
  - REQ-3.1
must_haves:
  truths:
    - "WizardState.step is typed 0|1|2|3 (not 0|1|2)"
    - "WIZARD_STEPS has 4 entries including 'Assumption Review'"
    - "Step3Generate.tsx is replaced by Step4Generate.tsx with identical logic"
    - "ProposalCreationWizard renders step 3 slot (placeholder) and step 4 (Step4Generate)"
    - "SKIP_TO_GENERATE dispatches step:3 not step:2"
    - "stateVersion:6 in DEFAULT_WIZARD_STATE causes outdated sessionStorage to reset"
    - "npm run test:run exits 0"
  artifacts:
    - path: "src/types/wizard.ts"
      provides: "Extended WizardState with assumptions, missingFields, extractionStatus, stateVersion:6"
      exports: ["WizardAssumption", "MissingField", "AssumptionStatus", "ConfidenceLevel", "ExtractionStatus"]
    - path: "src/components/wizard/Step4Generate.tsx"
      provides: "Renamed Step3Generate with step numbers updated to 3"
  key_links:
    - from: "src/types/wizard.ts"
      to: "src/components/ProposalCreationWizard.tsx"
      via: "WizardState type import"
      pattern: "import.*WizardState.*wizard"
---

<objective>
Extend wizard type contracts to support 4 steps and assumption state, rename Step3Generate → Step4Generate, update the wizard shell and step indicator, and fix existing tests for the new step numbering.

Purpose: Establishes the type foundation all subsequent Phase 6 plans build against. Blocks nothing in parallel — edge function (Plan 02) has no type dependency.
Output: wizard.ts with full assumption types, Step4Generate.tsx, updated wizard shell, green tests.
</objective>

<execution_context>
@C:/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-ai-assumption-extraction/06-CONTEXT.md
@.planning/phases/06-ai-assumption-extraction/06-RESEARCH.md
@src/types/wizard.ts
@src/components/ProposalCreationWizard.tsx
@src/components/wizard/WizardStepIndicator.tsx
@src/components/wizard/Step3Generate.tsx
@src/components/__tests__/ProposalCreationWizard.test.tsx

<interfaces>
<!-- Current WizardState shape from src/types/wizard.ts — extends this, do not replace -->
Current step type: step: 0 | 1 | 2
Current WIZARD_STEPS: ['Study Info', 'Document Upload', 'Template & Generate']

Required additions to WizardState:
  step: 0 | 1 | 2 | 3
  assumptions: WizardAssumption[]
  missingFields: MissingField[]
  extractionStatus: ExtractionStatus  // 'idle'|'extracting'|'complete'|'error'
  stateVersion: 6

New types to export:
  AssumptionStatus = 'pending' | 'approved' | 'rejected'
  ConfidenceLevel = 'high' | 'medium' | 'low'
  ExtractionStatus = 'idle' | 'extracting' | 'complete' | 'error'

  WizardAssumption {
    id: string           // temp UUID
    category: string     // 'sponsor_metadata'|'scope'|'timeline'|'budget'|'criteria'
    value: string        // assumption text (editable); maps to DB column 'content' on persist
    confidence: ConfidenceLevel
    source: string       // document filename or 'user-provided'
    status: AssumptionStatus
  }

  MissingField {
    field: string        // e.g. 'primary_endpoint'
    description: string  // human-readable
    filledValue?: string // set when user fills the inline field
  }

New action types for wizardReducer:
  | { type: 'SET_ASSUMPTIONS'; assumptions: WizardAssumption[]; missing: MissingField[] }
  | { type: 'UPDATE_ASSUMPTION'; id: string; updates: Partial<WizardAssumption> }
  | { type: 'ADD_ASSUMPTION' }
  | { type: 'FILL_MISSING'; field: string; value: string }
  | { type: 'SET_EXTRACTION_STATUS'; status: ExtractionStatus }
  SET_STEP: update union to step: 0 | 1 | 2 | 3

SKIP_TO_GENERATE: dispatch step: 3 (was step: 2)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend wizard.ts types and update ProposalCreationWizard shell</name>
  <files>src/types/wizard.ts, src/components/ProposalCreationWizard.tsx, src/components/wizard/WizardStepIndicator.tsx</files>
  <behavior>
    - WizardState.step accepts 0|1|2|3 (TypeScript error if 2 passed where 3 expected)
    - DEFAULT_WIZARD_STATE includes stateVersion:6, assumptions:[], missingFields:[], extractionStatus:'idle'
    - getInitialState() returns DEFAULT_WIZARD_STATE if stateVersion !== 6 (clears stale sessionStorage)
    - wizardReducer handles SET_ASSUMPTIONS, UPDATE_ASSUMPTION, ADD_ASSUMPTION, FILL_MISSING, SET_EXTRACTION_STATUS
    - ADD_ASSUMPTION appends blank WizardAssumption with crypto.randomUUID(), status:'pending', confidence:'high', source:'user-provided'
    - FILL_MISSING sets missingFields[field].filledValue and appends approved WizardAssumption with source:'user-provided'
    - WIZARD_STEPS = ['Study Info', 'Document Upload', 'Assumption Review', 'Template & Generate']
    - WizardStepIndicator renders 4 steps (no layout changes, just one more step)
    - SKIP_TO_GENERATE dispatches step:3 (not step:2); button tooltip says "Skip Upload and Assumption Review"
    - ProposalCreationWizard renders step===2 as a placeholder div "Step 3: Assumption Review (coming in Plan 03)" and step===3 as Step4Generate
  </behavior>
  <action>
1. Update `src/types/wizard.ts`:
   - Add type exports: AssumptionStatus, ConfidenceLevel, ExtractionStatus, WizardAssumption, MissingField
   - Extend WizardState with step:0|1|2|3, assumptions:WizardAssumption[], missingFields:MissingField[], extractionStatus:ExtractionStatus, stateVersion:6
   - Add DEFAULT_WIZARD_STATE entries for new fields
   - Add new action union variants to WizardAction

2. Update `src/components/ProposalCreationWizard.tsx`:
   - Add new action handlers in wizardReducer for SET_ASSUMPTIONS, UPDATE_ASSUMPTION, ADD_ASSUMPTION, FILL_MISSING, SET_EXTRACTION_STATUS
   - Update getInitialState(): check parsed.stateVersion === 6; if not, return DEFAULT_WIZARD_STATE (clears outdated sessionStorage shape)
   - Change SKIP_TO_GENERATE dispatch from step:2 to step:3
   - Update step render: step===2 → placeholder div, step===3 → Step4Generate (import from new path)
   - Update any `step === 2` guards that meant "generate step" to `step === 3`

3. Update `src/components/wizard/WizardStepIndicator.tsx`:
   - WIZARD_STEPS constant: insert 'Assumption Review' as index 2, push 'Template & Generate' to index 3
   - No layout changes — just one more step in the existing pattern

Run `npm run test:run` after each file. TypeScript compiler will surface all remaining `step:2` dispatch sites — fix them all.
  </action>
  <verify>
    <automated>cd "C:/Users/aaron/VS Code Projects/Jamo/jamo-v1" && npm run test:run</automated>
  </verify>
  <done>npm run test:run exits 0; WizardState type has step:0|1|2|3; WIZARD_STEPS has 4 entries; SKIP_TO_GENERATE dispatches step:3</done>
</task>

<task type="auto">
  <name>Task 2: Rename Step3Generate → Step4Generate and fix test fixtures</name>
  <files>src/components/wizard/Step4Generate.tsx, src/components/__tests__/ProposalCreationWizard.test.tsx</files>
  <action>
1. Copy `src/components/wizard/Step3Generate.tsx` to `src/components/wizard/Step4Generate.tsx`.
   - Update any internal dispatch calls: `{ type: 'SET_STEP', step: 2 }` (Back button) → `step: 2` stays (Back goes to Assumption Review at step 2). Correct.
   - Update any "step 3" display text references if present to "step 4".
   - The context indicator currently shows study info + document count quality signal. Add assumption count display: read `state.assumptions.filter(a => a.status === 'approved').length` and display "X assumptions approved" in the quality signal block. If 0 approved (e.g., arrived via Fast Draft), show "No assumptions (fast draft)".

2. Delete `src/components/wizard/Step3Generate.tsx` after confirming Step4Generate works.

3. Update `src/components/__tests__/ProposalCreationWizard.test.tsx`:
   - Any test that seeds sessionStorage with `step: 2` to reach the Generate panel must be updated to `step: 3`
   - Any test asserting WIZARD_STEPS length of 3 updates to 4
   - Any test asserting 'Template & Generate' is the last step still valid (it is, at index 3)
   - Add stateVersion:6 to any sessionStorage fixtures that set step

Run `npm run test:run` to confirm all existing tests still pass.
  </action>
  <verify>
    <automated>cd "C:/Users/aaron/VS Code Projects/Jamo/jamo-v1" && npm run test:run</automated>
  </verify>
  <done>Step4Generate.tsx exists; Step3Generate.tsx deleted; ProposalCreationWizard.test.tsx passes; step:3 fixtures reach generate panel correctly</done>
</task>

</tasks>

<verification>
npm run test:run exits 0. No TypeScript errors (tsc --noEmit). Step4Generate.tsx renders correctly in wizard at step===3. WIZARD_STEPS has 4 entries. stateVersion:6 guard clears stale sessionStorage.
</verification>

<success_criteria>
- src/types/wizard.ts exports WizardAssumption, MissingField, AssumptionStatus, ConfidenceLevel, ExtractionStatus
- WizardState.step is 0|1|2|3
- DEFAULT_WIZARD_STATE.stateVersion === 6
- wizardReducer handles all 5 new action types
- Step4Generate.tsx exists; Step3Generate.tsx deleted
- ProposalCreationWizard renders step===2 placeholder, step===3 Step4Generate
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-assumption-extraction/06-01-SUMMARY.md`
</output>
