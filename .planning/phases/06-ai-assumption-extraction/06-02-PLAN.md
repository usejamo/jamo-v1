---
phase: 06-ai-assumption-extraction
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-00"
files_modified:
  - supabase/functions/extract-assumptions/index.ts
autonomous: true
requirements:
  - REQ-3.1
  - REQ-3.2
  - REQ-3.6
must_haves:
  truths:
    - "extract-assumptions edge function accepts { proposalId } and returns { assumptions, missing }"
    - "Returned assumptions have category, value, confidence (as float), source"
    - "Edge function bulk-inserts into proposal_assumptions mapping value→content, float confidence→high|medium|low string"
    - "Claude JSON parse failure returns empty assumptions gracefully (no 500)"
    - "Function deployed and callable"
  artifacts:
    - path: "supabase/functions/extract-assumptions/index.ts"
      provides: "Deno edge function: fetch document_extracts, call claude-haiku, parse JSON, insert to proposal_assumptions"
  key_links:
    - from: "supabase/functions/extract-assumptions/index.ts"
      to: "proposal_assumptions table"
      via: "supabase service role client insert"
      pattern: "from\\('proposal_assumptions'\\)\\.insert"
---

<objective>
Build the `extract-assumptions` Supabase Edge Function that queries document_extracts for a proposal, calls Claude Haiku with a focused extraction prompt, parses the JSON response, and bulk-inserts results into proposal_assumptions.

Purpose: Core AI capability — structured extraction from uploaded RFP documents. Runs as fire-and-forget from Step 2 when all docs are parsed.
Output: Deployed Deno edge function at supabase/functions/extract-assumptions/index.ts.
</objective>

<execution_context>
@C:/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-ai-assumption-extraction/06-CONTEXT.md
@.planning/phases/06-ai-assumption-extraction/06-RESEARCH.md
@supabase/functions/extract-document/index.ts
@supabase/functions/retrieve-context/index.ts

<interfaces>
<!-- proposal_assumptions table schema (existing — no migration needed) -->
CREATE TABLE proposal_assumptions (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  proposal_id     UUID NOT NULL REFERENCES proposals(id) ON DELETE CASCADE,
  org_id          UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,
  category        TEXT NOT NULL,
  content         TEXT NOT NULL,        -- maps FROM WizardAssumption.value
  confidence      TEXT NOT NULL DEFAULT 'medium',  -- 'high'|'medium'|'low' strings (NOT float)
  status          TEXT NOT NULL DEFAULT 'pending',
  user_edited     BOOLEAN NOT NULL DEFAULT FALSE,
  source_document UUID REFERENCES proposal_documents(id),
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

<!-- document_extracts table — query by proposal_id -->
SELECT de.extracted_text, de.document_type, pd.filename
FROM document_extracts de
JOIN proposal_documents pd ON pd.id = de.document_id
WHERE pd.proposal_id = $proposalId

<!-- proposals table — need org_id for insert -->
SELECT org_id FROM proposals WHERE id = $proposalId
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build extract-assumptions edge function</name>
  <files>supabase/functions/extract-assumptions/index.ts</files>
  <action>
Model this function on `supabase/functions/extract-document/index.ts` (same serve() + CORS pattern). Check `supabase/functions/retrieve-context/index.ts` for the exact Anthropic SDK import pattern used in this codebase — mirror it exactly (whether `npm:@anthropic-ai/sdk` or HTTP API).

Function spec:

1. **CORS + serve boilerplate** — identical pattern to extract-document

2. **Input:** `{ proposalId: string }` from request body. Validate present, return 400 if missing.

3. **Fetch documents:** Query document_extracts joined to proposal_documents for the proposalId. Also fetch `org_id` from proposals table. Use service role client (SUPABASE_SERVICE_ROLE_KEY).

4. **Build prompt:**
   System: `You are a clinical research assumption extractor. Extract key assumptions from CRO proposal documents. Return ONLY valid JSON matching this exact schema: { "assumptions": [{ "category": "sponsor_metadata|scope|timeline|budget|criteria", "value": "string", "confidence": 0.0-1.0, "source": "filename or inferred" }], "missing": [{ "field": "snake_case_field_name", "description": "human readable description" }] }`

   User: Concatenate all document texts with headers like `--- Document: {filename} ---`. Truncate total to ~8000 tokens if needed (simple char limit: 32000 chars).

5. **Call Anthropic:** claude-haiku (per STATE.md active decision). max_tokens: 2000.

6. **Parse response:** Use regex `const match = content.match(/\{[\s\S]*\}/)` to extract JSON object from response text. Wrap in try/catch — on parse failure return `{ assumptions: [], missing: [], warning: 'parse_failed' }` with status 200 (graceful, not 500).

7. **Map confidence:** `a.confidence >= 0.8 ? 'high' : a.confidence >= 0.5 ? 'medium' : 'low'`

8. **Bulk insert to proposal_assumptions:**
   ```
   rows = parsed.assumptions.map(a => ({
     proposal_id: proposalId,
     org_id: orgId,
     category: a.category,
     content: a.value,           // DB column is 'content' not 'value'
     confidence: mapConfidence(a.confidence),
     status: 'pending',
     source_document: null,
     user_edited: false,
   }))
   await supabase.from('proposal_assumptions').insert(rows)
   ```

9. **Return:** `{ assumptions: parsed.assumptions, missing: parsed.missing }` — the frontend reads this response to populate WizardState. Status 200.

10. **Error handling:** Wrap entire handler in try/catch; return `{ error: message }` with status 500 on unexpected failures.

Deploy command (run after writing): `npx supabase functions deploy extract-assumptions --no-verify-jwt`
  </action>
  <verify>
    <automated>cd "C:/Users/aaron/VS Code Projects/Jamo/jamo-v1" && npm run test:run</automated>
  </verify>
  <done>supabase/functions/extract-assumptions/index.ts exists and deploys without error. Function returns { assumptions, missing } shape. npm run test:run exits 0.</done>
</task>

</tasks>

<verification>
`npx supabase functions deploy extract-assumptions --no-verify-jwt` succeeds. Function file follows exact same serve()/CORS/service-role-client pattern as extract-document. JSON parse failure path returns 200 with empty assumptions (not 500). DB insert uses `content` column not `value`.
</verification>

<success_criteria>
- supabase/functions/extract-assumptions/index.ts created and deployed
- Input: { proposalId: string }
- Output: { assumptions: [{category, value, confidence (float), source}], missing: [{field, description}] }
- DB insert maps value→content, float→string confidence
- Graceful JSON parse failure (regex extraction + try/catch)
- npm run test:run exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-assumption-extraction/06-02-SUMMARY.md`
</output>
