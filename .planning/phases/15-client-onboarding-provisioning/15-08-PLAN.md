---
phase: 15-client-onboarding-provisioning
plan: 08
type: execute
wave: 4
depends_on: [15-02, 15-04, 15-05, 15-06, 15-07]
files_modified: []
autonomous: true
requirements: [15-03, 15-04, 15-06, 15-07, 15-08, 15-11]
must_haves:
  truths:
    - "All five new edge functions are DEPLOYED to the live project (verified deployed, not just committed)"
    - "Phase 14.3 (Edge Identity Hardening) is deployed + verified before any real tenant is provisioned"
    - "The bootstrap script has been run live and a super_admin exists on the hosted project"
  artifacts:
    - path: "supabase/functions/admin-create-org/index.ts"
      provides: "deployed (confirmed via supabase functions list)"
  key_links:
    - from: "committed edge functions"
      to: "live project fuuvdcvbliijffogjnwg"
      via: "supabase functions deploy (explicit)"
      pattern: "deploy"
---

<objective>
Ship the backend live. Deploy all five new edge functions explicitly (execute-phase commits but does NOT deploy — SPEC acceptance criterion + MEMORY edge-functions-need-deploy), confirm the Phase 14.3 prerequisite gate is deployed, and run the bootstrap script against the hosted project so a super_admin exists (req 11 live). No source files change here — this is the [BLOCKING] ops boundary between "coded" and "live".

Purpose: Every UI plan's human-verify (admin panel, team tab) needs live edge functions, and `/admin` needs a real super_admin. This plan guarantees both before Wave 4.
Output: Five deployed functions + a live super_admin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-client-onboarding-provisioning/15-CONTEXT.md
@.planning/phases/14.3-edge-identity-hardening/14.3-SPEC.md

<interfaces>
MEMORY: edge-deploy-token-from-env — supabase CLI deploy needs the `sbp_` token from .env (ambient env token is legacy/invalid).
MEMORY: edge-functions-need-deploy — execute-phase commits but never deploys; deploy explicitly.
Deploy command shape: `supabase functions deploy <name> --project-ref fuuvdcvbliijffogjnwg` with the sbp_ access token from .env (SUPABASE_ACCESS_TOKEN).
Functions to deploy: admin-create-org, admin-invite-first-admin, admin-invites-lifecycle, team-invite, team-manage. (_shared/* is bundled with each consumer, not deployed separately.)
Bootstrap run: `npm run bootstrap:super-admin` with BOOTSTRAP_SUPER_ADMIN_EMAIL/PASSWORD + SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY in .env.
14.3 gate: confirm chat-with-jamo / salesforce-oauth-* / retrieve-context JWT-hardened versions are the deployed versions before provisioning real tenants (D-07 prerequisite).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Verify Phase 14.3 gate is deployed</name>
  <read_first>
    - .planning/phases/14.3-edge-identity-hardening/14.3-SPEC.md (the go-live gate)
    - MEMORY: edge-functions-need-deploy
  </read_first>
  <action>
    Confirm the Phase 14.3 (Edge Identity Hardening) edge functions are the DEPLOYED versions on `fuuvdcvbliijffogjnwg` — `chat-with-jamo`, `salesforce-oauth-initiate`, `salesforce-oauth-disconnect`, `retrieve-context` must derive identity from the JWT (not the body). Use `supabase functions list --project-ref fuuvdcvbliijffogjnwg` and/or check each function's deployed version/updated_at against the 14.3 commit. If 14.3 is NOT deployed, STOP and surface a blocker — real tenants must not be provisioned before the gate (D-07). This is [BLOCKING].
  </action>
  <acceptance_criteria>
    - `supabase functions list` shows the four 14.3 functions with deploy timestamps at/after the 14.3 execution date
    - If any 14.3 function is stale → blocker surfaced, plan halts
  </acceptance_criteria>
  <verify>
    <automated>MISSING — verified via `supabase functions list` against live project (ops check, no repo command)</automated>
  </verify>
  <done>Phase 14.3 identity hardening confirmed live; provisioning is cleared to proceed.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Deploy the five provisioning edge functions</name>
  <read_first>
    - MEMORY: edge-deploy-token-from-env (use the sbp_ token from .env)
    - the five index.ts files from plans 04/05/06 (confirm they exist before deploy)
  </read_first>
  <action>
    Deploy each function explicitly with the sbp_ token from .env: `admin-create-org`, `admin-invite-first-admin`, `admin-invites-lifecycle`, `team-invite`, `team-manage` — e.g. `SUPABASE_ACCESS_TOKEN=<sbp_ from .env> supabase functions deploy <name> --project-ref fuuvdcvbliijffogjnwg`. Ensure `SITE_URL` (used by createInvite's redirectTo) and `SUPABASE_SERVICE_ROLE_KEY` are set as function secrets/env on the project. After deploying, run `supabase functions list` and confirm all five appear with fresh deploy timestamps. Do NOT rely on the commit alone.
  </action>
  <acceptance_criteria>
    - `supabase functions list --project-ref fuuvdcvbliijffogjnwg` shows all five functions deployed with current timestamps
    - A smoke `functions.invoke` of `admin-create-org` with a non-super_admin token returns 403 (auth gate live)
  </acceptance_criteria>
  <verify>
    <automated>MISSING — verified via `supabase functions list` + a 403 smoke invoke (ops check; Deno/live-only)</automated>
  </verify>
  <done>All five provisioning functions are live on the hosted project.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Run the bootstrap to create the live super_admin (req 11)</name>
  <read_first>
    - scripts/bootstrap-super-admin.ts (from plan 07)
    - 15-01-SUMMARY.md (confirm the invites table + trigger are applied live — bootstrap depends on it)
  </read_first>
  <action>
    Ensure `.env` has `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `BOOTSTRAP_SUPER_ADMIN_EMAIL`, `BOOTSTRAP_SUPER_ADMIN_PASSWORD`. Run `npm run bootstrap:super-admin` against the hosted project. Confirm a `user_profiles` row with `role='super_admin'` now exists (via Supabase MCP execute_sql or dashboard) and that the internal org (`slug='jamo-internal'`) exists. Re-run once to confirm the idempotent skip ("super_admin already exists, skipping"). This is [BLOCKING] and depends on the plan-01 migration being applied live.
  </action>
  <acceptance_criteria>
    - A live `user_profiles` row with role='super_admin' exists (MCP `SELECT ... WHERE role='super_admin'` returns >=1)
    - Second run logs the idempotent skip and exits 0 without creating a duplicate
  </acceptance_criteria>
  <verify>
    <automated>MISSING — verified via Supabase MCP execute_sql (live super_admin row) + a second idempotent run (ops check)</automated>
  </verify>
  <done>A super_admin exists on the live project and re-running the bootstrap is a safe no-op.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy token → live project | sbp_ access token authorizes function deploy |
| provisioning before 14.3 | Untrusted tenants must not share the instance before identity hardening ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-28 | Spoofing (cross-tenant) | provisioning before 14.3 | mitigate | Task 1 hard-gates on 14.3 being deployed before any real tenant is provisioned (D-07). |
| T-15-29 | Tampering | stale edge deployment | mitigate | Explicit deploy + `functions list` timestamp verification — avoids the known "committed-but-not-deployed runs stale code" failure (MEMORY). |
| T-15-30 | Information Disclosure | deploy/bootstrap secrets | mitigate | sbp_ token, service-role key, and bootstrap password sourced from .env; never committed or logged. |
</threat_model>

<verification>
- `supabase functions list` shows five fresh deployments + 14.3 functions current.
- Live super_admin row present; second bootstrap run is a no-op.
</verification>

<success_criteria>
Five provisioning functions are deployed live behind the 14.3 gate, and a live super_admin exists so `/admin` is reachable.
</success_criteria>

<output>
After completion, create `.planning/phases/15-client-onboarding-provisioning/15-08-SUMMARY.md`. Record deploy timestamps and the super_admin email (not the password).
</output>
