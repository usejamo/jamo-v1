---
phase: 15-client-onboarding-provisioning
plan: 10
type: execute
wave: 4
depends_on: [15-08]
files_modified:
  - src/components/settings/TeamTab.tsx
  - src/pages/Settings.tsx
autonomous: false
requirements: [15-06, 15-07, 15-08]
must_haves:
  truths:
    - "The Team tab is visible in Settings only to admin/super_admin"
    - "An org admin can invite a teammate (role admin/user only) into their own org"
    - "An org admin can change a teammate's role and deactivate/reactivate within their org"
    - "A pending-invites sub-list scoped to the caller's own org supports resend/revoke"
    - "All privileged operations go through the deployed team-* edge functions"
  artifacts:
    - path: "src/components/settings/TeamTab.tsx"
      provides: "org-admin team management tab (invite, member list, role/deactivate, pending invites)"
      contains: "functions.invoke"
    - path: "src/pages/Settings.tsx"
      provides: "'Team' tab registered + gated to org admins"
      contains: "Team"
  key_links:
    - from: "TeamTab.tsx"
      to: "team-invite / team-manage"
      via: "supabase.functions.invoke"
      pattern: "functions.invoke\\('team-"
---

<objective>
Build the org-admin "Team" tab in Settings (D-11, reqs 6/7/8): registered like the existing Templates tab and gated to `admin`/`super_admin`. Card 1 = invite a teammate (email + role admin/user, never super_admin); Card 2 = team-members list with role badges, active/deactivated status, and Change Role / Deactivate / Reactivate actions; plus a pending-invites sub-list (own org only) with resend/revoke. Privileged operations call the deployed `team-*` edge functions; a role change may use the same-org RLS UPDATE policy but flows through team-manage for a uniform surface.

Purpose: Tier-2 self-serve teammate management, reusing the established Settings tab pattern.
Output: TeamTab component + Settings tab registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-client-onboarding-provisioning/15-UI-SPEC.md
@.planning/phases/15-client-onboarding-provisioning/15-PATTERNS.md
@.planning/phases/15-client-onboarding-provisioning/15-06-SUMMARY.md

<interfaces>
Tab registration (Settings.tsx:126-127, 464-467) — add 'Team' to SUB_TABS and to the isOrgAdmin filter EXACTLY like 'Templates'/'Reference Library':
```ts
const isOrgAdmin = profile?.role === 'admin' || profile?.role === 'super_admin'
const visibleTabs = SUB_TABS.filter(tab => (tab !== 'Templates' && tab !== 'Reference Library' && tab !== 'Team') || isOrgAdmin)
```
Component model: TemplatesTab.tsx two-card structure. Icons: inline SVG function-per-icon (TemplatesTab.tsx:4-38), no icon library. Row + 44px action buttons (:705-751). DeleteDialog verbatim (:391-440) for "Deactivate {name}?" (UI-SPEC copy). Warning/amber banner (:240-247) for Pending badge tier.
Edge fn contracts (deployed plan 08):
- `functions.invoke('team-invite', { body: { email, role } })` → `{ invite }` (role admin/user; org bound server-side to caller)
- `functions.invoke('team-manage', { body: { action: 'change_role', targetUserId, role } })`
- `functions.invoke('team-manage', { body: { action: 'deactivate'|'reactivate', targetUserId } })`
- pending invites own-org list: `supabase.from('invites').select('*')` is RLS-scoped to caller's org (plan 01 invites_select_own_org), OR reuse a scoped list; resend/revoke for own org go via team-* (or admin lifecycle if exposed) — keep to team scope.
UI-SPEC § 2: member row = name/email (font-semibold) + role badge (jamo-50 for admin, gray-100 for user) + status (Active green / Deactivated gray) + 44px actions with aria-labels. h1/headers font-semibold, NOT font-bold.
Copy: "Invite Teammate", "Change Role", "Deactivate"/"Reactivate"; empty states + "Deactivate {name}?" dialog from UI-SPEC Copywriting Contract.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the gated 'Team' tab in Settings</name>
  <read_first>
    - src/pages/Settings.tsx:126-127, 464-493 (SUB_TABS, isOrgAdmin filter, tab render switch)
    - 15-UI-SPEC.md § 2 (tab registration point)
  </read_first>
  <action>
    Edit src/pages/Settings.tsx: add `'Team'` to the `SUB_TABS` array and extend the `isOrgAdmin` visibility filter so `'Team'` is gated exactly like `'Templates'`/`'Reference Library'` (visible only when `profile?.role === 'admin' || profile?.role === 'super_admin'`). Render `<TeamTab />` when the active sub-tab is 'Team'. Do not change any other tab behavior.
  </action>
  <acceptance_criteria>
    - `grep "'Team'" src/pages/Settings.tsx` appears in both SUB_TABS and the isOrgAdmin filter
    - `grep "TeamTab" src/pages/Settings.tsx` shows the render wiring
    - Build passes
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Team" src/pages/Settings.tsx && npm run build 2>&1 | grep -qi "error TS" && echo FAIL || echo PASS</automated>
  </verify>
  <done>The Team tab is registered and visible only to org admins/super_admins.</done>
</task>

<task type="auto">
  <name>Task 2: TeamTab — invite card + member list with role change & deactivate (reqs 6, 7)</name>
  <read_first>
    - src/components/settings/TemplatesTab.tsx (icons :4-38, DeleteDialog :391-440, state/effects :446-548, row :705-751)
    - 15-UI-SPEC.md § 2 (two-card structure, member row, badges, aria-labels, copy)
    - 15-06-SUMMARY.md (team-invite/team-manage contracts + role cap)
  </read_first>
  <action>
    Create src/components/settings/TeamTab.tsx modeled on TemplatesTab's two-card layout. Card 1 "Invite a teammate": email input + role `<select>` limited to admin/user (NEVER super_admin) + "Invite Teammate" CTA → `functions.invoke('team-invite', { body: { email, role } })`; on error "Couldn't send the invite. Check the email address and try again." Card 2 "Team members": fetch org members (`supabase.from('user_profiles').select(...)` — RLS-scoped to own org) and render rows: name/email (font-semibold text-sm), role badge (jamo-50 pill for admin, gray-100 for user), status (Active=green-600, Deactivated=gray), and 44px icon actions with aria-labels — "Change Role" (→ `team-manage` change_role), "Deactivate"/"Reactivate" (→ `team-manage` deactivate/reactivate). Deactivate opens the verbatim DeleteDialog ("Deactivate {name}?" / "They will immediately lose access to this organization." / red "Deactivate"). Empty state "You're the only member" / "Invite teammates to start collaborating on proposals." Use inline SVG icons (no library); h1/card headers font-semibold.
  </action>
  <acceptance_criteria>
    - `grep "functions.invoke('team-invite'" src/components/settings/TeamTab.tsx` and `grep "functions.invoke('team-manage'" ...` both return hits
    - The role select offers only admin/user (`! grep -q "super_admin" src/components/settings/TeamTab.tsx` in the invite role options; super_admin not selectable)
    - `grep "Deactivate {" \|"Deactivate " src/components/settings/TeamTab.tsx` shows the dialog + red action; `grep "aria-label" ...` present
    - `grep -c "font-bold" src/components/settings/TeamTab.tsx` returns 0
    - Build passes
  </acceptance_criteria>
  <verify>
    <automated>grep -q "team-manage" src/components/settings/TeamTab.tsx && grep -q "aria-label" src/components/settings/TeamTab.tsx && npm run build 2>&1 | grep -qi "error TS" && echo FAIL || echo PASS</automated>
  </verify>
  <done>Org admins invite teammates (capped role) and change role / deactivate within their org through the team-* functions.</done>
</task>

<task type="auto">
  <name>Task 3: TeamTab — own-org pending-invites sub-list (resend/revoke) (req 8)</name>
  <read_first>
    - src/components/settings/TemplatesTab.tsx:705-751 (row/badge/action), :391-440 (revoke dialog)
    - 15-UI-SPEC.md § 2 (pending invites sub-list, scoped to own org)
  </read_first>
  <action>
    Add a pending-invites sub-list to TeamTab scoped to the caller's OWN org: fetch via `supabase.from('invites').select('*')` (RLS `invites_select_own_org` scopes it automatically) filtered to non-accepted, render the same row shape as the admin panel (email, role, status badge Pending amber / Accepted green / Revoked gray, invited-at) with 44px "Resend Invite" / "Revoke Invite" actions + aria-labels, going through the team-scoped lifecycle path. Empty state "No pending invites" / "Invites you send will appear here until they're accepted." Revoke uses the verbatim DeleteDialog ("Revoke invite?" copy). Ensure the list never shows another org's invites (RLS + no service-role client in the browser).
  </action>
  <acceptance_criteria>
    - `grep "No pending invites" src/components/settings/TeamTab.tsx` returns a hit
    - `grep "Revoke invite?" src/components/settings/TeamTab.tsx` returns a hit
    - Pending/Accepted/Revoked badge tiers present (amber/green/gray)
    - Build passes
  </acceptance_criteria>
  <verify>
    <automated>grep -q "No pending invites" src/components/settings/TeamTab.tsx && npm run build 2>&1 | grep -qi "error TS" && echo FAIL || echo PASS</automated>
  </verify>
  <done>The Team tab shows and manages the caller's own-org pending invites; cross-org invites are never visible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Settings → Team tab: invite teammate (role-capped), member list with role change + deactivate/reactivate, and an own-org pending-invites sub-list with resend/revoke — all via the deployed team-* edge functions.</what-built>
  <how-to-verify>
    1. Sign in as an org admin (the first admin created in plan 09). Open Settings → confirm the "Team" tab is visible; sign in as a plain user and confirm it is NOT visible.
    2. Invite a teammate as role `user` to a real inbox → confirm the invite email arrives and the pending list shows Pending; confirm the role select does NOT offer super_admin.
    3. Accept the invite (set password) → confirm the new member appears in your org with role user; confirm they cannot see another org's data.
    4. Change that member's role to admin → reload → persists. Deactivate them → confirm they can no longer sign in (auth ban). Reactivate → they can sign in again.
    5. Confirm you cannot target or see any user/invite outside your org.
  </how-to-verify>
  <resume-signal>Type "team-verified" when invite, role change, deactivate/reactivate, and own-org scoping all work, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TeamTab client → team-* fns | Org-scoped operations; server re-checks org + role cap |
| member/invite lists in browser | Must be RLS-scoped to caller's org |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-33 | EoP | role select offers super_admin | mitigate | UI select is capped to admin/user AND team-invite server rejects super_admin (defense in depth). |
| T-15-34 | Tampering | cross-org management | mitigate | team-manage server checks target.org_id === callerOrgId; browser lists use RLS-scoped anon client (no service role) so another org's rows never load. |
| T-15-35 | Information Disclosure | pending invites leak | mitigate | invites_select_own_org RLS scopes the sub-list to the caller's org; no cross-org service-role client in the browser. |
</threat_model>

<verification>
- Build passes; grep acceptance confirms edge-function routing, role cap, no font-bold, dialog/aria copy.
- Human-verify checkpoint covers invite/role/deactivate/scoping end-to-end (+ reqs 5/9 for the teammate invite email).
</verification>

<success_criteria>
The Team tab lets org admins self-serve teammates within their org, role-capped and org-scoped, through the deployed team-* functions.
</success_criteria>

<output>
After completion, create `.planning/phases/15-client-onboarding-provisioning/15-10-SUMMARY.md`.
</output>
