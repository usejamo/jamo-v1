---
phase: 15-client-onboarding-provisioning
plan: 09
type: execute
wave: 5
depends_on: [15-08]
files_modified:
  - src/pages/admin/AdminPanel.tsx
autonomous: false
requirements: [15-03, 15-04, 15-08, 15-02]
must_haves:
  truths:
    - "A super_admin sees an org list, can create an org (name+plan, live slug preview), and invite a first admin"
    - "A pending-invites list shows status; per-row Resend Invite and Revoke Invite work"
    - "All data operations go through the deployed admin-* edge functions (service-role), never direct cross-org table writes"
  artifacts:
    - path: "src/pages/admin/AdminPanel.tsx"
      provides: "platform admin panel UI (org list, create-org, invite-first-admin, pending invites)"
      contains: "functions.invoke"
  key_links:
    - from: "AdminPanel.tsx"
      to: "admin-create-org / admin-invite-first-admin / admin-invites-lifecycle"
      via: "supabase.functions.invoke"
      pattern: "functions.invoke\\('admin-"
---

<objective>
Build the `/admin` platform panel UI (D-10, reqs 2/3/4/8): an org list, a create-org form (name + plan + live auto-slug preview), a per-org invite-first-admin form, and a pending-invites list with per-row Resend Invite / Revoke Invite. Every operation calls the deployed `admin-*` edge functions via `supabase.functions.invoke` — the client never writes cross-org tables directly. Reuses TemplatesTab row/badge/dialog chrome and the UI-SPEC copy/color/typography contract.

Purpose: The super_admin's provisioning surface. Route gating + SuperAdminRoute wiring is added in plan 11; this plan builds the page itself.
Output: AdminPanel page (and any small admin subcomponents in the same file/folder).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-client-onboarding-provisioning/15-UI-SPEC.md
@.planning/phases/15-client-onboarding-provisioning/15-PATTERNS.md
@.planning/phases/15-client-onboarding-provisioning/15-04-SUMMARY.md
@.planning/phases/15-client-onboarding-provisioning/15-05-SUMMARY.md

<interfaces>
Edge fn contracts (deployed in plan 08):
- `functions.invoke('admin-create-org', { body: { name, plan } })` → `{ ... org }` (409 on slug exhaustion)
- `functions.invoke('admin-invite-first-admin', { body: { email, targetOrgId } })` → `{ invite }`
- `functions.invoke('admin-invites-lifecycle', { body: { action: 'list' } })` → `{ invites }` (each with organizations.name)
- `functions.invoke('admin-invites-lifecycle', { body: { action: 'resend'|'revoke', inviteId } })`
Page chrome (Settings.tsx:469-476) — but h1 uses `text-2xl font-semibold` (NOT font-bold, UI-SPEC Typography). Title "Platform Admin".
Row + pill badge + 44px icon action button (TemplatesTab.tsx:705-751). Icons: inline SVG function-per-icon (TemplatesTab.tsx:4-38) — new mail/user-plus/check/x/shield icons follow this shape; NO icon library.
DeleteDialog verbatim shape (TemplatesTab.tsx:391-440) for "Revoke invite?" (title/body/red confirm from UI-SPEC Copywriting).
baseSlug from src/lib/slug.ts (plan 04) for the live slug preview.
Status badge tiers (UI-SPEC Color): Pending=amber-50/200/700, Accepted=green-600, Revoked=gray (neutral).
Empty states + error copy: UI-SPEC Copywriting Contract (e.g. slug collision → "This organization name is already taken. Try a slightly different name.").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Org list + create-org form (reqs 3, plus org rows/empty state)</name>
  <read_first>
    - src/pages/Settings.tsx:456-493 (page shell/header)
    - src/components/settings/TemplatesTab.tsx:4-38 (icons), :705-751 (row + pill + 44px action), :446-548 (state/effect/mutation shape)
    - src/lib/slug.ts (baseSlug)
    - 15-UI-SPEC.md § 1 (org list, create-org form, focal point, copy)
  </read_first>
  <action>
    Create src/pages/admin/AdminPanel.tsx with the Settings-style page shell: h1 "Platform Admin" (`text-2xl font-semibold text-gray-900`) + subtitle. Fetch orgs on mount (via `supabase.from('organizations').select('*')` — super_admin RLS bypass on organizations already permits this read, or use a list action if preferred) and render a white-card org list: each row shows name (font-semibold text-sm), plan pill (gray-100, matches "Pre-built" pill), slug (text-xs text-gray-400), and an "Invite Admin" action. Empty state: "No organizations yet" / "Create your first client organization to get started." Add a create-org form (Name input + plan trial/paid toggle) with a live read-only auto-slug preview using `baseSlug(name)`; submit → `functions.invoke('admin-create-org', { body: { name, plan } })`; on 409/collision show "This organization name is already taken. Try a slightly different name."; on success refetch the list. "Create Organization" is the single accent CTA (jamo-500), secondary to the rows.
  </action>
  <acceptance_criteria>
    - `grep "functions.invoke('admin-create-org'" src/pages/admin/AdminPanel.tsx` returns a hit
    - `grep "baseSlug" src/pages/admin/AdminPanel.tsx` returns a hit (live slug preview)
    - `grep -c "font-bold" src/pages/admin/AdminPanel.tsx` returns 0 (Typography budget)
    - `grep "No organizations yet" src/pages/admin/AdminPanel.tsx` returns a hit
    - Build passes
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | grep -qi "error TS\|Build failed" && echo FAIL || echo PASS</automated>
  </verify>
  <done>Org list + create-org form render, use the edge function + live slug preview, and match UI-SPEC chrome/copy.</done>
</task>

<task type="auto">
  <name>Task 2: Invite-first-admin form + pending-invites list with resend/revoke (reqs 4, 8)</name>
  <read_first>
    - src/components/settings/TemplatesTab.tsx:391-440 (DeleteDialog verbatim), :240-247 (amber banner tier for Pending badge)
    - 15-UI-SPEC.md § 1 (invite-first-admin form, pending invites list, status badges, aria-labels)
    - 15-05-SUMMARY.md (lifecycle action contract)
  </read_first>
  <action>
    In AdminPanel.tsx add: (a) an invite-first-admin form triggered from an org row — single email input + read-only org context + "Send Invite" button → `functions.invoke('admin-invite-first-admin', { body: { email, targetOrgId } })`; on error show "Couldn't send the invite. Check the email address and try again." (b) a pending-invites list fed by `functions.invoke('admin-invites-lifecycle', { body: { action: 'list' } })`: each row shows email, target org name, role, a status badge (Pending=amber / Accepted=green / Revoked=gray per UI-SPEC), invited-at date, and right-aligned 44px icon actions "Resend Invite" and "Revoke Invite" — each with a descriptive `aria-label` (e.g. `Resend invite to {email}`). Revoke opens the verbatim DeleteDialog ("Revoke invite?" / "{email} will no longer be able to accept using this link." / red "Revoke") → `action:'revoke'`; Resend → `action:'resend'`, then refetch. Empty state: "No pending invites" / "Invites you send will appear here until they're accepted."
  </action>
  <acceptance_criteria>
    - `grep "admin-invite-first-admin" src/pages/admin/AdminPanel.tsx` and `grep "admin-invites-lifecycle" ...` both return hits
    - `grep "aria-label" src/pages/admin/AdminPanel.tsx` shows descriptive labels on resend/revoke buttons
    - `grep "Revoke invite?" src/pages/admin/AdminPanel.tsx` returns a hit (confirmation dialog copy)
    - `grep "amber-\|green-600\|Pending\|Accepted\|Revoked" ...` shows the three status tiers
    - No direct `supabase.from('invites')` write for resend/revoke (must go through the edge function)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "admin-invites-lifecycle" src/pages/admin/AdminPanel.tsx && grep -q "aria-label" src/pages/admin/AdminPanel.tsx && npm run build 2>&1 | grep -qi "error TS" && echo FAIL || echo PASS</automated>
  </verify>
  <done>Invite-first-admin + pending-invites lifecycle UI work through the edge functions with correct badges, dialogs, and aria-labels.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The /admin panel: org list, create-org (with live slug), invite-first-admin, and pending-invites with resend/revoke — all wired to the deployed admin-* edge functions. (Route gating is added in plan 11; for this checkpoint, reach the page via the plan-11 wiring or a temporary route.)</what-built>
  <how-to-verify>
    1. Sign in as the bootstrapped super_admin; open /admin. Confirm the org list renders (or the "No organizations yet" empty state).
    2. Create an org (name + plan); confirm the slug preview updates live and one org row appears after submit. Try a duplicate name → expect the "already taken" error or a suffixed slug.
    3. Invite a first admin to that org using a real external inbox you control; confirm the invite email arrives from the Resend sender with a working /accept-invite link (this also live-verifies reqs 4/5/9).
    4. In pending invites: confirm the new invite shows Pending (amber); click Resend Invite → a fresh email arrives; click Revoke Invite → confirm the dialog, then the old link no longer accepts (try it).
  </how-to-verify>
  <resume-signal>Type "admin-verified" when create-org, invite, resend, and revoke all work end-to-end, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AdminPanel client → admin-* fns | UI must not assume authorization; server re-checks super_admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-31 | EoP | AdminPanel UI gate | mitigate | UI reachability is UX-only; every operation calls a service-role edge function that re-asserts super_admin from the JWT (plans 04/05). A non-super_admin who forces the route still gets 403 from every action. |
| T-15-32 | Tampering | client-side org/invite writes | mitigate | Resend/revoke/create-org/invite all go through edge functions — no direct cross-org table write from the browser (grep acceptance). |
</threat_model>

<verification>
- Build passes; grep acceptance confirms edge-function-only writes + aria-labels + dialog copy.
- Human-verify checkpoint covers live create/invite/resend/revoke (+ reqs 4/5/9 end-to-end).
</verification>

<success_criteria>
The /admin panel provisions orgs and first admins and manages pending invites entirely through the deployed service-role functions, matching the UI-SPEC contract.
</success_criteria>

<output>
After completion, create `.planning/phases/15-client-onboarding-provisioning/15-09-SUMMARY.md`.
</output>
