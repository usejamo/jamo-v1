---
phase: 15-client-onboarding-provisioning
plan: 04
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - supabase/functions/_shared/invites.ts
  - supabase/functions/admin-create-org/index.ts
  - supabase/functions/admin-invite-first-admin/index.ts
  - supabase/functions/admin-create-org/test.ts
  - supabase/functions/admin-invite-first-admin/test.ts
  - src/lib/slug.ts
  - src/lib/__tests__/slug.test.ts
autonomous: true
requirements: [15-03, 15-04]
must_haves:
  truths:
    - "A super_admin can create an org with name+plan and a unique auto-generated slug"
    - "A super_admin can invite a first admin whose org_id/role are bound server-side"
    - "Both functions assert super_admin FROM the JWT, not from the panel"
    - "Slug collisions are resolved by suffix retry (bounded), not a pre-check race"
    - "The invites row is INSERTed and committed BEFORE auth.admin is called (D-03)"
  artifacts:
    - path: "supabase/functions/_shared/invites.ts"
      provides: "insert-pending-invite + inviteUserByEmail + compensate-on-failure sequence"
      exports: ["createInvite"]
    - path: "supabase/functions/admin-create-org/index.ts"
      provides: "super_admin org creation with unique slug"
    - path: "supabase/functions/admin-invite-first-admin/index.ts"
      provides: "super_admin first-admin invite, org/role server-bound"
  key_links:
    - from: "admin-invite-first-admin"
      to: "invites table + auth.admin.inviteUserByEmail"
      via: "_shared/invites.ts createInvite (INSERT then invite, D-03 order)"
      pattern: "inviteUserByEmail"
---

<objective>
Build the cross-org super_admin backend: a shared `_shared/invites.ts` helper implementing the D-01/D-02/D-03 sequence (INSERT pending invite → call `auth.admin.inviteUserByEmail` → compensate on failure), plus `admin-create-org` (name+plan, unique auto slug, req 3) and `admin-invite-first-admin` (org/role bound server-side, req 4). Both are service-role edge functions that assert `super_admin` from the verified JWT (D-08/D-10).

Purpose: This is the platform-admin provisioning core. The shared helper is reused by team-invite (plan 06) so the D-03 ordering lives in one place.
Output: Shared helper + 2 edge functions + Deno test scaffolds + slug unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-client-onboarding-provisioning/15-RESEARCH.md
@.planning/phases/15-client-onboarding-provisioning/15-PATTERNS.md

<interfaces>
Canonical shared auth helper (supabase/functions/_shared/auth.ts — REUSE, do not reimplement):
```ts
export function isInternalServiceRoleCall(req: Request): boolean
export function jsonError(status: number, message: string, corsHeaders?): Response  // {error} JSON body
export async function getAuthedUserAndOrg(req, corsHeaders?): Promise<{ userId: string; orgId: string }>  // throws Response on failure
```
Caller pattern (salesforce-oauth-initiate/index.ts:39-48):
```ts
let userId: string, orgId: string
try { ({ userId, orgId } = await getAuthedUserAndOrg(req, corsHeaders)) }
catch (e) { if (e instanceof Response) return e; throw e }
```
super_admin assertion to ADD after auth (new per D-08, same shape as org-mismatch check):
```ts
const { data: callerProfile } = await admin.from('user_profiles').select('role').eq('user_id', userId).single()
if (callerProfile?.role !== 'super_admin') return jsonError(403, 'super_admin required', corsHeaders)
```
Service-role client: `const admin = createClient(Deno.env.get('SUPABASE_URL')!, Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!)`
D-03 invite sequence (RESEARCH Code Examples / PATTERNS _shared/invites.ts): INSERT invites{email:lower, org_id, role, invited_by, status:'pending'} committed FIRST → `admin.auth.admin.inviteUserByEmail(email, { redirectTo: \`${SITE_URL}/accept-invite\` })` → on error, compensate: `admin.from('invites').update({status:'revoked'}).eq('id', invite.id)`.
Slug (RESEARCH Code Examples): baseSlug lowercases/strips/hyphenates/slice(0,60); insert loop 6 attempts, suffix `-${attempt+1}`, catch Postgres `23505` unique violation and retry; rethrow non-23505.
CORS + OPTIONS preflight boilerplate: section-ai-action/index.ts:45-48.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: _shared/invites.ts helper (createInvite sequence)</name>
  <read_first>
    - supabase/functions/_shared/auth.ts (full — module shape, jsonError, throw-Response convention)
    - supabase/functions/retrieve-context/index.ts:1-4 (import line for _shared helpers + CORS)
    - 15-PATTERNS.md § _shared/invites.ts (exact sequence + compensate)
    - 15-RESEARCH.md § Code Examples (invite creation) + Open Question 2 (shared helper recommendation)
  </read_first>
  <action>
    Create supabase/functions/_shared/invites.ts exporting `createInvite(admin, { email, orgId, role, invitedBy, siteUrl }): Promise<{ invite }>`: lowercase the email; INSERT the invites row `{ email, org_id: orgId, role, invited_by: invitedBy, status: 'pending' }` and `.select().single()` (D-03 step 1, committed first); if that errors throw `jsonError(400, err.message)`. Then call `admin.auth.admin.inviteUserByEmail(email, { redirectTo: \`${siteUrl}/accept-invite\` })` (D-03 step 2 — the trigger fires here and finds the committed row); if THAT errors, compensate by `admin.from('invites').update({ status: 'revoked' }).eq('id', invite.id)` and throw `jsonError(400, err.message)`. Return `{ invite }`. Also export `revokeInvite(admin, inviteId, authUserId?)`: mark the invites row `revoked` and, when an auth user exists, `admin.auth.admin.deleteUser(authUserId)` (FK-cascades the profile, D-06). Keep the module free of top-level Deno-serve so tests can import it.
  </action>
  <acceptance_criteria>
    - `grep "inviteUserByEmail" supabase/functions/_shared/invites.ts` returns a hit
    - `grep -n "status: 'pending'" supabase/functions/_shared/invites.ts` appears BEFORE the `inviteUserByEmail` line (D-03 ordering)
    - `grep "status: 'revoked'" supabase/functions/_shared/invites.ts` appears in the compensate path
    - `grep "redirectTo" supabase/functions/_shared/invites.ts` contains `accept-invite`
    - Module exports `createInvite` and `revokeInvite` (`grep "export" ...`)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "inviteUserByEmail" supabase/functions/_shared/invites.ts && grep -q "status: 'revoked'" supabase/functions/_shared/invites.ts && echo PASS</automated>
  </verify>
  <done>Shared helper implements the committed-invite-first sequence + compensate + revoke, importable by both admin and team functions.</done>
</task>

<task type="auto">
  <name>Task 2: admin-create-org edge function + slug unit test (req 3)</name>
  <read_first>
    - supabase/functions/salesforce-oauth-initiate/index.ts (auth try/catch + service-role client + CORS)
    - supabase/functions/_shared/auth.ts
    - 15-RESEARCH.md § Code Examples (slug generation with uniqueness retry)
    - 15-PATTERNS.md § service-role edge functions (imports/CORS/super_admin assertion)
  </read_first>
  <action>
    Create supabase/functions/admin-create-org/index.ts: OPTIONS preflight; `getAuthedUserAndOrg` in try/catch; build the service-role `admin` client; assert `callerProfile.role === 'super_admin'` (else 403). Parse `{ name, plan }` from body; validate `plan IN ('trial','paid')`. Implement `baseSlug(name)` and the bounded 6-attempt insert loop from RESEARCH (catch `23505`, append `-${attempt+1}`, rethrow non-23505); on exhaustion return `jsonError(409, 'This organization name is already taken. Try a slightly different name.')`. Return the created org row. Do NOT trust any client-supplied slug or org_id.

    Create src/lib/slug.ts exporting `baseSlug(name: string): string` (the pure function) so it is unit-testable client-side, and import it from the edge function OR duplicate the tiny pure fn if cross-runtime import is awkward — but the unit test targets `src/lib/slug.ts`. Create src/lib/__tests__/slug.test.ts asserting: spaces→hyphens, uppercase→lower, punctuation stripped, leading/trailing hyphens trimmed, length capped at 60.
  </action>
  <acceptance_criteria>
    - `grep "super_admin required" supabase/functions/admin-create-org/index.ts` returns a hit (JWT-asserted, not panel)
    - `grep "23505" supabase/functions/admin-create-org/index.ts` returns a hit (unique-violation retry, not pre-check)
    - `grep "IN ('trial','paid')\|trial\|paid" supabase/functions/admin-create-org/index.ts` shows plan validation
    - `npm run test:run -- slug` passes
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- slug</automated>
  </verify>
  <done>admin-create-org creates an org with a unique slug under super_admin authorization; slug logic is unit-tested.</done>
</task>

<task type="auto">
  <name>Task 3: admin-invite-first-admin edge function + Deno test scaffolds (req 4)</name>
  <read_first>
    - supabase/functions/_shared/invites.ts (the createInvite helper from Task 1)
    - supabase/functions/salesforce-oauth-initiate/test.ts and supabase/functions/_shared/auth.test.ts (Deno test + `ignore:true` contingency convention)
    - 15-RESEARCH.md § Validation Architecture (Deno-unavailable → grep acceptance)
  </read_first>
  <action>
    Create supabase/functions/admin-invite-first-admin/index.ts: OPTIONS; `getAuthedUserAndOrg` try/catch; service-role `admin`; assert `super_admin` from JWT (403 otherwise). Parse `{ email, targetOrgId }` — role is hardcoded to `'admin'` for a first admin (req 4; never read role from the body). Call `createInvite(admin, { email, orgId: targetOrgId, role: 'admin', invitedBy: userId, siteUrl: Deno.env.get('SITE_URL') })`. Return `{ invite }`. targetOrgId may differ from the caller's home org (cross-org is the point) — that is why this needs super_admin, not the same-org team path.

    Create test.ts scaffolds for both admin-create-org and admin-invite-first-admin following the repo's `Deno.test({ name, ignore: <deno-unavailable>, fn })` pattern (mirror salesforce-oauth-initiate/test.ts). Each asserts the authorization predicate: a non-super_admin caller yields 403; a super_admin path reaches createInvite/insert. Where live Deno is unavailable in the dev sandbox, mark `ignore:true` and rely on the grep acceptance below (matches the Phase 14.3 contingency).
  </action>
  <acceptance_criteria>
    - `grep "super_admin required" supabase/functions/admin-invite-first-admin/index.ts` returns a hit
    - `grep "role: 'admin'" supabase/functions/admin-invite-first-admin/index.ts` returns a hit AND the function does NOT read `role` from the request body (`! grep -q "body.role\|role } = await req.json\|, role }" index.ts` conceptually — role is server-fixed)
    - `grep "createInvite" supabase/functions/admin-invite-first-admin/index.ts` returns a hit
    - test.ts files exist for both functions containing `Deno.test`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "createInvite" supabase/functions/admin-invite-first-admin/index.ts && grep -q "role: 'admin'" supabase/functions/admin-invite-first-admin/index.ts && test -f supabase/functions/admin-invite-first-admin/test.ts && echo PASS</automated>
  </verify>
  <done>First-admin invite binds org (from body targetOrgId, super_admin-gated) + role='admin' server-side via the shared helper; test scaffolds present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| super_admin browser → admin-* edge fn | Cross-org privileged operations |
| request body → org/role | Client-supplied org/role must never be trusted for identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-12 | EoP | admin-create-org / admin-invite-first-admin | mitigate | `super_admin` asserted from the verified JWT (user_profiles lookup on `getAuthedUserAndOrg().userId`), NOT implied by reaching the panel (D-08/D-10). Non-super_admin → 403. |
| T-15-13 | Tampering | first-admin role | mitigate | role is hardcoded `'admin'` server-side; body role is never read (req 4/12). |
| T-15-14 | Spoofing | service-role key | mitigate | SUPABASE_SERVICE_ROLE_KEY read only from Deno.env inside the edge function; never returned to or accepted from the client. |
| T-15-15 | Tampering (race) | slug uniqueness | mitigate | DB UNIQUE constraint is source of truth; insert-catch-23505-retry, no check-then-insert window. |
| T-15-16 | DoS | slug retry loop | accept | Bounded to 6 attempts then a clear 409 — no unbounded loop. |
</threat_model>

<verification>
- `npm run test:run -- slug` green; build passes.
- Grep acceptance confirms JWT super_admin assertion, server-fixed role, D-03 ordering.
- Deno test scaffolds present (`ignore:true` where Deno unavailable).
</verification>

<success_criteria>
Shared invite helper + admin-create-org + admin-invite-first-admin exist, are super_admin-gated from the JWT, bind org/role server-side, and generate unique slugs. (Deploy happens in plan 08.)
</success_criteria>

<output>
After completion, create `.planning/phases/15-client-onboarding-provisioning/15-04-SUMMARY.md`. Export the createInvite/revokeInvite signatures for plans 05/06.
</output>
