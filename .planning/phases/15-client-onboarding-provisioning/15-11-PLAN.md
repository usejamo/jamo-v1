---
phase: 15-client-onboarding-provisioning
plan: 11
type: execute
wave: 5
depends_on: [15-03, 15-09, 15-10]
files_modified:
  - src/components/SuperAdminRoute.tsx
  - src/components/__tests__/SuperAdminRoute.test.tsx
  - src/App.tsx
  - src/context/AuthContext.tsx
  - src/pages/Login.tsx
autonomous: true
requirements: [15-02, 15-13, 15-05, 15-10]
must_haves:
  truths:
    - "/admin renders only for super_admin; admin and user are redirected to /"
    - "/accept-invite, /forgot-password, /reset-password are public routes (not inside ProtectedRoute)"
    - "signUp is gone from AuthContext and Login; the Test Org A/B UUIDs are gone from src/"
    - "Login shows only sign-in + a 'Forgot password?' link; the app builds and login still works"
  artifacts:
    - path: "src/components/SuperAdminRoute.tsx"
      provides: "role-based route guard for /admin"
      contains: "super_admin"
    - path: "src/App.tsx"
      provides: "wired /admin (SuperAdminRoute) + public auth routes"
      contains: "SuperAdminRoute"
  key_links:
    - from: "App.tsx /admin route"
      to: "SuperAdminRoute → AdminPanel"
      via: "nested inside ProtectedRoute"
      pattern: "SuperAdminRoute"
---

<objective>
Wire everything and remove the dead code. Add `SuperAdminRoute` (role guard, req 2) and nest `/admin` under it inside the existing ProtectedRoute tree; add `/accept-invite`, `/forgot-password`, `/reset-password` as PUBLIC routes (they authenticate via the emailed implicit-flow link, not the app session — Pitfalls 1/2). Then delete the dead self-serve signup path: `signUp` from AuthContext, the signup JSX + hardcoded Test Org A/B UUIDs from Login, and add the "Forgot password?" link (req 13).

Purpose: This is the interface-first "wire last" step — it consumes the pages built in plans 03/09/10 and closes reqs 2 and 13. Owning all of App.tsx/Login/AuthContext here avoids file-ownership conflicts across earlier plans.
Output: SuperAdminRoute + test, wired App.tsx, cleaned AuthContext + Login.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-client-onboarding-provisioning/15-UI-SPEC.md
@.planning/phases/15-client-onboarding-provisioning/15-PATTERNS.md

<interfaces>
ProtectedRoute to clone (src/components/ProtectedRoute.tsx, full):
```tsx
export function ProtectedRoute() {
  const { session, loading } = useAuth()
  if (loading) return <div className="min-h-screen flex items-center justify-center"><div className="text-gray-500">Loading...</div></div>
  if (!session) return <Navigate to="/login" replace />
  return <Outlet />
}
```
SuperAdminRoute swaps the `!session` check for `!profile || profile.role !== 'super_admin'` → `<Navigate to="/" replace />` (NOT /login — UI-SPEC § 1). Nest INSIDE the existing ProtectedRoute so unauth still hits /login first (RESEARCH Pattern 1).
App.tsx route tree (App.tsx:35-49): add `/accept-invite`, `/forgot-password`, `/reset-password` as siblings of `/login` (public, NOT in ProtectedRoute); add `/admin` inside `<Route element={<ProtectedRoute />}>` wrapped by `<Route element={<SuperAdminRoute />}>` → `<AdminPanel />`.
AuthContext dead code to remove (AuthContext.tsx): `signUp` from the interface (:16), the `async function signUp(...)` impl (:76-83), signUp in the context value (:86), and any destructuring callers.
Login dead code to remove (Login.tsx): `isSignUp` state (:18), `signUp` destructure (:19), sign-up branch in handleSubmit (:28-45), sign-up JSX block incl. Full Name + Org selector with the two hardcoded UUIDs (:112-146: `00000000-0000-0000-0000-000000000001` / `...002`). Add below the password field: `<Link to="/forgot-password" className="text-sm text-jamo-600 hover:text-jamo-700">Forgot password?</Link>`. Leave the existing h1 weight untouched (UI-SPEC — this file is not restyled).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SuperAdminRoute guard + unit test (req 2)</name>
  <behavior>
    - When loading, renders the Loading state (no redirect)
    - When profile.role === 'super_admin', renders the Outlet (children)
    - When profile.role is 'admin' or 'user', renders a Navigate to "/"
    - When there is no profile, renders a Navigate to "/"
  </behavior>
  <read_first>
    - src/components/ProtectedRoute.tsx (full — clone structure)
    - src/context/AuthContext.tsx (confirm useAuth exposes profile + loading)
    - 15-UI-SPEC.md § 1 (deny → Navigate to "/" not "/login")
    - an existing src/components/__tests__ render test for router-context mocking
  </read_first>
  <action>
    Create src/components/SuperAdminRoute.tsx: same shape as ProtectedRoute but reading `{ profile, loading } = useAuth()`; loading → the Loading div; `!profile || profile.role !== 'super_admin'` → `<Navigate to="/" replace />`; else `<Outlet />`. Create src/components/__tests__/SuperAdminRoute.test.tsx (Vitest + @testing-library/react + a MemoryRouter, mocking useAuth): assert super_admin renders the child Outlet content, and that 'admin' and 'user' and null-profile each redirect (no child content rendered). Satisfies `npm run test:run -- SuperAdminRoute`.
  </action>
  <acceptance_criteria>
    - `grep "role !== 'super_admin'" src/components/SuperAdminRoute.tsx` returns a hit
    - `grep "Navigate to=\"/\"" src/components/SuperAdminRoute.tsx` returns a hit (NOT /login)
    - `npm run test:run -- SuperAdminRoute` passes with super_admin-allow + admin/user/null-deny cases
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- SuperAdminRoute</automated>
  </verify>
  <done>SuperAdminRoute gates on super_admin, redirects others to /, and is unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Wire routes in App.tsx</name>
  <read_first>
    - src/App.tsx (full — route nesting convention :35-49)
    - src/pages/AcceptInvite.tsx, ForgotPassword.tsx, ResetPassword.tsx (plan 03), src/pages/admin/AdminPanel.tsx (plan 09)
    - 15-PATTERNS.md § App.tsx
  </read_first>
  <action>
    Edit src/App.tsx: import AcceptInvite, ForgotPassword, ResetPassword, SuperAdminRoute, and AdminPanel. Add three PUBLIC routes as siblings of `/login` (outside ProtectedRoute): `/accept-invite` → AcceptInvite, `/forgot-password` → ForgotPassword, `/reset-password` → ResetPassword. Add `/admin` INSIDE the existing `<Route element={<ProtectedRoute />}>`, wrapped in `<Route element={<SuperAdminRoute />}>` → `<Route path="/admin" element={<AdminPanel />} />` (its own layout or reuse Layout as appropriate). Do not restructure the providers.
  </action>
  <acceptance_criteria>
    - `grep "SuperAdminRoute" src/App.tsx` and `grep "AdminPanel" src/App.tsx` return hits
    - `grep "/accept-invite" src/App.tsx`, `grep "/forgot-password" src/App.tsx`, `grep "/reset-password" src/App.tsx` all return hits
    - The three auth routes are NOT nested under ProtectedRoute (public) — verify by structure
    - Build passes
  </acceptance_criteria>
  <verify>
    <automated>grep -q "SuperAdminRoute" src/App.tsx && grep -q "accept-invite" src/App.tsx && npm run build 2>&1 | grep -qi "error TS" && echo FAIL || echo PASS</automated>
  </verify>
  <done>/admin is super_admin-gated inside ProtectedRoute; the three auth pages are reachable as public routes.</done>
</task>

<task type="auto">
  <name>Task 3: Remove dead signup code + add forgot-password link (req 13)</name>
  <read_first>
    - src/context/AuthContext.tsx (signUp interface :16, impl :76-83, value :86)
    - src/pages/Login.tsx (isSignUp :18, signUp :19, branch :28-45, signup JSX + UUIDs :112-146)
    - 15-UI-SPEC.md § 5 (Login changes — only add the forgot-password link)
  </read_first>
  <action>
    Remove `signUp` entirely from src/context/AuthContext.tsx: the interface member, the `async function signUp` implementation, the context-value entry, and any now-unused imports. In src/pages/Login.tsx remove `isSignUp` state, the `signUp` destructure, the sign-up branch in handleSubmit, and the whole sign-up JSX block including the Full Name field, the Organization `<select>`, and both hardcoded Test Org UUIDs (`...001`, `...002`). Add a single `<Link to="/forgot-password" className="text-sm text-jamo-600 hover:text-jamo-700">Forgot password?</Link>` below the password field. Do NOT restyle the existing h1 (keep its current weight). Ensure `Link` is imported from react-router-dom. The login form now offers only sign-in + forgot-password.
  </action>
  <acceptance_criteria>
    - `grep -rn "signUp" src/` returns NO hits
    - `grep -rn "00000000-0000-0000-0000-00000000000" src/` returns NO hits (Test Org A/B UUIDs gone)
    - `grep "Forgot password?" src/pages/Login.tsx` returns a hit with a Link to /forgot-password
    - `npm run build` succeeds and `npm run test:run` is green
  </acceptance_criteria>
  <verify>
    <automated>! grep -rq "signUp" src/ && ! grep -rq "00000000-0000-0000-0000-000000000001" src/ && npm run build 2>&1 | grep -qi "error TS" && echo FAIL || echo PASS</automated>
  </verify>
  <done>Dead signup path and hardcoded org UUIDs are gone; Login offers sign-in + Forgot password?; app builds and login works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /admin route → super_admin | Client-side gate (UX); real enforcement is server-side |
| public auth routes | Must not sit inside the authenticated app tree |
| dead signup path | Removing it eliminates a client-driven profile-creation vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-36 | EoP | /admin route gate | mitigate | SuperAdminRoute denies non-super_admin (redirect to /); it is UX-only — the admin-* edge functions independently re-assert super_admin (D-10), so bypassing the client gate still yields 403. |
| T-15-37 | Tampering | residual signUp path | mitigate | signUp + client-supplied org/role selector fully removed (req 13); combined with the plan-01 trigger, no client path can create a user or self-assign an org/role. Grep acceptance proves zero hits. |
| T-15-38 | Spoofing | public auth routes inside app tree | mitigate | The three auth routes are siblings of /login (outside ProtectedRoute); they force the set-password form and never expose the authenticated app on a bare implicit-flow session (Pitfalls 1/2). |
</threat_model>

<verification>
- `npm run test:run -- SuperAdminRoute` green; full `npm run test:run` green; build passes.
- `grep -rn "signUp" src/` and the Test Org UUID grep both return zero hits.
- App.tsx wires /admin (gated) + three public auth routes.
</verification>

<success_criteria>
/admin is super_admin-gated, the auth pages are reachable, and all dead self-serve signup code + hardcoded org UUIDs are removed with the app still building and login working.
</success_criteria>

<output>
After completion, create `.planning/phases/15-client-onboarding-provisioning/15-11-SUMMARY.md`. Note that req-2 route denial and the full end-to-end (reqs 5/10 via live email) are covered by the plan 02/09/10 checkpoints + phase gate.
</output>
