---
phase: 15-client-onboarding-provisioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260713000002_invites_and_trigger_hardening.sql
autonomous: true
requirements: [15-12, 15-07, 15-06, 15-08]
must_haves:
  truths:
    - "An invites table exists with email, org_id, role, invited_by, status, timestamps"
    - "handle_new_user reads org/role ONLY from the invites table, never from client raw_user_meta_data"
    - "Creating an auth user with no matching pending invite RAISEs and rolls back"
    - "user_profiles has an is_active boolean column defaulting true"
    - "Org admins can UPDATE user_profiles within their own org via RLS; cross-org is blocked"
    - "The migration is applied to the live hosted project (not just committed)"
  artifacts:
    - path: "supabase/migrations/20260713000002_invites_and_trigger_hardening.sql"
      provides: "invites table + RLS + is_active column + profiles_admin_update_same_org policy + rewritten handle_new_user"
      contains: "CREATE TABLE"
  key_links:
    - from: "handle_new_user()"
      to: "invites table"
      via: "SELECT org_id, role FROM invites WHERE lower(email)=lower(NEW.email) AND status='pending'"
      pattern: "FROM invites"
---

<objective>
Build the DB foundation for all provisioning: a dedicated `invites` table (single source of truth, D-01), the rewritten `handle_new_user` trigger that binds org/role from that table (D-02/D-03/D-04, req 12), the `user_profiles.is_active` column and same-org admin UPDATE policy needed for member management (req 7), and RLS on `invites`. Then APPLY the migration to the live hosted project.

Purpose: This migration is the fan-in dependency for every other plan in the phase — edge functions, bootstrap, and UI all read/write these objects. It also closes the req-12 tamper vector (invitee cannot self-assign org/role).
Output: One committed + applied migration file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-client-onboarding-provisioning/15-SPEC.md
@.planning/phases/15-client-onboarding-provisioning/15-CONTEXT.md
@.planning/phases/15-client-onboarding-provisioning/15-RESEARCH.md
@.planning/phases/15-client-onboarding-provisioning/15-PATTERNS.md

<interfaces>
Existing trigger to REWRITE IN PLACE (from supabase/migrations/20260305000013_rls_policies.sql:82-98) — body only, trigger definition on auth.users stays:
```sql
CREATE OR REPLACE FUNCTION handle_new_user()
RETURNS TRIGGER LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$
BEGIN
  INSERT INTO user_profiles (user_id, org_id, role, full_name)
  VALUES (NEW.id, (NEW.raw_user_meta_data->>'org_id')::UUID,
          COALESCE(NEW.raw_user_meta_data->>'role','user'),
          NEW.raw_user_meta_data->>'full_name');
  RETURN NEW;
END; $$;
```
Existing helpers (read-only reference, prove SECURITY DEFINER bypasses RLS): private.get_user_org_id(), private.get_user_role() in supabase/migrations/20260305000012_rls_helper_functions.sql.
Existing org policy (the ONLY cross-org bypass — DO NOT add a second, D-08): orgs_super_admin in 20260305000013_rls_policies.sql:17-19.
organizations columns: id, name, slug UNIQUE, plan default 'trial', is_active, feature_flags jsonb, created_at, updated_at.
user_profiles columns TODAY: id, user_id, org_id NOT NULL, role, full_name, avatar_url, created_at, updated_at (NO is_active).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write invites table, is_active column, RLS policies, and rewritten trigger</name>
  <read_first>
    - supabase/migrations/20260305000013_rls_policies.sql (handle_new_user :82-98; orgs_super_admin :17-19; profiles_update self-only :26-28; same-org SELECT policy shapes :55-78)
    - supabase/migrations/20260305000012_rls_helper_functions.sql (get_user_org_id / get_user_role signatures)
    - supabase/migrations/20260305000002_organizations.sql (table/timestamp/index conventions)
    - supabase/migrations/20260305000003_user_profiles.sql (confirm no is_active column exists)
    - 15-PATTERNS.md § migration (exact SQL excerpts, already verified against these files)
    - 15-RESEARCH.md § Architecture Patterns Pattern 3 + Pattern 2
  </read_first>
  <action>
    Create supabase/migrations/20260713000002_invites_and_trigger_hardening.sql with these statements in order:

    1. `CREATE TABLE invites` with: `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`, `email TEXT NOT NULL`, `org_id UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE`, `role TEXT NOT NULL CHECK (role IN ('super_admin','admin','user'))`, `invited_by UUID REFERENCES auth.users(id)`, `status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','accepted','revoked'))`, `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`, `updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`. (No `expired` status — per RESEARCH Open Question 1, expiry is computed from Supabase link TTL, not stored.)
    2. Add index: `CREATE INDEX idx_invites_email_status ON invites (lower(email), status);` and `CREATE INDEX idx_invites_org_id ON invites (org_id);`
    3. `ALTER TABLE invites ENABLE ROW LEVEL SECURITY;`
    4. RLS on invites (same-org only; DO NOT add a super_admin bypass — cross-org reads go through service-role edge functions per D-08):
       - `CREATE POLICY "invites_select_own_org" ON invites FOR SELECT TO authenticated USING (org_id = (SELECT private.get_user_org_id()));`
       - `CREATE POLICY "invites_insert_own_org" ON invites FOR INSERT TO authenticated WITH CHECK (org_id = (SELECT private.get_user_org_id()) AND (SELECT private.get_user_role()) IN ('admin','super_admin'));`
    5. `ALTER TABLE user_profiles ADD COLUMN is_active BOOLEAN NOT NULL DEFAULT TRUE;` (req 7, Pitfall 3 — denormalized fast-read mirror of the auth ban).
    6. New same-org admin UPDATE policy on user_profiles (today only self-update exists at :26-28) — copy the Pattern 2 shape verbatim:
       ```sql
       CREATE POLICY "profiles_admin_update_same_org" ON user_profiles
         FOR UPDATE TO authenticated
         USING (org_id = (SELECT private.get_user_org_id())
                AND (SELECT private.get_user_role()) IN ('admin','super_admin'))
         WITH CHECK (org_id = (SELECT private.get_user_org_id()));
       ```
    7. Rewrite handle_new_user body ONLY (trigger `on_auth_user_created AFTER INSERT ON auth.users` is unchanged) to the Pattern 3 SQL: DECLARE v_org_id UUID, v_role TEXT; `SELECT org_id, role INTO v_org_id, v_role FROM invites WHERE lower(email)=lower(NEW.email) AND status='pending' ORDER BY created_at DESC LIMIT 1;` then `IF v_org_id IS NULL THEN RAISE EXCEPTION 'no pending invite for %', NEW.email; END IF;` then `INSERT INTO user_profiles (user_id, org_id, role, full_name) VALUES (NEW.id, v_org_id, v_role, NEW.raw_user_meta_data->>'full_name');` (full_name is cosmetic only — never org/role, closes req 12). Keep `SECURITY DEFINER SET search_path = public`.

    Do NOT re-declare the trigger. Do NOT weaken orgs_super_admin. Do NOT create any super_admin bypass policy on invites or user_profiles (D-08 forbids it explicitly).
  </action>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE invites" supabase/migrations/20260713000002_invites_and_trigger_hardening.sql` returns 1
    - `grep "FROM invites" ...20260713000002...` shows the trigger SELECT with `status = 'pending'`
    - `grep "RAISE EXCEPTION 'no pending invite" ...20260713000002...` returns a hit
    - `grep "ADD COLUMN is_active BOOLEAN NOT NULL DEFAULT TRUE" ...20260713000002...` returns a hit
    - `grep "profiles_admin_update_same_org" ...20260713000002...` returns a hit
    - `grep -c "super_admin" ...20260713000002...` shows super_admin ONLY inside the CHECK constraint and the profiles_admin_update policy role check — NO `FOR ALL ... super_admin` bypass policy on invites/user_profiles
    - File contains no `raw_user_meta_data->>'org_id'` and no `raw_user_meta_data->>'role'` (tamper vector removed)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "FROM invites" supabase/migrations/20260713000002_invites_and_trigger_hardening.sql && ! grep -q "raw_user_meta_data->>'role'" supabase/migrations/20260713000002_invites_and_trigger_hardening.sql && echo PASS</automated>
  </verify>
  <done>Migration file exists with invites table, is_active column, same-org RLS, and the hardened trigger; no client-supplied org/role remains.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply the migration to the live hosted project</name>
  <read_first>
    - .planning/phases/15-client-onboarding-provisioning/15-CONTEXT.md § Ops constraints (db push is diverged — use MCP apply_migration)
    - MEMORY: supabase-migration-history-diverged
  </read_first>
  <action>
    Apply the migration to project ref `fuuvdcvbliijffogjnwg` using the Supabase MCP `apply_migration` tool (preferred), passing the full SQL body of the file created in Task 1 with migration name `invites_and_trigger_hardening`. DO NOT run `supabase db push` — it is diverged/unusable in this project (CONTEXT.md + MEMORY). If MCP is unavailable, fall back to the Supabase Management API with `SUPABASE_ACCESS_TOKEN`. Always keep the committed repo migration file (Task 1) in sync with what was applied. After applying, confirm the objects exist by querying: `SELECT to_regclass('public.invites')` (non-null) and `SELECT column_name FROM information_schema.columns WHERE table_name='user_profiles' AND column_name='is_active'` (one row).
  </action>
  <acceptance_criteria>
    - Supabase MCP `list_tables` (or a `SELECT to_regclass('public.invites')`) shows the `invites` table present on the live project
    - `information_schema.columns` shows `user_profiles.is_active` present
    - The `handle_new_user` function on the live DB contains `FROM invites` (query pg_proc / `\sf` equivalent via MCP execute_sql)
    - This task is [BLOCKING] — do not proceed to Task 3 or downstream plans until apply confirms
  </acceptance_criteria>
  <verify>
    <automated>MISSING — live-DB apply verified via Supabase MCP list_tables/execute_sql (no repo command; Deno/psql unavailable in dev sandbox per repo contingency)</automated>
  </verify>
  <done>invites table, is_active column, RLS policies, and hardened trigger are live on `fuuvdcvbliijffogjnwg`; repo migration committed identically.</done>
</task>

<task type="auto">
  <name>Task 3: Author the req-12 tamper-proofing verification script (manual/live)</name>
  <read_first>
    - .planning/phases/15-client-onboarding-provisioning/15-VALIDATION.md § Manual-Only (15-12 row)
    - 15-RESEARCH.md § Security Domain (Tampering threat)
  </read_first>
  <action>
    Create a documented SQL verification snippet (as a comment block appended to the migration file, or a `supabase/migrations/verify/15-12-tamper.sql` helper) that a human/agent runs against the live DB via MCP execute_sql: (a) INSERT a pending invite for `tamper-test@example.com` with Org A + role `user`; (b) via `auth.admin.createUser` (or dashboard) attempt to create that user with `raw_user_meta_data` claiming a different org_id and `role='admin'`; (c) assert the resulting `user_profiles` row has Org A + role `user` (the tampered payload was ignored); (d) separately assert that `createUser` for an email with NO pending invite RAISEs `no pending invite`. This is manual-only per VALIDATION (needs live DB + hostile payload) — capture it as an executable checklist, not a Vitest test.
  </action>
  <acceptance_criteria>
    - A verify snippet exists containing both assertions: tampered-payload-ignored AND no-invite-RAISEs
    - `grep -rn "no pending invite" supabase/migrations/` finds the RAISE reference for the reviewer
  </acceptance_criteria>
  <verify>
    <automated>test -f supabase/migrations/verify/15-12-tamper.sql || grep -q "tamper" supabase/migrations/20260713000002_invites_and_trigger_hardening.sql && echo PASS</automated>
  </verify>
  <done>The tamper-proofing check is documented and runnable against the live DB; flagged manual-only in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auth.users INSERT → handle_new_user | Invitee-controlled `raw_user_meta_data` crosses into profile creation |
| authenticated client → invites/user_profiles | Org-scoped tenants attempt cross-org reads/writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering / EoP | handle_new_user | mitigate | Trigger reads org/role ONLY from `invites` server record; `raw_user_meta_data` org/role never referenced (req 12). Verified by Task 1 grep + Task 3 live test. |
| T-15-02 | EoP | auth user with no invite | mitigate | RAISE EXCEPTION on no pending invite rolls back the auth insert (D-04/D-05 guardrail). |
| T-15-03 | Information Disclosure | invites RLS | mitigate | `invites_select_own_org` scopes reads to caller's home org; no super_admin bypass policy added (D-08) — cross-org reads only via service-role edge functions. |
| T-15-04 | Tampering | profiles_admin_update_same_org | mitigate | USING + WITH CHECK both pin `org_id = get_user_org_id()`, so an org admin cannot update a profile in another org. |
</threat_model>

<verification>
- Migration file present with all 7 statement groups; grep acceptance in Task 1 passes.
- Live DB shows invites table + is_active column + hardened trigger (Task 2, via MCP).
- Tamper/no-invite behavior documented and runnable (Task 3).
</verification>

<success_criteria>
invites table + RLS, user_profiles.is_active, same-org admin UPDATE policy, and the invites-reading hardened trigger are committed AND applied live; the client-supplied org/role tamper vector is gone.
</success_criteria>

<output>
After completion, create `.planning/phases/15-client-onboarding-provisioning/15-01-SUMMARY.md`. Flag the req-12 tamper test as a manual live-verify item for the phase gate.
</output>
