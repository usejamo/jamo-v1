---
phase: 15-client-onboarding-provisioning
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/config.toml
  - src/__tests__/config-signup-disabled.test.ts
autonomous: false
requirements: [15-01, 15-09, 15-05, 15-10]
user_setup:
  - service: resend
    why: "Production email transport for Supabase Auth invite + recovery emails (D-09, req 9)"
    env_vars:
      - name: RESEND_SMTP_PASSWORD
        source: "Resend Dashboard → API Keys (used as SMTP password; SMTP user is 'resend')"
    dashboard_config:
      - task: "Verify sending domain (SPF, DKIM, DMARC DNS records)"
        location: "Resend Dashboard → Domains"
      - task: "Configure Supabase custom SMTP (host smtp.resend.com, port 465/587, user 'resend', pass = API key, sender = verified address)"
        location: "Supabase Dashboard → Project fuuvdcvbliijffogjnwg → Authentication → Emails/SMTP"
      - task: "Add /accept-invite, /forgot-password, /reset-password (local + prod Netlify domain) to Redirect URLs allow-list"
        location: "Supabase Dashboard → Authentication → URL Configuration"
      - task: "Raise Auth email rate limit to ~100/hour (custom SMTP default is 30/hour, Pitfall 5)"
        location: "Supabase Dashboard → Authentication → Rate Limits"
must_haves:
  truths:
    - "Committed config.toml has signup disabled everywhere (no true/false divergence)"
    - "config.toml SMTP block is uncommented and points at Resend"
    - "The three auth redirect routes are in additional_redirect_urls"
    - "Live invite + reset emails deliver from the Resend sender (not inbucket)"
  artifacts:
    - path: "supabase/config.toml"
      provides: "signup-disabled auth config + Resend SMTP + redirect allow-list + raised rate limit"
      contains: "enable_signup = false"
  key_links:
    - from: "inviteUserByEmail/resetPasswordForEmail redirectTo"
      to: "additional_redirect_urls allow-list"
      via: "config.toml + hosted dashboard"
      pattern: "accept-invite"
---

<objective>
Lock down auth config and wire production email. Flip `enable_signup` to false in the committed `config.toml` (both occurrences) so local no longer diverges from prod (req 1). Uncomment + configure the Resend SMTP block, raise the email rate limit, and add the three auth flow routes to the redirect allow-list (reqs 5, 9, 10 — Pitfall 6). Then a human completes the parts Claude cannot: Resend account, DNS verification, hosted-dashboard SMTP, dashboard redirect allow-list, and dashboard rate limit.

Purpose: Every email-dependent requirement (4, 5, 8, 9, 10) is blocked until production SMTP delivers and redirect URLs are allow-listed.
Output: Committed config.toml + a passing static config test + a completed human-setup checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-client-onboarding-provisioning/15-SPEC.md
@.planning/phases/15-client-onboarding-provisioning/15-RESEARCH.md
@.planning/phases/15-client-onboarding-provisioning/15-PATTERNS.md

<interfaces>
config.toml current state (verified by grep):
- line 154: `site_url = "http://127.0.0.1:3000"`
- line 156: `additional_redirect_urls = ["https://127.0.0.1:3000"]`
- line 169: `enable_signup = true`  → flip to false
- line 182: `email_sent = 2`  → raise (e.g. 100)
- line 204: `enable_signup = true`  → flip to false
- line 209: `enable_confirmations = false`
- line 217: `otp_expiry = 3600`
- lines 220-227: commented `# [auth.email.smtp]` block → uncomment + point at Resend
- lines 242,244: a second section already has enable_signup=false / enable_confirmations=false (leave as-is unless it is the [auth] under edit)
MEMORY: edge-deploy-token-from-env (sbp_ token in .env), demo-deploy-signups-disabled.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Disable signup, wire Resend SMTP, redirect URLs, and rate limit in config.toml</name>
  <read_first>
    - supabase/config.toml (lines 150-245 — the [auth] and [auth.email] and [auth.rate_limit] sections)
    - 15-PATTERNS.md § config.toml (exact lines to flip)
    - 15-RESEARCH.md § Common Pitfalls 5 (rate limit) and 6 (redirect allow-list)
  </read_first>
  <action>
    Edit supabase/config.toml:
    1. Set BOTH `enable_signup = true` occurrences (lines ~169 and ~204) to `enable_signup = false` (req 1 — no local/prod divergence).
    2. Uncomment the `[auth.email.smtp]` block (~220-227) and set: `enabled = true`, `host = "smtp.resend.com"`, `port = 465`, `user = "resend"`, `pass = "env(RESEND_SMTP_PASSWORD)"`, `admin_email = "<verified-sender@yourdomain>"`, `sender_name = "Jamo"`. Use the `env(...)` form so no secret is committed.
    3. Add the three auth-flow routes to `additional_redirect_urls` — append both local and production forms: `"http://127.0.0.1:3000/accept-invite"`, `"http://127.0.0.1:3000/forgot-password"`, `"http://127.0.0.1:3000/reset-password"`, plus the production Netlify domain equivalents (Pitfall 6 — silent fallback if missing).
    4. Raise `[auth.rate_limit] email_sent` from 2 to 100 (Pitfall 5 — custom SMTP default 30/hr still needs an explicit raise for invite bursts).
    Do NOT commit any real API key — only the `env(RESEND_SMTP_PASSWORD)` reference.
  </action>
  <acceptance_criteria>
    - `grep -c "enable_signup = true" supabase/config.toml` returns 0
    - `grep "host = \"smtp.resend.com\"" supabase/config.toml` returns a hit
    - `grep "pass = \"env(RESEND_SMTP_PASSWORD)\"" supabase/config.toml` returns a hit (no literal secret)
    - `grep "accept-invite" supabase/config.toml` returns a hit inside additional_redirect_urls
    - `grep "email_sent = 100" supabase/config.toml` returns a hit
    - No literal `re_` or `sbp_` secret string committed in config.toml
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "enable_signup = true" supabase/config.toml && grep -q "smtp.resend.com" supabase/config.toml && grep -q "accept-invite" supabase/config.toml && echo PASS</automated>
  </verify>
  <done>config.toml shows signup disabled (no divergence), Resend SMTP via env ref, three redirect routes, and a raised rate limit.</done>
</task>

<task type="auto">
  <name>Task 2: Static test asserting signup is disabled in committed config (req 1)</name>
  <read_first>
    - vitest.config.ts (repo root)
    - 15-VALIDATION.md § Per-Task map (15-01 row — grep-based config assertion)
    - an existing test under src for the import/style convention
  </read_first>
  <action>
    Create src/__tests__/config-signup-disabled.test.ts (Vitest): read `supabase/config.toml` as text via `fs.readFileSync`, assert it does NOT contain `enable_signup = true`, DOES contain `smtp.resend.com`, and DOES contain `accept-invite`. This is the automated guard for req 1 and the redirect allow-list. Keep runtime trivial (<1s).
  </action>
  <acceptance_criteria>
    - `npm run test:run -- config-signup-disabled` passes
    - Test asserts absence of `enable_signup = true` AND presence of `smtp.resend.com`
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- config-signup-disabled</automated>
  </verify>
  <done>A committed Vitest test fails if signup is ever re-enabled or SMTP config is removed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>config.toml now references Resend SMTP via env and lists the redirect routes. The hosted project still needs the account/DNS/dashboard side, which has no CLI/API an agent can complete unattended (Resend account creation + DNS records require domain ownership).</what-built>
  <how-to-verify>
    1. Create a Resend account and add your sending domain; add the SPF, DKIM, and DMARC DNS records Resend shows and wait for "Verified".
    2. Create a Resend API key; put it in `.env` as `RESEND_SMTP_PASSWORD=re_...` (never commit).
    3. In Supabase Dashboard (project fuuvdcvbliijffogjnwg) → Authentication → set custom SMTP: host `smtp.resend.com`, port 465, user `resend`, password = the API key, sender = your verified address, sender name `Jamo`.
    4. Authentication → URL Configuration → add `/accept-invite`, `/forgot-password`, `/reset-password` for BOTH the local origin and the production Netlify domain to the Redirect URLs allow-list.
    5. Authentication → Rate Limits → raise "Emails sent" to ~100/hour.
    6. Send one test invite (or use the Supabase "send test email") and confirm it arrives from the Resend sender (not inbucket/supabase default).
  </how-to-verify>
  <resume-signal>Type "smtp-verified" once a test email delivers from the Resend sender and the three redirect URLs are allow-listed, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.toml / repo → secrets | SMTP credentials must not be committed |
| emailed redirect link → app routes | Open-redirect surface if allow-list is loose |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Information Disclosure | SMTP credentials | mitigate | Password stored as `env(RESEND_SMTP_PASSWORD)` reference; real key only in `.env`/dashboard, never committed. Task 1 grep guards against literal secrets. |
| T-15-06 | Spoofing (email) | Resend sender domain | mitigate | SPF/DKIM/DMARC verification required before send (human checkpoint) — unverified domain cannot authenticate outbound mail. |
| T-15-07 | Tampering (open redirect) | additional_redirect_urls | mitigate | Only the three explicit auth routes (local + prod) are allow-listed; Supabase silently drops any other redirectTo (Pitfall 6). |
| T-15-08 | DoS | Auth email rate limit | accept | Raised to ~100/hr — bounded; a burst beyond that fails closed with 429 rather than unbounded send. |
</threat_model>

<verification>
- config.toml grep acceptance passes; no committed secrets.
- Vitest config test green.
- Human checkpoint confirms live Resend delivery + allow-listed redirects.
</verification>

<success_criteria>
Signup is disabled with no divergence; Resend SMTP delivers live invite/reset email from a verified sender; the three auth routes are allow-listed both in config.toml and the hosted dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/15-client-onboarding-provisioning/15-02-SUMMARY.md`. Record the Resend sender address and note SMTP delivery as human-verified.
</output>
