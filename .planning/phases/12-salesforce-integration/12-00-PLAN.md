---
phase: 12
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/salesforce-pkce.test.ts
  - tests/salesforce-state.test.ts
  - tests/SalesforceConnection.test.tsx
autonomous: true
requirements:
  - REQ-12.1
  - REQ-12.5
  - REQ-12.6

must_haves:
  truths:
    - "npm run test:run exits 0 after Wave 0 (all stubs skipped, full suite green)"
    - "Every REQ-12.1, REQ-12.5, REQ-12.6 behavior has a named it.skip target"
    - "No dynamic imports in test files (Vite resolves at transform time)"
  artifacts:
    - path: "tests/salesforce-pkce.test.ts"
      provides: "Nyquist stubs for PKCE verifier/challenge generation"
      contains: "it.skip"
    - path: "tests/salesforce-state.test.ts"
      provides: "Nyquist stubs for state sign/verify round-trip"
      contains: "it.skip"
    - path: "tests/SalesforceConnection.test.tsx"
      provides: "Nyquist stubs for disconnected/connected/error/graceful degradation states"
      contains: "it.skip"
  key_links:
    - from: "tests/salesforce-pkce.test.ts"
      to: "REQ-12.1"
      via: "named it.skip stubs"
      pattern: "it\\.skip.*PKCE"
    - from: "tests/SalesforceConnection.test.tsx"
      to: "REQ-12.5, REQ-12.6"
      via: "named it.skip stubs"
      pattern: "it\\.skip.*disconnected|connected|error|graceful"
---

<objective>
Create Wave 0 Nyquist-compliant test stub files for Phase 12. All stubs use `it.skip` (NOT dynamic imports). Full test suite stays green after this plan executes.

Purpose: Establish named verification targets for every REQ-12.1, REQ-12.5, and REQ-12.6 behavior before any implementation begins. The executor cannot move to Wave 1 until `npm run test:run` exits 0.

Output: Three test stub files in `tests/`. No production code. No imports of files that don't exist yet.
</objective>

<execution_context>
@C:/Users/aaron/VS Code Projects/Jamo/jamo-v1/.planning/phases/12-salesforce-integration/12-00-PLAN.md
</execution_context>

<context>
@C:/Users/aaron/VS Code Projects/Jamo/jamo-v1/.planning/ROADMAP.md
@C:/Users/aaron/VS Code Projects/Jamo/jamo-v1/.planning/STATE.md

<interfaces>
<!-- Established project stub test pattern (from STATE.md Active Decisions) -->
<!-- RULE: Use `it.skip` — NOT dynamic imports. Vite resolves all imports at transform time. -->
<!-- RULE: Do NOT import files that don't exist yet — causes transform-time failure. -->

Stub pattern:
```typescript
import { describe, it } from 'vitest'

describe('FeatureName', () => {
  it.skip('does something', () => {})
  it.skip('handles edge case', () => {})
})
```

From vitest.config.ts (established):
- pool: forks, singleFork: true
- environment: happy-dom
- Quick run: npm run test:run (no --coverage flag)
- Filter by file: npm run test:run -- tests/salesforce-pkce.test.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PKCE test stubs (tests/salesforce-pkce.test.ts)</name>
  <files>tests/salesforce-pkce.test.ts</files>
  <read_first>
    - C:/Users/aaron/VS Code Projects/Jamo/jamo-v1/.planning/phases/12-salesforce-integration/12-VALIDATION.md (Wave 0 requirements, exact behavior names)
    - C:/Users/aaron/VS Code Projects/Jamo/jamo-v1/.planning/phases/12-salesforce-integration/12-RESEARCH.md (PKCE verifier/challenge spec: RFC 7636 S256, generateCodeVerifier, generateCodeChallenge)
  </read_first>
  <action>
Create `tests/salesforce-pkce.test.ts`. Use ONLY `import { describe, it } from 'vitest'` — no imports of non-existent files.

Stubs to write (all `it.skip`):
1. "generateCodeVerifier returns a 43-character base64url string"
2. "generateCodeVerifier output contains only base64url chars (A-Z a-z 0-9 - _)"
3. "generateCodeChallenge produces S256 challenge: base64url(SHA-256(verifier))"
4. "generateCodeChallenge challenge is different from verifier"
5. "base64URLEncode strips = padding characters"
6. "base64URLEncode replaces + with - and / with _"

File must start with: `// Wave 0 stubs — REQ-12.1 PKCE generation`
  </action>
  <verify>
    <automated>cd "C:/Users/aaron/VS Code Projects/Jamo/jamo-v1" && npm run test:run -- tests/salesforce-pkce.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>File exists at tests/salesforce-pkce.test.ts. npm run test:run -- tests/salesforce-pkce.test.ts exits 0 with 6 skipped tests and 0 failures.</done>
  <acceptance_criteria>
    - grep -c "it.skip" tests/salesforce-pkce.test.ts returns 6
    - grep "generateCodeVerifier\|generateCodeChallenge\|base64URLEncode" tests/salesforce-pkce.test.ts returns 6 matches
    - grep "import.*from.*vitest" tests/salesforce-pkce.test.ts returns exactly 1 import line (vitest only, no production file imports)
    - npm run test:run -- tests/salesforce-pkce.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create state signing test stubs (tests/salesforce-state.test.ts)</name>
  <files>tests/salesforce-state.test.ts</files>
  <read_first>
    - C:/Users/aaron/VS Code Projects/Jamo/jamo-v1/.planning/phases/12-salesforce-integration/12-VALIDATION.md (Wave 0 requirements)
    - C:/Users/aaron/VS Code Projects/Jamo/jamo-v1/.planning/phases/12-salesforce-integration/12-RESEARCH.md (signState/verifyState HMAC-SHA256 pattern, state format: base64(orgId:nonce).sig)
  </read_first>
  <action>
Create `tests/salesforce-state.test.ts`. Use ONLY `import { describe, it } from 'vitest'` — no imports of non-existent files.

Stubs to write (all `it.skip`):
1. "signState returns a string containing exactly one dot separator"
2. "signState encodes org_id and nonce in the payload section"
3. "verifyState returns orgId and nonce when signature is valid"
4. "verifyState returns null when signature has been tampered"
5. "verifyState returns null when state token has wrong format (no dot)"
6. "signState + verifyState round-trip returns original orgId and nonce"

File must start with: `// Wave 0 stubs — REQ-12.1 state HMAC sign/verify`
  </action>
  <verify>
    <automated>cd "C:/Users/aaron/VS Code Projects/Jamo/jamo-v1" && npm run test:run -- tests/salesforce-state.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>File exists at tests/salesforce-state.test.ts. npm run test:run exits 0 with 6 skipped tests and 0 failures.</done>
  <acceptance_criteria>
    - grep -c "it.skip" tests/salesforce-state.test.ts returns 6
    - grep "signState\|verifyState\|round-trip" tests/salesforce-state.test.ts returns 6 matches
    - grep "import.*from.*vitest" tests/salesforce-state.test.ts returns exactly 1 import line (vitest only)
    - npm run test:run -- tests/salesforce-state.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create SalesforceConnection component test stubs (tests/SalesforceConnection.test.tsx)</name>
  <files>tests/SalesforceConnection.test.tsx</files>
  <read_first>
    - C:/Users/aaron/VS Code Projects/Jamo/jamo-v1/.planning/phases/12-salesforce-integration/12-VALIDATION.md (Wave 0 requirements — 12-02-01 through 12-02-05)
    - C:/Users/aaron/VS Code Projects/Jamo/jamo-v1/.planning/phases/12-salesforce-integration/12-UI-SPEC.md (state machine: disconnected/connected/error/graceful degradation)
    - C:/Users/aaron/VS Code Projects/Jamo/jamo-v1/.planning/phases/12-salesforce-integration/12-CONTEXT.md (D-11, D-12, D-13, D-14, D-16 — exact error copy and behavior specs)
  </read_first>
  <action>
Create `tests/SalesforceConnection.test.tsx`. Use ONLY `import { describe, it } from 'vitest'` — NO import of SalesforceConnection (file does not exist yet). NO dynamic imports.

Stubs to write (all `it.skip`):
1. "renders disconnected state with Production/Sandbox radio and Connect Salesforce button"
2. "defaults to Production radio selected"
3. "renders connected state with sf_username and Disconnect button"
4. "renders Connected status badge with green dot in connected state"
5. "renders inline error banner when sf_error=user_denied query param is present"
6. "renders inline error banner when sf_error=state_mismatch query param is present"
7. "renders inline error banner when sf_error=token_exchange_failed query param is present"
8. "renders inline error banner when sf_error=userinfo_failed query param is present"
9. "renders inline error banner when sf_error=unknown query param is present"
10. "removes sf_error from URL after rendering error banner (D-15)"
11. "renders disconnected state (not thrown) when salesforce_connections returns null (REQ-12.6)"
12. "renders disconnected state (not thrown) when salesforce_connections fetch errors (REQ-12.6)"

File must start with: `// Wave 0 stubs — REQ-12.5, REQ-12.6 SalesforceConnection component`
  </action>
  <verify>
    <automated>cd "C:/Users/aaron/VS Code Projects/Jamo/jamo-v1" && npm run test:run -- tests/SalesforceConnection.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <done>File exists at tests/SalesforceConnection.test.tsx. npm run test:run exits 0 with 12 skipped tests and 0 failures.</done>
  <acceptance_criteria>
    - grep -c "it.skip" tests/SalesforceConnection.test.tsx returns 12
    - grep "import.*from.*vitest" tests/SalesforceConnection.test.tsx returns exactly 1 import line
    - grep "SalesforceConnection" tests/SalesforceConnection.test.tsx does NOT appear in any import statement (only in describe/it strings)
    - npm run test:run -- tests/SalesforceConnection.test.tsx exits 0
    - npm run test:run exits 0 (full suite green after all 3 stubs created)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production code | Wave 0 contains no production code; no trust boundary active |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-W0-01 | Information Disclosure | test stub files | accept | No secrets or real tokens in stub files; stubs are `it.skip` only |
</threat_model>

<verification>
After all three tasks complete:
- `npm run test:run` exits 0 (full suite green)
- 24 total skipped tests across the 3 new files
- Zero new test failures introduced
- All 3 files exist under `tests/`
</verification>

<success_criteria>
- tests/salesforce-pkce.test.ts exists with 6 it.skip stubs covering generateCodeVerifier, generateCodeChallenge, base64URLEncode
- tests/salesforce-state.test.ts exists with 6 it.skip stubs covering signState, verifyState, round-trip
- tests/SalesforceConnection.test.tsx exists with 12 it.skip stubs covering disconnected, connected, all 5 error codes, URL cleanup, and 2 graceful degradation cases
- npm run test:run exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/12-salesforce-integration/12-00-SUMMARY.md`
</output>
