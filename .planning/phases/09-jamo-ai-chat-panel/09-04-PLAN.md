---
phase: 09-jamo-ai-chat-panel
plan: 04
type: human-verify
wave: 4
depends_on: [09-03]
files_modified:
  - src/components/__tests__/AIChatPanel.test.tsx
  - .planning/phases/09-jamo-ai-chat-panel/09-SUMMARY.md
autonomous: false
requirements: [REQ-6.1, REQ-6.2, REQ-6.3, REQ-6.4, REQ-6.5, REQ-6.6, REQ-6.7]

must_haves:
  truths:
    - "All AIChatPanel test stubs from 09-00 are activated (unskipped)"
    - "Full test suite passes"
    - "Human has verified chat flows in the browser"
  artifacts:
    - path: "src/components/__tests__/AIChatPanel.test.tsx"
      provides: "Activated tests — no remaining it.skip stubs"
    - path: ".planning/phases/09-jamo-ai-chat-panel/09-SUMMARY.md"
      provides: "Phase completion summary"
---

<objective>
Activate skipped tests, verify the full test suite passes, and complete a human sign-off on all Phase 9 features in the browser.

Purpose: Final quality gate before the phase is marked complete.
Output: All tests passing. Human verification checklist completed and logged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-jamo-ai-chat-panel/09-CONTEXT.md
@src/components/__tests__/AIChatPanel.test.tsx
@src/utils/__tests__/chatContext.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate AIChatPanel test stubs</name>
  <files>src/components/__tests__/AIChatPanel.test.tsx</files>
  <action>
Read `src/components/__tests__/AIChatPanel.test.tsx`.

Replace every `it.skip(` with `it(` to activate all tests created in plan 09-00.

For each test, implement the body using Vitest mocking patterns consistent with the existing test files in `src/components/editor/__tests__/`:

1. **'renders gap badge count on rail when gapCount > 0'** — render `<AIChatPanel ... gapCount={3} />`, assert badge text "3" is in the document.

2. **'hides gap badge when gapCount is 0'** — render with `gapCount={0}`, assert no badge element visible.

3. **'calls insertContentAt on accept of edit proposal'** — mock `editorRefs` Map with a mock handle (`{ insertContentAt: vi.fn() }`), simulate an edit message acceptance, assert `insertContentAt` was called.

4. **'streams content into message bubble without layout thrash'** — mock `supabase.functions.invoke` to return a readable stream emitting a few `content_block_delta` events. Assert `streamingContent` state updates and final message contains expected text.

5. **'shows explain chip when activeSectionKey is set'** — render with `activeSectionKey="understanding"`, assert "Explain this section" chip is visible.

6. **'hides explain chip when no section targeted'** — render with `activeSectionKey={null}`, assert chip not in document.

7. **'persists messages to proposal_chats on send'** — mock `supabase.from('proposal_chats').insert`, submit a message, assert insert was called with correct `proposal_id` and `message_type`.

8. **'displays citations in explain response'** — add an assistant message with `citations` array to messages state, render, assert citation source text appears in the document.

For the Supabase mock, use the existing pattern from other test files (check how `src/context/AuthContext.tsx` tests mock supabase). Keep mocks minimal — mock only what's needed per test.

Run `npx vitest run src/components/__tests__/AIChatPanel.test.tsx` and fix any failures before declaring done.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/AIChatPanel.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 8 AIChatPanel tests pass. No skipped tests remain in this file.</done>
</task>

<task type="auto">
  <name>Task 2: Full suite green</name>
  <files></files>
  <action>
Run the full test suite:
```bash
npx vitest run --reporter=verbose 2>&1 | tail -20
```

If any tests are failing that were passing before Phase 9 work began, investigate and fix. Do not skip or delete existing tests to make the suite pass.

Common issues to check:
- ProposalDetail tests: if mocking AIChatPanel, the new props may need to be added to the mock
- SectionWorkspace tests: editorRefsRef is optional so existing tests should still pass — verify
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | grep -E "Tests|passed|failed" | tail -5</automated>
  </verify>
  <done>Full suite passes. Zero failures. Zero regressions from pre-Phase-9 baseline.</done>
</task>

<task type="human">
  <name>Task 3: Human verification — browser sign-off</name>
  <files></files>
  <action>
Start the dev server (`npm run dev`) and verify each feature in the browser with a live proposal that has generated sections.

**Verification Checklist:**

### Gap Analysis & Badge
- [ ] After triggering proposal generation, the Jamo chat rail shows an orange badge with the correct gap count once generation completes
- [ ] Badge does NOT auto-open the chat panel (panel stays collapsed)
- [ ] Opening the panel shows Jamo's intro message ("I found N things worth addressing...") without a standard greeting
- [ ] Up to 2 individual gap messages appear, each ending with a question
- [ ] If more than 2 gaps, a consolidation message appears as the 3rd
- [ ] Badge clears after gap messages are injected

### Live Chat
- [ ] Typing a message and pressing Enter/Send calls the real Edge Function (no demo responses)
- [ ] Streaming content appears in the chat bubble as it arrives
- [ ] Messages persist — refreshing the page does NOT lose chat history (Supabase backed)
- [ ] Panel header shows the active target section name (D-08)

### Edit Proposal Flow
- [ ] Asking Jamo to "rewrite" or "expand" a section streams the edit into a preview bubble
- [ ] Accept/Reject buttons appear when streaming completes
- [ ] Clicking Accept injects the content into the correct TipTap section
- [ ] Cmd+Z undoes the injection
- [ ] Clicking Reject dismisses the preview — section unchanged

### Explain This Section
- [ ] "Explain this section" quick chip is visible when a section is targeted
- [ ] Clicking the chip sends the explain request
- [ ] Response includes inline citations (source document name + passage)
- [ ] Chip is hidden when no section is targeted

### Document-Grounded Questions
- [ ] Asking "what does the RFP say about..." triggers RAG retrieval
- [ ] Response cites source documents
- [ ] General questions (not RAG-related) respond without RAG delay

### Regression Check
- [ ] Section generation still works normally (Phase 7)
- [ ] Section editing (expand/condense/rewrite from toolbar) still works (Phase 8)
- [ ] Version history still accessible

**Pass criteria:** All checklist items checked. Note any deviations or issues in the SUMMARY.md.
  </action>
  <done>Human has checked all items above. Phase 9 features work as designed per CONTEXT.md decisions D-01 through D-20.</done>
</task>

</tasks>

<verification>
```bash
# All tests pass
npx vitest run 2>&1 | grep -E "Tests|passed|failed" | tail -3

# No skipped tests in AIChatPanel test file
grep "it.skip" src/components/__tests__/AIChatPanel.test.tsx | wc -l
# Expected: 0
```
</verification>

<success_criteria>
- All 8 AIChatPanel tests activated and passing
- Full test suite passes with zero failures
- Human has verified all chat flows in browser (checklist above)
- Phase 9 SUMMARY.md written documenting decisions confirmed and any deviations
</success_criteria>

<output>
After completion, create `.planning/phases/09-jamo-ai-chat-panel/09-SUMMARY.md` documenting:
- What was built
- Any deviations from the plan and why
- Human verification outcome (pass/fail per checklist item)
- Known issues or follow-up items

Then update `.planning/STATE.md` to mark Phase 9 as complete.
</output>
