---
phase: "13"
plan: "05"
type: execute
wave: 4
depends_on:
  - "13-01"
  - "13-02"
  - "13-03"
  - "13-04"
files_modified:
  - supabase/migrations/20260507000029_rename_in_review_to_in_progress.sql
autonomous: false
requirements:
  - REQ-11.1

must_haves:
  truths:
    - "Migration 20260507000029 has been applied to the remote Supabase project"
    - "All existing proposals rows with status='in_review' now have status='in_progress' in the live DB"
    - "Human has verified the push succeeded and confirmed no in_review rows remain"
  artifacts:
    - path: supabase/migrations/20260507000029_rename_in_review_to_in_progress.sql
      provides: "Applied migration"
      contains: "UPDATE proposals SET status"
  key_links:
    - from: supabase/migrations/20260507000029_rename_in_review_to_in_progress.sql
      to: proposals table (remote)
      via: "supabase db push"
      pattern: "supabase db push"
---

<objective>
[BLOCKING] Push the in_review→in_progress data migration to the remote Supabase project and verify it applied cleanly. This plan is autonomous:false because supabase db push may require interactive auth confirmation.

Purpose: Without this push, the TypeScript rename in Plans 01–04 is inconsistent with live DB data — proposals with status='in_review' in the DB would not render correctly in the UI.
Output: Remote DB updated, all in_review rows renamed to in_progress.
</objective>

<execution_context>
@C:/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-dashboard-proposal-lifecycle/13-CONTEXT.md
@.planning/phases/13-dashboard-proposal-lifecycle/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Push schema migration to remote Supabase</name>
  <files>supabase/migrations/20260507000029_rename_in_review_to_in_progress.sql</files>
  <read_first>
    - supabase/migrations/20260507000029_rename_in_review_to_in_progress.sql (confirm UPDATE statement is correct before pushing)
  </read_first>
  <action>
Attempt to push the migration using the Supabase CLI:

```bash
npx supabase db push
```

If the CLI requires login, run:
```bash
npx supabase login
```
Then retry `npx supabase db push`.

If the push completes successfully, verify the migration applied by checking the remote DB. The Supabase MCP tool can be used to verify:
```sql
SELECT COUNT(*) FROM proposals WHERE status = 'in_review';
-- Expected: 0 rows
SELECT COUNT(*) FROM proposals WHERE status = 'in_progress';
-- Expected: all formerly in_review rows
```

If `npx supabase db push` produces interactive prompts that cannot be suppressed without the `SUPABASE_ACCESS_TOKEN` env var set, escalate to the checkpoint task below.
  </action>
  <verify>
    <automated>npx supabase migration list 2>&1 | grep "20260507000029"</automated>
  </verify>
  <done>
    - Migration 20260507000029 appears in `supabase migration list` as applied
    - SQL query `SELECT COUNT(*) FROM proposals WHERE status = 'in_review'` returns 0
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Supabase migration pushed: UPDATE proposals SET status='in_progress' WHERE status='in_review'
  </what-built>
  <how-to-verify>
    1. Open the Supabase Dashboard: https://supabase.com/dashboard/project/fuuvdcvbliijffogjnwg/editor
    2. Run this SQL query:
       ```sql
       SELECT status, COUNT(*) FROM proposals GROUP BY status;
       ```
    3. Confirm: no row with status = 'in_review' appears in results
    4. Confirm: migration 20260507000029 appears in the migrations table:
       ```sql
       SELECT name FROM supabase_migrations.schema_migrations ORDER BY inserted_at DESC LIMIT 5;
       ```
    5. Open the app in the browser. Navigate to any proposal. Confirm status shows "In Progress" (not "In Review") for previously in_review proposals.
  </how-to-verify>
  <resume-signal>Type "approved" if the DB shows no in_review rows and the UI shows "In Progress". Describe any issues if not.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → remote Supabase DB | supabase db push authenticates via Supabase CLI token; elevated DB role for migrations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-11 | Tampering | supabase db push | mitigate | Migration is idempotent — UPDATE WHERE status='in_review' affects 0 rows if already run; safe to re-push |
| T-13-12 | Denial of Service | DB migration during usage | accept | MVP scale; UPDATE on proposals table is fast (small table); no downtime risk |
</threat_model>

<verification>
1. `npx supabase migration list` shows 20260507000029 as applied
2. SQL: `SELECT COUNT(*) FROM proposals WHERE status = 'in_review'` → 0
3. App loads and previously in_review proposals display "In Progress" status badge
</verification>

<success_criteria>
- Remote Supabase DB has no rows with status='in_review'
- Migration appears as applied in supabase migration list
- Human confirms via dashboard SQL and browser visual check
</success_criteria>

<output>
After completion, create `.planning/phases/13-dashboard-proposal-lifecycle/13-05-SUMMARY.md`
</output>
