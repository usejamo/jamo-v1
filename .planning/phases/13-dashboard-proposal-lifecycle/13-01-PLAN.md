---
phase: "13"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260507000029_rename_in_review_to_in_progress.sql
  - src/types/proposal.ts
  - src/data/proposals.json
autonomous: true
requirements:
  - REQ-11.1

must_haves:
  truths:
    - "ProposalStatus type no longer contains 'in_review' — it contains 'in_progress'"
    - "All proposals.json seed records that had status 'in_review' now use 'in_progress'"
    - "Migration SQL updates existing DB rows with status='in_review' to status='in_progress'"
  artifacts:
    - path: supabase/migrations/20260507000029_rename_in_review_to_in_progress.sql
      provides: "Data migration updating in_review → in_progress in proposals table"
      contains: "UPDATE proposals SET status"
    - path: src/types/proposal.ts
      provides: "ProposalStatus union type"
      contains: "in_progress"
  key_links:
    - from: src/types/proposal.ts
      to: src/pages/Dashboard.tsx
      via: "ProposalStatus import"
      pattern: "ProposalStatus"
    - from: supabase/migrations/20260507000029_rename_in_review_to_in_progress.sql
      to: proposals table
      via: "UPDATE statement"
      pattern: "UPDATE proposals"
---

<objective>
Rename the proposal status value `in_review` to `in_progress` at the data layer: new Supabase migration that updates existing rows, updated TypeScript type, and updated seed/fixture JSON. This is the foundation all other Phase 13 plans depend on.

Purpose: D-16/D-17 require a full rename — not a label change. The migration + type change must land first so downstream code can import the correct type string.
Output: Migration SQL file, updated ProposalStatus type, updated proposals.json seed data.
</objective>

<execution_context>
@C:/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-dashboard-proposal-lifecycle/13-CONTEXT.md
@.planning/phases/13-dashboard-proposal-lifecycle/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration to rename in_review → in_progress in DB rows</name>
  <files>supabase/migrations/20260507000029_rename_in_review_to_in_progress.sql</files>
  <read_first>
    - supabase/migrations/20260305000004_proposals.sql (proposals table definition, status comment)
    - supabase/migrations/20260305000013_rls_policies.sql (check for any in_review string references in RLS — confirmed none exist but verify)
  </read_first>
  <action>
Create `supabase/migrations/20260507000029_rename_in_review_to_in_progress.sql` with exactly this content:

```sql
-- Phase 13: Rename proposal status 'in_review' → 'in_progress'
-- The status column is TEXT (not an enum), so this is a data migration only.
-- No ALTER TYPE required.

UPDATE proposals
SET status = 'in_progress'
WHERE status = 'in_review';

COMMENT ON COLUMN proposals.status IS
  'Lifecycle status. Values: draft | in_progress | submitted | won | lost';
```

The status column is TEXT (confirmed in 20260305000004_proposals.sql line 6) — no Postgres enum to rename. The migration simply updates existing rows and replaces the comment that listed `in_review`.
  </action>
  <verify>
    <automated>grep -n "UPDATE proposals SET status" "supabase/migrations/20260507000029_rename_in_review_to_in_progress.sql"</automated>
  </verify>
  <done>Migration file exists with UPDATE statement targeting status = 'in_review'</done>
</task>

<task type="auto">
  <name>Task 2: Rename in_review → in_progress in TypeScript type and seed JSON</name>
  <files>src/types/proposal.ts, src/data/proposals.json</files>
  <read_first>
    - src/types/proposal.ts (current ProposalStatus union — line 1)
    - src/data/proposals.json (seed records with status field)
  </read_first>
  <action>
**src/types/proposal.ts — line 1:**
Change:
```typescript
export type ProposalStatus = 'draft' | 'in_review' | 'submitted' | 'won' | 'lost'
```
To:
```typescript
export type ProposalStatus = 'draft' | 'in_progress' | 'submitted' | 'won' | 'lost'
```
No other changes to this file.

**src/data/proposals.json:**
Find every record with `"status": "in_review"` and change the value to `"in_progress"`. There are at least 2 such records (confirmed at lines 30 and 96). Search the entire file for `"in_review"` and replace all occurrences with `"in_progress"`.

After both edits: run `npm run test:run` to confirm no TypeScript compile errors or test regressions from the type change. Any existing test referencing `'in_review'` as a status string must also be updated to `'in_progress'`.
  </action>
  <verify>
    <automated>npm run test:run</automated>
  </verify>
  <done>
    - src/types/proposal.ts line 1 contains 'in_progress' and does NOT contain 'in_review'
    - src/data/proposals.json contains no occurrences of "in_review"
    - npm run test:run exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → Postgres | SQL runs with elevated DB role; UPDATE is org-agnostic (intentional — renames all org data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | Migration file | accept | Migration is additive data-only UPDATE; no DROP or schema destruction; reviewed before push |
| T-13-02 | Denial of Service | proposals table UPDATE | accept | Table scan is bounded (proposals count is small for MVP orgs); acceptable at this scale |
</threat_model>

<verification>
1. `grep "in_review" src/types/proposal.ts` → no matches
2. `grep "in_review" src/data/proposals.json` → no matches
3. Migration file exists at `supabase/migrations/20260507000029_rename_in_review_to_in_progress.sql`
4. `npm run test:run` exits 0
</verification>

<success_criteria>
- ProposalStatus type contains 'in_progress', not 'in_review'
- proposals.json seed data uses 'in_progress'
- Migration SQL ready to push (done in Plan 05)
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/13-dashboard-proposal-lifecycle/13-01-SUMMARY.md`
</output>
