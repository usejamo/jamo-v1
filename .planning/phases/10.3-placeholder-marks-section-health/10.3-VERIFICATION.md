---
phase: 10.3-placeholder-marks-section-health
verified: 2026-04-28T00:00:00Z
status: gaps_found
score: 16/17 must-haves verified
overrides_applied: 0
gaps:
  - truth: "Integration test exercises full TipTap Editor.create roundtrip: placeholderPatternToSpan span → Editor.create content → getHTML() preserves UUID"
    status: failed
    reason: "src/lib/placeholderHtml.test.ts does not contain a TipTap Editor.create roundtrip test. The describe block is titled 'UUID roundtrip stability (HTML contract)' and only verifies the HTML string contract (data-placeholder-id attribute presence). No import of Editor from @tiptap/core, no import of PlaceholderMark, no Editor.create or new Editor() call, no afterEach destroy(). Plan 03 acceptance criteria explicitly requires full TipTap instantiation — 'the roundtrip tests are required' and 'must not be skipped or made conditional'."
    artifacts:
      - path: "src/lib/placeholderHtml.test.ts"
        issue: "Missing TipTap Editor.create roundtrip. Test file has 4 describe blocks but the UUID roundtrip block is HTML-contract-only, not a full Editor.create instantiation roundtrip. No @tiptap/core import, no PlaceholderMark import, no afterEach."
    missing:
      - "Add import { Editor } from '@tiptap/core' and import { PlaceholderMark } from '../components/editor/extensions/PlaceholderMark' to the test file"
      - "Add UUID roundtrip stability describe block that calls new Editor({ extensions: [StarterKit, PlaceholderMark], content: html }) and verifies getHTML() preserves data-placeholder-id UUID"
      - "Add afterEach to call editor.destroy() on all editors created in the roundtrip block"
      - "Add test for resolution plugin behavior (editing inside placeholder mark removes the mark)"
      - "Rename the existing HTML-contract-only describe to something that does not claim to be a TipTap roundtrip, or replace it with the full roundtrip as specified"
---

# Phase 10.3: Placeholder Marks & Section Health Verification Report

**Phase Goal:** Replace fragile [PLACEHOLDER: ...] string detection with a TipTap custom mark that persists through DB round-trips with stable UUIDs. Add a generalized `issues` field to SectionEditorState driven by UPDATE_SECTION_ISSUES dispatches. Status dot reads from `issues` in parallel with the existing compliance_flags bridge.
**Verified:** 2026-04-28
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | IssueCategory, SectionIssue types exported from workspace.ts | ✓ VERIFIED | workspace.ts lines 34-40: `export type IssueCategory` and `export interface SectionIssue` both present |
| 2 | SectionEditorState has issues field initialized to {} | ✓ VERIFIED | workspace.ts line 52: `issues: Partial<Record<IssueCategory, SectionIssue[]>>` on SectionEditorState |
| 3 | UPDATE_SECTION_ISSUES action type exists in WorkspaceAction union | ✓ VERIFIED | workspace.ts line 77: `{ type: 'UPDATE_SECTION_ISSUES'; payload: { section_key: string; category: IssueCategory; issues: SectionIssue[] } }` |
| 4 | workspaceReducer handles UPDATE_SECTION_ISSUES by replacing the category array | ✓ VERIFIED | SectionWorkspaceContext.tsx lines 160-177: case spreads `section.issues` and sets `[category]: issues` |
| 5 | escapeHtml escapes &, <, >, " correctly | ✓ VERIFIED | escapeHtml.ts: four chained .replace() calls in correct order (&, <, >, ") |
| 6 | placeholderPatternToSpan produces a span with data-placeholder-id and data-placeholder-label attributes | ✓ VERIFIED | placeholderHtml.ts: returns template literal with both attributes |
| 7 | migratePlaceholders converts [PLACEHOLDER: label] to a span with a UUID | ✓ VERIFIED | migratePlaceholders.ts: regex `/\[PLACEHOLDER:\s*([^\]]+)\]/g` with crypto.randomUUID() |
| 8 | PlaceholderMark TipTap extension parses span[data-placeholder-id] and renders amber pill | ✓ VERIFIED | PlaceholderMark.ts: parseHTML `{ tag: 'span[data-placeholder-id]' }`, renderHTML class `bg-amber-100 text-amber-800 rounded px-0.5` |
| 9 | SectionEditorBlock registers PlaceholderMark in TipTap extensions | ✓ VERIFIED | SectionEditorBlock.tsx line 20: `import { PlaceholderMark }`, line 55: `PlaceholderMark` in extensions array |
| 10 | migratePlaceholders runs on rawContent before useEditor; if content changed, saveNow is called immediately | ✓ VERIFIED | SectionEditorBlock.tsx lines 46-47: `rawContent` then `migratedContent`, useEffect lines 70-75 calls `saveNow(migratedContent)` when `migratedContent !== rawContent` |
| 11 | Placeholder analyzer effect dispatches UPDATE_SECTION_ISSUES category:placeholder after editor updates | ✓ VERIFIED | SectionEditorBlock.tsx lines 78-110: analyzer useEffect with 200ms debounce dispatches `UPDATE_SECTION_ISSUES` with `category: 'placeholder'`, keyed `[editor, dispatch, sectionKey]` |
| 12 | resolveStatus returns needs-review when Object.values(editorState.issues).some(list => list.length > 0) | ✓ VERIFIED | SectionNavPanel.tsx lines 17-19: `const hasIssues = Object.values(editorState.issues).some(list => list.length > 0)` and `if (hasIssues \|\| hasLegacyFlags) return 'needs-review'` |
| 13 | Nav panel renders placeholder issue labels with 'Missing: ' prefix via displayLabel | ✓ VERIFIED | SectionNavPanel.tsx lines 63-78: `displayLabel = category === 'placeholder' ? \`Missing: ${issue.label}\` : issue.label` rendered in JSX span |
| 14 | Edge function post-process pass converts [PLACEHOLDER: ...] to spans before DB write | ✓ VERIFIED | index.ts lines 502-514: flush() calls `.replace(/\[PLACEHOLDER:\s*([^\]]+)\]/g, ...)` on `fullText` assigning to `processedText`, passed to writeSectionById/writeSection |
| 15 | escapeHtmlInline in the edge function is byte-for-byte identical to the body of escapeHtml in src/lib/escapeHtml.ts | ✓ VERIFIED | index.ts lines 95-101: same four .replace() calls in same order; SYNC OBLIGATION comment present |
| 16 | Test guards against [PLACEHOLDER: ...] being treated as a markdown link | ✓ VERIFIED | placeholderHtml.test.ts lines 75-83: "does not treat [PLACEHOLDER: ...] as a markdown link" test present and passing logic |
| 17 | Integration test exercises full TipTap Editor.create roundtrip: placeholderPatternToSpan span → Editor.create content → getHTML() preserves UUID | ✗ FAILED | Test file only has HTML string contract tests — no TipTap Editor.create instantiation. Missing: Editor import, PlaceholderMark import, new Editor() call, afterEach destroy() |

**Score:** 16/17 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/types/workspace.ts` | IssueCategory, SectionIssue, issues field, UPDATE_SECTION_ISSUES action | ✓ VERIFIED | All four items confirmed present |
| `src/context/SectionWorkspaceContext.tsx` | UPDATE_SECTION_ISSUES reducer case | ✓ VERIFIED | Lines 160-177, correct spread pattern |
| `src/lib/escapeHtml.ts` | escapeHtml utility | ✓ VERIFIED | 7-line file, all four replacements |
| `src/lib/placeholderHtml.ts` | placeholderPatternToSpan shared helper | ✓ VERIFIED | Imports escapeHtml, produces correct span |
| `src/lib/migratePlaceholders.ts` | migratePlaceholders legacy backfill | ✓ VERIFIED | Correct regex, crypto.randomUUID() |
| `src/components/editor/extensions/PlaceholderMark.ts` | TipTap Mark extension with resolution plugin | ✓ VERIFIED | name: 'placeholder', inclusive: false, correct parseHTML/renderHTML, resolutionPlugin |
| `src/components/editor/SectionEditorBlock.tsx` | PlaceholderMark registered, migration pass, analyzer effect | ✓ VERIFIED | All three changes confirmed |
| `src/components/editor/SectionNavPanel.tsx` | resolveStatus with hasIssues; displayLabel with Missing: prefix | ✓ VERIFIED | Spec-exact form confirmed |
| `supabase/functions/generate-proposal-section/index.ts` | escapeHtmlInline + placeholderPatternToSpan + flush post-process | ✓ VERIFIED | All three confirmed, processedText used in write calls |
| `src/lib/placeholderHtml.test.ts` | Integration test with full TipTap Editor.create roundtrip | ✗ STUB | File exists with 4 describe blocks but UUID roundtrip describe is HTML-contract-only — no Editor.create, no PlaceholderMark import, no afterEach |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| migratePlaceholders.ts | placeholderHtml.ts | import placeholderPatternToSpan | ✓ WIRED | Line 1: `import { placeholderPatternToSpan } from './placeholderHtml'` |
| PlaceholderMark.ts | span[data-placeholder-id] | parseHTML tag selector | ✓ WIRED | Line 115: `[{ tag: 'span[data-placeholder-id]' }]` |
| SectionEditorBlock.tsx | PlaceholderMark | extensions array in useEditor | ✓ WIRED | Line 55: PlaceholderMark in extensions array |
| SectionEditorBlock.tsx | UPDATE_SECTION_ISSUES | dispatch in analyzer effect | ✓ WIRED | Lines 95-98: dispatch call confirmed |
| SectionNavPanel.tsx | editorState.issues | Object.values(editorState.issues) | ✓ WIRED | Line 17: spec-exact form |
| SectionNavPanel.tsx | displayLabel | category === 'placeholder' ? Missing: label : label | ✓ WIRED | Lines 65-67: confirmed |
| generate-proposal-section/index.ts | writeSectionById/writeSection | placeholderPatternToSpan called in flush before write | ✓ WIRED | Lines 505-511: processedText passed to write functions |
| placeholderHtml.test.ts | Editor.create | @tiptap/core Editor import | ✗ NOT_WIRED | No Editor import in test file; roundtrip test uses HTML string assertions only |

### Behavioral Spot-Checks

Step 7b: SKIPPED — no runnable entry points without a running dev server. All checks are React/TipTap browser-only components.

### Requirements Coverage

No `requirements:` field declared in any plan frontmatter (all three plans have `requirements: []`). No REQUIREMENTS.md mapping to check.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| src/lib/placeholderHtml.test.ts | 92 | describe titled "UUID roundtrip stability (HTML contract)" only verifies HTML strings — no actual TipTap Editor.create instantiation despite plan requiring it | ✗ Blocker | The §9.1 roundtrip regression guard is absent; if PlaceholderMark parseHTML/renderHTML changes UUID handling, nothing will catch it |

### Human Verification Required

None — all remaining gaps are code-verifiable.

### Gaps Summary

One gap blocks full goal achievement: the Plan 03 test file (`src/lib/placeholderHtml.test.ts`) was written with an HTML-contract-only roundtrip describe block instead of the required full TipTap `Editor.create` instantiation roundtrip. The plan's acceptance criteria explicitly requires `Editor.create` or `new Editor(` in the test, `afterEach` calling `editor.destroy()`, and tests for the resolution plugin behavior. None of these are present.

The core runtime implementation (Plans 01 and 02) is complete and correct: all 8 Wave 1 foundation items verified, all 7 Wave 2 wiring items verified. The gap is solely in the regression test coverage mandated by Plan 03.

---

_Verified: 2026-04-28_
_Verifier: Claude (gsd-verifier)_
