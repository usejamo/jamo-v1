import { describe, it } from 'vitest'

// ProposalCreationWizard tests — stubs only (component not yet built)
// Wave 0: All tests skipped until implementation plans complete

describe('ProposalCreationWizard', () => {
  it.skip('REQ-1.1: renders 3-step indicator with labels Study Info, Document Upload, Template & Generate', () => {})

  it.skip('REQ-1.2: Step 1 renders sponsor name, therapeutic area, indication, study phase fields', () => {})

  it.skip('REQ-1.2: Step 1 hard-required fields block forward navigation when empty', () => {})

  it.skip('REQ-1.5: Skip to Fast Draft button jumps to Step 3 from Step 1', () => {})

  it.skip('REQ-1.6: wizard state serialized to sessionStorage on field change', () => {})

  it.skip('REQ-1.6: wizard state hydrated from sessionStorage on remount', () => {})

  it.skip('REQ-1.7: forward navigation blocked and errors shown when required fields are empty', () => {})

  it.skip('REQ-9.4: Step 3 renders Generate button and calls createProposal on click', () => {})
})
